# 任务 019 · v3.5 执行（J-001~010）+ 回归场（2026-09-07，只追加）

## 执行记录
- 测试 42→51 条全绿。落地：J-001(P0) append 同 id 拒绝；J-002 removed 槽不交付；J-003 overrides 闭环；J-004/005 build-docs --lib 默认落库+state 随包升级；J-006 入口命令清单全命令面；J-007 migrate→eval 提示；J-008a(D1=a) experimental install.md 适配页；J-009 reflect-synthesis 随包；J-010(D2) --redeliver。
- **本轮最大发现（审查低估修正）**：J-003 现场调试揭示 verify --fix 写 overrides 的代码**自 v3.1 verify 重写时就丢失**（BR-7 patch 只重写了判定段）——`--fix` 一直是空操作，J-003 的「写而不读」实为「根本没写」。补回写入段+读取应用段+via=override 全链闭环。
- **工程教训再+1**：patch 脚本攒批 write 中途 assert 失败=全量静默丢弃（v3.4 刚记过，本轮 patch A 又踩）——已固化为「逐 rep 落盘制」（rep() 内即 save()）；heredoc 转义第三次咬人（\n 三层解析）→ 全面改用 Write patch 文件 + chr(10) 构造。
- **回归场（库调试v3.2）**：--redeliver 处置 v3.4 遗留漂移 → build-docs --lib 刷渲染件 → 校验模式「漂移 1（experimental 适配页注入导致 install.md 更新，属 J-008a 预期）/ 未在册 0 / 渲染件 0 / 根一致 / 环境痕迹行」；eval 复跑 recall@5=0.485 稳定（parity 达标）。
- 发布：锚点 71515c3c5361515c…；发布目录原地 51/51；RELEASE 追加。

## 待办（v3.6 候选）
- experimental 内容体全量渲染化（C-000 终章，J-008b）；
- reflect 素材包与根库 R 条目的同步机制（合成产出回流根库 knowledge/）；
- M-04 会话入口验证（下会话，用户级）；
- 漂移 1（experimental install.md J-008a 注入差异）已属预期态，观察一代。
