# 任务 021 · v3.7 执行（L-001~008）+ 回归场（2026-09-07，只追加）

## 执行记录
- 测试 57→63 条全绿。落地：L-001 _body_for 统一单文件落盘决策点（redeliver/_deliver 分叉消灭）；L-002 verify --absorb-overrides（裁决回写建议→REGISTRY-ABSORB.md+清旁路，单一事实源复位）；L-003 guard/absorb-md 汇总入账本；L-004 eval 劣化检测（>5% 宪章警告 exit 2，负测试实测）；L-005 跨月链真测试（模块级 strftime patch，次月首行 prev=上月尾实证）；L-006 类型强转；L-008(D1) experimental install.md 全量渲染化（适配页退役至模板 md）。
- **现场重大修复：Windows write_text CRLF 陷阱**——referee/reflect-synthesis 的 manifest 记账用 LF 哈希而盘上 write_text 写 CRLF，账实天生不符（redeliver 漂移复现的根因，跨 v3.5~3.6 潜伏）。统一 write_bytes 根治。这是 newline 陷阱第四变体（前三：v3.3 渲染件检测、素材包、构建产物）。
- **patch 工程再进化**：patch A 攒批 write 又丢一次 → 全项目固化为「Write patch 文件（chr(10) 构造）+ 逐 rep 落盘 + if 幂等判据」三件套；锚点构造改按行定位（不再手写长转义串）。
- **回归场终验**：--redeliver 后漂移 0（L-001 实证）、doctor 四态全绿（42/42/42+链连续）、eval 0.485 稳定、校验模式「漂移 1/未在册 0/渲染件 0/根一致」（漂移 1=版本演进内容体，--redeliver 可随时处置）。
- 发布：锚点 59231a90f70c3ddb…；发布目录原地 63/63；RELEASE 追加。

## 待办（v3.8 候选）
- 漂移 1 处置（--redeliver 一次即清，属运维动作非缺陷）；
- 双库同步（随 M-4）、M-04 入口验证（下会话）；
- 体系级：v3 线已连续 7 版零回归——评估「冻结窗口」（功能冻结，转入使用期观察）。
