# L9 交付映射补丁（v3.8.2/L9-DELIVER）说明

- **补丁文件**：`L9交付映射补丁.patch`（unified diff，8 个 hunk，126 行）
- **可直接替换的整文件**：`bootstrap_v3.8.2_补丁后.py`（补丁应用后的完整文件；原始 `bootstrap_v3.py` 未改动）
- **配套数据**：`level_mapping_补丁后.json`（替换 `bootstrap_data/level_mapping.json`，否则 `t_absorb_mapping` 会因键集不一致失败）
- **生成/复跑**：`make_patch_补丁生成器.py`（在临时副本应用补丁、重生成 mapping、跑全量测试，全程不碰源树）

## 一、补丁改了什么

| hunk | 位置 | 内容 | 修复的核对结论 |
|---|---|---|---|
| 1 | VERSION | 3.8.1 → 3.8.2（与文件头、总表数据源标注对齐；不需要可单独回退此 hunk） | B2 |
| 2 | MODULE_MAPPING | 新增 5 个 L9 语义键：`l9:instrumentation`（含 scripts 的 .txt 工具）、`l9:bca`、`l9:dual-memory`、`l9:dmn`、`l9:goalstack` | A24/A29/A32/A33 装不出 |
| 3 | LEVELS["L9"] | adds 由 3 项扩为 8 项（instrumentation 置前，满足 dmn/goalstack 对它的依赖）；gate 文案改为「确认后交付」，不再谎称「正文随 absorb 展示」 | gate 承诺无实现 |
| 4 | DEPS | 补 4 条依赖边：bca→l8:governor、dual-memory→emotion、dmn/goalstack→instrumentation（与各 install.md 自述依赖一致） | 依赖图完整性 |
| 5 | `_deliver` | 交付时跳过 `__pycache__/` 与 `.pyc/.pyo`（存量 bodies 里的缓存垃圾不会落盘） | .pyc 乱码交付 |
| 6 | cmd_install gate | L9 慢车道提示文案与 hunk 3 同义修正 | 同上 |
| 7 | cmd_absorb | 吸收源头加 `.pyc/.pyo/__pycache__` 黑名单（根治：重吸后 bodies 不再含二进制） | bodies 卫生 |
| 8 | 测试区 | 新增 3 个回归测试（见下） | 机器门 |

**新增回归测试**
- `t_l9_full_delivery`：隔离安装 L9，断言 13 个关键文件全部落盘（bca 公理+ABLATIONS、dmn、dual-memory、goalstack、instrumentation、scripts 的 3 个 state_*.txt），resolve("L9") 无缺失，且全树无 .pyc / __pycache__。
- `t_l8_not_deliver_l9`：L8 安装后不存在 experimental-l9 目录（档位边界不越级）。
- `t_l9_module_deps`：4 条新依赖边在位，且 `detect_cycle()` 全图无环。

## 二、验证结果（临时副本，Python 3.14.7）

```
run_tests: 71/71 全部 PASS（原 68 + 新 3），exit code 0
  PASS t_l9_full_delivery
  PASS t_l8_not_deliver_l9
  PASS t_l9_module_deps
```

补丁前同副本为 70/71（仅 `t_absorb_mapping` 因 level_mapping 旧快照失败——用配套 json 替换后转绿，属预期）。

## 三、应用方式（二选一）

**方式 A：直接用整文件**
1. 备份原 `bootstrap_v3.py`；
2. 用 `bootstrap_v3.8.2_补丁后.py` 替换；
3. 用 `level_mapping_补丁后.json` 替换 `bootstrap_data/level_mapping.json`；
4. `python bootstrap_v3.py run-tests` 应见 71/71。

**方式 B：应用 diff**
```
cd library-bootstrap-v3.0
copy bootstrap_v3.py bootstrap_v3.py.bak
git apply --ignore-whitespace review-20260908/L9交付映射补丁.patch   # 或 patch -p1 < ...
copy /Y review-20260908\level_mapping_补丁后.json bootstrap_data\level_mapping.json
python bootstrap_v3.py run-tests
```

## 四、应用后的建议后续（不在本补丁范围）

1. **重吸 bodies 清毒**：择机对干净源树跑一次 `absorb`（hunk 7 会自动拒收 .pyc），把已混入 bodies 的 6 个 `.cpython-314.pyc` 从源头清掉；在那之前 hunk 5 保证它们不会被交付。
2. **慢车道语义**：L9 仍保留人类令牌门（`--yes`），补丁只补「确认之后该交付的东西真的交付」，不改变门本身。
3. **内容体编号自查**：constitution-bca 的 install.md 写「追加为第七～第十条」，而 L8 CHARTER_L8 已有七条，存在条目编号撞车，建议内容体侧改为「第八～十一条」或改为「BCA-1～BCA-4」编号——属内容修订，未纳入代码补丁。
4. 补丁未触碰前轮审查报告中的任何 P0/P1 缺陷；它们与本补丁正交，可独立排期。
