---
name: cbb-pipeline
description: 把任意小说（长篇/多卷/网文/轻小说等）构建成证据可核验的正典知识库：每条事实带原文引文四元组、三态写入、append-only 账本、五层证据门审查。Use when 用户要建世界观/角色/设定知识库、小说入库、小说文本抽取、lore/正典/canon 构建、证据可溯源知识库、知识图谱、伏笔台账、实体别名治理、终审抽检，或要求"每条结论可回溯到原文"——即使用户没有点名该技能也应使用。不适用于：无需溯源的一次性摘要、单文档简单问答。
license: CC-BY-4.0 (content)
compatibility: 需 Python 3.10+（Windows 用 py -X utf8 启动器）。三件基础设施不可抛弃、全程随流程跑：知识图谱（连通且归属已判）／嵌入向量模型／重排模型——缺项默认阻断开书，带理由降级会自动记一笔导出债务。判定者与执行者是宿主 Agent，本地推理端点仅列为观察项。真源永远是账本，图与索引皆为可重建的派生视图
metadata:
  version: "2.0"
  origin: 通用架构总纲 v2.0（16 仓吸收 + 迷深实例 44.5% 实战沉淀 + 田·声明式审计纪律，2026-09）
---

# 小说资料库构建流水线

**核心原则：逻辑不活在模型上下文里，活在库的账本上。** 任何事实进正典必须：带原文证据（可逐字回落）→ 过机械门 → 与既有事实对撞（重复与矛盾分开处理）→ 三态落位 → 全程 append-only 可回放。抽取无状态，记忆在库；模型负责读和判，**系统负责记、对、废**。

## 何时使用 / 何时不用

**用**：开新小说库（有章节标记可切分的长篇/多卷小说）；续跑（已有 BUILD-STATE 与库，从游标继续）；收尾审查（全量入库后按五层证据门验收）。
**不用**：单文档一次性摘要；不需要溯源的闲聊问答；语料小到一屏能读完（直接读更快）。

## 流程总览（⓪ + 七步）

```
⓪基础设施自检 scripts/cbb/tools/环境自检.py --project-token <本项目命名空间>
            → 三件基础设施（图／嵌入／重排）＋主链必需项＋图库归属，四态回执，写 工作区/环境自检.json
            → carrier 只许 graph（归属已判）或 file（未判定/共享库，同一套规则换载体不换标准）；
              缺基础设施⇒阻断开书（exit 2），--allow-degraded "<理由>" 才放行且自动记一笔导出债务
①初始化   填 project.yaml（八项参数，见 references/架构与profile.md）
            → scripts/init_project.py 生成工作区+BUILD-STATE
②边界     解析单元标记（语料自身标记=声明面真值）+ 伪锚点（禁墙钟）
③试车     单单元端到端 + 基准样例对照（预注册门槛，不过→停单写 Issue）
④小队列   3~5 单元连跑（含 1 个"脏单元"试防御）
⑤主队列   调度员派全新上下文子代理逐单元抽取（规范+判例+上下文包+切片）
            → gate → 隔离分流 → store 双轨入库
⑥收口     每 N 单元轻收口（游标+scoped commit）；区段收口跑批次机械自检
            收口必带：环境自检回执未达项 + 导出债务笔数
⑦终审     五层证据门 G1~G5 → 隔离区报告 → 交接文书 → 提交审核线
            前置：L4 归属已判 且 导出债务=0，否则 ⑦ 记未达项不得判通过
            启用了图/RAG 派生层 ⇒ 再加一条前置：tools/派生层对账.py 判 PASS
            （图与向量库计数不等、或有缺坐标的节点＝半途写没被发现，不许当可用参考面）
```

## 步骤⓪ · 基础设施自检与图库归属判定（先于开书，不可绕过）

**为什么单列一步**：L4 参考层在 `references/架构与profile.md` 六层不变式里写的是"强制参考面，不是可选装饰"，
但这句话此前只出现在按需读取的第二层文档里——按渐进式披露读 SKILL.md 的人，直到终审都不会看到它，
于是把图库当"有就用、没有就算了"的装饰。本步把这个前置提到最前面，一次跑清楚。

**三件基础设施（不可抛弃、全程随流程跑）**：`B-图` 知识库图载体（G1 连通 **且** G4 归属已判）、
`B-嵌入` 向量模型（E1）、`B-重排` 重排模型（R1）。缺任一项默认**阻断开书**（exit 2）；
确需降级必须 `--allow-degraded "<理由>"`，自检件会自动往 `导出债务.jsonl` 追加一笔 `infra-degraded`，
回执转 `DEGRADED`、`终审可用=false` —— **降级只能带着债走，不许无痕通行**。

四态严格区分，**不许互相顶替**：`READY`（闸门件通过）／`BLOCKED`（闸门件不过，写清缺哪项怎么补）／
`N/A`（仅 `--disable` 显式声明，保留原状态痕）／`OBSERVED`（观察项，只报告不闸门）。
**未探≠N/A；未探=BLOCKED。**

**判定者与推理端点的分工**：这套流程的语义判定者与执行者是**宿主 Agent**（J1 落 `DECLARED`）。
本地聊天模型/付费 API 属**观察项**，不参与闸门——要不要让本地模型接手 NLI 矛盾预筛是选型问题，
用 `--exam --model <名>` 真打三题微考拿证据，别拿"服务在听"当"能当判定器"，也别反过来因它判不准而卡住开书。

五条实测换来的判据：

1. **图库归属先于图库可用性。** 机器上"有一个在跑的 Neo4j"不等于"可以用"：共享库或上一项目留下的库
   会让跨单元巡检一条查询扫到别人的实体，产出"零孤悬/零矛盾"的**假干净**。判据＝库内节点是否带本项目
   `group_id/canon_group` 标记；总数>0 而归属分布零回行 ⇒ 未判定 ⇒ `BLOCKED`，只许带债降级或走 `file` 兜底。
   （实测：本机 Neo4j 5.26.30 **community 版**，无多库隔离能力，901 节点里 0 个带本项目标记。）
   正解不是"每条查询记得加过滤"（纸闸门），而是**另起实例**：`scripts/cbb/tools/图库隔离.py create
   --project-token <名> --auth-none --run`——只绑 127.0.0.1、发布端口先预检、口令只走仓外 env-file；
   建完 `verify` 复验归属，`plan` 可先看不动手。归属判据与 G4 共用一份实现（`ownership_verdict`）。
2. **"连通"与"就绪"是两个槽位。** 图这项必须 G1（连通）与 G4（归属）都过才算 READY——
   只看连通就是把"能连上"当"可以用"，正是共享库事故的入口。
3. **bolt 端口别信 discovery 自报值。** `bolt_routing` 常返回容器内地址（`localhost:7687`），宿主机映射在别处；
   连通判定走 HTTP `NEO4J_HTTP`，bolt 探不通要在回执里注明是映射端口问题而不是服务挂了。
4. **首包慢≠不可用。** 本地模型服务冷加载（8B 嵌入实测 10~16s）会让第一次调用超时；回执须带实测耗时，
   超时判 `BLOCKED` 时提示调大 timeout 复测，不许一次失败就永久记不可用。
5. **端口可能被系统占走。** 上游默认的 `:1234` 在部分 Windows 机器上属 Hyper-V/WinNAT 保留段
   （实测 1209–1308 整段排除，`lms server start --port 1234` 报 EACCES）；而应答 `:8080` 的进程
   未必是 LM Studio 自己（本机实为 Docker 转发）⇒ 端点一律走 `EMBED_HTTP`/`RERANK_HTTP` 环境变量配，别硬编码。

退出码：`0` 可开书（三件基础设施就绪；观察项/增值件不过只进「未达项」）；
`2` 主链必需项缺失、基础设施缺项且未给降级理由，或判定面为空。

## 目录地图（按需读取，勿一次全读）

| 文件 | 内容 | 何时读 |
|---|---|---|
| [scripts/cbb/tools/环境自检.py](scripts/cbb/tools/环境自检.py) | 步骤⓪：外部环境自检＋图库归属判定（READY/BLOCKED/N-A 三态、carrier、导出债务、判定者身份） | **开书之前必跑**，以及每次换机器/换服务后 |
| [references/架构与profile.md](references/架构与profile.md) | 六层不变式／八项参数／本体 profile 与受控词表写法／术语映射／四类病预防 | ①②步开新项目时 |
| [references/抽取规范.md](references/抽取规范.md) | 执行者逐单元抽取规范（R6 四面防御/契约/别名四分类/引文预检/封套纪律） | 每单元抽取前（子代理必读） |
| [references/编排与并发.md](references/编排与并发.md) | 调度员编排/上下文包/并发与单元认领/限流挂死诊断/NLI 预筛 | 派工与遇阻时 |
| [references/审查与收口.md](references/审查与收口.md) | 单位收口三件套/批次机械自检/五层证据门/抽样公式/十条纪律 | 收口与终审时 |
| [references/检索增强与图谱对账.md](references/检索增强与图谱对账.md) | 多路检索路间纪律（第五路图游走/零查询期LLM/自造词准入门）/图对账四断言面与悬挂端点两分法/网络补充层三闸/增量同步契约 | 接检索增强与图谱对账时 |
| [全流程说明书.md](全流程说明书.md) | 全流程每环节做什么/为什么/失效会怎样（含实测数字与降级路径） | 想理解整体设计或向他人解释时 |
| [references/图谱派生层.md](references/图谱派生层.md) | GraphRAG／LightRAG 三拓扑选型与成本实测、**实体名四分类处置**（谁写的字谁负责）、半途写对账、本机五道环境门 | 要把外部图谱索引器接进来时；跑完整卷索引前先读 §5 的成本账 |
| [references/判例模板.md](references/判例模板.md) | 跨单元裁决判例集格式（封顶 30 条） | 裁决时 |
| [scripts/cbb/](scripts/cbb/) | 六模块+四契约（coordinate/anchor/extract/gate1/quarantine/store） | 实现与校验 |
| [scripts/cbb/tools/](scripts/cbb/tools/) | 上下文包/嵌入查重/图导出/账本链/NLI 矛盾对/图桥（各配单测） | 对应场景直接运行 |
| [assets/project.template.yaml](assets/project.template.yaml) | 项目参数模板（通用八项+本体 profile 段） | 开新项目填配置 |

## 通用性适配（v2 关键）

本技能**不绑定小说**。换语料只换三样：①单元切分规则（章/节/页/文档/条目——用语料自身标记做边界）②本体 profile（库类型+必填字段+受控词表）③基准样例。正文流程、契约、门、账本、审查体制全部不变。**小说实例的词表与判例不迁移**，只作样例参考。

## Common Mistakes（实例换来的，优先排查）

1. **把上下文包当事实源**——它是先验；与原文冲突以原文为准＋存疑分开建
2. **跨章事实塞进 observations**——observations 只放本单元直接观察；跨章事实走 event/relation/setting（否则入库即丢）
3. **没有受控词表就开跑**——类型自由文本会让"粒度差异"淹没真矛盾（实例：604 件假矛盾、人工 128/129 全是返工）
4. **均匀随机抽样**——缺陷按执行批次聚集，置信度虚标；必须按批次分层（每层 8~10 条）
5. **"判定面为空"读成通过**——缺席/为空/通过三者在输出上同形；空必须非零
6. **多副本靠记忆同步**——副本差集是静默的；锚哈希＋机械比对
7. **契约写歪**——canonical 写成字符串、evidence 用单对象、observations 用字符串数组，全是门 1 拦截项
8. **引文改写**——引文必须是切片原文逐字子串，落盘前自检
9. **全库补齐做一半**——剩余单元进来还得重做；全库类动作排在主队列完成之后
10. **轮次式自审**——"跑 N 轮"数的是过程不是证据；收口看三件套，终审看五门
11. **把外部件 BLOCKED 读成"这层无所谓"**——增值件可以不用，L4 参考层不行：入库可以不碰图，终审不碰图就是欠债过关；未探＝未探，写 N/A 就是把"没查"混进"查过没问题"
12. **共用图库当自己的**——机器上有个在跑的 Neo4j ≠ 可以用。上一项目/别的项目留下的库会让跨单元巡检一条查询扫到别人的实体，产出"零孤悬/零矛盾"的假干净；启用 graph 载体前先跑步骤⓪ 的归属判定，拿不到"全部带本项目标记"就走 file 兜底（同一套规则，两载体）
13. **一次超时就永久记不可用**——本地模型冷加载首包几秒到几十秒是常态；回执要带实测耗时并按"调大 timeout 复测"口径下结论，别拿首发的超时当服务的状态
14. **把派生层的自造名一律当幻觉删掉，或一律收下**——两种都是错。四类分开处置（写法变体归一／括注转别名／原始回执可命中而终值不可＝上游吃的字／真自造再分"可合成"与"纯命名"）；D 类只当检索别名与主题候选，**禁入受控词表与去重键**，零支撑直接退回。跑 `tools/实体名归因.py`，别凭印象判
15. **拿"跑成功了"当对账**——图与向量库是两处存储，中途挂会**半途写**：节点在图里、向量库里没有，检索时长得跟"这本书没有这条"一样。批次自检的三方对账同理：**读不到 STATE 不是"没有不一致"，是缺一角**（现判 SKIP 不判 PASS）
16. **外部件直连就当能用**——本地 OpenAI 兼容端点各有拒接形态：`response_format.type` 只认两种值、思考型模板把正文全留在 `reasoning_content` 交回空串、litellm 不补 `/v1`、端口落在系统保留段。空正文比报错危险得多：读成"本块零实体"，整卷跑完交出一份零孤悬零矛盾的漂亮报告。中转层要么回收、要么**中断**，绝不返回空串
17. **发布件里写死上一项目的实例名**——`迷深实战-*`、某台机器的容器端口、仓外长路径当默认值，后果不是"不通用"而是**门静默失效**（批次自检找错 STATE 文件名即由此来）。路径一律从 store 反推（`tools/路径惯例.py`），图库基址不给就报错，不许拿活端口当默认

## 验收判据（每步可核验产出）

| 步 | 通过判据 |
|---|---|
| ⓪环境自检 | 回执落盘且主链必需项全 READY；`carrier` 有值且写明依据；图库未判归属 ⇒ 只许 `file`；BLOCKED 项逐条进「未达项」（判定面为空＝FAIL，退出码 2） |
| ②边界 | 单元数=声明面清单；边界表抽 5 单元对原文核对 |
| ③试车 | 基准样例对照达预注册门槛；评分数字入 STATE |
| ⑤主队列 | 每区段批次自检全绿（或 FAIL 逐条处置）；游标=账本头=commit 三方一致 |
| ⑦终审 | G1~G5 全过＋每门有正负对照＋每个数字可复算＋宣称有口径上界；**且 L4 归属已判、导出债务=0**（不成立记未达项，不许判通过） |
| ⑦终审（启用图/RAG 派生层时加验） | `tools/派生层对账.py` PASS（图与向量库计数等、无缺坐标节点、正典名单非空）；`tools/实体名归因.py` 的 D 类**不得出现在 confirmed 记录里**，每条都带 `grounded_to` 或标『非原文逐字』；`tools/graph_audit.py` 四断言面差异逐条有处置 |

## 核心纪律速查（全文见 references/审查与收口.md）

1. **三态写入**——错误可以产生，confirmed 的库里它进不来；矛盾不静默合并
2. **证据即公民**——每条记录带（卷,单元,行,引文）四元组，逐字预检；缺坐标判不可见
3. **先验非事实源**——上下文包喂先验，原文才是事实
4. **失效即写入**——新说法推翻旧说法时写失效章（=新说法最早证据章），旧事实永久保留
5. **隔离区一等公民**——处理不了显式隔离出报告，绝不静默丢弃
6. **执行者无状态**——每单元新上下文，候选自写盘，回执 ≤200 tokens
7. **断点纪律**——游标+逐批 scoped commit；STATE=缓存，磁盘+git=事实
8. **量尺机械化**——禁自抽自评；抽样按批次分层；判分器先上岗考试
9. **成本乘法线**——进"每单元必读"的东西 ×剩余单元数；只准放枚举，禁放散文
10. **判定权分离**——执行无权自宣验收；出口=证据，不是轮数
11. **派生图带边即活**——悬挂端点两分法（变体归一放导出层/缺口走隔离区准入），禁自动删；段收口跑图对账四断言面

## v2.0 已知边界（诚实声明 · 2026-09-22 裁决后更新 · 2026-09-24 与同包代码对表）

**已落地代码件**（scripts/cbb/tools/，各配单测）：受控词表三件套 `词表.py`（自举/体检/记录核查＋粒度纪律）、失效宣告 `失效宣告.py`（严格三情形＋幂等＋图边视图合成）、矛盾分流 `矛盾分流.py`（四分类＋auto/batch_confirm 双模式）、裁决日期位与状态对账（quarantine `status_report`）、契约 `spec_version` 可选字段（v2.1）、哨兵 B/D 修正（graphiti_ready）。样例词表 #1 由冻结实例库自举产出（见项目侧 通用化落地-20260922/）。
**2026-09-24 补——两件其实已在位**（此前被本段误列为"未含"，执行者据此会直接跳过这两道门）：批次机械自检器 `批次自检.py`（十项失效模式逐项断言；输出 JSON 即留痕；退出码 0＝全 PASS 或仅 SKIP、1＝任一 FAIL；**FAIL 不许口头豁免**，合法例外〔空章/缺号章〕须有登记依据）、双独立复查的残余缺陷估计量 `捕获再捕获.py`（Chapman 估计量；独立性是命门——两人共用同一份抽取规范/上下文包会正相关，N̂ 严重低估）。二者各配单测（`test_批次自检.py` 3 例／`test_捕获再捕获.py` 4 例）。
**2026-09-26 补（外部审计落地的一批门修复）**：① `路径惯例.py`（＋8 例）——从 store 反推项目根/工作区、图库基址**不给就报错**；发布件里 19 处上一项目实例名默认值与 7 处某机器容器端口默认值全部拆除（检索层的查询日志从"默认开＋写死别项目目录＋失败静默"改成"默认关＋参数化＋失败现形"）。② `批次自检.py` 三处：STATE 文件名不再写死（**裸名 `BUILD-STATE.md` 优先**，旧代码找 `迷深实战-BUILD-STATE.md` ⇒ 换项目永远读不到而 c1 仍报 PASS）；读不到 STATE ⇒ **SKIP** 不是 PASS；git 判不了 ⇒ `None` 不是 `True`；c8/c9 零记录 ⇒ SKIP（空库刷不出绿）；输出加 `skip` 计数与「未达项」清单。③ 图库读写**全面带命名空间**：`neo4j_export` 三处 MERGE 键、`连续性巡检` 与 `graph_audit` 全部读查询；`graphiti_bridge.group_id` 取消默认值。④ `连续性巡检` 自称只读却走可写端点，现加机械断言拒写语句。⑤ `graph_audit.py`/`web_supplement.py` 补上随仓单测（13 例）——**有闸门没测试等于没闸门**；`web_supplement.guard_block_admission` 补"无来源无标记即拒"。⑥ 新件 `实体名归因.py`＋`派生层对账.py` 与 `references/图谱派生层.md` 曾于 `5342fbd` 入仓但**挂点被后续提交覆盖成孤儿**，本次回补：目录地图、⑦ 前置、验收判据、Common Mistakes 14-17 全部重新指回。
**仍未含**（路线图）：把"独立抽样 → 双人分查 → 对账 → N̂ → 处置"串成一条命令的**流程编排件**（估计量已在 `捕获再捕获.py`，抽样侧 `抽样排序.py`/`重排器.py` 在位，缺的是编排与留账）；覆盖复算登记件（U-D10——当前按手工配方走，缺号章/元文本章的理由登记无机器核）；派生层**坐标回填件**（把 GraphRAG/LightRAG 的块级 `source_id` 换成 `(卷,单元,行)`，手工配方在 `references/图谱派生层.md` §5）；社区报告**事实性抽检件**（现只验"出得来、是中文、字段齐"，未验断言能否回落原文）。
**2026-09-24 再补（步骤⓪ 与两处口径更正）**：① 新增 `环境自检.py`＋`test_环境自检.py`（18 例）——主链必需四项＋L4 图库归属判定＋嵌入/重排/NLI/graphiti 增值件三态回执；本机实跑一例即抓出"图库 901 节点零个带本项目标记"⇒ 该库属别的项目，只许走 file 兜底。② 更正一处会误导人的旧表述：frontmatter 原写"嵌入扫描/图导出为可选增强，缺省不影响主链"，与 `references/架构与profile.md` 里 L4"强制参考面、终审必须依赖"相互矛盾——按渐进披露读法的人只会读到前者；现两处的口径已统一（主链零外部服务，终审必须有 L4）。③ 深融⑤ 的 graphiti 增值层**挂点在、实现缺席**（需 `graphiti-core` 与图库归属已判两项前置，缺任一即 BLOCKED，不许当作已启用）。
