import io, re

base = r"D:\zcode专用！！！！危险！！！！！！！！！"
p = base + r"\knowledge\_index.md"
s = io.open(p, encoding="utf-8").read()
m = re.search(r"- 自最后反思以来 wrap-up 次数：(\d+)（", s)
n = int(m.group(1)) + 1
s = s.replace("wrap-up 次数：" + m.group(1) + "（", "wrap-up 次数：" + str(n) + "（", 1)
s = s.replace("）\n- 最后反思：", "；" + str(n) + "①迷深实战v1.3调度员编排（task-058）：主代理不亲抽改逐章前台子代理新上下文+回执≤200tokens候选走磁盘+并发旋钮CONCURRENCY=3试探上行+抽取规范/判例.md外置固化（R6逐字源码取证）；教训=吸收了参数没吸收编排）\n- 最后反思：", 1)
anchor_line = [l for l in s.splitlines() if "task-056-迷深实战中断接收与根因" in l and l.startswith("- `")][0]
t58 = "- `trajectories/2026-09-16/task-058-迷深实战v1.3调度员编排.md` — 上下文税诊断（主代理背章节+候选载荷ch0038 696行，12章/时大半是税）→三详报一致结论（U-A11 逐章子代理/U-A08 no accumulation）而施工参数已吸收编排未执行；工单v1.3=调度员编排+并发旋钮+抽取规范.md（R6逐字源码取证）/判例.md（封顶30条，预置试车期3判例）；五弊端全有护栏无一架构级否决；预期3-5×（非承诺）"
s = s.replace(anchor_line, anchor_line + "\n" + t58, 1)
io.open(p, "w", encoding="utf-8", newline="\n").write(s)
print("index updated: count ->", n, "; task-058 line appended")
