import io, re

base = r"D:\zcode专用！！！！危险！！！！！！！！！"
p = base + r"\knowledge\_index.md"
s = io.open(p, encoding="utf-8").read()
m = re.search(r"- 自最后反思以来 wrap-up 次数：(\d+)（", s)
n = int(m.group(1)) + 1
s = s.replace("wrap-up 次数：" + m.group(1) + "（", "wrap-up 次数：" + str(n) + "（", 1)
s = s.replace("）\n- 最后反思：", "；" + str(n) + "①并发加速试探+NLI预筛准备（task-067，工单v1.9）：并发起步3每批零失败+2试探上行、稳定值持久化STATE后续班车直用；U-C03.8 NLI预筛准备件（矛盾对生成器+GLM三分判定只分流不裁决+上岗小考≥0.70）；昨夜战果核证=A/B重测过门并发3放量、一夜17章游标71、悬空0/673召回90.6-100%）\n- 最后反思：", 1)
anchor_line = [l for l in s.splitlines() if "task-065-graphiti就绪层v1.8" in l and l.startswith("- `")][0]
t67 = "- `trajectories/2026-09-18/task-067-并发加速试探与NLI预筛准备.md` — 工单v1.9：并发加速试探（3起步每批零失败+2上行，失败回落最近稳定值记STATE，两批同值=平台上限停探）+U-C03.8 NLI预筛准备件（矛盾对生成器/GLM三分判定只分流不裁决/上岗小考分离度≥0.70）；昨夜核证=A/B重测过门并发3放量一夜17章悬空0/673召回90.6-100%修正循环1轮收敛"
s = s.replace(anchor_line, anchor_line + "\n" + t67, 1)
io.open(p, "w", encoding="utf-8", newline="\n").write(s)
print("index updated: count ->", n, "; task-067 line appended")
