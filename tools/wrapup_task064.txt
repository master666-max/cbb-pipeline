import io, re

base = r"D:\zcode专用！！！！危险！！！！！！！！！"
p = base + r"\knowledge\_index.md"
s = io.open(p, encoding="utf-8").read()
m = re.search(r"- 自最后反思以来 wrap-up 次数：(\d+)（", s)
n = int(m.group(1)) + 1
s = s.replace("wrap-up 次数：" + m.group(1) + "（", "wrap-up 次数：" + str(n) + "（", 1)
s = s.replace("）\n- 最后反思：", "；" + str(n) + "①上下文包编排v1.6（task-064）：用户架构反驳采信——百万字下子代理是唯一可扩展架构，亲抽回落非终态；AB门FAIL真因=子代理冷启动缺系列先验→补上下文包（库喂抽取闭环≤2K：人物册/活跃线/前情/判例，先验非事实源），A/B改迭代制+覆盖率指标机械化（R-018防量尺循环））\n- 最后反思：", 1)
anchor_line = [l for l in s.splitlines() if "task-063-通报单回执与反向包吸收" in l and l.startswith("- `")][0]
t64 = "- `trajectories/2026-09-17/task-064-上下文包编排v1.6.md` — 用户架构反驳采信：百万字下子代理唯一可扩展，亲抽回落非终态；AB门FAIL真因=冷启动缺系列先验；v1.6=上下文包（库喂抽取闭环：人物册/活跃线/前情/判例≤2K，先验非事实源防污染）+A/B迭代制+量尺机械化；哲学闭环=图谱既是产出物又是抽取先验"
s = s.replace(anchor_line, anchor_line + "\n" + t64, 1)
io.open(p, "w", encoding="utf-8", newline="\n").write(s)
print("index updated: count ->", n, "; task-064 line appended")
