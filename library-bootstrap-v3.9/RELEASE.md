# bootstrap_v3.9 发布记录（衔尾蛇工单执行版）

> 发布日期：2026-09-09 · 锚点 sha256 前 16 位见 `py -X utf8 bootstrap_v3.9.py --hash`
> 谱系：v3.8.1(9d8b3b0d) → v3.8.1勘误(02931add) → v3.8.2中性化(5f8199aa) → 豆包L9-DELIVER补丁(v3.8.2补丁后 ef2c645a) → **+衔尾蛇工单 W-1/W-2/W-3/W-4/W-6 → v3.9（本版）**
> 工单全文与证据：`大审查/衔尾蛇/reports/v3.9_法层参数与引擎修复工单.md`（实验证据 `大审查/衔尾蛇/data/`）

## 本版变更（全部来自衔尾蛇沙盒 280+ run 的实证）

| 项 | 级别 | 内容 |
|---|---|---|
| W-1 | P1 主件 | `_rank_entries` 打分五参数外置 TUNABLES（`law_w_kw/law_w_content/law_w_imp/law_w_age/law_filter_zero`）；**`law_filter_zero` 默认 1**（24 种子+真实库 20/20 双世界复现，MRR 0.27–0.67→1.00）；其余四参数维持 v3 沿革值并标注证据等级 |
| W-2 | P1 | install 与 build-docs --lib 时把法层参数快照写入 `state.json["law_params"]`（只抄不裁决，不开放自动修改） |
| W-3 | **P0** | `cmd_run_tests` 补 `except SystemExit`——sys.exit 是测试唯一错误通道，旧版捕不到导致静默假绿（实测修复后暴露真实通过率） |
| W-4 | **P0** | `engine append` 的 state 更新段套锁目录互斥（并发丢更新 75%→0；沙盒 X1/X4 实证） |
| W-6 | P2 | REFLECT_SYNTHESIS 追加第 5 条：法层参数未含 law_filter_zero=1 的旧库暂缓合成条目入库（X2d 轴耦合实证） |

W-5（轨迹撞号，知识库侧）不在本包范围，【待确认】搁置。

## 与 v3.8.2 补丁后的差异声明

仅上表五项 + 文件名自指（bootstrap_v3.py → bootstrap_v3.9.py，BASELINE_BLACKLIST 同时收编新旧名）。**不动**：K=5/评测协议（元层）、merkle 口径、交付映射、慢车道语义。

## 应用记录

- 首个应用目标：`库调试v3.2`（L9，安装源 v3.8.1 原版 9d8b3b0d）——应用后文件夹更名 `库调试`。
- 应用前基线（v3.8.2 引擎实测，2026-09-09）：eval n=33 recall@5=0.485 / MRR=0.284（与 2026-09-07 记录完全复现）；17 条 miss 中 17 条 top1=PT-005（高重要度零命中霸榜=P1-6 病灶现行犯）——filter_zero=1 的直接治疗对象。


## 修订记录（2026-09-09 深夜）

**W-1 默认值修订**：law_filter_zero 默认 1→**0**。首应用到库调试v3.2（L9 真实库）实测 recall@5 劣化 0.485→0.394——真实查询存在与金标零词法交叠的召回需求，此前全靠 importance 通道保底，filter_zero 会切断该通道；合成世界未暴露此点（查询从条目派生，词法必然交叠）。修订版锚点 sha256 前 16 位 = c265530d（复测 recall@5=0.485 / MRR=0.284，与基线逐位一致）。filter_zero=1 保留为可选项（MRR 收益真实：0.284→0.394，供接受召回损失的库显式开启）。


## 验收补充（2026-09-09）

- **run-tests 71/71 全绿**（开发环境 cwd = 本文件夹，bootstrap_data 在位）——本项目历史上首次完整自测通过；此前 v3.8.x 谱系因 W-3 缺陷最多只能真实跑完 9 项即静默中止。W-3 修复的验收门正式关闭。
- test_compare（v3.8.2 vs v3.9/W-5，71×2）：判定补丁零回归（失败集完全相同且均为 cwd 环境依赖）。
- 谱系现状：74e5d44d（+W-5）→ d5e4fb77（+E1 共识默认值：law_w_content 1.5 / law_w_imp 0.2082），当前发布= d5e4fb77。

## 修订记录（2026-09-16 · W-9 落地 + 谱系事故登记）

**W-9 判定落地**：law_w_age 默认 **0.1→0.05**。依据：确定性剂量曲线 BMD=0.075，取安全阈值 2/3 留余量；真实库 aging 引信实测 7 天即触发（W-9 报告：`大审查/衔尾蛇/reports/W-9_law_w_age重审_结果报告.md`）。连续运行侧独立实证：w_age=0.1 旧剂量下同仪器老化漂移 −0.0606 recall@5/日（`大审查/衔尾蛇/reports/W-4_首报警案例_判定链-20260916.md` §四）。

**当前发布锚点**：sha256 前 16 位 = **201275cb**（本文件夹 bootstrap_v3.9.py 与 大审查/衔尾蛇/proto/vendor/bootstrap_v3.9.py 逐位一致，T1 入库 commit ea11520，审计复跑复核）。

**谱系事故登记（如实披露，待裁决）**：上节"当前发布= d5e4fb77"自本节起**失效**。201275cb 由 make_v39 从 v3.8.2 快照 + W-1~W-9 补丁链再生成，该补丁链**不含 E1 补丁**——E1 共识的 law_w_imp=0.2082 在再生成中被静默回退为 0.5（W-1 时代值），无 supersede 登记。处置二选一待下圈裁决：①按 E1 验收判据重跑后再应用 0.2082；②正式 supersede E1 参数（记录理由）。在裁决前，**现役引擎参数以 201275cb（w_imp 0.5 / w_age 0.05）为准**；引用 d5e4fb77 的文档（W-5 终稿 §一）以勘误注记为准（见同报告 §五.3）。

## 修订记录（2026-09-16 晚 · E1-R：用户裁决 A，E1 共识再应用落地）

**裁决**：用户令「a！是按你的建议」——选项 A（找回 0.2082）并与 CUSUM 再基线捆绑。
**再验收（判据先行，PASS）**：预注册 `大审查/衔尾蛇/reports/E1-R_再验收预注册-20260916.md`；运行件 `proto/e1r_reaccept.txt`。仪器指纹钉死（适配器+expected集+确定性排序，自测锚点 CUR=0.697 逐位复现）。门 1 held 非劣化 3/3 seed（E1R 0.8/0.8/0.7 vs CUR 0.7/0.8/0.7）；门 2 全量 E1R **0.7273** ≥ CUR 0.697（逐位回到 E1 首部署读数 0.727），MRR 0.554 vs 0.482，配对 bootstrap LB=+0.0000。
**落地**：E1-R 补丁入 make_v39 补丁链（11 补丁全锚点命中），**当前发布锚点 = sha256 前 16 位 25410918**（vendor ≡ 发布区逐位一致），run-tests **71/71**，TUNABLES = {w_kw 3.0, w_content 1.5, **w_imp 0.2082**, w_age 0.05, filter_zero 0}。上节"待裁决"就此关闭：裁决=①，0.5 值退役归档。监控序列换段：eval_history 新段（E1R-0.05，带仪器指纹四元组）自 2026-09-16 起算，旧段（≤0.1 剂量观察窗）封存。

## 修订记录（2026-09-16 深夜 · P-020b：两轴节入 install 模板——家族第二例整改）

**变更**：make_v39 新增 P-020b 补丁——install 模板 AGENTS.md 尾部追加「演化授权（两轴模型 v1）」节（E0-E4 六行，正文与 09-16 手工恢复版逐字一致，去会话标记）。整改对象：昨夜引擎升级按模板重写 库调试工作区/AGENTS.md 时抹除该节（P-020 家族第二例）——此后再生成/重装自带两轴节，手工层不再悬空。
**当前发布锚点 = sha256 前 16 位 56704c90**（12 补丁全锚点命中，vendor ≡ 发布区逐位一致），run-tests **71/71**，TUNABLES 不变（0.2082/0.05 正典延续）。25410918 退役归档（谱系：…→201275cb→25410918→56704c90，后两步均无参数变更、纯工程补丁）。

## 修订记录（2026-09-19 · M3：v3.10 加固波第一批落地——CJK-bigram+平局破序，用户裁决条目 081 选项②）

**变更**：make_v39 新增三补丁——①平局破序（eval/retrieve 双排序点 `(−score, id)`，W-5 残留开口"平局彩票"关闭）；②CJK-bigram 查询分词（`_rank_entries` 查询侧，W-11 达门件 recall 0.7273→0.7879 同日对照）；③新增两测试 t_cjk_bigram_channel / t_tiebreak_deterministic。
**当前发布锚点 = sha256 前 16 位 c2d1f2ea**（16 补丁全锚点命中，vendor ≡ 发布区逐位一致），run-tests **73/73**，参数正典不变。56704c90 退役归档。
**换段**：库调试工作区金标基线重测归档 **recall@5=0.758 / MRR=0.683**（2026-09-19 engine eval；工单预期 0.7879 与实测 1 查询位差已归因=跨日自然老化，同刻对照 S0 0.7273/S2 0.7576 增益真实）；监控序列新段 `E1R-bigram-v310` 起算（指纹增 tokenization 轴），首点 0.7273/0.6768 双跑逐位一致。state.json 经 W-2b 通道同步。Shewhart 激活/件四/件五留 v3.11。实施报告：`大审查/衔尾蛇/reports/M3_W11裁决执行_实施报告-20260919.md`。
