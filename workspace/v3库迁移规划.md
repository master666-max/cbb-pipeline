# 规划 · 根工作区 v4 双库 → v3.2 L9 库迁移（2026-09-06）

> 性质：规划先行，用户批准后执行。这是**对当前活跃库动手术**——全程移动制（铁律 1），每阶段出清单、可回滚。
> 载体：bootstrap_v3.py v3.2.0（锚点 c247f37b…，27/27 断言）。

---

## 一、对象与边界（先划清谁动谁不动）

**动的（旧结构，清除=移动入 `legacy-v4/`，内容一字不丢）**
- `knowledge/`（含 skills/pitfalls/patterns/reflections/decisions/trajectories/references/tools 脚本/_index）
- `AGENTS.md`（旧 v4 会话入口——**移入 legacy-v4，由 v3 渲染版上位**）
- `tools/`、`env/`（v4 体系文件，随 knowledge 语义归 legacy）

**不动的（内容与生产资料，原位保留）**
- `archive/`（工件库，D-001「引用不搬移」，v3 README 声明其为历史工件区）
- `soul/`（SOUL.md/USER.md——人格与画像，v3 无对应目录，原位保留并写入入口惯例）
- `workspace/`（本会话热区：v3-dev、v21-rev、各工单——迁移后按 wrap-up 归档纪律另行处理）
- `library-bootstrap-v3.0/`、各 legacy 包、`实验性双库工作区/`（D-002 参照档案）
- C 盘 auto-memory（不在工作区，天然不动）

## 二、六阶段执行序

### 阶段 0 · 盘点与预检（不动任何文件）
- 全目录清点（文件数/字节数），生成移动清单预览
- 检测根库有无 lifelog 链（预期：v4 无 L8 安装 → 无链可续，audit 从 genesis 起，v4 时代审计历史不可考——**如实声明**）
- 确认 `bootstrap_data/bodies.json` 在位（absorb 已抽 131 体，源树移走后不再依赖）

### 阶段 1 · 结构清除（移动制，一次清单）
- `knowledge/`、`AGENTS.md`、`tools/`、`env/` → `legacy-v4/`（**逐目录移动，输出清单**，内容原样）
- 移动后根目录只剩：archive/、soul/、workspace/、legacy-v4/、各包与文档

### 阶段 2 · 建立 v3.2 L9 新库
- `py -X utf8 bootstrap_v3.py install L9 --yes --target .`（**慢车道：宪章+BCA 公理先展示确认**，--yes 即你的批准）
- 产物：v3 渲染 AGENTS.md（新会话入口）、memory/intermediate|longterm|attic、audit/（genesis 起链）、ledger/changes.jsonl、evals/、skills/、spec.md、citations.md、modules/packs/presets/references（131 内容体交付）+ L8 守卫段注入 + referee 落盘
- Merkle 根抄录 RELEASE/人侧记录

### 阶段 3 · 旧库内容迁入（写一次性迁移器 migration_v4_to_v3.py，放 workspace/v3-dev/）
- **语义条目**：pitfalls(P-001~006)/patterns(PT-001~007)/reflections(R-001~006)/decisions(D-001~004) 逐条 → JSON 十字段（id 沿用原编号、keywords 从关键词行、links 从 [[]] 提取、content=正文、importance：active=7/superseded=3、confidence、source_event_id="genesis-v4"）
- **情景轨迹**：trajectories/task-001~015 → 整体保留于 legacy-v4，**每条建一个索引条目**（content=一句话摘要+路径指针，importance=3）——全文转 JSON 无收益
- **soul**：原位保留，AGENTS.md 无法覆盖人格加载 → 在 memory/longterm 建一条「人格加载惯例」指针条目（importance=9）
- **本会话工作入库**（用户指令 1 的整理部分）：v3 重构系列关键经验（审查链收敛于工件、verify 三态、单文件边界诚实声明等）以 3~5 条新 JSON 条目 append；轨迹 task-010~015 建索引条目
- 迁移断言：条目数守恒（源条目数=落库数）、ID 无重、每条 source_event_id 可溯

### 阶段 4 · 测试与验收全跑
- `run-tests` 27/27（包级）
- 新库冒烟：engine retrieve 抽样 5 条（含「Windows py 启动器」「审查链收敛」等）验证迁移内容可召回；wrapup 入链；retire 空转不误伤；Merkle 根对账
- verify 网络窗口允许则跑（题录 24 条）

### 阶段 5 · 收尾
- wrap-up：workspace 临时文件清单化（patch_content2.txt、_zhipu_review*.txt 等→留档或 attic）
- v3.3-1 工单（迁移过程问题）/ v3.3-2 工单（质量监测发现）→ 统合 v3.3 工单
- 轨迹 task-016 + memory 更新

## 三、质量监测框架（贯穿全程 → v3.3-2 工单素材）
- 每阶段断言：文件数守恒表、哈希抽查（迁移前后各抽 10 文件）、audit 链逐行校验、changes.jsonl 动作齐全
- 监测点：install 对非空目标的行为、内容体与 legacy-v4 的路径冲突、JSON 转换的字段丢失、中文/特殊字符、AGENTS.md 切换对 harness 的影响、Merkle 根可复算性
- 所有异常按「建库 bug / 库结构 bug / 运行 bug / 迁移器 bug」四类登记

## 四、裁决点（批准时一并拍板，否则按默认）

| # | 裁决 | 默认 |
|---|------|------|
| M1 | 旧结构处置：整体移 legacy-v4/（推荐，一次可逆）vs 逐条 attic | 整体移 legacy-v4/ |
| M2 | archive/：原位保留（D-001）vs 移入 legacy-v4 | 原位保留 |
| M3 | soul/：原位保留+指针条目 vs 并入 v3 结构 | 原位保留+指针条目 |
| M4 | AGENTS.md：v3 渲染版上位（旧版入 legacy-v4）——**会话入口更换，影响后续所有会话** | 上位（本任务必经） |
| M5 | trajectories：整文件留 legacy-v4+索引条目 vs 全文转 JSON | 索引条目 |
| M6 | 迁移器归宿：workspace/v3-dev/ 一次性工具 vs 收编进 bootstrap_v3.py | 一次性工具（不污染发布包） |

## 五、风险声明（如实）
1. **本会话正在根库上运行**：AGENTS.md 切换后，本会话的后续指令仍按旧上下文走；新入口对会话的影响从**下一个会话**开始显形——这是 C-000 类「文档滞后」的活例，v3.3-2 会监测。
2. v3 库结构无 archive/soul 概念，二者以「原位保留+声明」方式共存，属已知的形态妥协（C-000 的 b 残余）。
3. v4 的 tools/search_knowledge 等被 legacy-v4 化后，旧 AGENTS.md 引用全部断链——预期内（新入口用 engine retrieve）。
4. 规模：约 1~1.5 会话。
