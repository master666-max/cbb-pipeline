# v3.3 统合工单 —— v3.3-1（建库/迁移发现）+ v3.3-2（质量监测）+ 历史候选

> 统合来源：workspace/v33-1工单.md（W1~W7）+ workspace/v33-2工单.md（M-01~06）+ 历史候选（v3.2 遗留 C-009、真实库 migrate、guard 锚定深化）。
> 目标版本：v3.3.0 ｜ 执行载体：bootstrap_v3.py ｜ 规模：约 1 会话。

---

## 第 1 章 P0（语义正确性，发布门会红）

### Y-001（=W5）写操作后刷新 Merkle 根
- append/retire 成功后重算 `merkle_root(exclude={"state.json"})` 写回 state.merkle_root（shadow 不写，保持公理 G 零状态）。修复后「校验模式根不一致」只对真篡改报警。
- 回归：append→校验模式根一致；retire→根一致。

### Y-002（=W2+W3）C-001 检查顺序 + DATA_DIR 寻址
- bodies 空检查移到幂等校验分支之后；DATA_DIR 增加「--data 参数 > 环境变量 > state.json 同目录回退」寻址。
- 回归：无 bootstrap_data 的 cwd 对已装库重跑 install 走校验模式。

### Y-003（=W4）校验模式「未在册」口径收窄
- 排除运行时面（memory/audit/ledger/evals/state.json）与渲染产物（AGENTS.md/skills/README/citations/spec/速查——它们有「手改即违例」自己的执法语义）；未在册检查只覆盖内容体目录。
- 回归：迁入库重跑校验模式零误报；真塞外来文件仍报。

## 第 2 章 P1（功能补全）

### Y-004（=W1）函数层 Path/类型契约统一
- 全函数扫描 lib/src/target 入参统一 Path 化；回归：str 入参直调 engine append 不崩。

### Y-005 C-009 检索评测吸收（v3.2 唯一遗留）
- golden 33 条（workspace/v21-rev/golden_queries.json）→ evals/ 评测脚本 `engine eval --lib .`，输出 recall@5/MRR 基线并入库记录；建立 v3 检索基线（对齐 migrate parity 门）。

### Y-006 guard 锚定深化
- `guard --anchor <file>`：读人侧锚定表（文件名→sha256），对基准黑名单文件逐个对账，输出 match/mismatch；补「锚定表生成器」`guard --snapshot` 导出当前基准哈希供人侧保管。

### Y-007（=M-02）反思触发的批量导入区分
- importance_accum 记录来源计数；wrapup 触发提示标注「批量导入累积」或阈值按来源上浮（常量化，标注待校准）。

## 第 3 章 P2（治理与测试面）

### Y-008（=W6）源库 PT-006 空洞登记
- legacy-v4 侧登记 void 不补录；根工作区 knowledge/patterns.md 补一行 void 注记（新建注记文件，不改原文件——铁律 1）。

### Y-009（=M-06）测试扩展清单
- 大条目 append（>1MB）、中断恢复（模拟 append 崩溃后的链与账本一致性）、citations/REGISTRY 漂移检测（渲染产物手改报警）、changes.jsonl 与 audit 行数守恒断言。

### Y-010（=M-03）根重算性能登记
- 条目数百规模内 Merkle 全树重算可接受；超 500 条目触发「增量哈希树」评估（雷达式触发器，TUNABLES 登记不动手）。

## 第 4 章 执行序与发布门

1. P0 三条（Y-001~003）+ 回归断言（预计 +4 条测试）
2. P1 四条（Y-004~007）
3. P2 三条（Y-008~010，Y-008 是根库侧一次性行为）
4. 发布门：run-tests 全绿（预计 27→33 条）+ verify 网络窗口 + 库调试v3.2 重跑校验模式零误报（**用真实迁移库当回归场**）
5. 真实存量库 migrate（v2.x → v3）：仍是用户级动作，Y-001~003 修复后其根对账语义才可信——**建议 v3.3 发布后执行**
