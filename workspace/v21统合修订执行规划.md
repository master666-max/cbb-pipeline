# v2.1 统合修订执行规划书

> 2026-09-06 起草。**只规划，经批准后执行。**
> 修订对象：《统合修订工单_library-bootstrap_v2.1_2026-09-06.docx》（38 条工单，P0×16 / P1×10 / P2×12，附 5 个执行附录）。
> 修订载体有两个，必须同步：①《说明书》文档本体（v2.0.2 → v2.1，docx+pdf）；②library-bootstrap 源树与发布包（脚本/模板/测试，v2.0.2 → v2.1）。
> 执行纪律沿用工单规定：先 P0 后 P1 再 P2 不许跳级；W-016 是 v2.1 发布门槛；每条工单完成即在变更记录登记工单号与验收证据；bug 回流为回归断言（PT-005 纪律）。

---

## 一、工单性质拆解：38 条落在哪三个层面

| 层面 | 工单 | 改什么 |
|---|---|---|
| A. 纯文档层（只改说明书） | W-001~009、011（声明部分）、015、021、032~036（成文部分） | 章节、表格、口径、措辞 |
| B. 源树代码/模板层（改 library-bootstrap + 发布包） | W-010（SHA-256 三脚本迁移）、W-012（lint 合计检查）、W-013（打包黑名单+铁律6）、W-014（schema_version）、W-017（Δ 映射脚本）、W-019（仲裁表）、W-020（打分公式）、W-022（评测集）、W-023（lint 五项）、W-024（token 台账）、W-025（归因+豁免）、W-028~031、W-037（T18 判据）、W-038（PENDING 出路） | 脚本、模板、run_tests 回归断言 |
| C. 需要真实运行数据层 | W-016（A1/A2/A3 验收：两臂各 40 会话 + 30 条强势错误用例）、W-022 基线运行、W-026 统计口径落地 | 跨多会话收集，受体验账号额度约束 |

关键判断：约 20 条可纯案头完成；约 10 条要写代码+回归测试；2 条（W-016/W-022）需要真实运行数据，是节奏瓶颈。

## 二、执行波次（4 波）

### 波次 0 · 基线冻结与准备（半天）
1. 说明书 v2.0.2 当前版（docx/pdf）复制进 attic/ 留底；生成工作目录 workspace/v21-rev/。
2. 从工单 38 条建「修订台账」表格（编号/状态/验收证据），每完成一条登记一行——这张表本身就是 W-006 要求的断言明细表的前身。
3. 逐条把工单的「验收判据」预填进台账，形成执行清单。

### 波次 1 · P0 文档自洽批（W-001~015，15×S，预计 1-2 个会话）
按依赖顺序执行：W-001（断言矩阵重排，先定 T26-T29 草案）→ W-006（断言对账，等 W-001）→ W-002（交叉引用校验，**顺手实现引用闭包检查脚本**，登记 PT-006，后续所有文档修订都靠它兜底）→ W-003~005、007~009（口径/编号/计数五连）→ W-010（SHA-256：源树三脚本替换 + 附录 E 迁移步骤 + T11 改造 + T30 新增）→ W-011（威胁模型三档 + 外部锚定通道）→ W-012（lint 合计检查 + T32）→ W-013（铁律 6 + 打包黑名单 + T18 扩展）→ W-014（schema_version 第七字段 + T33）→ W-015（小样本修辞 + lint 文风检查）。
- 本波新增回归断言：T30-T33。
- 本波产物：说明书 v2.1 草稿（P0 部分修订完）、源树 SHA-256 版、lint 扩展版。

### 波次 2 · W-016 发布门槛（二选一，见裁决点 1）
- **路线 B（建议）**：L9 从超绝统合版移出，标「实验包（未验收）」，统合版降为 L0-L8 三合一；L9 独立分包保留。文档 11.6 节如实记录「验收未运行，降级处理」。半天完成，v2.1 可发布。
- **路线 A**：按附录 A 预注册协议跑真验收——先协议入归档，再收集两臂各 40 会话 + A2 前后各 20 + A3 用例 30 条。L 级，需 120+ 会话的跨轮收集，**体验账号额度下预计需数周**。结果无论好坏写入 11.6 节。
- 也可 B 先行发 v2.1，A 作为 v2.2 的长期任务并行收集（验收数据攒够后 L9 升回统合版）——**这是我推荐的混合路线**。

### 波次 3 · P1 机制规格批（W-017~026，10 条，预计 2-4 个会话）
关键路径：**W-022 评测集先行**（golden query 30-50 条，从 usage.log/lifelog 回放构造，附录 C 规程）——它是 W-018/W-020 的依赖，也是 P2 的总闸门。然后：W-017（Δ 六类事件映射表脚本化 + T34 可复现断言）→ W-018（λ 双速率叙述修正）→ W-019（arousal 仲裁表 + T35）→ W-020（GWT 打分公式 0.6/0.3/0.1 + T36）→ W-021（L9 模块清单表：**实测源树 experimental-l9 为 8 个模块目录，建议按"补清单"路线列全八件**）→ W-023（lint 五项扩展，附录 D 蓝本）→ W-024（token 台账 + T38）→ W-025（归因修正 + 类型豁免 + T39/T40）→ W-026（A1 统计口径：若波次 2 走路线 B，本条落地协议与指标定义，运行留待验收数据收集）。

### 波次 4 · P2 架构演进批（W-027~038，12 条，预计 4-6 个会话，可拆到 v2.2）
依赖 W-022 基线。其中源树实装类：W-027（真 PPR，先挂 experimental 对比评测，胜出才转正）、W-028（邻居注记 + T41）、W-029（边级双时态 + T42）、W-030（辩论改法庭 + 选拔实测化 + T43）、W-031（整合三档 + T44）、W-037（T18 白名单判据 + 双对照）、W-038（PENDING 三出路 + T47）。文档类：W-032（git 关系）、W-033（目录总图 + 降级矩阵两附录）、W-034（备份灾恢）、W-035（中文匹配策略 + T45）、W-036（时间窗裁决表 + T46）。
**本波建议整体作为 v2.2 范围**（见裁决点 3），v2.1 只在波次 1-3 完成后发布。

## 三、修订台账与证据纪律
- workspace/v21-rev/REVISION-LEDGER.md：38 行工单台账（编号/波次/状态/验收证据/变更说明），完成一条登记一条——同时满足工单第 0 章「变更记录」要求与 W-006 断言明细表要求。
- 每条源树层工单完成即跑 run_tests 全套；每新增断言即回流为常驻回归。
- 说明书每次修订跑「引用闭包检查」（W-002 产出的 PT-006）+ postcheck + verify，全过才落版。

## 四、风险与对策
| 风险 | 对策 |
|---|---|
| 说明书改动面大（约 40 处），拼装笔误风险高（前车之鉴：require(( 事故） | 引用闭包脚本先行（W-002 提前实现）；每轮修订后 postcheck+verify+抽页渲染 |
| SHA-256 迁移影响已装库（实验性记忆库工作区的 v2.0.2 库） | 严格按附录 E 六步：旧 manifest 存 .sha1.bak、diff 只做同算法内部比对、迁移幂等（T30）；迁移脚本随 v2.1 包分发 |
| 体验账号 MCP 额度有限 | 波次 1-3 纯案头+本地脚本为主，几乎不耗 MCP；W-016 路线 A 与 W-022 的会话回放量大，建议走降级路线 B 避开 |
| L9 降级影响四轨发布语义 | 发布包 README 与提示词速查同步改档位说明；L9 实验包独立标注「未验收」 |
| 断言总数膨胀后 W-006 对账困难 | 断言明细表与 run_tests 同源维护（断言表驱动测试），不再手写对账 |

## 五、产物清单（v2.1 发布时）
1. 《说明书 v2.1》docx+pdf（38 条全部落版或如实标注状态）
2. 源树 v2.1（knowledge/skills/library-bootstrap）：SHA-256、lint 扩展、Δ 映射、仲裁表、打分公式、schema v3.1（七字段）、打包黑名单、评测集
3. 发布包四轨 v2.1（含 L9 实验包独立标注，若走路线 B）
4. REVISION-LEDGER（38 条台账）+ 说明书引用闭包检查脚本（PT-006）+ run_tests（T26-T48 新增约 20 断言）
5. A1/A2/A3 预注册协议归档件（无论是否本轮运行，协议先入档——预注册纪律）

## 六、工作量与节奏建议（按体验账号约束）
- 波次 0+1：1-2 个会话（纯案头+本地脚本）
- 波次 2 路线 B：0.5 个会话；路线 A：数周跨轮收集
- 波次 3：2-4 个会话（评测集构造最重）
- 波次 4（若纳入 v2.2）：4-6 个会话
- **建议本次执行范围：波次 0-3 → 发布 v2.1；波次 4 列入 v2.2 待办。**

## 七、【待确认】清单
1. **W-016 路线**：B（L9 降级为实验包，先发 v2.1）/ A（跑满 120+ 会话真验收）/ 混合（B 先发，A 长期收集后升回）。**建议混合**。
2. **P2 是否纳入本轮**：建议 v2.1 只做 P0+P1，P2 排 v2.2（工单自身也说 P2 依赖 W-022 基线，且 12 条里一半是架构级改动）。
3. **W-004 宪章条数**：方案 A（补第六条成六条，工单推荐）/ 方案 B（改写 10.3 保持五条）。建议 A——10.3 的实际援引内容（主权边界：不改用户数据）本就该是宪章级条款。
4. **W-021 八件套**：实测源树为 8 模块（constitution-bca/instrumentation/emotion/goalstack/dual-memory/gwt/sleep-pipeline/dmn），建议按"补清单表"路线把八件列全（工单允许的两个选项中更诚实的那个）。
5. **铁律 6 措辞**（W-013）：拟「打包清单硬排除——soul/、state/、USER.md、env/ 及一切含用户运行时数据的路径，禁止进入任何分发包」，直接用工单原文。
