# 合成自测 · 手算标准答案（阶段2 自测门）

> 手算人：总分析师（不经任何脚本生成；expected.json 为本页手算结果的誊写）。
> 判定方式：`py -X utf8 sandbox/run_selftest.txt` 逐键比对 analyze.txt 输出与 expected.json（字符串级比对，1 与 1.0 视为不同）。

## 用例 S1（曲线/斜率/采纳）— sandbox/synth/s1.jsonl
- **A**：rounds 1-5，scores 0.1→0.5。
  - 斜率手算：x̄=3, ȳ=0.3；Σ(dx·dy)=(-2)(-0.2)+(-1)(-0.1)+0+(1)(0.1)+(2)(0.2)=0.4+0.1+0+0.1+0.4=**1.0**；Σdx²=4+1+0+1+4=**10**；slope=1.0/10=**0.1**。
  - delta=0.5−0.1=**0.4**；采纳行 5 行，>0 的代=第2/4/5 代共 **3** → rate=3/5=**0.6**；均值=(0+1+0+1+1)/5=**0.6**；missing=0/5=**0.0**。
- **B**：rounds 1-4 全 0.5。cov=0 → slope=**0.0**（非 null，var>0）；delta=**0.0**；采纳 4 行全 0 → rate=**0.0**、均值 **0.0**。
- 源汇总：runs=2、rows=9、有曲线(≥2点)=2、slopes=[0.1,0.0]→mean **0.05**/min 0.0/max 0.1、rate 均值 (0.6+0)/2=**0.3**、触发 0。

## 用例 S2（漂移检出/护栏触发）— sandbox/synth/s2.jsonl
- **C**：3 行中 2 行 drift=true（round 3.8、9.4）→ detection_rounds=[3.8,9.4]、first=**3.8**、triggers=**2**；曲线仅 1 点(1.0,0.1) → delta/slope=null；missing=2/3=**0.666667**。
- **D**：单行 drift=true round 2.0 → det=[2.0]、first=**2.0**、triggers=**1**、missing=**1.0**。
- **E**：终态行（round=null, gs=0.9）→ 无曲线、triggers=0、missing=0.0。
- 源汇总：runs=3、rows=5、有曲线 0（C 只有 1 点）、slopes n=0 全 null、rate 均值 null、**total_triggers=3**。

## 用例 S3（终态行/缺值/同round双值）— sandbox/synth/s3.jsonl
- **F**：rounds 1-3 (0.2,0.4,0.6)+终态行(null,null)。x̄=2,ȳ=0.4；Σdxdy=(-1)(-0.2)+0+(1)(0.2)=**0.4**；Σdx²=2 → slope=**0.2**；delta=**0.4**；missing=1/4=**0.25**；无采纳行→rate/mean=null。
- **G**：仅终态行 gs=0.9 → 无曲线；missing=0.0（gs 非空）。
- **H**：同 round=1 两值 0.1/0.3（两行都保留）→ curve=[[1,0.1],[1,0.3]]、first=0.1、last=0.3、delta=**0.2**；n=2<3 → slope=**null**。
- 源汇总：runs=3、rows=4+1+2=**7**、有曲线=F、H 共 **2**、slopes n=1 mean=min=max=**0.2**、rate 均值 null、触发 0。

## 判定记录
- 2026-09-09 第一轮：**PASS**（`py -X utf8 sandbox/run_selftest.txt` 输出 "SELFTEST PASS: analyze.txt 输出与手算 expected.json 完全一致(字符串级)"，8 个 run × 16 指标键 + 3 个源汇总全部逐位一致；含 1 vs 1.0 类型敏感比对）。
