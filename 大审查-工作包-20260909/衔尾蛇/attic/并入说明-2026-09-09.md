# 并入说明 · 衔尾蛇副本 → attic（2026-09-09）

## 发生了什么

- **操作**：根级目录 `衔尾蛇副本/` 整体移入本 attic，更名为 `衔尾蛇副本-冻结快照-20260909/`。用户指令：「把衔尾蛇副本并入衔尾蛇文件夹内，并留下说明」。
- **执行者**：ZCode（考古会话）。工具：`robocopy /MOVE /E`（Windows 原生，Unicode 路径）。
- **规模**：40,800 文件 / 35.46 MB / 10,327 目录，0 失败。

## 完整性验证（移动后实测）

- 目的地文件计数：`find … -type f | wc -l` = **40,800**，与移动前基线（workspace-audit/data/files.jsonl 中 `衔尾蛇副本/` 计 40,800 条）一致。
- 哈希抽查：随机 8 文件（seed=7）逐一 sha256 对照阶段0 基线，**8/8 一致**（脚本 `workspace-audit/tools/verify_move.txt`，2026-09-09 运行）。
- **全量对账（2026-09-09 追加，应用户"确保完全并入"指令）**：`workspace-audit/tools/verify_move_full.txt` 对基线全部 40,800 条逐一 sha256 核对——**一致 40,800 / 缺失 0 / 哈希不符 0 / IO错误 0，反向多出 0**。并入完备性为穷尽验证，非抽样。
- **源目录已删除**：robocopy /MOVE 在复制完成后即删除源 `衔尾蛇副本/`；2026-09-09 复核根级无残留。基线 commit `77fb1a0` 的 git 对象库中仍保留全部文件原样记录，可随时重建。

## 这个快照是什么

即原 `衔尾蛇副本/副本说明.md`（随目录保留在快照内）所述：`大审查/衔尾蛇/`（本实验正本）2026-09-09 的完整冻结副本，为「大审查工作区移交他方 AI 使用期间防误动」而预留。其未竟事项快照（v3.9 工单 W-1~W-6 执行被移交中断、test_compare_v39 跑一半取消、轨迹撞号【待确认】）以该文件为准。

## 与正本的关系

- **权威版本仍是 `大审查/衔尾蛇/` 正本**（快照说明原文立场，用户并入指令不改变此关系——并入只为目录整洁，快照不回灌）。
- 快照与正本大量文件同哈希（阶段0 精确重复表可证）；正本在冻结时点之后若有演进，以正本为准。
- 注意：本 attic 现位于 `大审查/` 嵌套 git 仓库工作树内，**未加入其版本控制**（untracked）；是否 commit 由正本仓库管理者裁决。

## 回滚方式

整个目录 `mv` 回根级并更名 `衔尾蛇副本` 即可完全还原；此外考古基线（根仓库 commit `77fb1a0`）亦含全部 40,800 文件的原样记录。
