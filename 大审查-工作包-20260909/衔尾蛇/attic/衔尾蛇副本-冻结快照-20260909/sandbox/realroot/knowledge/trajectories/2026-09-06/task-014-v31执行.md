# 任务 014 · v3.1 执行（V-001~016，2026-09-06，只追加）

## 依据
- 工单：workspace/v31修复工单.md（P0 V-001~004 / P1 V-005~009 / P2 V-010~016），用户指令「开干v3.1」。

## 执行记录
- 全部 16 条落地（P2 未降版：retire/幂等均做完）。测试 15→19 条（新增 delivers/verify_offline/bad_ts/retire，加强 tamper 对账重算与 smoke merkle 断言）。
- **首跑抓 bug（PT-005 再实锤）**：
  ① `t_verify_offline_honest` 的 `os.chdir(tmp)` 未恢复，污染后续三条测试的相对路径（交付 0 个/absorb_mapping 找不到 bodies）——chdir 保存/恢复修复。教训：测试内改全局状态必须对称恢复。
  ② `--out` 挂错解析器：原骨架 `s = add_parser("run-tests"); add_parser("build")` 连环赋值延续，`--out` 加到了 run-tests 上——拆行修复。
  ③ 陈旧数据型失败：level_mapping.json 是 v3.0 absorb 产物，重跑 absorb 后绿——「测试失败先查测试依赖的生成物版本」。
- **verify 三态语义首次实战生效**：本轮网络受限，14 条题录如实报「未对账，不计入通过」——不再出现 v3.0 时代「离线假零不一致」的误导。发布记录如实披露，网络恢复后补跑留痕。
- **发布**：根目录 library-bootstrap-v3.0/ 原地更新至 v3.1.0；包锚点 sha256=f03aa37ef91024fa0377d865c5acbfe4d1442b2f4699a4d6623d6b59936f41c4；发布目录原地 run-tests 19/19；RELEASE.md 重写。

## 待办
- 网络恢复后补跑 verify 留痕（14 条 match 预期）。**补跑尝试已做（2026-09-06 稍晚）**：两次均 14 error，直连诊断 http/https 均 WinError 10054（远端重置）——网络中间层临时阻断 export.arxiv.org，非包缺陷；v3.0 时点 14/14 零不一致留痕仍有效，RELEASE.md 已追加补跑记录。
- 真实存量库 migrate（用户级动作，写 audit/ 前出清单）。
- v3.2 候选：install 漂移对账的「等用户裁决」交互流、retrieve 语义层（TF-IDF 兜底）、x-game 等 6 新增包的模板建设。
