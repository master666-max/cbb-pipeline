# 任务 018 · v3.4 执行（H-001~007）+ 回归场终验（2026-09-07，只追加）

## 执行记录
- 测试 36→42 条全绿。落地：H-001 build-docs --lib 渲染件升级通道；H-002/D1=a 宿主痕迹分层（环境痕迹行实战）；H-003 engine reflect 取材打包器（单文件边界诚实形态）；H-004 absorb-md 收编（--dry-run 守恒+缺文件容错登记）；H-005 golden 随包+eval 增强；H-006 doctor 三方对账；H-007 runtime_ignore_dirs。H-008 TF-IDF 登记不做（44<触发器 100）；H-009/H-010 登记。
- **patch 工程教训（本轮密集出现）**：①patch 脚本把所有 rep 攒到最后一次 write——中途 assert 失败=全部修改静默丢弃（patch B 教训）→ b2 改为幂等重放（if 条件防重复），但 if 条件被测试 fixture 的同款字样误判（第二次教训：幂等判据要用唯一锚点）；②heredoc 转义地狱（\n 三层解析）→ 改用 Write patch 文件+Read/Edit 工具原文传递；③构造行漏引号自造 SyntaxError→先 Read 看实际状态再修，不猜。
- **回归场终验（库调试v3.2）**：升级通道实战（build-docs --lib 刷 AGENTS/README，且补 Y-001 时序：渲染刷新也是写操作）；**eval 全集 recall@5=0.485 / MRR=0.284 / misses 17——首份真实基线入库，高于 v2.x 参照线 0.455**；reflect 素材包实战；校验模式：漂移 1（版本演进预期）/未在册 0/渲染件 0/根一致/环境痕迹行。
- 发布：锚点 63b4d054a19ce876…；RELEASE 追加；发布目录原地 42/42。

## 待办（v3.5 候选）
- experimental-l8/l9 内容体 v2.x 路径指南的渲染化对齐（C-000 b 残余最后一块）；
- reflect 素材包的 harness 侧合成提示词内嵌（同 referee 形态）；
- 漂移 1 的处置（版本演进内容体重交付）；
- 下会话验证 v3 入口三级加载（M-04）。
