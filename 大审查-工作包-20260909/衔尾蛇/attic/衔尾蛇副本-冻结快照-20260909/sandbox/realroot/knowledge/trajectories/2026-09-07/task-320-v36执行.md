# 任务 020 · v3.6 执行（K-001~006 + K-007a）+ 回归场（2026-09-07，只追加）

## 执行记录
- 测试 51→57 条全绿。落地：K-001 absorb-md 幂等三态（增量迁移语义成立）；K-002 doctor 审计链逐行重算（v2.x T11 防线归位，篡改精确报行）；K-003 engine reflect-done（消灭绕账本行为，素材尾注/渲染版双改）；K-004 入口引用修正；K-005 build-docs --lib 友好退出；K-006 素材包同秒序号；K-007a(D1=a) 试点渲染化三 install.md（constitution/lifelog/mutation-engine）。
- **回归场（库调试v3.2）**：doctor 四态全绿（memory 42/audit 42/ledger 42/审计链连续）；reflect-done 实战；渲染件 0/未在册 0/根一致。
- **执行中虫**：①heredoc 转义第四次咬人（\\\\n 在 rep 锚点）→ 全面弃 heredoc；②fixture 断言数未随容错跳过调整；③旧测试 t_experimental_adapter_page 取到试点文件（预期行为）→ 排除试点；④f-string `\{` 无效转义警告。⑤新抓 v3.7 候选：--redeliver 的重写逻辑不认识试点渲染化（对 PILOT 文件按 v2.x 原文重写）→ 应复用 _deliver 单文件路径。
- 发布：锚点 2d2ecb6dfb9ea832…；RELEASE 追加；发布目录原地 57/57。

## 待办（v3.7 候选）
- --redeliver 复用 _deliver 单文件路径（试点渲染化/适配页一致）；
- K-007b experimental 全量渲染化（试点实战一代后）；
- 双库同步（随 M-4）、M-04 入口验证（下会话）。
