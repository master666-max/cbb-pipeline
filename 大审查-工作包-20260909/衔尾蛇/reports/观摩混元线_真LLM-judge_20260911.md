# 观摩记录 · 混元线真 LLM-judge 结果（2026-09-11 凌晨 · ZCode 只读观摩）

> **性质**：应指令观摩隔壁混元线"用真实 LLM 跑的结果"。只读，零写入混元目录（其 E150 res 文件仍在落盘中）。
> 观摩对象：`混元/LLM-judge接入/`（最新时间戳 2026-09-11 01:04）。

## 一、观摩到的核心结果

### E149 · 真 LLM judge 驱动演化的最小闭环——不可行，且失败方式极有价值

- 配置：M-Prometheus-14B（本地 LM Studio，temp=0，Qwen2.5-14B 底座 + 48 万条 judge 特训，[arXiv:2504.04953](https://arxiv.org/abs/2504.04953)）驱动法层参数演化，n=6 × 24 代，真值=语义世界构造标签（非 LLM 判断）。
- **真值增益 −0.0988 ± 0.0141（t=−7.00，0/6 正）**；judge 自报增益 +0.2142；**自报-真实差 +0.3129/24 代**——Goodhart 动力学在真 LLM judge 上**自发涌现**。E14/E85 两个玩具世界警告第一次有了真 LLM 实例与量级。
- 机制（其报告标注置信度）：演化找到 judge 偏好但真值不喜欢的区域（高置信）；[RESULT]1-5 量化平台 + 噪声门 = 免费采纳坏变动（中置信）；搜索空间仅 110 个 (config,query) 对（实锤——负结果不可外推到大空间）。

### E150 · 抗 Goodhart 接线（rubric 锚定 vs absolute 重跑）——两臂已出数

| 臂 | 真值增益 | 自报-真实差 |
|---|---|---|
| E150b absolute（=E149 同管线复跑 + 取证补课） | −0.0988（t=−7.00，0/6 正） | +0.3129（逐位复现——缓存确定性验证） |
| **E150a rubric 锚定**（评分标准显式锁"只看主题相关性+内容正确性，风格一律不影响"） | **−0.0225（t=−1.89，1/6 正）** | **+0.0583（压缩 81%）** |

按其锁定判据初判：rubric 臂配对改善方向明确、自报-真实差大幅压缩——**"盲评/剥除是唯一有效解药"（混元 E127）获得真 LLM 版确认**。正式判定待其本线出报告。

### 试点（P1–P5）与工程发现

- P2 辨别力 +0.6783（t=13.68）决定性；P3 JSON 服从 100%；**位置偏见实测**：同三段文本排列翻转 → 评分 0.85→0.2 崩塌（确定性，非抖动）——canonical order 修复已入管线；**这是玩具确定性 judge 上不存在的全新操纵面**。
- temp=0 不保证逐字确定（批处理非确定）→ 缓存需模糊命中。
- 吞吐 3.52 calls/s → 全量 47h，三个压缩杠杆立项（成对比较省 40%、稳定缓存、提并发）。

## 二、对衔尾蛇线边界体系的影响（矩阵增量）

1. **边界③（可写性×剥除覆盖）获得真 LLM 级确认**：E149（absolute，伪通道大开，Goodhart +0.31）vs E150a（rubric 锚定=对 judge 剥除风格通道，Goodhart 压缩 81%）——F10 修订版边界③的"剥除覆盖"半边从玩具证据升级为真 LLM 证据。混元 E127（玩具盲评）→ E150（真 LLM rubric）构成同一边界的两级阶梯。
2. **偏见账本首个真 LLM 数字**：+0.3129/24 代（M-Prometheus-14B absolute 模式）——锚定审计三条件的"标定/效力"侧从此有真 LLM 参照量级。
3. **新操纵面入册**：judge 位置偏见（确定性、可修复但必须修）——加入"查询构造假象检查单"家族：评测管道的顺序规范化与词面交叠率、温度确定性并列。
4. **与衔尾蛇线的连接**：E149 的"绝对评分被对抗性利用"正是锚定审计"分数永不外报、只许比较"红线的真 LLM 版理由；E150a 的 rubric 锚定 = 锚定的真 LLM 形态——**锚定审计三条件（独立+标定+效力）全部获得真 LLM 语境的对应物**。

## 三、登记

- 矩阵边界③行补注（真 LLM 确认）；本线对混元 40 轮口径的引用不变。
- 后续观摩点：E150 的取证分析（哪些文本被顶上来）、第 37 轮成对比较式 judge、Qwen3-30B 对照的膨胀系数普适性检验。
