# 第六轮 · X9 碰撞面普查与危险切分预报器 —— 预注册（判据先冻结，执行前文档）

> 日期：2026-09-10 · 地盘：`衔尾蛇/` · 前置：X8 归因（K1 前缀碰撞 × 权重膨胀 × 切分定位；丢失任务全 K1）
> 本轮为**测量线**（总法则：发明新启发式 0/5，测量已有可观测量 3/3+）：零新机制，只普查、只预报。文献检索按用户指令嵌入实验循环（设计期 + 演化运行期两次触点，记录见报告）。

## 一、已测结构（执行前事实，世界构造的确定性推论）

- 关键词 K0–K19，每词恰 4 条目。**唯一子串毒关键词 K1**（⊂ K10–K19，10 个孩子）→ K1 查询的 kw 碰撞面 = 11 词 × 4 条目 = **44 条目**（待脚本实测对账）。
- 主题 T0–T15，每题 5 条目。**唯一子串毒主题 T1**（⊂ T10–T15，6 个孩子）→ 内容侧碰撞面待实测（上限 7 题 × 5 = 35）。
- 其余关键词/主题均无子串孩子（无 K20+，无 T16+）。

## 二、L1 暴露表（演化前即可计算，已冻结；audit 含毒任务数）

| 组 | 种子: audit_K1 / audit_T1 |
|---|---|
| 回溯（1–8，X7/X8 数据） | s2: 0/1 · **s3: 1/1** · **s5: 1/1** · **s8: 2/1**（其余 0/0） |
| 前瞻（9–24，本轮新跑） | s9: 0/1 · s11: 1/0 · s12: 0/1 · **s13: 3/1** · s14: 0/2 · s18: 2/0 · s19: 1/1 · s20: 1/0 · s21: 0/1 · s24: 0/1（其余 0/0） |

## 三、预报器（先冻结，再揭示）

- **L1（纯演化前）**：该种子 audit 含 ≥1 个毒任务（golden kw=K1 或 topic=T1）。
- **L2（演化后、看 audit 前）**：终点参数越界——`w_kw > 6 ∨ w_content > 3 ∨ w_imp > 0.05 ∨ w_age > 0.02`（阈值来源：健康终点全域上界——X7-P 八终点 w_kw∈[2.1,6.0]、w_content∈[1.0,2.25]、w_imp=0、w_age=0；两受伤终点全部越界。如实注明：阈值由回溯组拟合，前瞻组为其留出检验）。
- **预报规则**：`受伤(audit_recall5<0.95) ⟺ L1 ∧ L2`。

## 四、判据（执行前冻结）

1. **普查对账**：K1 kw 碰撞面实测 = 44（±0，组合论 11×4）；T1 内容碰撞面实测登记。
2. **回溯一致性（32 run 中的前 16：x8-NG/TG）**：TP=2、FP=0、FN=0。
3. **前瞻检验（16 新 run：seeds 9–24，NG 臂，同流键 "PL"）**：
   - **FP=0 ∧ FN=0 → SUPPORTED**（碰撞面×切分定位的预报器在留出种子上全对）；
   - FP+FN=1 → PARTIAL；
   - 其余 → NOT SUPPORTED。
4. **剂量关系（描述性，不做门）**：L2 阳性 run 中，audit 毒任务数与召回损失量（1−audit_recall5）正相关方向登记。
5. 全部 run doctor 8/8。

## 五、已知简化（登记在案）

- L2 阈值取自回溯组（n=2 阳性样本），阈值本身可能不是最优边界——但前瞻组检验的是"规则整体"而非阈值微调；若 PARTIAL/NOT SUPPORTED，如实登记并报告混淆矩阵。
- 预报器只回答"哪种切分会受伤"，不回答"膨胀何时发生"（后者是选择压动力学，另一问题）。
- 前瞻组预期受伤数 0–4（暴露 10 种子 × L2 发生率未知）；若前瞻组零受伤，SUPPORTED 判定退化为"零误报 + 高暴露零漏报"，报告将注明该弱点。
