# 第八轮 · X11 反事实语义实验（关键词精确成员）—— 预注册（判据先冻结，执行前文档）

> 日期：2026-09-10 · 地盘：`衔尾蛇/` · 已裁决（2026-09-10 用户指令：「做，但是隔离要做好」）
> **隔离方案（用户指令的落实，先于一切实验代码）**：见 §四。X0 等价门在本实验**预期失配**——失配即实验点，须单独量化分歧度，不得据此判定主线失效。

## 一、文献触点

- 设计期：精确成员匹配的可行性建立在**关键词表是人工策展集**这一事实上——成员测试无需分词器，与内容侧不同。中文 IR 的分词敏感性作为背景带查证（演化运行期补第二触点，记录入报告）。
- 承接：Furnas 1987 词汇问题（X9 已锚）、QPP 检索后档（X10 已锚）。

## 二、假设与判据（执行前冻结）

- **H-X11**：仅将关键词命中从子串改为列表成员（内容侧不动），毒通道即被根除——24 种子（1–24，含全部已知受伤种子 5/8/11/13）NG 臂（同流键 "PL"、同验收、无链接）零受伤。
- **主判据**：
  - 24/24 零受伤 → **SUPPORTED（根除）**；
  - 受伤 ≥1 且 <4，且 4 个已知受伤种子全部转健康 → **PARTIAL（通道根除但存在残余通道）**；
  - 其余 → **NOT SUPPORTED**。
- **副判据（不劣化）**：配对差 `audit_recall5(反事实) − audit_recall5(真语义)` 逐种子登记，均值 ≥ −0.01（语义修复不得以整体召回为代价；X9/X8 数据为配对基线）。
- **普查断言**：反事实语义下 K1 查询碰撞条目 = 4（仅自身族），由 44 降到 4——脚本内置断言。
- **分歧度量化**：种子 1 默认参数下，80 任务 × 两语义的 top-5 差异任务数登记（预期少数；全同则反事实退化无信息，判定 INCONCLUSIVE）。
- doctor 8/8 口径不变。

## 三、已知简化（登记在案）

- 只动关键词通道：内容侧子串语义保留——残余受伤若出现，首选解释为 T1 内容通道（X9 的 T1 家族碰撞面），如实登记。
- 链接扩散路径中的同型子串测试不动（X7 已证链接端点惰性，且本轮无链接算子）。
- 反事实结论**不外推为真引擎行为**——真实库关键词分布与合成世界不同，W-7 验收以真实库 shadow 对账为准（工单补遗）。

## 四、隔离清单（执行前自查，逐项断言）

1. 反事实核心另立：`proto/x11/evocore_exact.txt`——与主线 `proto/evocore.txt` 的 diff 必须且只能是关键词成员测试一处（脚本自动 diff 断言 ≤3 行改动）。
2. vendor 断言：`bs3_snapshot.txt` sha256 前 16 位 = `ef2c645aea64fb9e`，脚本首行校验。
3. 库目录前缀 `sandbox/x11-*`；输出 `data/x11-*.json`；不读写既有 `lib-x6/x7/x8/x9/x10-*` 与 `data/x6..x10*`。
4. 不运行 X0 等价门主线脚本；分歧度由本实验自带的对账段产出。
5. 主线文件零改动；git 提交显式路径。
