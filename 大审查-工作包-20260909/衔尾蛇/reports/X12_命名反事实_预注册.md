# 第九轮 · X12 命名反事实（标签零填充）—— 预注册（判据先冻结，执行前文档）

> 日期：2026-09-10 · 地盘：`衔尾蛇/` · 隔离模板：X11（用户指令「隔离要做好」沿用）
> 定位：与 X11 构成 2×2 反事实矩阵的另一格——X11 改**匹配语义**（子串→成员），X12 改**世界命名**（标签零填充 T00–T15/K00–K19），匹配语义保持真引擎子串不变。
> 文献触点：沿用 Furnas 1987 词汇问题（命名空间设计决定别名碰撞面）；运行期补查（见报告）。

## 一、机制依据

X9/X11 已证：伤害通道 = 查询词作为子串命中他族标签（K1⊂K10–K19 出现在关键词与内容中）。标签零填充后所有标签等长且前缀互不为子串（"K01"⊂"K10" 不成立）→ 碰撞家族结构性消失。**合法低 IDF 通道保留**：同主题/同关键词条目按构造仍含查询词（T05 查询仍命中 5 个 T05 条目的内容）——这不是碰撞，是世界设计。故 X12 检验的问题是：**伤害主要是命名伪影，还是合法低 IDF 淹没在权重膨胀下也足以致伤？** 事先无把握，真实验。

## 二、隔离清单（沿 X11 模板）

1. 反事实核心 `proto/x12/evocore_named.txt`：与主线 diff 必须全部位于 build_world 内（标签生成 3 处），脚本断言。
2. vendor sha 断言 `ef2c645aea64fb9e`。
3. 库 `sandbox/x12-ng-s*`、输出 `data/x12-*`；不触既有库与数据。
4. 不跑 X0 等价门；分歧为世界级（标签全体变化），以通道普查断言替代：反事实世界 K01 查询子串命中条目 = 4（仅自身族，无跨族）。
5. 主线文件零改动，显式路径提交。

## 三、判据（执行前冻结）

- **主判据（24 种子，NG 臂，同流键 "PL"，与 x8/x9 配对）**：
  - 0/24 受伤 → **SUPPORTED（命名伪影主导：碰撞家族移除即根除伤害）**；
  - 受伤 1–3 且 4 个已知受伤种子（5/8/11/13）全部转健康 → **PARTIAL（命名伪影为主，合法低 IDF 淹没在个别终点仍可致伤）**；
  - 其余 → **NOT SUPPORTED**。
- **副判据**：配对差均值（X12 − 真语义基线）登记；不劣化阈值同 X11（≥ −0.01）。
- doctor 24/24。

## 四、已知简化

- 零填充只改标签字面，不改变条目/任务构造逻辑（i%16/i%20 结构不动）。
- 结论外推边界：合成世界的标签碰撞是**人造放大器**；真实库无 K1 型前缀家族，但内容侧子串歧义（中文无分词）同类不同率——X12 判 SUPPORTED 不等于真引擎无此问题，只说明合成伤害的主通道是命名伪影。
