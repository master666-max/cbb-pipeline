# X17 · drift 断崖第二实现复现 —— 预注册（判据先冻结，执行前文档）

> 日期：2026-09-11 · 模板：X13 配对流 + X12 零碰撞世界 · 被检命题：混元第三十五轮 E144"崩溃的不是危险数量，是 drift"（单线结论，F7 边界①的实验支柱）
> **诚实前提**：X 世界的危险组合与混元不同（伪特征通道已关，X6a）——本复现检验的是"**drift 类危险 vs 其他危险的边际结构**"，非逐格对齐。

## 一、四个危险的 X 世界操作化（叠加顺序沿混元 E144 范式：逐项累积）

| 序 | 危险 | 操作化 | 混元对应 |
|---|---|---|---|
| A0 | 无 | 标准 NG（默认参数起点） | 0 危险 |
| A1 | +③ 参数膨胀 | 初始 mainline = {w_age:0.05, w_content:3.4}+默认（演化可自愈，X2a 先例） | —（参数级危险） |
| A2 | +评判者漂移 | 选择分 = `sc_train(cfg) + 0.1×(top5 importance 均值)`——评判者额外赏识高重要度窗口（选择信号被污染，audit/真值不变） | +surf/write 族 |
| A3 | +世界漂移 | 每 5 代追加 4 条"新进高重要度条目"（既有词表/主题、importance 9–10、当日创建）——分布漂移，冻结任务集不知情 | +drift（断崖候选） |
| A4 | +corrupt | 第 15 代 mainline 参数被随机化（独立 rng `9000+seed`，不消耗主流） | +corrupt |

- 全臂同流键 "PL"；A3 追加条目改变库状态 → sc 缓存键加入条目计数（防陈旧缓存污染）；A4 随机化用独立 rng。
- 世界：X12 零碰撞（无①②通道干扰）；seeds 1–24；GENS=25、KIDS=3、TOL=0.005。
- 度量：各臂终局 `audit_recall5`；**边际_i = mean(audit_i) − mean(audit_{i−1})**，逐种子配对。

## 二、判据（执行前冻结）

- **主判据**：A3 的边际为最大负边际且配对 t 的 |t| ≥ 2（vs A2）→ **SUPPORTED（断崖在 drift，E144 获第二实现）**；若最大负边际出现在 A1/A2/A4 → **NOT SUPPORTED（断崖换位/不存在）**；边际全小（|t|<2）→ **INCONCLUSIVE（本世界危险梯度不足以产生断崖——本身即边界登记）**。
- **副判据（描述性）**：各臂受伤种子数阶梯；corrupt 的边际（预期小，复现 E144 的"+corrupt 无显著边际"）。
- doctor 全臂全种子通过。

## 三、已知简化（登记在案）

- 叠加顺序为先验选择（沿混元范式）；不同叠加序可能不同——登记为局限，不做全序扫描（2⁴ 全析因 384 run 超本轮预算）。
- A2 的"赏识重要度"是 judge 漂移的一种具体形式（E14 族）；混元的 surf 是表象通道——不同污染内容，同"选择信号污染"类。
- A3 追加条目走真引擎 append（chain+刷根），doctor 口径不变。
