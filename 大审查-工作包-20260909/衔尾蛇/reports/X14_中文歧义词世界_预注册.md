# 第十一轮 · X14 中文歧义词世界 —— 预注册（判据先冻结，执行前文档）

> 日期：2026-09-10 · 地盘：`衔尾蛇/` · 隔离模板：X11/X12（用户指令沿用）
> 定位：回答 W-7 的 real-world so-what——X12 证明标签碰撞是合成世界伪影后，唯一悬而未决的是：**真实式语义歧义（中文词表天然含子串关系，如"测试"⊂"压力测试"）能否独立致伤？** 本轮把世界的碰撞源从标签伪影换成真实式词族，主题标签零填充（隔离命名因素），匹配语义保持真引擎子串。

## 一、世界构造（执行前冻结）

- 16 个零填充主题 T00–T15（无标签碰撞）× 80 条目；**8 个歧义词族**，每族（头词, 子词1, 子词2）：
  测试/压力测试/单元测试 · 学习/机器学习/深度学习 · 网络/网络安全/网络协议 · 安全/信息安全/安全策略 · 数据/数据库/数据挖掘 · 系统/操作系统/系统架构 · 模型/语言模型/模型压缩 · 索引/倒排索引/索引压缩
- 条目 i：主题 `T{i%16:02d}`，词 = 24 词表 `words[i%24]`（每词 3–4 条目），keywords=[词]，content 含主题与词；30% 同主题链接（沿袭）。
- 任务查询 = `{主题} {词}`（结构同真世界）；效用/效用 λ/K/演化协议全部沿用冻结元层，NG 臂（无门无链接，同流键 "PL"）。
- **碰撞普查断言**：头词"测试"的子串命中条目 = 12（4+4+4，头+两子词族）；真世界 K1=44 的现实版缩小（prevalence 15% vs 55%）。

## 二、判据（执行前冻结）

- **主判据**：24 种子 NG 演化（真子串语义）：
  - ≥1 种子受伤（audit_recall5<0.95）→ **迁移成立**：语义歧义通道在真实式词表下可独立致伤——W-7 维持，且升级注记"真实词表同样暴露"；失败任务的金标词**必须全部属于歧义词族头词**（通道归因核对），否则另有通道，如实登记。
  - 0/24 → **语义歧义在本 prevalence（15%）下不足以致伤**：W-7 降级为观察项，真实库风险以 W-8 审计规格兜底。
- **副判据**：剂量方向——受伤种子的失败任务金标词，其族内碰撞条目数应高于全体任务均值；doctor 24/24。

## 三、已知简化（登记在案）

- 词族与填充文本为构造词表，非真实工作区语料——是"真实式歧义"的最低保真版本。
- 只测 NG 臂：本轮问题是"通道是否存在"，非"护栏是否有效"。
- 主线文件零改动；`proto/x14/evocore_zh.txt` 的 diff 必须全部位于 build_world 内（脚本断言）；vendor sha 断言；库 `sandbox/x14-*`、数据 `data/x14-*`。
