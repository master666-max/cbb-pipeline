# 第九轮 · X12 命名反事实 + 审计处方实证校验 —— 报告

> **结论先行**：①**X12 判 SUPPORTED**——标签零填充（碰撞家族结构性清零）后 24/24 零受伤，连 X11 中幸存的双毒 s5 也治愈，配对差 +0.026：合成世界伤害的主通道是**标签命名空间设计伪影**，关键词/内容两条子串碰撞通道都从它流出。②**审计处方表实证成立**——4 个已知受伤终点 × 10k 次蒙特卡罗抽样，实测检出率与超几何理论最大偏差 0.012（误差内），现行 8 任务审计对真受伤检出率仅 19–28%；**定向 8 任务（毒族任务集）100% 检出全部 4 案，完胜随机 42 任务**——"定向"胜过"扩容"，此为 W-8 的最终形态。
> 日期：2026-09-10 · 预注册：[X12_命名反事实_预注册.md](X12_命名反事实_预注册.md) · 脚本：`proto/x12/`（`evocore_named.txt` diff 行 36/38/39 全在 build_world 内，脚本断言；`x12_named.txt`）· 数据：`data/x12-*.json`（24 run）+ `x12_summary.json` + `audit_mc_validation.json` · 日志：`reports/x12_run_20260910.log` · 机时 122s（X12）+ 30s（审计实证）
> 过程注记（如实）：X12 首跑失败一次——用 sed 从 X11 派生脚本时 `Path/"x11"/"evocore_exact.txt"` 的路径成员字符串未被替换，导致加载了错误核心（被普查断言当场拦下，零脏数据）。教训：**派生脚本必须显式重写，文本替换派生被普查断言否决**——自测门纪律再次生效。

## 一、2×2 反事实矩阵（X11+X12 闭合）

| | 真语义（子串匹配） | 精确成员（X11） |
|---|---|---|
| **原始命名**（K1⊂K10–K19） | 4/24 受伤（s5/s8/s11/s13） | 1/24 受伤（s5，内容通道） |
| **零填充命名**（X12） | **0/24 受伤**（本轮） | 未测（双修复，必然 0） |

- X12 24/24 task=1.00，含真语义下全部 4 个受伤种子；配对差 +0.026。
- **归因定论**：命名空间设计是根因主通道——前缀可碰撞的标签（K1/K10 型）同时制造关键词通道与内容通道的子串碰撞；X11 的语义修复只堵关键词一条，X12 的命名修复两条件同时消失。
- 隔离对账：vendor sha 断言 ✓、diff 行 36/38/39 全在 build_world ✓、碰撞家族清零断言 ✓、24/24 doctor ✓。

## 二、审计处方实证（W-8 背书）

| 受伤终点 | 失败任务 | n=8 实测检出 | n=42 实测检出 | 定向 8 任务（毒族集） |
|---|---|---|---|---|
| x8-s5 | 2/80（全 K1 通道） | 19% | 78% | **检出** |
| x8-s8 | 3/80（全 K1） | 28% | 90% | **检出** |
| x9-s11 | 3/80（全 K1） | 29% | 90% | **检出** |
| x9-s13 | 2/80（全 K1） | 19% | 78% | **检出** |

- 理论 vs 实测最大偏差 0.012（10k 抽样标准误 ≈0.005）——**处方表实证成立**。
- 失败任务全部落在毒族（K1/T1 通道），无一例外——通道定向审计集（本世界仅 8 个毒族任务）即可 100% 覆盖全部已知伤害。
- **W-8 最终形态**：优先级为 ①通道定向任务集（小而全效）→ ②随机扩容 n≥42（代价高）→ ③不声明审计规格（检出力 <30%，禁止）。

## 三、对 W-7/W-8 工单补遗的回写

- W-7（关键词语义修复）维持，但**外推边界收紧**：X12 证明合成伤害主通道是命名伪影；真实库无 K1 型前缀家族，W-7 的真实收益预期应下调——其价值定位为"消除一类结构性风险 + 使验收与检索语义对齐"，而非"修复已知真实伤害"。真实库的实际收益以解冻后 shadow 对账为准。
- W-8 升级为"定向优先"规格（§二）。
- 方法论登记：**2×2 反事实矩阵**（语义 × 命名）是本轮最有复用价值的设计——单格反事实会误判根因层级（X11 单独看会得出"语义修复不彻底"的浅结论，X12 补齐后才知道两层都是命名伪影的下游）。

## 四、复现

```bash
py -X utf8 大审查-工作包-20260909/衔尾蛇/proto/audit_mc_validate.txt   # 审计实证，约 30 秒
py -X utf8 大审查-工作包-20260909/衔尾蛇/proto/x12/x12_named.txt       # 命名反事实 24 run，约 2 分钟
```
