# X19 · 门位分离真引擎检验（混元 E7 的 X 世界版）—— 预注册（判据先冻结）
> 日期：2026-09-11 · 用户已授权（"全都要"含此项；边界=已有门的参数移动，非新门，与 F4 同构）
> 动机：混元 E7 门位分离（双门放准入 0/20 卡死 vs 放晋升 20/20）；X8 的留出集门在**晋升位**失效（棘轮绕行）。问题：同一个门移到**准入位**，能否拦住 X8 的伤害？

## 一、设计（冻结）

- 世界/协议：X12 零碰撞世界、NG 骨架、seeds 1–24、同流键 "PL"（与 x8-NG 配对：轨迹在首次准入分歧前逐位相同）。
- **AG 臂（准入门）**：archive 准入条件 = train 规则（`sc_tr(child)−sc_tr(parent) > −TOL`）**∧ held 规则（`sc_hd(child) ≥ sc_hd(parent) − TOL`）**；主线加冕 = train 规则（与 NG 全同）。
- 对照：x8-NG 存量数据（晋升位门=TP8 已测；无门=NG）。
- 记录：audit_recall5、受伤种子、doctor。

## 二、判据（执行前冻结）

- **主判据（AG vs NG 的受伤种子数）**：NG=4（5/8/11/13）。
  - AG 受伤 ≤1 → **SUPPORTED（门位是 X8 失败的关键变量：准入位拦截有效）**；
  - AG 受伤 2–3 → **PARTIAL（准入位部分有效）**；
  - AG 受伤 ≥4 → **NOT SUPPORTED（门位无关，X8 失败另有原因）**。
- **机制观察（描述性）**：AG 的 archive 规模 vs NG（准入门收紧archive → 变异池变化 → 终点是否被改写）；受伤种子的终点参数对照。
- doctor 24/24。

## 三、已知简化

- 准入门比较基准是 parent（非主线）——准入位没有"现役基线"，这是位置差异的本质，如实登记。
- held 门为硬非回退（同 X8 的 ε=TOL）。
