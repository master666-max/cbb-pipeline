# Round 11 报告 · 梯度定律：门控有效性的单一调节变量（REVIEW-DRIVEN）

> 判据：judge_r11_gradient.txt（SELFTEST PASS，先锁后跑 ✓；重放验证门 0/100 全等）。
> 登记完整性事故：registry 三条 R11 hash 初记为未计算的错值，同一命令的 sha256sum 输出暴露不一致，当场改真值并留痕（a26333cd/87c38e4d/f1240ad2）。

## Findings（全部数字来自锁定脚本输出）

| 假设 | mechanical | 判定 | 先验→事后 |
|---|---|---|---|
| H-R11-1 平坦梯度单调律 | 低−高=0.7346 | **SUPPORTED** | 0.70 → 0.90 |
| H-R11-2 尖峰梯度单调律 | NA（mid n=18 守卫） | **INCONCLUSIVE** | 0.60 → 0.65 |

1. **梯度律在平坦世界干净成立**：低带 0.8914 > 中带 0.6118 > 高带 0.1568（n=267/85/287，两段 gap 0.2796/0.4550）。单一变量（局部梯度 |∇U|）把三个发现统一：R10 的 U 形（低梯度=峰顶∪荒漠两处）、R4 的双因素塌缩、R9 的荒漠功能件——门控有效性 = 局部信噪比的单调函数。
2. **尖峰世界方向一致但欠功效**：0.8304 > 0.5000 > 0.1739，mid 带 n=18<30 守卫。窄峰的可驻留区小，mid 梯度事件天然稀少。
3. 理论对齐成功的一轮：OBL-003 要求的"阈值与理论预测对齐"（|ΔU|≈|∇U|·step vs 噪声）在数据形态上兑现——判据不再贴边。

## 对上轮结论的修正
- R10 的 U 形从"终态描述"升级为"梯度的单调函数"（U 形是 u 做横轴的伪影：u 与梯度在平坦世界非单调映射）。

## 下轮方向
- R12=收官红队轮（证伪方向，挑战本轮最自信结论）：梯度律的**带边界敏感性**——扰动边界（0.05/0.3 与 0.15/0.7 两套）下单调性是否存活；加上**两世界池化**检验（合并后 mid n=103 跨过守卫）。数据复用 r11_gradient.json，零新模拟成本。
