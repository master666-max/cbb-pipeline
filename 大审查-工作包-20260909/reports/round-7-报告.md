# Round 7 报告 · 红队挑战 R5 双 SUPPORTED：分层复测

> 判据：judge_r7_stratify.txt v2（SELFTEST PASS，判据规则与 v1 逐字相同；v1 有"selftest FAIL 后仍执行"流程违规，已记 registry）。
> 手算错误累计第 4 处（A 0.019→0.015、B 0.13→0.14）——自测门全部抓住，无一漏网。

## Findings（全部数字来自锁定脚本输出）

| 假设 | mechanical | 判定 | 先验→事后 |
|---|---|---|---|
| H-R7-1 天花板效应挑战 | NA（小增益层 n=0） | **INCONCLUSIVE**（守卫） | 0.55 → 0.15 |
| H-R7-2 族特异性挑战 | NA（其他源层 n=0） | **INCONCLUSIVE**（守卫） | 0.45 → 0.15 |

1. **证伪不可执行，且原因是结构性的**：41 个合格 run 中小增益 run（|gain|<0.1）数量为 **0**——现有档案的演化世界总增益普遍 ≥0.1，天花板效应假设无分层可分。
2. **平台化的"跨族"实质不成立**：≥8 点的 run 中其他源（豆包/混元）数量为 **0**，40/40 平台证据全部来自 x2_clean + 1 个 x1_static（静态基线的平台是构造性的）。R5-H2 的 SUPPORTED 判定维持（判据未含跨族条款），但其 boundary 必须收紧为**单族结论**。
3. **红队战果（诚实版）**：本轮没有证伪掉任何结论，但把两条 SUPPORTED 的证据底盘摸清了——"0.9268 一致率"与"0.875 平台化"全部建立在单一实验家族的大增益 run 上。外推风险从隐性变显性。

## 对上轮结论的修正
- R5-H2 的 boundary_conditions 修正为"x2_clean 单族 + 1 静态基线；跨源长序列证据为零"（原 boundary 已注"几乎全为 x2_clean"，本轮给出机械坐实 n=0）。

## 流程事故（如实记录）
- v1 selftest FAIL 后未停（两处手算错 + None 格式化崩溃），仍锁定并跑真实数据；判据规则未动，v2 修复后复跑输出一致。违反"SELFTEST PASS 后先锁后跑"，registry 双版本留痕。

## 下轮方向
- R7=证伪 → R8 换道【机制·LIT-REPLICATE】：E13/E7 两阶段门控（候选先过廉价门再过昂贵门）在最小噪声引擎中的复现——两段独立含噪评估能否以低于"加大 margin"的代价压假采纳率（直接对话 R2 的 margin 权衡与 R7 之前 E41 复现）。判据按 OBL-003 双侧区间。
