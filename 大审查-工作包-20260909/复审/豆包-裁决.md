# 豆包裁决（主审通读：L9 补丁说明全文 + 补丁 patch + P0 段抽查；六份大件基于代理提取 + 与主审实测的交叉验证点核对，交叉吻合度高，采信）

## v3.8 代码审查工单（完整版，30 题 P0×6/P1×7/P2×8/P3×9）—— **A-：覆盖面最广的逐行审查**

- **P0 六条逐条判定**：
  - P0-01 Merkle 无域分离（叶/内部节点同哈希）：**属实**（第二原像攻击——豆包自认需 SHA-256 碰撞、计算不可行，属标准实践缺口非可利用漏洞，定级诚实）。
  - P0-02 assert 做安全校验（-O 失效）：**属实**（GLM Q-002/混元 BUG-024/BUG-001 三家交叉）。
  - P0-03 版本号 3.8.1 vs 3.8.2 漂移：**属实（对 3.8.1 基线）**——v3.8.2 中性化已修（N-A）。
  - P0-04 build 死分支：**属实**（我 grep 确认 `if lib and out == Path("./dist")` 残留——v3.7 J-004 改造后死代码）。
  - P0-05 eval 空评测集除零：**属实**（我实测复现）。
  - P0-06 absorb-md 硬编码日期：**属实**（GLM⑯/混元 BUG-002 交叉）。
- **P1 七条**：增量 Merkle 登记未实现/检索全量扫描+中文整串/全局态+monkeypatch 污染（引 t_verify_offline_honest 注释自证——用我自己的代码注释当证据，好手）/chain_append 无锁/密钥 3 模式/migrate glob 恒真+空 IndexError/拒绝路径整读 JSON——全部与源码一致。
- **P2 八条**：单文件 11 职责/测试占 49.6%/cmd_engine 184 行/写后刷根复制粘贴且不一致（reflect 漏）/目录名魔法数字（阈值 7/权重 3/扩散 0.5/衰减 0.1 未入 TUNABLES）/变更编号八套无文档——全部属实且豆包 P2 独有的「编号无文档」确实（我留下了 C/V/Y/H/J/K/L/N/BR/X 十套编号的考古层）。
- **「确认无问题」无专节**（不如混元），但 P0 六条中四条被我独立实证 → 提取可信。
- **总评 A-**：覆盖面四家最广（含性能验收目标与四阶段计划），行号级定位准确率高；扣分：Merkle 定级 P0 与自认「不可利用」存在张力（应降 P2），部分行号在 v3.8.2 已漂移。

## v3.8 架构优化专项工单（ARCH-01~14）—— **B+：诊断全面，处方偏重**

- 十项架构问题（无存储抽象/无事务/写紧耦合/无扩展点/错误处理乱/配置硬编码/无 DI/读写共享/无迁移框架/无可观测性）——诊断全部成立（与 GLM O-1/O-5、qwen AR 交叉）。
- **特色包账本状态机 12 包零实现**——「最差异化的部分恰恰完全没实现」，与混元 V4-06 独立双证。
- **处方争议**：WAL+补偿事务/CQRS/DDD/SPI+沙箱在「单文件+纯标准库+单写者」约束下过度（豆包自警过度设计风险成立）；47-68 人天估价偏高。
- **有价值独有项**：state.json write_text 非原子可半写损坏（并入唯一写出口修）；`reflect` 不刷根的边界模糊确认；可观测性/版本化迁移框架登记 v4。

## v4.0 完全重构工单（REBUILD-2026-001）—— **B：唯一完整 greenfield 蓝图，但被自家反方削弱**

- 四层 DDD+CQRS+事件总线+UoW+SQLite FTS5+审计链触发器禁 UPDATE/DELETE+WAL+插件 SPI+权限模型——设计完整度最高。
- **约束冲突**：pydantic/typer/jinja2/pyyaml 破坏纯标准库；SQLite 破坏文件形态；多文件 monorepo 破坏单文件交付——三项都需公理级修订。
- 96 人天/5-6 个月/12 项风险登记（含「50% 低估风险」自认）/发布门禁含「至少 1 个外部用户试用」——诚实。
- **裁决**：作为 v4/v5 的**选项库**保留（触发器：条目 >10k/多库/远程需求），不作为当前路线。

## v4.0 架构可能性深度探索 —— **A-：定位最清醒的一份**

- **核心判断**：「v3 的设计直觉是对的（双平面/永不删除/慢车道=事件溯源与治理雏形），问题是工程实现不是设计理念」+「v4 是地基不是天花板」+ 反对一次到位——**与豆包自家 greenfield 工单构成内部反方**，且与混元增量路线、GLM 载重墙收敛。
- 三跃迁（事件溯源+CQRS/混合检索+RAG/CAS+Merkle DAG）与 7 项「留接口」清单（事件总线+投影/六边形零依赖/可插拔检索/内容寻址兼容 ID/content_type 多模态/巩固框架/automation.level 开关）——接口设计可直取。
- 低分否决清单（Actor/联想回忆/多代理/开放协议 ★★）——否决有据。
- 本地优先坚持 ✓。

## v4.0 自演化引擎设计 —— **B：七阶段管线最完整，但无实验**

- L0-L9（安装档位）×E1-E5（演化对象风险）双体系、不可演化核心 SHA-256 锁定（防「规则修改者」攻击）、七维度验证、影子 24h→灰度 10/30/50/100%→自动回滚、速率限制（E1 5/天…E4 1/月双人复核）、三阶段路线（v4.0 只建安全基础设施 21d 不启用自动演化）。
- **开篇自认**「v3 的 L8/L9 只提供概念框架——没有变异生成机制、没有隔离验证环境、没有多目标评估、没有灰度应用、没有感知监控」——**此自认部分过重**：BASELINE_BLACKLIST/guard 三模式/令牌门/锚定快照为已落地防御代码（豆包自家引用总表修订版㉛同认），「无生成引擎/无隔离验证/无灰度」成立。全文设计无实验（与混元 17 轮实验互补——**两者应合并读**：豆包给管线工程学，混元给实验数据）。

## agora 群体智慧沙盒实验 —— **A-（独立实验，方法学借鉴）**

- 与 bootstrap 源码无关（被审对象是 82 行聚合器），但：①18 实验+8 设计失败+7 自我推翻+两个预期反面结果——实验纪律与混元同档；②可复现（一键 18 秒、固定种子、results 存档）；③对本体系的借鉴：ρ 相关性下 51 人≈4.6 独立人（多元审查的有效样本量警告——**直接适用于本次四家大审查：四家若共享训练分布，独立结论数 <4**）；顺序公开 0.695 劣于同时封板 0.816（审查应背对背）；median/Krum 抗 f<0.5 恶意；「聚合机制不是中立管道」三候选人可分别胜出。

## L9 交付映射补丁 —— **A：全场唯一「代码级交付物」**

- 8 hunks 全部核实：5 个 L9 映射键/L9 adds 3→8/DEPS 补 4 边/.pyc 黑名单/gate 文案修正/3 回归测试；自验 71/71（临时副本）+补丁前 70/71 归因（旧 level_mapping）诚实。
- **附带发现**：存量 bodies 混入 6 个 .cpython-314.pyc（absorb 无二进制黑名单——已实证）；constitution-bca install.md「第七~第十条」与宪章七条撞车（v3.8.1 渲染化后该文件已被渲染产物替代，撞车消解——对旧基线属实）。
- 应用注意：与 3.8.2 docstring 中性化需 rebase；level_mapping 配套 json 必须同用。

## 引用论文总表_修订版 —— **B+（题录核对有价值）**

- 依据「论文实现核对报告」机器比对：补漏 A4/A10、落地三层级（T1 代码层 12/T2 内容体层 13/T3 参考挂账 8）、名不副实显式改写（A1/A5/A8/A12/A14/A18/A19 标「未实现」边界）、删死引用。
- **价值**：把「引用了」与「实现了」分开——这是对 REGISTRY「图纸兑现率」的量化（GLM v4.0-1 的定性版有此洞察，豆包给了定量分层）。

## 豆包系列总裁决

- **六份链条自洽**：审查（30 缺陷）→架构（14 项）→greenfield→深度探索（反方）→自演化（设计）→L9 补丁（交付物）——且 L9 补丁证明豆包是四家中唯一「从审查走到可应用代码」的。
- **可信度 A-**：P0 六条中四条经我独立实证；L9 交付缺失经我核验矩阵独立复现；扣分：Merkle 定级张力、v4 greenfield 与自家深度探索矛盾未自我裁决。
- **核心贡献排序**：①L9 交付缺失（唯一功能级）+现成补丁；②30 缺陷全覆盖（P0 六条四实一修一真）；③「工程实现 vs 设计理念」的清醒定位；④bodies .pyc 污染发现；⑤性能验收门禁体系。
