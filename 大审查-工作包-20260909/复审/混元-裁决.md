# 混元裁决（主审通读：修复工单全篇/V4 工单集全篇/v4.0 架构设计全篇/自演化方案全篇/A 轮全篇/17 轮总报告头尾+落地工单集关键节；17 份轮次日志按总报告口径采信，抽查未逐读）

## v3.8 修复工单（33 张：BUG-001~025 + ARCH-001~008）—— **A：全场最重的单份审查**

- **方法学最强**：2415 行静态通读 + 实际跑通全链路 + 17 组定向缺陷探针；每张工单带行号+复现命令+修复方案+验收 checkbox；**附「确认无问题勿破坏」反向清单**（prev_override 防分叉/Merkle 排除自身持有者/--redeliver 显式授权/verify 三态/慢车道 N-004——五项全部正确识别为有效设计，证明审查者真读懂了代码而非模式匹配）。
- **BUG-001（P0）run-tests SystemExit 捕不住**：属实（我方三次实测同款）。**且其对 render_quickref 的批评需修正我此前判决**：GLM 说「硬编码 68/68」（判 REFUTED 对了一半），混元指出 `{tests_n}/{tests_n}` 是「注册数/注册数」恒等式——**从未被执行结果校验，违反公理 G**。合并判决：GLM 的「硬编码」表述错，混元的「恒等式冒充测量」对。
- **BUG-002（P0）证据锚空串绕过 + genesis-v4 幽灵事件 + 日期静默改写**：三条全部属实——genesis-v4 确实不在审计链（我写迁移器时未把 bulk-import 入链，真实缺陷）；「在把引文纪律写进铁律的系统里，这是静默伪造」的定性成立。
- **BUG-003（P0）install 中断重跑覆盖用户文件**：属实（GLM Q-021.6 交叉）。
- **BUG-004（P0）并发丢更新**：机制代码级确认；12 进程 5/60 为【实验声称】（C 级数字，机制可信）。根治方案（accum 改账本重算，比加锁彻底）**优于** qwen/豆包的纯加锁方案。
- **BUG-005 guard 三形态逃逸**（patch 文件/删除/改内容不改名）：比 GLM Q-013 更全面——diff 路径解析+删除检测+不可解析需人工裁决的三层方案**采信**。
- **BUG-006 检索缺陷 + 「recall@5 虚高→eval 基线失去判别力」**：boss/ghost/plain 复现逻辑成立。**此条是对 L-004 基线 0.485 的釜底抽薪质疑**——基线数字本身因 importance 主导缺陷而虚高，修正后基线会变（可能下降——这是判别力恢复的证据）。
- **BUG-010 换行注入**：「一条注入即可废掉整个库的审计真相源——审计面只追加，断链无法修复只能整体移 attic」——攻击链完整，比单条登记深刻。
- **BUG-011 verify 9/33 永不对账但 status 硬写 verified + 「一致≠同一篇」（包含关系判据）+ 无离线/超时**——比此前所有轮次深，全部属实。
- **BUG-013 七项死配置**（代码引用 0 次却渲染进 spec，「公理 A 失真」）：属实。
- **BUG-015 Merkle exclude 按文件名匹配 → 任意层级同名 state.json 逃根**：属实（真漏洞，我 v3.7 M-7 写的）。
- **BUG-021 CRLF 未修完**（cmd_build/cmd_install AGENTS 段仍 write_text）：属实——**我 v3.7 的修复确实没修完**。
- **BUG-002/011/013/015/021 + ARCH 全部成立**。「附：确认无问题」清单价值高。
- **总评：A（四家最高单份）。实证密度、行号精度、反向核对清单三项全场第一。**

## v3.9 v4 重构工单集（V4-01~13）—— **A-：数据模型与信任模型的正确处方**

- **V4-01 fold**（12 进程 60/60 原型声称）：根治方案优于加锁。
- **V4-02 事件溯源**（删 memory 重建实测声称）：「四条平行真相」诊断与「公理宣告强度>实现承载强度」的根因——与 GLM/qwen 三方收敛。
- **V4-03 chain_tip 三元组**：**全场最硬的安全发现**——实测截断攻击（删尾 2 行+同步改 state → doctor 检出 False）、「Merkle 锚定内容集合不锚定序列长度」、「严重性高于 guard 绕过——打击审计面本身」；三场景矩阵（库内锚防②/人侧锚防③）正确。**采信，优先级最高**。
- V4-04 信号矩阵（「既报故障又报正常的信号等于没有信号」）、V4-06 包状态机（与豆包交叉「最差异化部分零实现」）、V4-07 令牌三档（弱回显/中 token 核销/强签名）、V4-08 时钟注入（「t_cross_month_real 一年只有极少数时候能覆盖」——对 v3.7 补测的精准批评）、V4-09 文本落盘唯一出口（CRLF 未修完）、V4-10 测试契约化（「测试锁死内部实现→分层半途而废退回单文件」——重构失败最常见死法）、V4-11/12 事件版本化与迁移器（dry-run 不一致拒绝）——全部成立。
- V4-13 明确不做清单（300 条目 merkle 0.005s 实测、性能非瓶颈）——与其他家「登记不动手」收敛且有数据。

## v4.0 架构设计方案 —— **A-：唯一带可运行原型的完全重构设计**

- 「公理宣告强度>实现承载强度」根因+三实测证据（audit 无 payload/accum 5/60/截断检不出）——与 GLM「审计链里没有真相」、qwen「事件溯源说了没做」三家收敛。
- 七原则（P5 正常路径全绿/P6 能力即声明/P7 确定性为 v3 反例定制）/五决策（事件溯源/单行 JSON 真相+渲染视图分离——顺带消灭换行注入/三元组锚定抄录人侧/单写者锁+seq/分层+zipapp）/13 事件类型/fold/五投影全可重建/18.5 人日六阶段/迁移七步（dry-run 拒绝迁坏库）。
- 附录 A 原型实测四项（事件流唯一真相/并发 60/60/截断三场景/性能 0.005s）+「id 重复在 fold 时天然被拒——文件系统查重在事件溯源下不需要」——**P1-02 的结构性消解**。
- 附录 B 与 v3 十二行对照表——完整。
- **可信度 A-**（原型 bs3v4.py 在目录中实证存在；实测数字 C 级但机制代码可读）。

## 自演化架构设计方案 + 17 轮总报告 + A 轮 —— **A-（思想价值全场最高）**

- **自我推翻的诚实**：开篇认错（「把依赖未满足当成不该做」）并以实证修正（演化循环 25 代自动发现人工审查才发现的 P1-6 同款 w_imp 缺陷——「自演化是发现 eval 缺陷最强的手段，两者是共生关系」）。
- **E1 三实验**：8 种子留出集 0.033→0.814 全正；E2 作弊不可表达（元层冻结，结构防作弊非检测——「guard 被绕过正因走检测路线」）；E3 双门防过拟合（伪特征留出集反转，无门 −0.052 实为倒退，双门 0/12 采纳）。
- **三层模型**（元层人签/法层自主/行层自由）+「变更只能向上申请不能向下修改——系统无法修改评判自己的尺子，结构性消灭 objective hacking」。
- **17 轮总报告**：80 实验/19 设计失败/15 自我推翻；代码分布（自测 43.8%——测试比核心还大）；E14 评判者漂移（自报 +1.50 真实 −0.83，初版数据被缓存 bug 污染后修正且严重 9 倍——数据修正披露诚实）；E40 底线（漂移时 v3 −0.1124 vs 修正 +0.1492）；E64 预算 <150 恒负。
- **A 轮（最高峰）**：E81 表象特征攻击——judge 看文本时系统学会堆展示权重（自报 +2.89 真实 −0.008），**锚定审计零触发**（结构性：锚定集判断来自同一 judge，「外部参照经同一有缺陷观测通道不构成外部性」）；E81c 真值抽样唯一有效（人直接判「有没有用」，每 2 代 +0.090=无审计 3.8 倍）；E83 留出集门收益随伪特征相关性单调上升。四实验共同结构：「系统总在自己参与生成的观测通道里被评价」——公理 G 的严格形式（测量通道有问题时连「测不出差异」都测不出→必须周期性更换测量通道）。
- **落地工单集**：EV-01~21+A 轮 EV-19~21；执行前三条（E75 15 倍成本零收益/E76 极简 89% 收益/E77 护栏 +35% 安全性能不冲突）；三不可能性（E78 无法自知失败/E57 无法被自己的数据检验/E64 注意力门槛）；**12 条防回退清单**（全部带实验编号；共同点「让机制更聪明的改良全失败，测量已有可观测量的全成功」）。
- **产物核验瑕疵**：原型清单列 evolve2.py（缺失）；17 轮报告复现段列 evolve4.py/run_e9.py（缺失）；run_e14c.py 首行硬编码 Linux 路径（沙盒带回拷贝）。**可信度：设计 A-/实验数字 C（方向采信、数字未复现）**。

## bootstrap_完全重构_可能性空间探讨 —— **B+（被自演化方案部分推翻但保留价值）**

- 「93% 代码不在解真正的题；核心三件套 165 行=6.8%；内容分发>核心——最高杠杆是删掉一半」（AST 归类）；git 后端 7 项免费能力；「检测式→能力式」；三愿景 A/B/C 与「C 先别做」——后被自演化方案以实验推翻（混元自我推翻链的一环，诚实保留）。
- 与 GLM「20% 载重墙」独立同构（双方都给出「如果只做一小部分做什么」清单）。
