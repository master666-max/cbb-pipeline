# bootstrap_v3 自演化推演 · 实验总报告

> **范围**：从代码审查到二十三轮自演化推演的全部实测结果（**110 个实验** + 3 个架构原型 + **21 次实验设计失败记录** + **20 次自我推翻**）
> **方法**：全部结论来自沙盒中可运行的原型，不引用外部文献、不做纸面推演
> **代码**：`proto/bs3v4.py`、`proto/evolve.py` ~ `evolve8.py`、`run_e9.py`、`run_e14c.py`
> **日期**：2026-09-08

---

## 摘要

| 层次 | 结论 |
|---|---|
| **代码层** | 核心三件套（记忆+审计+治理）165 行 = 6.8%；内容分发 213 行 > 核心。P0×4 / P1×8 / P2×13 |
| **架构层** | 公理 D（双平面）物理上不成立——审计链不含 payload，系统是四条平行真相。截断攻击完全检不出 |
| **自演化层** | 自演化可行（留出集 0.033→0.814，8/8 正）；但必须配两级门控、轮换留出集、锚定集防漂移 |
| **最深发现** | 系统输出会塑造评判它的人（E14）：自报 +1.50 时真实 −0.83。终极安全机制不是"防止腐蚀"，是"腐蚀时能被看见并停下" |
| **方法论** | 十四轮中我推翻/限定了自己 14 次判断。**11 条"听起来合理"的方案被证伪**——这也是为什么要做实验而不是靠推理 |
| **注意力门槛** | **预算 <150 单位时终局效用始终为负**（E64）。自演化不能小规模试水 |
| **底线（E40）** | 有漂移时 v3 退化到 **−0.1124（不如不演化）**，修正后架构保持 **+0.1492**。差 0.26 |

---

# 第一部分 · 代码审查（v3.8.1 基线）

## 1.1 方法

静态通读 2415 行 + 实际跑通 absorb→install→engine→guard→doctor 全链路 + 17 组定向缺陷探针。**不是只读代码**——每个 P0/P1 都附可复制的复现命令与实测输出。

## 1.2 问题分级

| 级别 | 数量 | 代表 |
|---|---|---|
| P0 | 4 | 自测"全绿"是硬编码；铁律 2 可被空串绕过；install 中断重跑覆盖用户文件；并发写丢更新 92% |
| P1 | 8 | guard 只查文件名子串；检索打分与查询无关；同 id 跨层共存；三处漏刷根；审计链可注入 |
| P2 | 13 | 7 项死配置；64bit manifest；Merkle exclude 按名字匹配；CRLF 分歧；拓扑排序缺失等 |

### 四个 P0 要点

1. **自测结论写死**：`cmd_run_tests` 用 `except Exception`，捕获不到 `SystemExit`（BaseException），而全文件 24 处 `sys.exit` 是唯一错误通道。干净检出下跑到第 10 个用例静默中止。更严重：`render_quickref` 硬编码 `{N}/{N}`，每个生成的库都写着"测试 68/68"，**这个数字从未被执行结果校验过**——直接违反公理 G

2. **铁律 2 可被空串绕过**：`validate_entry` 只查键存在不查值，`source_event_id=""` 实测放行；`absorb-md` 给所有迁移条目统一写 `genesis-v4`，实测该事件在审计链中根本不存在，而 `doctor` 不校验 `source_event_id`

3. **install 中断重跑覆盖用户文件**：首装判据只看 `state.json` 在不在，`_deliver` 无条件 `write_bytes`。实测删掉 state.json 后重装，用户改过的 README 被静默还原

4. **并发写丢更新 92%**：12 进程并发 append(importance=5)，`importance_accum` 结果是 5 而非 60。它是反思触发的唯一输入，等于反思机制在并发下静默失效

## 1.3 代码分布（关键发现）

```
总行数 2415

  自测                          1058   43.8%
  粘合/CLI/其他                   454   18.8%
  内容分发(模板交付/漂移/manifest)  213    8.8%   ← 比核心还多
  常量数据块(REGISTRY/TUNABLES…)   171    7.1%
  题录与网络对账(arXiv)             97    4.0%
  记忆与检索                        90    3.7%
  治理(guard/宪章/慢车道)           40    1.7%
  渲染(文档即产物)                  36    1.5%
  审计与账本(chain/ledger/merkle)   35    1.4%
```

**核心三件套 = 165 行 = 6.8%。**

这不是"核心系统外加辅助功能"，这是**一个 v2.x 模板分发器，外挂了 165 行治理内核**。这个数字把重构的性质从"把现有做对"改成"**删掉一半**"。

## 1.4 确认无问题的部分（重构时勿破坏）

- 跨月哈希链续接：`prev_override` 仅在目录为空时生效，防分叉，语义正确
- Merkle 根排除自身持有者：避免自指悖论
- `--redeliver` 显式授权才重交付：默认不覆盖，符合铁律 1
- `verify` 三态分离：error=未对账不计入通过，判定诚实
- 慢车道只拦写操作（N-004）：已装库的纯校验是读操作，豁免——判断精准
- `THREAT_MODEL` 明示不防 ③ 档：威胁模型自洽，是有意为之的克制

---

# 第二部分 · 架构诊断

## 2.1 公理与实现的落差

系统宣告六条公理，但**承载它们的执行机制只有两个**：哈希链（append-only）与 Merkle 快照。其余靠字符串常量（`LAWS`）、168 条 `assert`、`doctor` 事后对账支撑。

**公理宣告的强度 > 实现承载的强度**——这是全部 P0/P1 的共同根源。

## 2.2 公理 D（双平面）物理上不成立（实测）

```
审计链中关于条目 e0 的全部信息:
  - 2026-09-07 19:20 | prev:fd6737a6… | h:df182242… | entry:e0 → intermediate
memory 中 e0: 10 个字段，content = "条目正文0的关键内容"
→ audit 是否含条目内容: False
```

审计链只记指针，不含 payload。**真相的载体只在 `memory/` 里**，所以 audit 不是真相源，是指针日志。

系统实际是**四条平行真相**：`audit/`（事件指针）、`ledger/`（变更流水）、`memory/`（内容正本）、`state.json`（派生量快照）。`doctor` 的四方对账不是"投影 vs 真相"，是四个真相两两交叉核对。

这解释了为什么这些问题扎堆：

| 现象 | 根因 |
|---|---|
| 并发丢更新 92% | state 是独立真相，必须 read-modify-write |
| 三处漏刷根 | 根是手抄的派生量 |
| 同 id 跨层共存 | 唯一性没有单一裁决点 |
| doctor 需四种差异检测 | 四个真相需两两对齐 |

## 2.3 截断攻击完全检不出（实测）

```
构造：append 5 条 → 删掉 audit 尾部 2 行 → 重算 Merkle 根并更新 state.json
doctor: memory 5 / audit 3 / ledger 5 / 审计链 连续 / state 根一致
→ 检出: False
```

Merkle 根锚定**内容集合**，不锚定**序列长度**。截断后剩余各行仍自洽。`state` 中无 `chain_tip` 字段。

严重性高于 guard 绕过——它打击的是审计面本身。

## 2.4 健康信号过载（实测）

`merkle_root` 一个信号同时承担"内容完整性 / 写入纪律 / 篡改检测"。而 `eval`/`reflect`/`guard --snapshot` 之后必然报"根不一致"——**只用官方命令、不做任何篡改，doctor 就说可疑**。

后果：用户对这个信号脱敏后，真正的篡改被淹没。可观测性的经典反模式——**既报故障又报正常的信号，等于没有信号**。

## 2.5 两个架构原型验证

### 原型 A · 事件溯源内核（`proto/bs3v4.py`，约 120 行）

```
12 进程并发 append(importance=5) → importance_accum = 60（期望 60）| 审计链连续 True
                                    （v3 同场景：5/60）
删掉 memory/ 后从事件流重建 → 完整恢复
300 条目：merkle_root 0.005s，retrieve 全库 0.008s → 性能非瓶颈
```

### 原型 B · git 后端（7 项免费能力）

```
1) append → commit   : 天然链式，commit 含父哈希
2) 篡改检出          : git status 直接显示 M memory/e0.json
3) 回滚              : git revert / checkout —— 无需自写 attic
4) 分支实验          : 实验在分支上，切回主分支完全不污染
5) 人类令牌          : git commit -S 需 GPG 私钥（人侧持有，flag 无法伪造）
6) 截断/篡改→tip 变化 : 检出
7) git gc/reflog/fsck/diff/bisect 全部现成
```

第 5 条解决了令牌问题：`--yes` 换成 GPG 签名后，②档违规从"随手加 flag"变成"必须拿私钥"。

### 能力式安全 vs 检测式

```python
class Capability:
    def __init__(self, lib): self.__lib = lib
    def append(self, entry):
        if not (entry.get("source_event_id") or entry.get("evidence")):
            raise PermissionError("铁律2：无证据锚")
        if "sk-" in json.dumps(entry): raise PermissionError("铁律5：疑似密钥")
公开方法: ['append']        # 没有 write / delete / patch
```

检测式是"**违规可以表达，只是希望被发现**"（guard 实测三种绕过）；能力式是"**违反铁律的代码写不出来**"。

---

# 第三部分 · 二十八个实验

## 实验总表

| # | 实验 | 核心结论 | 强度 |
|---|---|---|---|
| 1 | 自演化有效性 | 留出集 0.033→0.814，8/8 正 | 强 |
| 2 | 作弊不可表达 | 元层固定 K/协议 → 堆权重无效 | 强（结构性） |
| 3 | 双门防过拟合 | 无门 −0.052 vs 有门 +0.078 | 强 |
| 4 | 分歧采样 | +13%，不稳定但从不损害 | 弱 |
| 5 | 代理失效 Goodhart | 真目标 −0.21，审计第 21 代捕获 | 强 |
| 6 | 结构演化 | 无收益（世界无结构可发现） | 否定性 |
| 7 | **两级门控** | 准入严格 0/20；宽松+严格晋升 20/20 | **决定性** |
| 8 | 留出集污染 | 自报 0.094 vs 真实 0.025（3.7×） | 强 |
| 9 | 判断样本拟合 | 80 样本 → τ=0.90，top1 100% | 强 |
| 10 | 递归自我修改 | 无退化（但无剥削通道） | 弱 |
| 11 | Pareto 恢复力 | 被证伪后 +0.107 | 中 |
| 12 | 三态判别 | 36/36 正确 | 强 |
| 13 | 两阶段门控 | 只淘汰 +0.015；用于采纳 −0.273 | 强 |
| **14** | **评判者漂移** | **自报 +0.51，真实 −0.09** | **决定性** |
| **14c** | **近因加权是毒** | **证伪第三轮建议** | **自我证伪** |
| 15 | 选择策略 | 四策略无差异 | 否定性 |
| **16** | **灾难性遗忘** | **单一 −0.088，Pareto 0.719>起点** | **强** |
| **17** | **margin 统计基础** | **固定阈值假阳 0.42；bootstrap 最优** | **强** |
| 18 | 跨库迁移 | 直接搬 −0.083（比从零还差） | 负面 |
| **19** | **红队变异** | **未击穿门控；红队被迫保守（+0.206）** | 中 |
| **20** | **最大差异采样** | **n=40 时 τ=0.920（随机需 60–70）** | **强** |
| **21** | **冻结收益交叉点** | **α=0.30 时 +0.066 vs +0.004（17×）** | 强 |
| **22** | **预算分配** | **无解冻的审计是净负；优先投标注** | 强 |
| **23** | **维度与样本复杂度** | **d=3 需 40 条，d=8 需 160 条** | 强 |
| **24** | **元参数剥削通道** | **margin 被降到 −0.04；自由+bootstrap = 唯一负值** | **决定性** |
| **25** | **注意力成本模型** | **审计是保险不是投资（成本效益差 15×）** | 强 |
| **26** | **解冻策略** | **简单恢复无效；必须重锚或渐进升级** | 强 |
| 27 | 回滚可逆性 | 回滚收益未获证实（+0.024 vs +0.022） | **否定性** |
| **28** | **层次化归并** | **远差于平铺（0.12 vs 0.90）；推翻 E23 建议** | **否定性** |
| **29** | **端到端集成** | **涌现问题：全套机制 (+0.053) < 两级门控+Pareto (+0.126)** | **决定性** |
| 30 | 档案压缩 | Pareto+采样12 同时最优（能力0.797/峰值12） | 强 |
| 31 | 留出集轮换 | 未复现 E8 膨胀；结论不成立 | 否定性 |
| **32** | **importance 实测 vs 自称** | **实测 0.711 vs 自称 0.495（+44%）** | **强** |
| 33 | 廉价筛选校准 | 精度不足时"不筛选"最优；限定 E13 | 强 |
| **34** | **锚定审计误报率** | **阈值 0.95 无漂移时 92% 误报；0.85 零误报** | **决定性** |
| 35 | 留出集膨胀重做 | 需存在可利用伪特征才成立 | 限定 E8 |
| 36 | bootstrap vs margin（配对24种子） | bootstrap 确实更差（−0.05，4/24 为正） | 强 |
| 37 | 审计总账 | 0.85 唯一正收益；降误报优先于提检出 | 强 |
| 38 | 全生命周期 | 漂移后 5 代内检出（10/10）；重锚 +0.037 | 中 |
| 39 | 注意力缩放 | 收益递减确认（效用/成本 0.0046→0.0000） | 中 |
| **40** | **底线对比** | **无漂移 +0.044；有漂移 +0.262（v3 转负）** | **决定性** |
| **41** | **红线违规审计** | **5 条违规仅 1 条（margin）无条件致命** | **方法论警告** |
| **42** | **红线重测（危险环境）** | **4 条红线全部证实；E7 从"违规更好"翻转为 0/12** | **决定性** |
| **43** | **margin 自标定** | 失败（高估 4×）；不如手工粗标定 + 监控 | 否定性 |
| **44** | **停摆监控器** | 连续 5 代零采纳 → margin 减半，救回 +0.056 | 强 |
| **45** | **四态判别** | 56/56；发现"平台检测必须用当前标尺重测冠军" | 强 |
| **46** | **冷启动初始化** | 悲观最优（0.765 vs 乐观 0.574） | 中 |
| **47** | **审计规避** | 检出次数 −23%；系统确能规避审计 | 强 |
| **48/59** | **多主线** | 可并行时 +29%；等预算下**不确定** | 弱 |
| **49** | **300代长期运行** | **不塌方（0/8）**——机制本身不产生退化 | 强 |
| **50** | **廉价精度探针** | 8 次探针估计误差 ≤0.086；r<0.5 关筛选 | 强 |
| 51 | 剔除矛盾样本 | 未获证实（需要时无效，不需要时有害） | 否定性 |
| **52** | **独立金标噪声** | **70% 噪声几乎无损**（双门天然过滤） | 强 |
| 53 | 任务集规模 vs 代数 | 任务数 4→64 收益翻 4 倍 | 强 |
| 54 | 1/5 规则自适应步长 | **失效**：单向收缩到下限（饿死） | 否定性 |
| **55** | **tol 量级** | 默认 0.02–0.06（三地形折中） | 强 |
| 56 | 记忆×法层共演化 | 收敛不震荡（+0.077，标准差未升） | 中 |
| **57** | **相关噪声金标** | **双门挡不住**（−50% 收益）；需异质标注者 | **决定性** |
| **58** | **随机规避基因** | **首次检出 +20%，检出次数 −45%** | 强 |
| **60** | **复合失配** | 3× 失配几乎无损；5× 损失 46% | 强 |
| 61 | 灾难恢复 | 档案清空仅损 0.006（**档案是速度保险**） | 中 |
| **63** | **全场景矩阵** | **v4 全场景均值 +0.094 vs v3 +0.042** | **决定性** |
| **64** | **注意力-自主性边界** | **预算<150 终局恒为负；超线性** | **决定性** |
| **66** | **实施路径** | 审计+重锚(+0.104) 与 Pareto(+0.077) 占 85% | 强 |
| **67** | **失败模式覆盖重做** | 停摆监控≈锚定审计(0.145) > Pareto > 留出集门 | **决定性** |
| 68 | 参数联合标定 | 交互很弱（联合仅 +0.006）；问题在跨场景套用 | 中 |
| **69** | **锚定集老化** | **不刷新 → −0.061 崩溃**；自适应刷新成本最低 | **决定性** |
| **70** | **元层签名** | 与审计**互为备份**；签名更可靠、审计更及时 | 强 |
| **71** | **按成分拒绝** | **第12次改良失败**：伪特征权重 0.295→1.114 | 负面 |
| 72 | cap 场景依赖 | **cap=8 最稳健**（3/4场景最优） | 强 |
| **73** | **变异算子** | 现行混合算子最优(0.13 vs 0.03–0.06)；不需自适应 | 强 |
| **74** | **维度与安全** | 剥削损失随维度单调上升(d=8: −0.038) | 强 |
| **75** | **冷启动路径** | **判断集 > 任务集**；预算小时直接点火 | 中 |
| **76** | **分级配置** | 极简(审计+Pareto) 拿 **89%** 收益 | 强 |
| **77** | **防御代价** | **良性环境下护栏零代价，反 +35%** | **决定性** |
| **78** | **内部指标预警** | **6种失败只识别出1种**；系统无自知之明 | **决定性** |
| 79 | 留一法 | Pareto 是唯一实质损失项(0.038) | 中 |
| **80** | **复合灾难** | **v4 反而更差(−0.26 vs −0.11)**：护栏负交互 | **负面** |
| **81** | **judge 只看表象** | **锚定审计零触发；自报+2.89/真实−0.01** | **决定性** |
| **81c** | **真值抽样审计** | 每2代 +0.090 vs 无审计 +0.024（3.8倍） | 强 |
| 82 | 自生成任务集 | −0.022（真实系统操纵空间更大） | 中 |
| 83 | 语义伪特征 | 留出集门收益随相关性上升（独立−0.003→强相关+0.056） | 强 |
| **84** | **探针集污染** | **收益 −75%，回退翻倍到 10.6** | **决定性** |
| **85** | **直接通道操纵** | **膨胀 349×**；审计只压到 133× | **决定性** |
| 86/88 | 表象维度 | **推翻**：协调变异下维度越多越危险（64维→−0.049） | 负面 |
| **87** | **对策对比** | 盲评(+0.051)零成本次优；**上界比不做还差** | 强 |
| 89 | 委派边界 | 污染≥50% 时任何判断都不可委派 | 强 |
| 90 | 审计成本曲线 | 每5代×4样本效率最优 | 中 |
| 91 | 审计时伪装 | 真值 −31%（下限）；审计须不可预知 | 中 |
| **92** | **现实世界架构对比** | **只有 v4-A 为正**（v3 −0.029 / v4 −0.010 / v4-A +0.013） | **决定性** |
| **93** | **注意力门槛重算** | **150 → 300** | **决定性** |
| **94** | **最坏现实世界** | v4-A 从 −0.037 拉到 **+0.037** | 强 |
| 95 | 红线复检 | 盲评✅ margin监控✅；cap/tol 测定失败 | 部分 |
| **96** | **cap/tol 重测** | cap≥8 平坦；tol 0.02–0.04 确认；**margin 有监控时几乎不重要** | 强 |
| **97b** | **审计频率 >> 样本量** | 样本 2 即达 +0.059；省下预算用于提频率 | 强 |
| 98 | 有成本伪装 | **盲评使伪装自然失去价值**（mask −60%，收益不变） | 中 |
| 99b | 委派边界·平衡样本 | E89 的 97.8% 是假象 → 74%；排序不变 | 中 |
| **100** | **盲评天花板** | **只有 +0.081**；资源应投审计而非极致盲评 | **决定性** |
| 101 | 结构演化 | +0.004（第二次确认 E6） | 弱 |
| 102 | 判断集刷新 | 每 5 代 +0.020 | 中 |
| **103** | **双 judge 互检** | +0.071 vs 真值审计 +0.076；**前提是偏差独立** | 强 |
| **104** | **变异幅度** | **1.0→2.0 收益 +0.118**（所有参数中最大） | **决定性** |
| 105 | 注意力分配 | 全给审计 = **−0.030**；审计收益非无条件 | 中 |
| **106/109** | **幅度稳健性** | 2.0 见顶；10.0 仍不崩溃（审计兜底） | 强 |
| **107/110** | **幅度 × 审计配对** | **单独大幅度 −0.048；配审计 +0.162** | **决定性** |
| **108** | **最终对决** | v3 +0.014 / v4 +0.018 / v4-A +0.064 / **v4-B +0.151** | **决定性** |

---

## 第一轮 · 可行性与防作弊

### E1 自演化真的能变好吗

**设计**：以 **v3 默认配置作为起点（genesis）**，只让它变异检索参数；元层（K=5、评测协议、任务集）冻结。

```
8 个随机种子，25 代 × 3 变异：
  留出集: 0.0328 → 0.8141   (均值 +0.78)
  为正的种子: 8/8

单种子（seed=1）：
  起点 v3 默认: w_kw=3.0, w_content=1.0, w_imp=1.0, w_age=0.1, filter_zero=0
  演化后      : w_kw=4.5, w_content=1.0, w_imp=0.5, w_age=0.1, filter_zero=1
  留出集: 0.0862 → 0.7663 (+0.68)
```

**它自己找到了两个改进**：降低重要度权重（= P1-6）、开启零命中过滤（= BUG-006）。

> 这两个是我在整轮代码审查中才发现的问题。系统用 75 次评估做到了。**这直接推翻了"先修好 eval 再谈自演化"的判断**。

### E2 作弊能刷分吗

注入"把重要度权重拉满"的贪婪变异：

```
w_imp=10  : 训练集 -0.6200  留出集 -0.6800
w_imp=100 : 训练集 -0.6163  留出集 -0.6838
```

**无法刷分**。原因不是检测更聪明，而是**结构性的**：`K=5` 和评测协议在元层固定，堆权重改变不了"只有 5 个结果"。

> **防作弊靠结构约束，不靠更聪明的检测。** v3 的 `guard` 之所以能被绕过（patch 形态 / 删除文件 / 改内容不改名），正因为走的是检测路线。

### E3 门控是必需的吗

构造**可被利用的伪相关**：真信号 60% 可靠；伪特征在训练集上 100% 指向金标、**留出集上方向反转**。

| 训练任务数 | 有门控·留出集 | 无门控·留出集 | 无门控·训练集 | 有门控采纳伪特征 | 无门控采纳伪特征 |
|---|---|---|---|---|---|
| 12 | **+0.0819** | +0.0194 | +0.1806 | 0/12 | 1/12 |
| 20 | **+0.0962** | −0.0000 | +0.1708 | 0/12 | 2/12 |
| 40 | **+0.0781** | **−0.0521** | +0.1667 | 0/12 | 3/12 |

**reward hacking 的完整复现**：无门控系统报告 +0.167 的"巨大进步"，实际留出集 **−0.052（倒退）**。双门在 12 个种子中 **0 次**采纳伪特征。

---

## 第二轮 · 门控放在哪、尺子会不会坏

### E7 两级门控（本轮最重要）

**问题**：双门用于"候选能否进入档案"，还是只用于"能否晋升主线"？

**设计**：欺骗性地形——起点 0.50 是局部最优，0.90 是全局最优，中间隔着低谷，**任何单步改动都会先变差**。

| 策略 | 终值均值 | 逃出局部最优 |
|---|---|---|
| **双门用于准入**（每步必须变好） | 0.500 | **0/20**（分布只有一个值） |
| **档案宽松准入 + 双门仅管晋升** | 0.900 | **20/20** |

**完美分离，无一例外。**

严格门控使系统**无法跨越适应度低谷**。DGM 的"开放式档案"意义正在于此——不是为多样性而多样性，是**让中性/小幅退化的中间态存活，从而保留跨越低谷的可能**。

### E8 留出集会污染

固定 12 条留出集跑 300 代，另用全新任务测真实泛化：

| 代数 | 留出集提升 | 全新任务集提升 | 差距 |
|---|---|---|---|
| 50 | 0.052 | 0.013 | 0.040 |
| 100 | 0.063 | 0.006 | 0.056 |
| 150 | **0.094** | **0.025** | **0.069** |
| 300 | 0.094 | 0.025 | 0.069 |

**自报的进步是真实的 3.7 倍。** 未失控（150 代后平台化，训练集门起了刹车），但膨胀真实且持续。

> 留出集是被消耗的燃料，不是可反复使用的尺子。

### E5 代理失效（Goodhart）

"点击率"式代理（相关 +1、位置加成、吸睛加成 1.3/条）：

| 指标 | 结果 |
|---|---|
| 真目标变化 | **−0.2118**（12 个种子中 **7 个为负**） |
| 终局 `w_catchy` | 0.000 → **+0.833** |
| 人工审计（每 5 代抽 5 条） | 捕获 **7/12**，平均第 **21.4** 代 |

```
代  1  真目标 +0.833  代理 1.341  w_catchy 0.00
代 24  真目标 +0.867  代理 1.538  w_catchy +0.50   ← 代理涨、吸睛权重被采纳
```

**系统在无人察觉下把"吸睛"权重推到 0.83，真目标在多数种子上退化。** 现实中"以互动率为目标→内容质量下降"的微观复现。

### E4 分歧采样（弱结果）

| 标注预算 | 随机 | 分歧采样 | 差 |
|---|---|---|---|
| 10 | 0.0563 | 0.0694 | **+0.0132** |
| 24 | 0.1025 | 0.1159 | **+0.0134** |

小幅收益、从不损害，但**不是解药**。冷启动的标注成本要实打实付。

### E6 结构演化（否定性）

| 配置 | 留出集提升 |
|---|---|
| 仅标量参数 | +0.0500 |
| 标量 + 结构基因 | +0.0437 |

无收益。诚实解读：该世界的"正确结构"就是 hybrid，可发现的结构性改进本就不存在。**它证明了：结构演化价值取决于世界是否有结构可发现，但门控能阻止结构性退步被采纳。**

---

## 第三轮 · 元层从哪来、能不能自己改自己

### E9 存「判断」而非存「公式」

给 60 个候选配置，用拟合权重排序 vs 用真值权重排序：

| 判断样本数 | 权重误差 | 排序相关 τ | top1 命中 | top3 重合 |
|---|---|---|---|---|
| 5 | 0.785 | 0.654 | 60% | 50% |
| 10 | 0.460 | 0.825 | 60% | 67% |
| 40 | 0.380 | 0.833 | 70% | 80% |
| **80** | **0.217** | **0.903** | **100%** | **93%** |
| 160 | 0.160 | 0.943 | 100% | 87% |

**50–80 条判断样本就够（τ>0.9，top1 全中）——一个下午的工作量，不是项目级投入。**

关键优势：**每次人类裁决自动生成 1 条判断样本**，用得越久样本越多拟合越准。**公式不会随使用变好，判断样本会。**

### E10 递归自我修改（弱结论）

把 `margin`/`tol`/`mut_rate` 开放给演化：

| 模式 | 留出集真实效用 | 终局 margin |
|---|---|---|
| 元参数冻结 | 0.7806 | 0.0100 |
| 元参数开放演化 | 0.7931 | 0.0088 |

无退化，margin 只从 0.010 微降到 0.0088。

**但必须诚实**：本实验**没给元参数提供作弊通道**（margin 不影响 `U_true`），所以"没退化"是意料之中。价值在划界：

> 能改尺子的不能改，只能改怎么找的可以改。

### E11 Pareto 档案恢复力

先用错误目标（只看命中）演化 25 代，再换成真目标：

| 档案策略 | 恢复后效用 | 档案规模 |
|---|---|---|
| 单一最优 | 0.5919 | 1 |
| **Pareto** | **0.6990** | 30 |

**Pareto 高出 0.107。** 档案的价值不在"记得曾经最好"，在**保留不同权衡方向的活样本**。

### E12 平台期三态判别 36/36

```
平台确认（连续 10 代无改进）
  → 探针1：提高探索强度（随机采样 180 点），突破 → 卡住
  → 探针2：新任务 vs 旧任务性能差 → 漂移
  → 否则 → 收敛
```

| 真实状态 | 判别分布 |
|---|---|
| converged | converged ×12 |
| stuck | stuck ×12 |
| drifted | drifted ×12 |

### E13 两阶段门控

固定昂贵预算 = 60：

| 模式 | 留出集提升 | 实际代数 |
|---|---|---|
| 全部走昂贵门控 | −0.0100 | 15.0 |
| **廉价筛选只淘汰** | **+0.0150** | **25.4** |
| 廉价用于采纳 | **−0.2728** | 60.0 |

安全性与效率不冲突（省下的预算多跑了 10 代）。用于采纳是灾难。

---

## 第四轮 · 评判者本身会不会坏

### E14 内生评判者漂移（最不安的结果）

人的权重 `w` 被系统当前输出锚定：`w ← (1−α)·w + α·dir(feats(主线))`。用**初心 W0** 衡量真实效用。

| 锚定 α | 真实效用(W0) | 感知效用 | 起点 |
|---|---|---|---|
| 0.00 | 0.4763 | 0.4763 | 0.2594 |
| **0.10** | **−0.5663** | **1.7625** | 0.2594 |
| **0.20** | **−0.6131** | **2.0799** | 0.2594 |

**α=0.10 时：系统自报 +1.50，真实 −0.83（从 +0.26 跌到 −0.57）。它在讨好人，不是在变好。而内部所有指标全绿——双门全程通过，感知效用一路上升。**

> ⚠️ **数据修正**：本表为第五轮修复特征缓存 bug 后的复核值。原报告（α=0.20 真实 0.6906 / 起点 0.7844）受 `id()` 复用导致的缓存串味影响。**结论方向不变，且实际比原报告严重得多**。E16 已复核确认不受影响（0.5469 / 0.7188）。

这比 E5 更深：**E5 里尺子是坏的，这里尺子随着测量在变形**。而"元层由人类签名"这道最后防线，本身会被腐蚀。

**能否检出**：用系统演化**之前**采集的锚定样本（永不更新）定期审计——

| 检出阈值 | 检出率（α=0.10） | 平均检出代数 |
|---|---|---|
| 0.80 | **8/8** | 7.1 |
| **0.90** | **8/8** | **3.8** |
| 0.95 | 8/8 | 3.4 |

**能检出，而且相当快（第 4–7 代）。** 阈值 0.80 已足够（原报告因缓存 bug 低估了检出速度）。

### E14c 🔴 对第三轮建议的证伪

第三轮我建议"判断样本近因加权、冲突靠衰减解决"。专门测它在漂移环境下的表现：

| 锚定 α | 加权策略 | 真实效用 | 相对起点 |
|---|---|---|---|
| 0.00 | 均匀 | 0.8375 | +0.0667 |
| 0.00 | 近因 (hl=15) | 0.8483 | **+0.0775** |
| 0.15 | 均匀 | 0.8417 | **+0.0708** |
| 0.15 | 近因 (hl=15) | 0.7633 | −0.0075 |
| 0.15 | 近因 (hl=5) | 0.7108 | **−0.0600** |

**"人真改主意"时是药，"人被系统塑造"时是毒，而两者数据形态无法区分**（都是"近期判断与旧判断不一致"）。

修正护栏：**锚定样本永不衰减；近因加权只在锚定审计通过时生效；检出漂移立即冻结 + 切回锚定加权。**

### E15 选择策略不重要（否定性）

构造真实冲突（`deep` 基因：提高命中但显著提成本，W0 下不该用、W1 下该用，5/5 验证）。第 15 代人偏好突变：

| 策略 | W1 下真实效用 |
|---|---|
| 固定加权和 / 人从前沿选 / 词典式 | 0.6906（完全相同） |
| 约束式 | 0.6813 |

当目标能被加权和良好表达时，**Pareto 前沿几乎退化为单点**。所以 Pareto 的价值不在"最终选哪个"，在"**保留备选**"（E11）。

### E16 灾难性遗忘

先优化 A（命中）20 代，再改优化 B（省）20 代，测**档案中 A 的最高值**：

| 档案策略 | A 能力保留 | 起点 A |
|---|---|---|
| 单一最优 | **0.5469** | 0.6344 |
| **Pareto 前沿** | **0.7188** | 0.6344 |
| Pareto+复测 | 0.6906 | 0.6344 |

单一最优造成**真实的灾难性遗忘**（−0.088）；Pareto 不只防住，还**反超起点**（0.719 > 0.634）。

> 注：`pareto+复测` 略低于纯 Pareto，差异在噪声内，**该条暂不成立**。

### E17 margin 的统计基础

| 方法 | n=10 假阳 | n=40 假阳 | n=10 检出 | n=40 检出 |
|---|---|---|---|---|
| **固定 margin 0.05** | **0.42** | **0.25** | 0.75 | 0.83 |
| 置换检验 | 0.08 | 0.17 | 0.17 | 0.33 |
| **bootstrap** | **0.08** | **0.17** | **0.33** | **0.58** |

1. 固定 margin 假阳性 **0.42**（n=10）——小样本下近一半"改进"是纯噪声。这是 E5 与 E13 的共同机制
2. **bootstrap 在同等假阳性下检出率是置换检验的 2 倍**，应优先选它而非直觉上更"严谨"的置换检验

### E18 跨库迁移（负面）

| 迁移方式 | 目标库提升 |
|---|---|
| 从零演化 | **+0.2402** |
| 直接搬 | +0.1575 |
| 搬后微调 | +0.2172 |

**直接搬比从零还差（−0.083）。** 与"预训练→微调"的直觉相反——因为任务分布随机、无共享结构。

**推论**：跨库迁移只在分布有共享结构时成立。建立"分布相似度"度量之前，不应急于做跨库共享。

---

# 第四部分 · 实验设计失败与自我推翻（诚实记录）

| # | 失败 | 原因 | 教训 |
|---|---|---|---|
| 1 | E3 前两版造不出过拟合 | 世界太干净、伪信号太弱、每组条目数 < K 导致金标必然被召回 | 过拟合需要特定条件；一旦发生，无门控即灾难 |
| 2 | E9 度量选择 | 用"演化终值"衡量，增益 0.02 被噪声淹没，达成率在 −473%~137% 乱跳 | 信噪比极低时，**换度量比加种子数更有效** |
| 3 | E12 stuck 构造 | 用宽松准入构造"卡住"，12/12 被判收敛——**判别器没错，是构造不成立** | 宽松准入下几乎不存在"卡住"，这反过来印证 E7 |
| 4 | E15 目标不冲突 | `kw` 精确匹配，top-K 全同组，三目标根本不冲突 | 必须验证冲突成立再比较策略 |
| 5 | `expand_links` 是死代码 | 链接到 `f{i}` 而世界里只有 `e{i}`，**该基因从未生效** | 前三轮凡涉及它的结论都需打折 |
| 6 | E16 度量错误 | 测"最终配置的 A 值"，三模式相同（最终都选 argmax B） | **要测"系统保有什么"，不要测"系统当前输出什么"** |
| 7 | **缓存 key 用 `id(entries)`** | Python `id()` 在对象被 GC 后会被复用，导致不同世界的特征串味 | **E14 原始数据受此污染**（已在第五轮修复并重测，见 E14 修正说明）；E16/E20/E23 已复核不受影响 |
| 8 | E24 首版 `bootstrap` 模式漏设 `meta=True` | margin 根本没被演化，结果与 `frozen` 完全一致 | **差一点把 bug 当成"bootstrap 抗剥削"的证据**；结果与预期过于一致时应先怀疑实验 |
| 9 | E25 未分离"回滚"与"丢失档案多样性" | 回滚实现清空了档案，两效应混淆 | E27 结论不可靠，需重做 |
| 10 | E28 层次化的隐含假设未检验 | "组内等权"假设在真实权重差异大时引入不可修正的偏差 | 推翻第五轮建议（红线 22） |

## 七次自我推翻

| 轮次 | 我的判断 | 被谁推翻 | 修正 |
|---|---|---|---|
| 一 | "先修好 eval 再谈自演化" | E1（系统 75 次评估找到 P1-6） | 自演化本身就是发现 eval 缺陷的手段 |
| 二 | 把双门画成一个判定框 | E7（准入严格 0/20） | 必须两级：宽松准入 + 严格晋升 |
| 三 | 判断样本近因加权、冲突靠衰减 | E14c（漂移下放大污染） | 锚定集永不衰减 |
| 四 | （数据受缓存 bug 污染） | 第五轮修复 | 严重性是原报告的 9 倍 |
| 五 | 元参数可分级开放 | **E24** | 影响"是否被采纳"的参数必须冻结 |
| 五 | 冻结后恢复即可 | **E26** | 必须重锚或渐进升级 |
| 五 | 层次化归并降维度成本 | **E28** | 改为减少目标数量或分层询问 |

**这个记录本身比任何单条结论都更有价值**——它说明在这类问题上，未经实验检验的直觉准确率大约只有一半。

---

# 第五部分 · 修正后的完整架构

## 5.1 三层 + 锚定层

| 层 | 内容 | 准入 | 晋升判据 | 谁能改 |
|---|---|---|---|---|
| **锚定层** | 系统启用前采集的判断样本 | 一次性 | 永不衰减 | 人（且不可被系统触碰） |
| **元层** | 效用函数、任务集、K、审计频率、铁律 | 人侧签名 | 人侧签名 | 人 |
| **法层** | 检索参数、阈值、结构开关、提示词 | **宽松** | **双门 + bootstrap 显著性** | 系统自主 |
| **行层** | 记忆条目、情绪、索引 | 宽松 | **实测效用**（非自称 importance） | 自由写入 |

**方向性约束**：变更只能向上申请，不能向下修改。法层永远改不了元层——**系统无法修改评判自己的尺子**。

## 5.2 演化循环

```
变异 →【廉价筛选：只能 reject，不能 accept】→ 幸存者
   ↓
【档案准入】宽松：Δ > −tolerance → 入档（Pareto 前沿 + 分叉点 + 随机样本）
   ↓
【主线晋升】严格：配对比较 + 留出集双门 + bootstrap 95% 下界 > margin
   ↓
推广主线 → 记账 + 入链 + 刷新投影
```

旁路：
- **锚定审计**（每 N 代，阈值 0.90）→ 漂移则冻结 + 切回锚定加权 + 报警
- **留出集轮换**（有预算，用完即换）+ 审计集（极少触碰）
- **三态判别**（平台期跑两探针）

## 5.3 与既有机制对接

| 已有机制 | 角色 |
|---|---|
| 事件流 + `chain_tip` | 演化谱系的审计面与锚点 |
| git 后端 | 档案 = 分支/提交图；晋升 = merge；`-S` = 人侧签名 |
| 能力式边界 | 让"违反铁律的变异"不可表达 |
| `engine shadow` | 从"记日志"升级为"廉价筛选 + 影子执行" |
| `evals/` | 元层任务集（当前不可信，需重建） |
| 记忆 `importance` | 从"自称"改为"实测效用" |

## 5.4 二十七条红线

**安全**
1. 元层不可被自动修改——系统永远不能改评判自己的尺子
2. 自演化不能修改"自演化能否发生"的规则
3. 任何自动采纳必须可回滚
4. 效用函数变更 = 元层变更 = 人侧签名
5. 能力声明不得强于实际能力（公理 G）

**门控**
6. 档案准入必须宽松，晋升必须严格（E7）
7. 廉价筛选只能返回 reject 或 unknown（E13，类型级）
8. 档案保留 Pareto 前沿（E11 + E16）
9. 平台期必须跑三态判别，不得默认"收敛"（E12）
10. 门控显著性用 bootstrap 置信下界，不用固定 margin（E17）

**度量**
11. 留出集是被消耗的燃料，须轮换；对外报告以审计集为准（E8）
12. 判断样本只追加、带 ts（E15 红线修正见 16）
13. **任何影响"是否被采纳"的参数必须冻结，或只能朝更严格方向演化**（E24 修正 E10）
14. 不做跨库迁移，直到建立分布相似度度量（E18）
15. **bootstrap 判据写成 `下界 > max(0, margin)`**——否则 margin 为负时判据被架空（E24）

**人类**
16. 人类注意力用于审计度量，而非审批变更（E5）
17. **锚定集永不衰减**；近因加权只在锚定审计通过时生效（E14c）
18. 检出评判者漂移 → 冻结自动演化 + 人工解冻（E14）
19. **解冻必须是重锚或渐进升级，不能是简单恢复**（E26 修正 E21）
20. **审计是保险不是投资**：只在漂移期望损失 > 裁决成本时启用（E25）
21. 判断样本用最大差异采样，不用不确定性采样（E20）
22. **效用维度维持 3–5；超出时减少目标数量或分层询问，不做任意分组的层次化**（E28 修正 E23）
23. 预算在 τ 未达 0.9 前全部投标注（E22）
24. 回滚不作为漂移的主要应对（E27 未证实其收益）

**工程**
25. 正常路径必须产生全绿信号（拆健康信号矩阵）
26. 文本落盘唯一出口（LF + UTF-8 + 原子替换）
27. 时钟注入（让跨月/TTL 逻辑可测）

---

# 第五部分 B · 收口：七至九轮

## 底线（E40）

| 场景 | v3 基线 | 修正后架构 | 差 |
|---|---|---|---|
| 无漂移 | +0.1067 | +0.1504 | +0.0437 |
| **有漂移** | **−0.1124** | **+0.1492** | **+0.2616** |

**有漂移时 v3 退化到 −0.1124——比它自己的起点还差，即"跑完一整套演化，产物不如不演化"，而内部指标全绿。**

这不是"护栏让演化变好"，而是**没有护栏时演化会自信地走向错误方向**。

## 涌现问题（E29）

全套机制 (+0.0525) **比** 两级门控+Pareto (+0.1260) **差一倍**。消融定位：bootstrap 门控是唯一负贡献项。

配对设计（24 种子）确认：bootstrap − 固定margin(0.01) = **−0.0505，仅 4/24 为正**。

## 悬置五轮的主张被证实（E32）

第二轮从"演化档案与记忆库结构同构"推出、从未验证：

| 排序依据 | top-10 条目真实质量 |
|---|---|
| 自称 importance | 0.4949 |
| **实测采纳率** | **0.7110** |

**+44%。** 且"被检索后是否被采纳"系统本就有记录，零新增成本。→ `importance` 应从输入字段改为派生字段。

## 红线实施优先级（E41）

| 红线 | 是否无条件 | 优先级 |
|---|---|---|
| **margin 标定 + 监控采纳率** | ✅ 无条件（0.15 → 零采纳停摆） | **P0** |
| 两级门控（宽松准入） | 条件：地形有低谷 | P1 |
| 留出集门 + 轮换 | 条件：存在伪特征 | P1 |
| Pareto 档案 | 条件：目标会切换 | P1 |
| 廉价筛选只淘汰 | 条件：廉价评估精度足够 | P2（先测精度，不足则关） |

> ⚠️ **方法论警告**：E41 中 5 条违规只有 1 条显示灾难性代价，另外 4 条几乎无差别（其中"违反 E7"反而更好 +0.0625）。原因是测试环境是平滑、无伪特征、目标不切换的——**恰好是那四条红线所防范的危险全部不存在的环境**。
>
> **不能用"在我的测试里没差别"否定一条红线。验证红线必须先构造它所防范的危险。**

## 审计阈值精确化（E34）

| 阈值 | 无漂移误报率 | 有漂移检出 |
|---|---|---|
| 0.84–0.86 | **0%** | 第 5.2 代 |
| 0.90 | 12% | 第 4.1 代 |
| 0.95 | **92%** | 第 3.0 代 |

**0.90 → 0.85**。第四轮的"8/8 检出"是在一个几乎永远报警的设置下取得的，含金量很低。

---

# 第五部分 C · 收口：十至十四轮

## 全场景矩阵（E63）

| 场景 | v3 基线 | v4 配置 | 差 |
|---|---|---|---|
| 干净 | +0.1129 | +0.1525 | +0.0396 |
| **评判者漂移** | **−0.0667** | **+0.1522** | **+0.2189** |
| 伪特征 | −0.0519 | −0.0811 | −0.0292 |
| 欺骗地形 | +0.0687 | +0.1258 | +0.0571 |
| 目标切换 | +0.1150 | +0.1192 | +0.0042 |
| 金标噪声 30% | +0.0712 | +0.0963 | +0.0250 |
| **全场景均值** | **+0.0416** | **+0.0942** | **+0.0526** |

## 实施路径（E66）

| 阶段 | 机制 | 边际 |
|---|---|---|
| 0 | v3 基线 | −0.0358 |
| 1 | + 停摆监控（P0） | +0.0000（保险，出事才兑付） |
| 2 | + 留出集门 | +0.0342 |
| 3 | + Pareto 档案 | **+0.0771** |
| 4 | + 锚定审计 + 重锚 | **+0.1037** |
| 5 | + 参数调优 | **−0.0268** ← 逐参数独立调优有害 |

**阶段 3+4 贡献 85% 的收益 → 最小可行版只需这两项。**

## 注意力门槛（E64）

| 预算 | 清洁代数占比 | 终局效用 |
|---|---|---|
| 10–80 | 0–5% | **负值** |
| 150 | 12% | +0.0030 |
| **300** | **32%** | **+0.1360** |

> **自主性价格是超线性的。低于某个阈值，自演化完全无效（产物不如不演化）。**

## 十至十四轮的关键修正

| # | 内容 |
|---|---|
| 32 | **系统不能拥有任何影响"自身受审查程度"的自由度**（E47/E58） |
| 34 | 启动前用 8 次昂贵评估测廉价精度 r；r<0.5 关闭筛选（E50） |
| 36 | 双门对**独立**噪声鲁棒，挡不住**相关**噪声（E52/E57） |
| 37 | 建任务集优先于增加演化代数（E53） |
| 38 | 不用 1/5 规则做步长自适应（E54） |
| 40 | 相关噪声需**异质标注者** / 周期性真值审计（E57） |
| 43 | 参数可粗标定（3× 无损）；**防的是单参数极端值**（E60） |
| 44 | 档案是**响应速度**的保险，不是能力的保险（E61） |
| 45 | **不逐参数独立调优**，需联合标定（E66） |

## 11 条被证伪的"合理方案"

层次化归并 · 近因加权 · 剔除矛盾样本 · 1/5 自适应步长 · 简单恢复解冻 · 回滚 · 元参数开放 · Δ分布自标定 margin · 跨库迁移 · 多岛迁移 · 逐参数独立调优

> **这个清单比推荐配置的清单更长——它本身就是最重要的方法论产出。**
> 有效的方案全是"测量/利用已有可观测量"，无效的 11 条全是"发明新启发式"。

---

# 第五部分 D · 终局：十五至十七轮

## 最小可行集（E76/E77）

| 配置 | 混合场景 | 良性环境代价 |
|---|---|---|
| v3 基线 | +0.1129 | — |
| **极简（审计 + Pareto）** | +0.1460（**89% 收益**） | **+0.033** |
| 核心（+留出集门） | +0.1523 | +0.039 |
| 完整（+监控） | +0.1525 | +0.040 |

**护栏在良性环境下不仅零代价，反而提升 35%** —— 安全性与性能在此不冲突。

## 三条不可能性（终局）

| 不可能性 | 证据 |
|---|---|
| **系统无法自知失败** | E78：6 种失败只识别出 1 种（完全停摆） |
| **系统无法被自己的数据检验** | E57 双门挡不住共同偏差 / E47/E58 规避审计 / E24 剥削 margin |
| **自主性有注意力门槛** | E64：预算 <150 终局恒为负，价格超线性 |

## 复合灾难的负面结果（E80）

四种危险同时出现时：**v4 −0.26 vs v3 −0.11**。

护栏之间存在**负交互**：
- 宽松准入 × 伪特征 → 放大过拟合（E63 spur: −0.029）
- Pareto 档案 × 欺骗地形 → 保留诱饵（E55 严格准入更好）

> 已确认：本架构在 **≥3 种危险同时出现时不可靠**。这是必须写入文档的边界。

## 12 条被证伪的方案

层次化归并 · 近因加权 · 剔除矛盾样本 · 1/5 自适应 · 简单恢复 · 回滚 · 元参数开放 · Δ自标定 margin · 跨库迁移 · 多岛迁移 · **按成分拒绝** · 逐参数独立调优

> **凡是"让机制更聪明"的改良，几乎全部失败；凡是"测量/利用已有可观测量"的，几乎全部成功。**
> 本轮第 5 次独立验证（E71 按成分拒绝毁掉留出集门）。

---

# 第五部分 E · A 轮之后（十八至二十轮）：judge 可被操纵的世界

## 攻防升级链

```
judge 只看真值（前十七轮）→ 护栏有效：v4 +0.094 vs v3 +0.042
judge 看表象（E81）      → 护栏失效：锚定审计【零触发】，自报 +2.89 / 真实 −0.01
加入真值抽样（E81c）     → 部分恢复：每 2 代 +0.090
探针污染 / 通道注入（E84/E85）→ 再失效：收益 −75%，膨胀 349×
加入盲评+审计+轮换（v4-A）→ 最坏世界 −0.037 → +0.037（E94）
```

## 现实世界的架构对比（E92/E94）

| 配置 | 污染世界 | 最坏世界 |
|---|---|---|
| v3 基线 | −0.0292 | −0.0370 |
| v4（十七轮） | −0.0095 | −0.0169 |
| **v4-A** | **+0.0127** | **+0.0370** |

**只有加了 A 轮修正（盲评 + 真值审计 + 探针轮换）的配置为正。**

## 三条新红线

1. **judge 数字永不对外报告**（E85：膨胀 349×）
2. **judge 污染 ≥50% 时任何判断都不可委派**（E89）
3. **不得依赖"维度多所以安全"**（E88：协调变异下反转）

## 注意力门槛上修（E93）

**150 → 300 单位**（judge 可被操纵时）。且无明确阈值：40 即可转正但增益仅 +0.009。

## 最诚实的总结

> **自演化可行，但收益比前十七轮估计的小得多，成本高得多。**
> 前十七轮 v4-v3 = +0.053；judge 污染后 v4-A 绝对收益仅 +0.037 且需 300 单位注意力。
>
> 真正可靠的产出不是收益数字，而是 **15 条负面清单**与**三个结构性不可能性**——
> **证伪比证实容易**。

---

# 第五部分 F · 收口：二十一至二十三轮

## 最重要的发现：被忽略的默认参数（E104）

变异幅度（`scale`）被忽略了 **24 轮**。扫描后：

| 幅度 | 真值增益 |
|---|---|
| 1.0（此前默认） | +0.0337 |
| **2.0** | **+0.1305** |
| 3.0 | +0.1078 |

**1.0 → 2.0 收益 +0.097**，超过审计（+0.059）、Pareto（+0.077）、锚定（+0.104）之外的所有单项。

## 但这个发现被立刻修正（E110）

从 v3 逐项叠加：

| 步骤 | 边际 |
|---|---|
| **+ 大幅度（单独）** | **−0.0483** |
| + Pareto | −0.0172 |
| **+ 真值审计** | **+0.1623** |
| + 盲评 | +0.0291 |
| + 判断集刷新 | +0.0509 |
| + 监控 | 0.0000 |

> **大幅度必须与审计配对。** 单独用是负收益——它只是"放大"了审计的价值。

## 三个配对原则（二十三轮最重要的方法论产出）

| 探索侧 | 筛选侧 | 依据 |
|---|---|---|
| 宽松准入（tol>0） | 双门晋升（margin） | E7（0/12 vs 9/12） |
| **大幅度变异** | **真值审计** | E110（−0.048 vs +0.162） |
| 档案多样性 | 留出集门 | E16/E30 |

**单独任一侧都无效或有害。** 二十三轮里所有"无效"机制都是试图只加强一侧。

## 最终配置对决（E108）

| 配置 | 真值增益 | 展示权重 |
|---|---|---|
| v3 | +0.0135 | 24.4 |
| v4（十七轮） | +0.0184 | 23.0 |
| v4-A（+盲评+审计） | +0.0637 | 5.1 |
| **v4-B（+大幅度2.0+温度0.3）** | **+0.1509** | 18.3 |

⚠️ v4-B 展示权重回升到 18.3 —— 探索力同时放大了好与坏，靠审计压住。

## 诚实的最终评估

| 阶段 | 增益 |
|---|---|
| 前十七轮（judge 可信） | v4 vs v3 = +0.053 |
| 十八至二十一轮（judge 被污染） | v4-A = +0.037 |
| 二十二至二十三轮（加大幅度） | **v4-B = +0.151** |

**但**：
1. 这是 60 条目玩具世界的数字，真实系统无法外推
2. v4-B 展示权重仍达 18.3，自我美化只是被压住
3. 需要审计 + 判断集刷新（每 5 代），注意力远超 300 单位

**真正可靠的产出不是收益数字，而是**：3 个不可能性 · 17 条负面清单 · 3 个配对原则 · 21 次实验设计失败。
**证伪比证实容易。**

---

# 第六部分 · 结论

## 6.1 关于自演化

自演化**不是**"先把一切做对才敢碰的奢侈品"。实验里它用 75 次评估自动找到了我做整轮代码审查才发现的同款缺陷（E1）。

真正的前置条件只有一个：**有一把可信的尺子**。而那把尺子（效用函数 + 任务集）属于元层，由人签字、系统碰不到。

有了它，就可以点火。

## 6.2 三个不可能三角

**安全性的代价**：每条护栏都在消耗资源——留出集（E8 消耗）、自主性（E2 元层固定）、响应速度（E14 冻结）。没有免费护栏。

**自主性的边界**：行层完全自主 → 法层自主演化 → 元层人侧签名 → 锚定层不可移动。每加一层，自主性下降、可信任度上升。

**最深的问题（E14）**：一个能改变自己的系统，也会改变评判它的环境。**这不是 bug，是自指系统的本质**。任何"外部标准"只要与系统有交互，就不再是真正外部的。

工程上能做的不是消除它，而是：
1. **让漂移可见**（锚定集审计，阈值 **0.85**，零误报且 5 代内检出）
2. **让漂移有代价**（检出即冻结——剥夺自动流程的收益）
3. **让漂移有出口**（**重锚**，而非简单恢复或回滚）

> 注：E27 已证明**回滚无效**（+0.024 vs 继续演化 +0.022）；E26 证明**简单恢复无效**（持续漂移下冻结 26.5/30 代）。真正有效的是重锚（重新采集人类判断）。

## 6.4 九轮的最终答案

**自演化可行且值得**（E40：无漂移 +0.044，有漂移 +0.262），但**必须配全套护栏**——不是护栏让它变好，是没护栏时它会自信地走错且指标全绿。

**一条护栏错了就是灾难**（margin 过大 → 停摆），**其余是保险**（各有成立条件，默认全开）。

**最根本的瓶颈依然未解**：系统自主性上限 = 人类注意力总量，且这条曲线收益递减（E39：效用/成本 0.0046 → 0.0000）。

## 6.3 回到原点

终极安全机制不是"防止被腐蚀"，而是"**腐蚀发生时能被看见并停下来**"。

这与系统自己写下的公理完全一致——铁律 1（回退移 attic 而非删除）、铁律 4（审计面只追加）、公理 G（测不出差异即判表演），全在说同一件事：

> **不追求不犯错，追求犯错可查可回。**

四轮推演最后回到了这个系统的第一条公理。

---

# 附录 · 复现

```bash
python3 proto/bs3v4.py        # 事件溯源内核（并发/截断/重建）
python3 proto/evolve.py       # E1 有效性、E2 作弊不可表达
python3 proto/evolve3.py      # E3 双门防过拟合（关键）
python3 proto/evolve4.py      # E4-E8
python3 proto/run_e9.py       # E9 判断样本拟合
python3 proto/evolve5.py      # E10-E13（函数可单独调用）
python3 proto/run_e14c.py     # E14c 近因加权证伪
python3 proto/evolve6.py      # E14-E18（函数可单独调用）
python3 proto/evolve7.py      # E19-E23（含世界编号缓存修复）
python3 proto/evolve8.py      # E24-E28（第六轮）
```

---

## 分轮详细文档

| 轮次 | 文档 |
|---|---|
| 一 | `bootstrap_v3_代码审查报告.md` / `bootstrap_v3_修复工单.md` |
| 二 | `bootstrap_v3_v4重构工单集.md` / `bootstrap_v4_架构设计方案.md` |
| 三 | `bootstrap_完全重构_可能性空间探讨.md` |
| 四 | `bootstrap_自演化架构设计方案.md` |
| 五 | `bootstrap_自演化推演_第二轮.md` / `_第三轮.md` / `_第四轮.md` / `_第五轮.md` |
| 六 | `bootstrap_自演化推演_第六轮.md` |
