# 交接包验证报告

> 在**全新解压的干净目录**中执行（不是原工作目录），模拟本地 agent 的第一视角。

---

## 1. 文件完整性

```
[1] 关键文件存在性
  [OK] 15/15 关键文件齐全

[2] 文件统计
  proto                 87 个
  (顶层)                  86 个
  pilot                  3 个
  bootstrap_data         2 个
  合计                   178 个  (2444.9 KB)

[3] 实验编号覆盖
  轮次报告文件数：41

[4] 内核可导入
  [OK] 内核可导入，关键配置项齐全（8 项）
  [OK] CORE_DIMS 存在
==================================================================
✅ 交接包完整 —— 零损失
```

## 2. 内核验收

```
PASS 53 / FAIL 0
```

覆盖：
- T1–T16 基础机制（能力式红线、盲评、配对、停摆、Pareto、预算、元层隔离…）
- T17–T20 锚定探针（检出 6/6、误报 0/6、boost 以 blind 为前提、零副作用）
- **T21–T24 演化活跃度**（核心维度识别、与停摆互补、drift 后告警）

## 3. 试点套件

```
python3 pilot/analyze.py sample_traces.jsonl
→ ✅ 信号、可改进性、预算三项均达标 → 可以进入 S1（判断集采集）
```

（附带的 1198 条模拟轨迹解压即可跑，无需外部数据）

---

## 4. 三项验证命令

```bash
python3 verify_package.py                    # → ✅ 交接包完整
python3 proto/precompute.py                  # 预计算（可中断续跑）
python3 proto/verify_kernel.py               # → PASS 53 / FAIL 0
python3 pilot/analyze.py sample_traces.jsonl # → ✅ 可以进入 S1
```

---

## 5. 已知不含的内容（诚实说明）

| 不含 | 原因 |
|---|---|
| 真实业务数据 | 用户未提供，也不需要 |
| 真实 LLM-as-judge 的调用记录 | 150 个实验的 judge 均为人工加权函数 |
| E138、部分 E1xx 的原始落盘 JSON | 早期实验未落盘，结论已写入报告 |
| 第 1 轮独立报告 | 内容已并入总报告第一部分 |

## 6. 已排除的非项目文件

工作区中 `conflib/`、`conflib2/`、`gitlib/`、`mylib/`、`v2x/`
为环境自带库，与本项目无关，未打包。
