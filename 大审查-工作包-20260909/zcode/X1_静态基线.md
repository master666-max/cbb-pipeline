# X1 · 静态基线组 G0/G1 + 存储三探针 — 报告

> **结论先行**：①G0 静态库审计集效用全种子为负（−0.24~−0.64）——v3 默认参数在真实 age 语义下确实"不如不检索"；②一次性人工调参（G1）+0.79（8/8 正，CI 下界 +0.66）——这是给 X2 演化的**及格线**；③三探针全部复现报告代码审查的预测：并发丢更新 75%、截断攻击密码学层全盲、绕过引擎编辑被 doctor 当场抓住。
> 日期：2026-09-08 · 脚本：`proto/x1_static.txt` · 数据：`data/x1_static.jsonl`、`data/x1_probes.json`

## 1. G0/G1 基线（8 种子，审计集效用）

| seed | G0 | G1 | 差 |
|---|---|---|---|
| 1 | −0.4062 | +0.6438 | +1.05 |
| 2 | −0.6375 | +0.2188 | +0.86 |
| 3 | −0.2437 | +0.1937 | +0.44 |
| 4 | −0.5687 | +0.4500 | +1.02 |
| 5 | −0.5687 | +0.1938 | +0.76 |
| 6 | −0.4437 | +0.5188 | +0.96 |
| 7 | −0.3687 | +0.2875 | +0.66 |
| 8 | −0.5500 | +0.0500 | +0.60 |

**配对差：均值 +0.793，正种子 8/8，bootstrap 95% 下界 +0.655。**
G1 配置 = `{w_imp: 0.5, filter_zero: 1}`（报告 E1 演化自己找到的答案的手工版）。注意本沙盒世界用的是**源码真实 age 语义**（`−0.1×age_days`，非 evolve.py 的 ÷100），年龄惩罚主导——这预示 X2 演化有比 G1 更大的空间（降 `w_age`），是真引擎与模拟世界的重要差异点，X2 报告将专门验证。

## 2. 并发探针（8 进程真引擎 CLI append）

| 项 | 值 |
|---|---|
| importance_accum 增量 | **10 / 40（丢失 75%）** |
| 条目文件 | 8/8 全部落盘（条目层无损） |
| 审计链 | 连续（本轮时序下无交错断链） |
| 五态 | 全一致（state 根一致——根与条目集自洽，**丢的是 accumulator，不是内容**） |

**解读**：报告 P0-4 的 lost-update 在 8 进程下实测复现（其 12 进程为 92%）。损害集中于 `state.json` 的 read-modify-write——反思触发器（importance_accum）在并发下静默失效，但审计链与条目本体完好。运维含义：**并发写场景必须走事件流（X4 验证方向），state 只配单进程写**。
（过程备注：首轮探针输出被会话端输出管道截断，本轮数值从持久状态确定性恢复——`accum_base=Σbuild_world(8).importance`，doctor 只读重测。恢复过程已如实标注。）

## 3. 截断探针（只读副本，两层判定）

构造：删 audit 尾部 2 行（entry:e78、e79）+ 攻击者重算 Merkle 根写回 state.json。

| 层 | 结果 | 含义 |
|---|---|---|
| **密码学层**（链连续 / 根一致） | **全盲**（doctor 报"审计链 连续 / state 根一致"） | 复现报告 2.3：Merkle 根锚定内容集合不锚定序列，截断后剩余行自洽 |
| 集合层（四方对账） | 报出 `memory 80 / audit 78`（memory有而audit无 2 条） | 但该信号与"三处漏刷根"类已知 bug **同型**（报告 2.4 健康信号过载），实战中会被脱敏 |

**结论**：v3 的审计面对"删尾巴"攻击无密码学防御；唯一可见信号淹没在噪声通道里。→ 支持 X4 事件溯源内核（chain_tip 锚定序列）的对照验证。

## 4. 遗忘探针（lib-g0-s1）

1. 绕过引擎直接编辑 10 条 `t_invalid` → **doctor 立刻报"根不一致"**（写后刷根纪律抓到了绕行）；
2. `engine retire`：出仓 `{e0..e7}`（t_invalid 100 天前 > TTL 90）**精确等于期望**，`{e8,e9}`（10 天前）**零误伤留守**；
3. retire 自身刷根 → doctor 恢复五态全一致。

**结论**：TTL 出仓机制零误伤；且"引擎外编辑 → doctor 报警 → 引擎操作自愈"的闭环有效——审计面抓得住绕行，逃生舱（attic）留痕完整。

## 5. 对后续实验的输入

- **X2 及格线**：审计集 ΔU ≥ +0.79（G1），或解释为何没到；
- **H3 已知例外清单**（写入总报告）：并发 accumulator 丢失、截断密码学层盲区、引擎外编辑需 doctor 事后抓——三者均已实测复现；
- lib-g0-s1 已被遗忘探针消费（10 条 t_invalid / 8 条入 attic），**后续实验禁复用该库做基线**（数据已采集完毕，无影响）。

## 复现命令

```bash
py -X utf8 大审查/衔尾蛇/proto/x1_static.txt   # 幂等：已消费的探针自动恢复/跳过
```
