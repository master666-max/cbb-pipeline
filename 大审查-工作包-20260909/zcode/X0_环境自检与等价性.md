# X0 · 环境自检与适配器等价性 — 报告

> **结论先行：X0 PASS（7.5 秒，10/10 步全过）。法层适配器与真引擎在 100 个查询上 top-20 逐条一致，参数真实生效。X1/X2 放行。**
> 日期：2026-09-08 · 脚本：`proto/x0_selfcheck.txt` · 原始数据：`data/x0_results.json`、`data/x0_equivalence.json`

## 结果

| 步骤 | 结果 | 关键数据 |
|---|---|---|
| S1 哈希锚定 | PASS | 原件=快照=`ef2c645aea64fb9e`，字节级一致 |
| S2 run-tests 观测 | PASS（观测项） | **复现源码 P0-1**：71 个自测只跑 9 个即静默中止（rc=1），与报告"第 10 个用例中止"分毫不差 |
| S3 absorb | PASS | **真源树成功**（未用 fallback）：123 个内容体 ← `knowledge/skills/library-bootstrap/` |
| S4 install L2 | PASS | state.json 就位，merkle_root=`1574a5570319ab07…` |
| S5 世界写入 | PASS | 80/80 经真引擎 `append` 入链；doctor：memory 80 / audit 80 / ledger 80 / 链连续 / 根一致 |
| S6 元层冻结 | PASS | 任务集 sha16=`6c754e8945ec8c85`（40/32/8），`meta_layer.json` 落盘 |
| S7 等价性门 | **PASS** | **100/100 查询 top-20 与 `m._rank_entries` 逐条一致**（含扩散路径与零命中路径） |
| S8 负对照 | PASS | w_kw 3→4 翻转 65/100 查询排序——参数流真实生效，排除假等价 |
| S9 guard/eval 冒烟 | PASS | guard 快照落盘；真引擎 eval：train recall@5=0.125 / MRR=0.044 |
| S10 G0 基线 | PASS | 见下表 |

## G0 基线（v3 默认参数，seed=1）

| 集合 | utility | recall@5 |
|---|---|---|
| train（40） | −0.5537 | 0.125 |
| held（32） | −0.5813 | 0.0938 |
| **audit（8，永不参与演化）** | −0.4062 | 0.250 |

**解读**：utility 为负是真实引擎病灶的忠实呈现——`w_age=0.1` 乘以原始 age_days（世界 0–400 天，均值 −20 分）完全压倒关键词命中（×3，最多约 6 分）。这正是报告 E1 预测演化能自己修复的方向（降 w_imp/w_age、开 filter_zero）。基线低不是 bug，是演化空间。

## 对规划书的偏离

- 无。S3 走了真源树路径（规划书预案的 fallback 最小 bodies 未触发）。

## 复现命令

```bash
py -X utf8 大审查/衔尾蛇/proto/x0_selfcheck.txt
# 前置：大审查/衔尾蛇/proto/vendor/bs3_snapshot.txt 与 豆包源码 sha256 前 16 位均须 = ef2c645aea64fb9e
```
