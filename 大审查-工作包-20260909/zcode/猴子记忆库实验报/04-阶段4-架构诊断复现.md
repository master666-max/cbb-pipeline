# 阶段4 · 架构诊断复现（报告第二部分 2.2–2.5）

> 脚本：`sandbox/repro/repro_a1_four_truths_truncation.txt`、`sandbox/proto/proto_a_event_sourcing.txt`、`sandbox/proto/proto_b_git_backend.txt`
> 输出：`sandbox/runs/repro_a1.log`、`runs/proto_a.log`、`runs/proto_b.log`

## ① 公理 D 双平面落差：审计链不含 payload —— ✅ 证实（报告 2.2）

实测打印审计链中关于 e0 的全部信息：

```
- 2026-09-09 01:16 | prev:09046834… | h:68c2ecd2… | entry:e0 → intermediate
```

- 仅指针（id + 目标层），**不含 content/keywords/confidence**（e0 的 content 字符串在审计链全文 0 命中）
- memory 中 e0 = 10 字段完整正本；ledger 行 = `entry=e0 → intermediate`（同为指针）；state.json 派生量键中**无 chain_tip 字段**
- 「真相的载体只在 memory/，audit 是指针日志」证实；「四条平行真相」（audit/ledger/memory/state）与报告 2.2 的结构定性一致
- （勘误：repro_a1.log 中「importance 是否在 audit 行」一栏为子串误命中——哈希 hex 中含数字"3"，不构成反证，以 content 判定为准）

## ② 截断攻击 —— ✅ 核心证实，「检出False」表述在字面场景下略过度（报告 2.3）

**V1（报告字面场景：append 5 条 → 只删 audit 尾 2 行 → 重算根写回 state → doctor）**：

```
doctor 首行: doctor: memory 5 / audit 3 / ledger 5 / 审计链 连续 / state 根一致   ← 与报告逐字相同
断链告警: False ｜ 根不一致: False
集合差告警行: ['[memory有而audit无] e3, e4']
```

- 报告展示的 doctor 首行**逐字复现**；链校验（逐行重算）与根对账对尾部截断**双失明**——「Merkle 根锚定内容集合、不锚定序列长度」「state 无 chain_tip」证实。
- 但 doctor 的 memory↔audit 集合差**仍报出 e3,e4 为孤儿条目**（报告展示输出中略去了该行，「检出: False」在字面场景下表述过度）。

**V2（完整截断/改写历史：audit 尾 2 行 + memory e3/e4 + ledger 2 行同步抹除 + 重算根）**：

```
doctor: memory 3 / audit 3 / ledger 3 / 审计链 连续 / state 根一致
  五态 全一致
```

**完全全绿**——能同步改写四面真相的攻击者可无痕抹除历史，报告 2.3 的核心结论在此威胁模型下完全成立（且与 THREAT_MODEL 自认「不防③档蓄意 agent」自洽）。

## ③ 健康信号过载 —— ✅ 证实（报告 2.4；数据见阶段3 repro_p1_stale_root）

报告序列 eval→reflect→guard--snapshot→doctor：**3/3 步全部误报「根不一致」**（零篡改、纯官方命令）。实测另有 shadow、guard --anchor 两处同样误报（共 5 处）。「一个信号既报故障又报正常 = 没有信号」的定性成立——且误报源恰是漏刷根的同一批命令，两个 P1 现象同根。

## ④ 原型 A · 事件溯源内核（约 160 行重建）—— ✅ 报告数字全部复现（报告 2.5）

按报告规格重建（事件流 events.jsonl 唯一真相 + 哈希链 + 跨进程文件锁 + 投影重放；零命中过滤与空串拒绝内置）：

| 报告数字 | 实测（5 轮） | 判定 |
|---|---|---|
| 12 进程并发 append(imp=5) → importance_accum=60 | **[60,60,60,60,60]**（v3 同场景 5） | ✅ 精确 |
| 审计链连续 True | [True×5] | ✅ |
| 删 memory/ 从事件流重建 → 完整恢复 | 12 条→12 条**逐字节一致** | ✅ |
| 300 条目 merkle_root 0.005s | 89.2ms | ⚠️ 同量级（毫秒级）但偏慢 18×：我的实现含 fsync 且每次入流全量投影（O(n²) 追加）；结论「性能非瓶颈」成立 |
| 300 条目 retrieve 全库 0.008s | 46.7ms（命中 300 条） | ⚠️ 同上，毫秒级成立 |

原型还内置了 v3 缺失的两项修复以供对照：state 含 **chain_tip** 字段（截断检出锚点）、铁律 2 拒绝空串证据锚（`PermissionError`）。

## ⑤ 原型 B · git 后端七项能力 —— ✅ 8/8 子检查全过（报告 2.5）

| # | 能力 | 实测 |
|---|---|---|
| 1 | append→commit 天然链式 | 3 提交，非根提交均带 40hex 父哈希 ✓ |
| 2 | 篡改检出 | 改 memory/e0.json → `git status` 即 ` M memory/e0.json` ✓ |
| 3 | 回滚 | `git checkout --` 还原、工作区净空，无需自写 attic ✓ |
| 4 | 分支实验隔离 | 实验分支改写 e0 后切回 main，内容未污染 ✓ |
| 5 | 人类令牌（GPG） | 临时密钥完整闭环：有私钥 `-S` 提交且 `%G?=G`（Good Signature, Verifier）；**空钥匙串 `-S` 提交被拒**——「--yes 换成签名后，令牌从随手加 flag 变成必须拿私钥」实测成立 ✓ |
| 6 | 截断/篡改→tip 变化 | reset 截断→tip 变（2aa8→c55a）；amend 篡改→tip 变（→8ab4）✓ |
| 7 | gc/reflog/fsck/diff/bisect | 五项全部现成可用 ✓ |

（环境注：gpg 为 Git 自带 MSYS 构建 2.4.9；GNUPGHOME 以 MSYS 路径格式传递——Windows/MSYS 混合环境工程细节，已写入脚本注释。）

## 阶段4 小结
架构层四项诊断（payload 缺席、截断失明、信号过载、原型 A/B 数字）全部实测成立；唯一表述偏差：报告 2.3 的「doctor 检出: False」在字面场景忽略了集合差孤儿告警（完整截断场景则确凿成立）。报告 2.5 两个原型的全部可复现数字在独立重建下复现（性能数字同量级、方向一致）。
