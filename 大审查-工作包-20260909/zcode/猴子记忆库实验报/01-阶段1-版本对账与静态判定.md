# 阶段1 · 版本对账 + P0/P1/P2 静态判定

> 方法：`py -X utf8 sandbox/repro/phase1_static_audit.txt`（AST 函数级行数 + 正则计数），输出 `sandbox/runs/phase1_static.log`
> 三态口径：**代码中已存在**（报告描述与源码一致）/ **已被修复**（源码已含修复证据）/ **报告描述与代码不符**；动态部分留阶段2/3 实测。

## 1. 版本对账

| 项 | 实测 | 判定 |
|---|---|---|
| 报告基线 v3.8.1 | `VERSION = "3.8.1"`（L20） | 一致 |
| 源码头注释「v3.8.2 中性化修正」 | 头注释 L15 提及，但 VERSION 常量仍 3.8.1；所指 RELEASE.md **不存在于本目录** | 头注释指向的文件缺失；版本常量未随头注释升级（记账瑕疵，非报告结论错误） |
| 报告 1.1 称「静态通读 2415 行」 | wc 2415 行 | 一致 |

**结论**：报告审查对象 = 本文件（v3.8.1），v3.8.2 仅为「变更史挪出文件头」的注释性提及，无行为差异证据。

## 2. 报告数字性结论复核（全部命中）

| 报告数字 | 实测 | 判定 |
|---|---|---|
| 「全文件 24 处 sys.exit」 | 24 | ✅ 证实 |
| 「168 条 assert」（2.1 节） | 168 | ✅ 证实 |
| 「测试 68/68」 | TESTS 注册 68；run-tests 汇总打印 len(TESTS) | ✅ 证实（数字同源） |
| 「7 项死配置」（P2） | broadcast_weights / reflect_trigger / index_hot_budget / eval_n / review_intervals / merkle_refresh_trigger / governance_budget —— 正文零引用恰 7 项 | ✅ 证实 |
| 总行数 2415 | 2415 | ✅ 证实 |
| 自测 1058 行 43.8% | AST 口径 1063 行 44.0%（含 @t 装饰器与 cmd_run_tests 的边界差 5 行） | ✅ 证实（口径内） |
| 治理 40 行 / 审计与账本 35 行 | cmd_guard 恰 40；chain_append(19)+line_hash(1)+merkle_root(9)+_ledger_append(6)=35 | ✅ 精确命中 |
| 记忆与检索 90 行 | 独立口径 37–90（取决于 engine 分支归属；报告未给分类边界） | ⚠️ 无法精确复刻，量级一致 |
| 核心三件套 165 行 = 6.8% | 独立口径 104–165 行 = 4.3–6.8% | ⚠️ 区间覆盖，**性质结论（内容分发 213–245 行 > 核心）在任何口径下均成立** |

## 3. P0×4 逐条静态判定

| # | 报告指控 | 静态判定 | 代码证据（行号以原件为准） |
|---|---|---|---|
| P0-1a | `cmd_run_tests` 用 `except Exception` 捕不到 `SystemExit`，错误通道全靠 sys.exit → 静默中止 | **代码中已存在** | L2377-2383：`except Exception as ex`；全文件 sys.exit=24；干净状态下第 10 例 t_merkle_root_consistency 调 cmd_install→空 bodies→sys.exit(C-001)（阶段0 已实测中止） |
| P0-1b | `render_quickref` 硬编码 `{N}/{N}`，「测试 68/68 从未被执行结果校验」 | **已被修复（半）** | L309 签名 `render_quickref(tests_n: int)`；L640/L794 调用传 `len(TESTS)`；L1242 `t_no_handwritten_counts` 在位；全文件无 `68/68` 字面量。**残余问题**：打印的仍是「注册数」而非「实跑通过数」，且 P0-1a 未修 → 静默中止时该数字更具误导性 |
| P0-2 | `validate_entry` 只查键存在不查值，`source_event_id=""` 放行；absorb-md 统一写 genesis-v4 而该事件不存在；doctor 不校验该字段 | **代码中已存在** | L244 `if "evidence" not in e and "source_event_id" not in e`（键存在性）；L498/510/521 genesis-v4 写死×3；doctor L1032-1074 全文无 source_event_id |
| P0-3 | 首装判据只看 state.json；`_deliver` 无条件 write_bytes；删 state.json 重装覆盖用户文件 | **代码中已存在** | L763 `is_first = not (target/"state.json").exists()`；L726 `dest.write_bytes(data)` 无条件；L845-850 README/AGENTS 无条件重渲染（校验模式 L773-839 有 state.json 才走、不覆盖——与报告「中断重跑」场景一致） |
| P0-4 | 12 进程并发 append 丢更新 92%，importance_accum=5≠60 | **代码中已存在（竞态结构）** | L936-941：读 state.json → `+int(importance)` → 写回，无文件锁/无原子替换；92% 为动态量，阶段2 实测 |

## 4. P1 具名 5 条静态判定（报告 1.2 表列出代表；其余 3 条未具名）

| 指控 | 静态判定 | 证据 |
|---|---|---|
| guard 只查文件名子串 | **代码中已存在** | L1134 `any(b.lower() in name.lower() for b in BASELINE_BLACKLIST)`——只看候选目录内文件名，不看内容/不看库内现状；L1138 SOP 仅查存在性 `any("MUTATION-SOP" in str(p))` |
| 检索打分与查询无关 | **代码中已存在（表述需精确化）** | L906 `kw_hit*3 + c_hit + importance − 0.1*age_days`——后两项与查询无关；零命中时排序完全由 importance−age 决定 |
| 同 id 跨层共存 | **代码中已存在** | L927-932：dest 由 importance 决定（≥7→longterm 否则 intermediate），`epath.exists()` 只查目标层；doctor 的 mem 集合按 stem 去重看不见重复 |
| 三处漏刷根 | **代码中已存在，且实际不止三处** | eval（L949-971 写 baseline+账本不刷根）、reflect（L996-1023 写素材+账本不刷根）、guard --snapshot（L1105-1114 写 anchor_snapshot.json+账本不刷根）——另发现 **shadow（L1095-1098）与 guard --anchor（L1115-1127）同样写库不刷根**。报告说 3 处，实测静态至少 5 处 → 报告**低估** |
| 审计链可注入 | **代码中已存在（注入面=任意文本）** | chain_append(audit_dir, text) 接受任意 text；`engine shadow` 是官方注入入口（任意 --text 入链）；无认证/无事件类型校验 |

**P1 其余 3 条：无法验证**——报告仅给代表名单，完整清单在《bootstrap_v3_代码审查报告.md》（报告附录引用，未随文件夹提供）。E1 侧证：P1-6≈「检索重要度权重过高」（与具名「检索打分与查询无关」同源）、BUG-006≈「零命中条目不过滤」（L906/L944-946 可证零命中条目以 importance−age 得分参与 top-K）——此两条按可证据部分计入。

## 5. P2 抽查 5 条静态判定（报告具名的恰好 5 条）

| 指控 | 静态判定 | 证据 |
|---|---|---|
| 7 项死配置 | ✅ 证实（恰 7 项，见上表） | phase1_static.log |
| 64bit manifest | ✅ 证实 | L716 `"hash_algo": "sha256-16"`；L727 `hexdigest()[:16]`（16 hex=64bit） |
| Merkle exclude 按名字匹配 | ✅ 证实 | L262-263 `p.name not in exclude`——按文件名而非相对路径匹配（任意层同名 state.json 都会被排除） |
| CRLF 分歧 | 静态半证实（待动态） | L742/L748 注释自认 write_text 在 Windows 写 CRLF 问题并改 write_bytes（referee/reflect-synthesis 两处已修）；但 AGENTS.md/README.md/条目 JSON 仍 write_text（L845-850/L933）→ Windows 上落盘 CRLF，跨平台根不一致风险仍在 |
| 拓扑排序缺失 | ✅ 证实 | resolve L301 `return sorted(got), missing`——按字典序返回，依赖只做存在性检查不做排序（DEPS 仅用于环检测+缺依赖） |

**P2 其余 8 条：无法验证**（未具名）。

## 6. 阶段1 小结

- 报告的可数结论（24/168/68/7项/2415/40/35）**全部精确命中**——报告作者确实细读过这份源码。
- P0×4：4 条指控的机制**全部仍在代码中**；唯 P0-1 的 render_quickref 硬编码子项已被参数化修复（报告写作时点的描述与当前代码不符，属「已被修复」）。
- 报告的保守性偏差：漏刷根实为 ≥5 处而非 3 处（报告低估，方向一致）。
