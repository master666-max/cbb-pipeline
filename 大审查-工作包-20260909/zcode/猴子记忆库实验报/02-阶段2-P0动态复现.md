# 阶段2 · P0×4 动态复现（全部证实）

> 复现环境：Windows 11 / Python 3.14.7 / Git Bash；脚本 `sandbox/repro/repro_p0{1..4}*.txt`（各自独立可运行），输出 `sandbox/runs/repro_p0{1..4}.log`
> 加载方式：importlib 直载 `sandbox/bootstrap_v3.py` 副本（原件只读，sha256 与基线一致）

## P0-1 自测静默中止 —— ✅ 证实（机制 + 两个实测场景）

**报告原文**：`cmd_run_tests` 用 `except Exception`，捕获不到 `SystemExit`（BaseException），而全文件 24 处 sys.exit 是唯一错误通道。干净检出下跑到第 10 个用例静默中止。

| 场景 | 实测 | 判定 |
|---|---|---|
| 干净检出（无 bodies，阶段0 基线） | 9 PASS 后第 10 例 `t_merkle_root_consistency`→cmd_install→`sys.exit(C-001)` 中止；**无汇总行**，exit 1 | ✅ 与报告「第 10 个用例静默中止」逐字吻合 |
| 注入 SystemExit(带消息) 于第 11 位 | 10 PASS、0 FAIL、无汇总行，后续 58 用例未跑，以 SystemExit 退出 | ✅ |
| 注入 SystemExit(0) 于第 11 位 | 同上且 **exit code=0**——「看起来成功的静默中止」 | ✅ 并加重：伪造成功态 |
| render_quickref 硬编码 {N}/{N} 子项 | 已修复：`render_quickref(tests_n)` 参数化、调用点传 `len(TESTS)`、`t_no_handwritten_counts` 在位、无 `68/68` 字面量 | ⚠️ **报告该子句与当前代码不符（已被修复）**；但打印的仍是注册数而非实跑通过数，配合静默中止更具误导性 |

## P0-2 铁律 2 空串绕过 —— ✅ 证实（三个子句全中）

| 报告子句 | 实测 |
|---|---|
| `source_event_id=""` 实测放行 | 官方 append 放行=True 落盘=True；`validate_entry` 返回 `[]`（只查键存在：L244） |
| absorb-md 统一写 genesis-v4 且该事件不存在 | 迁移 3 条 source_event_id 集合={'genesis-v4'}；审计链全文 **0 次**出现 genesis-v4 |
| doctor 不校验 source_event_id | 含空锚条目的库 doctor 输出「五态 全一致」；doctor 代码区无该字段 |
| （对照扩展） | `source_event_id='totally-made-up-event'` 同样放行——铁律 2 对**假事件 id** 也全盲 |

## P0-3 install 中断重跑覆盖用户文件 —— ✅ 证实（+附带损伤）

实测序列：装 L2 → 手改 README（加私有笔记）+ 手改交付文件 modules/m-core/install.md → 删 state.json → 重装：

- README 原样还原（私有笔记消失）=True；交付文件手改标记消失=True
- 重装输出**无任何覆盖警告**（`覆盖/还原/警告` 0 命中）
- **附带损伤（报告未提）**：importance_accum 6→0 静默归零（state.json 重建为初始值）、merkle_root 变化
- 用户条目文件（memory/）幸存——覆盖面限于交付物+渲染件，与 `_deliver` 前缀集合一致

## P0-4 并发写丢更新 —— ✅ 证实（92% 精确复现）+ 新发现撕裂读

12 进程并发 append(importance=5)（Windows spawn，真进程）：

| 时序 | 20 轮 accum 分布 | 平均丢失率 |
|---|---|---|
| 串行对照 | 60（期望 60） | 0% |
| **barrier（对齐读时刻）** | **{5: 20}——20/20 轮恰好 5** | **91.7%（报告 92%=(60−5)/60）** |
| natural（自然起跑） | {20:11, 25:5, 30:4} | 61.3% |

- 条目本体 12/12 全部落盘（丢失的只有 state.json 的 importance_accum）——与报告「它是反思触发的唯一输入，等于反思机制在并发下静默失效」的机制定性一致
- **新发现 F2-1（报告未提）**：并发下出现**撕裂读崩溃**——3 个 worker 在 L938 读 state.json 时撞上其他进程写半截，`JSONDecodeError: Expecting value: line 1 column 1` 裸抛（非 SystemExit，CLI 下是 traceback 而非友好报错）；此时条目与审计/账本已写、state 更新整体丢失。比丢更新更糟：无锁读改写不仅是最后写者赢，还会把库置于「条目在、账在、state 中毒」状态。

## 阶段2 小结
P0×4 全部动态证实；唯 P0-1 的 render_quickref 硬编码子句已被修复（版本对账见阶段1）。附加两项报告未记载的发现：importance_accum 重装归零（P0-3 侧）、撕裂读崩溃（P0-4 侧）。
