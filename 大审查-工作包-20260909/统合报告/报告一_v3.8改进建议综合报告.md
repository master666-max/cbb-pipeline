# 报告一 · v3.8 改进建议综合报告（重写版 2026-09-08）

> 方法：主审逐份通读四家产出（GLM 5 / Qwen 6 / 混元 9 / 豆包 9），每份出具裁决（见 复审/ 四文件）；缺陷主张对照 v3.8.2 终版源码实证（部分经 verify_matrix.py，部分主审走读判定）。
> 本报告是**裁决后的最终清单**：每条 = 结论 + 来源 + 主审判定 + 修复要点。不再罗列「谁说了什么」的完整过程（过程见四份裁决文件）。

---

## 一、四家产出质量定序

| 家 | 产出 | 主审评价 |
|---|------|---------|
| 混元 | 修复工单 33 张（行号+复现+验收+反向核对清单）+ 17 轮实验 | **修复工单 A（全场最重单份）**；实验方向 B+/数字 C |
| 豆包 | 审查 30 题 + L9 现成补丁（自验 71/71） | **A-**（唯一交付可应用代码；L9 缺失为唯一功能级抓捕） |
| Qwen | v3.9-1 三大 P1 + AR 七项 + POSS/EVO | **A**（三大 P1 定位最精准；工单间引用最严密） |
| GLM | Q 系 22 票 + ARC 17 项 + v4.0 三部曲 | **A-/B+**（刷根矩阵首发；Q-006 症状误判；v4.0-3 质量最高） |

四家误报共 4 条，全部有成因：Qwen P0-00 转录伪影（自认）、GLM Q-006 未溯源数据流、GLM「硬编码 68/68」表述错（混元更准的版本成立）、混元原型清单 3 脚本缺失。

## 二、裁决后的最终缺陷清单（合并去重）

### P0（功能/安全硬伤，立即修）

| # | 缺陷 | 来源（交叉） | 修复要点 |
|---|------|------------|---------|
| 1 | **L9 交付残缺**：MODULE_MAPPING/LEVELS 只登记 3/8 模块，五模块装不出；gate 文案盲签；L8/L9 依赖边缺失 | 豆包（现成补丁 8 hunks + 3 回归测试） | 应豆包补丁（rebase 3.8.2）+ 配套 level_mapping |
| 2 | **MIGRATED 渲染 KeyError**：migrate 库上 build-docs --lib / install 校验即崩 | GLM Q-005（核验矩阵复现） | LEVELS 增 MIGRATED 或渲染降级分支 |
| 3 | **append id 无字符集校验 → 路径穿越**（`../../evil` 写穿 memory/）+ importance/日期/置信度无类型校验 | GLM Q-003、混元 BUG-009 | _ID_RE + 类型校验 + normalize 前移 |
| 4 | **密钥扫描漏报**（sk-ant-/gho_/xoxb-/AIza/PEM）+ 误报（sk- 子串） | GLM Q-004、豆包 P1-05（实测 sk-ant 漏检） | SECRET_PATTERNS 扩充（GLM 给的正则直接用） |
| 5 | **审计链换行注入**：event text 含换行即破坏链格式；审计面只追加→一条注入废掉真相源 | 混元 BUG-010（攻击链完整） | 入口净化或拒绝控制字符 |
| 6 | **eval 劣化覆写基线（棘轮向下）**：劣化→写基线→exit 2，二次劣化不再报 | Qwen P1-03、混元探讨-2、豆包（两证+代码确认） | 劣化分支冻结基线，写 baseline-rejected.json 留证 |
| 7 | **guard/reflect/shadow/eval 四类写操作漏刷根** | GLM①（首发）+Qwen AR-01+豆包（三家收敛，核验矩阵四项全中） | 唯一写出口（见报告二 AR-01/LibraryTx） |

### P1（正确性/一致性）

| # | 缺陷 | 来源 | 修复 |
|---|------|------|------|
| 8 | append 同 id 跨层双写（查重仅单目录） | Qwen P1-02、GLM⑦、混元 BUG-007（三家同证+复现） | 全库含 attic 查重；升层走显式 promote |
| 9 | doctor 链校验缺同文件相邻 prev==上行 h（改一行+级联重算可逃逸） | Qwen P1-01（攻击构造精确） | expected_prev 逐行断言，自哈希与链接两条独立检查 |
| 10 | migrate 无 state.json → 半身库（accum/根/反思全静默失效） | GLM⑩ 关联（N-002 已建 state 但 MIGRATED 崩） | P0-2 修复后即通；补 build-docs 兼容测试 |
| 11 | retire 扫描含 attic（自移+重复记账） | Qwen P2-01、混元 BUG-025 | 排除 attic；moved 只计真实移动 |
| 12 | absorb >512KB 不进 hit_keys → 误移 removed（交付消失） | Qwen P2-03 | skipped 集合剔除镜像删除 |
| 13 | guard 三形态逃逸（patch 文件/删除/改内容） | 混元 BUG-005（最全）、GLM Q-013、豆包 | diff 路径解析+删除检测+不可解析需人工 |
| 14 | install 中断重跑覆盖用户改动（首装判据仅 state.json + 无条件 write_bytes） | 混元 BUG-003、GLM Q-021.6 | 脏目录拒绝 + 写前移 attic + inprogress 标记 |
| 15 | 证据锚空串绕过 + absorb-md genesis-v4 幽灵事件 + 日期静默改写 | 混元 BUG-002（复现） | 判空 + 真实事件 id 回填 + 拒绝非法日期 |
| 16 | verify 9/33 条永不对账但 status 硬写 verified；包含关系判据「一致≠同一篇」；无离线/超时开关 | 混元 BUG-011 | doc-unverifiable 状态 + --offline/--timeout-budget + 判据收紧 |

### P2/P3（择要）

死配置七项（TUNABLES 代码引用 0 次，公理 A 失真）/ manifest 64bit vs 根 256bit 口径 / Merkle exclude 按文件名（同名藏匿面）/ resolve 死条件+顺序耦合 / build 死分支 / render_citations 字段残缺 / 渲染件 write_text CRLF 残留（cmd_build/install AGENTS 段）/ doctor 含 attic / 测试封闭化（依赖真实 bodies+monkeypatch 污染）/ 测试弱断言五处 / 变更编号八套无文档。

## 三、四家独有贡献（不可漏取）

- **豆包**：L9 补丁（现成）；bodies .pyc 污染发现；性能验收门禁体系（10k 条 retrieve<500ms 等登记）；「特色包状态机 12 包零实现」（混元 V4-06 交叉）。
- **Qwen**：三 P1 攻击构造（重算 H2' 断链/跨层/棘轮覆写）；AR-05 @engine_op(writes=False)「读操作结构上不可能碰 state」；EVO 六攻击用例；「过全部测试但违反铁律 1 的变异必须被提交边界拒绝」的不变式终极用例。
- **GLM**：刷根矩阵；「自指但有固定点」E 阶梯切分（参数面 E0 自主/判据面 E5 双钥）；统计纪律四条（最小样本 30/置信下界/预算替代 Bonferroni/漂移解耦）；点火判据第 2 条「从未回退过的系统=没被开过」。
- **混元**：反向核对清单方法；chain_tip 三元组+截断攻击实测；accum 改账本重算（优于加锁）；「确认无问题勿破坏」清单；12 进程并发实验。

## 四、执行序（v3.8.3 → v3.9 前置）

1. **v3.8.3（P0 批次，0.5 会话）**：#1 豆包补丁应用 → #2 MIGRATED → #5 换行消毒 → #6 棘轮冻结 → #7 唯一写出口（四家收敛，见报告二）→ #4 密钥扩充 → #3 类型硬化。
2. **v3.9 前置（P1 批次，1 会话）**：#8~#16 按依赖排序。
3. **每条带回归断言**（四家提供的测试设计直接取用），发布门 = run-tests 全绿 + doctor 五态 + verify 退出码。

## 五、方法论结论（本次统合自身）

- 四家 120+ 主张抽样实证 24 项：**属实率 ~79%，误报 4 条全部可归因**（转录伪影/未溯源/动态误读/定级张力）。
- **交叉印证价值实证**：P0-C 刷根矩阵（三家独立）与 P1-A/B/C（两家独立）全部实证成立；单家独有主张的误报率显著更高——**多模型背对背审查 + 主审实证仲裁**的组合被证明有效。
- 本轮主审自己的教训已单独归档（见轨迹 task-022 修正记录与 memory）：统合报告第一版因「代理摘要代替读原文」被废，本版为逐份通读后的重写。
