# 工作包说明 · 大审查-工作包-20260909

> 打包时间：2026-09-09 23:31 · 执行：ZCode · 用户指令：「把大审查目录，做成一个临时工作包，放到主工作区外的临时工作区，以便主工作区冻结时工作」

## 这个包是什么

主工作区（`D:\zcode专用！！！！危险！！！！！！！！！\`）中 `大审查\` 目录的**完整可工作副本**，供主工作区冻结期间在此临时工作区继续实验工作。

- **源**：`D:\zcode专用！！！！危险！！！！！！！！！\大审查\`（原地保留，主区冻结不删不改）
- **包**：`D:\临时工作区\大审查-工作包-20260909\`（本目录，可读可写，工作在这里进行）
- **完整性**：robocopy /E 复制后经全量对账（`verify_workpkg.txt`）——源 82,878 文件 vs 包 82,878 文件，双向零差异，逐一 sha256 **82,878/82,878 一致**。

## 包内结构速览

- `大审查/.git` — 嵌套 git 仓库随包带走（打包时 HEAD=`c31ad39`，含 19+ 未跟踪项）；`zcode/.git`（HEAD=`e91bdef`）同样在包内。
- `衔尾蛇/` — 自演化引擎实验正本（X0–X6c、v3.9 工单）；其 `attic/衔尾蛇副本-冻结快照-20260909/` 为 40,800 文件冻结快照（并入操作随包延续）。
- `混元/`、`豆包/`、`deepseek/`、`glm/`、`qwen/`、`codebuddy/`、`复审/`、`统合报告/`、`unified/` — 各模型线档案。
- 根级账本：`lab-notebook.md`、`data-archive.csv`（26 假设）、`meta_log.csv`、`directions.csv`、`meta_check.csv`。

## 使用规则（建议，非强制）

1. **正本-包关系**：主区冻结期间，本包是唯一活跃工作副本；主区解冻后如需回灌，逐文件对账后人工裁决，不整目录覆盖。
2. **嵌套仓库**：包内 git 操作照常可用（本包 git 历史独立演进）；回灌时按两边 HEAD 对账。
3. **与考古任务的接口**：主工作区的考古账本 `workspace-audit/`（progress.md、claim_ledger.csv 等）引用了大量本包内文件的 `file:line` 锚点——**在本包内新增/修改文件不影响主区账本**；但若未来要把本包的工作成果并入主区考古视野，需增量重扫而非依赖旧锚点行号。
4. 快照纪律沿用：`attic/` 内冻结快照不回灌、不改写；新工作产物另立目录。

## 主工作区冻结声明（打包时点状态）

- 打包时主区 `大审查/` 为 82,878 文件 / 124.42MB（robocopy 字节口径），源=包哈希级等价。
- 主区另有：考古产出 `workspace-audit/`（基线 commit `77fb1a0`，阶段2 七线入账 1,620 现行断言后交接）；根级 `衔尾蛇副本/` 已并入本包所含的 attic（源目录已删，见并入说明）。
