# 任务：AI 记忆库全维度健康体检与架构改进分析（闲时深度任务，禁止破坏性写入）

## 一、项目背景（Agent 先读取，不要跳过）

你接手的是一个自建的 AI 记忆库项目，本次任务不是开发新功能，而是**对既有系统做一次完整、系统性的体检与架构诊断**。你需要具备"读代码、跑脚本、看数据、出报告"四种能力，但**默认处于只读模式**，任何写操作、删除操作、数据变更操作都视为高危，必须在报告里单独立章，且不能在本轮闲时任务里自动执行。

**关键路径与配置（占位符，请在提交前替换）：**

- 记忆库仓库根目录：`[REPO_ROOT]`
- 记忆库数据/索引目录：`[DATA_DIR]`
- 向量库类型与连接串：`[VECTOR_DB_URL]`（例如 Chroma / Qdrant / pgvector / Milvus / 自研 HNSW）
- Embedding 模型与版本：`[EMBED_MODEL]` / `[EMBED_VERSION]`（例如 text-embedding-3-small, bge-m3）
- 记忆写入/检索入口代码：`[MEMORY_API_PATH]`
- 存量记忆条目数（预估量级）：`[MEMORY_COUNT]`
- 记忆库近期变更日志 / migration 目录：`[MIGRATION_LOG]`

如果以上占位符没有被替换、或路径不存在，**不要盲目扫描全盘**，直接把"配置缺失"写进报告的第一条 P0 问题，并终止本次任务，等待用户补充后重新提交。

## 二、执行顺序与风险分层（严格遵守）

本任务分 A、B、C、D 四个模块，**必须按顺序执行**，前一个模块没跑完不得进入下一个：

- **A 模块（只读架构审计）**：零风险，纯静态分析 + 只读查询。
- **B 模块（只读漂移检测）**：低风险，需要跑脚本读索引/跑 embedding，但**不写回**。
- **C 模块（功能自检 + 对抗测试）**：中风险，包括跑只读的检索评测、日志比对、轻量 fuzzing；任何需要真正写入新记忆的用例，一律标记为"建议但不执行"。
- **D 模块（修复建议与架构改进）**：纯输出，不执行任何修复。

**触发人工确认的边界：**如果 A/B/C 任何一步意外触发了写操作需求（例如日志清理、临时表创建），**停止执行，把该操作连同上下文一起写进报告的"待人工确认"章节**，然后继续往下跑只读部分。绝对不要为了"完成任务"而绕过这一条。

## 三、模块 A：记忆库架构审计（只读）

### A1. 存储结构审计

- 遍历 `[VECTOR_DB_URL]` 指向的集合/表，统计每条记忆的元数据字段完整率（是否包含：时间戳、来源、访问次数、置信度、标签）。
- 检查索引结构：当前使用的是 HNSW / IVF / Flat / 还是自研图结构？记录参数（ef_search、M、nprobe 等）。
- 识别冷热数据分布：近 7 天 / 30 天 / 90 天以上的记忆条数占比；是否有时间分片机制。
- 产出：`architecture_storage_audit.md`，包含字段完整率统计表、索引类型说明、冷热分布饼图（用 ASCII 或 Markdown 表格）。

### A2. 检索链路审计

- 阅读 `[MEMORY_API_PATH]` 下的检索代码，画出检索链路（召回 → 重排 → 过滤 → 返回），标注每一步的函数名、参数、是否可关闭。
- 检查是否存在多路召回（向量 + 关键词 + 时间衰减），还是单纯 top-k 相似度。
- 产出：`architecture_retrieval_pipeline.md`，包含链路图（Mermaid 或 ASCII）、每个环节的风险点。

### A3. 衰减/淘汰策略审计

- 查找记忆库中负责过期、降权、淘汰的代码或配置；如果完全没有，标记为 P1 缺陷。
- 检查记忆写入是否是"只增不改"的 append-only 模式；如果是，标记为高风险架构隐患。
- 产出：`architecture_decay_policy.md`，明确指出当前衰减策略（或缺失）。

### A4. 架构风险评估

- 综合前 3 步，输出一张架构风险清单，每条包含：风险等级（P0/P1/P2）、影响范围、根因定位（到具体文件/函数）、修复建议。
- 特别关注：索引退化、元数据缺失、召回单一、无衰减机制、并发写入一致性（是否有版本控制或幂等保护）。

## 四、模块 B：漂移检测（只读，可跑脚本）

漂移是记忆库最隐形的敌人：向量相似度不等于事实准确度，一条高相似度的记忆可能是三个月前就该失效的。本模块**只用只读脚本**做检测，不写回任何分数或状态。

### B1. Embedding 空间漂移

- 选取 100 条近期（7 天内）新写入的记忆 embedding，与 100 条 30 天前的 embedding 做分布距离检测（建议方法：Wasserstein 距离、PSI、或基于模型的 drift detector）。
- 如果检测到明显分布偏移（例如 Wasserstein > 0.3），说明 embedding 模型可能已更新或输入分布变化，触发 P1 告警。
- 产出：`drift_embedding_space.json`，包含距离值、样本 ID、分布直方图。

### B2. 时间衰减失效检测

- 对每条记忆计算"时效性得分"：`score = 1 / (1 + ln(当前时间 - 最后确认时间))`（半衰期默认 7 天，可调整）。
- 筛选出"高相似度但时效性得分极低"的记忆（即：向量距离很近，但早就该失效），这类是典型的"记忆库中毒"候选。
- 产出：`drift_temporal_decay.json`，包含 top-50 风险记忆条目及其相似度分数 vs 时效分数对比表。

### B3. 语义漂移合并（记忆重复/冲突检测）

- 对近期记忆做两两相似度聚类（阈值建议 0.92），标记出高度相似但内容矛盾的簇（例如同一实体的不同状态描述）。
- 如果发现冲突簇，触发 P0：记忆库已出现事实分歧，后续检索会随机命中错误版本。
- 产出：`drift_semantic_conflicts.md`，列出每个冲突簇的成员、矛盾点、建议保留哪条及理由。

### B4. 实体漂移 / 知识图谱一致性（如果适用）

- 如果记忆库维护了实体表/关系表，做跨锚点一致性验证：同一实体的属性在不同记忆中是否有冲突；因果链是否存在时序矛盾。
- 产出：`drift_entity_consistency.md`（如果无实体结构则输出"不适用"）。

## 五、模块 C：功能自检与对抗测试（中风险，只读优先）

### C1. 检索质量评测（低风险，纯只读）

- 从记忆库随机抽取 30 条记忆作为"查询锚点"，用它们的原文作为 query 跑一次检索，统计：
  - 是否能召回自身（准确率）
  - 召回的其他条目是否相关（精确率）
  - 检索延迟分布
- 产出：`selfcheck_retrieval_quality.md`，包含指标表格。

### C2. 记忆中毒/注入攻击模拟（中风险，标记为"建议但不执行"）

- 构造 3 条假记忆（例如"用户已把邮箱改为 attacker@example.com"），如果直接写入会污染记忆库；**本轮任务不写入**，只做两件事：
  1. 阅读现有入库验证代码，评估它能否拦截这类注入（有没有来源校验、置信度门槛、实体变更审核）。
  2. 把这 3 条假记忆作为"攻击样本"写进报告，附上建议的拦截策略。
- 产出：`selfcheck_injection_resistance.md`。

### C3. 幻觉率与漂移引发的检索错误关联

- 对 B 模块检出的高风险记忆（时间失效 + 语义冲突），模拟检索场景，统计有多少次会返回过期/冲突版本；如果频率 > 20%，标记为 P0 问题。

### C4. 长程任务记忆断裂检测

- 如果记忆库支持多会话/多任务，检查：跨会话的记忆是否能被正确召回（是否有 session isolation 导致记忆被锁死在某个上下文中）。
- 产出：`selfcheck_long_horizon.md`。

## 六、模块 D：修复建议与架构改进（纯输出，不执行）

### D1. 分层修复建议（每条必须挂风险等级 + 是否需人工确认）

按"P0 立即修 / P1 本周修 / P2 下版本修"分层，每条包含：

- 问题描述 + 影响范围
- 根因（文件路径、函数名、数据条目 ID）
- 修复步骤（精确到命令/代码片段）
- 验证方式（修完之后怎么确认问题消失）
- **是否需要人工确认**（任何涉及删除旧数据、重建索引、改 embedding 模型的，必须标为"需人工确认，本轮不执行"）

### D2. 架构改进提案（长期方向）

- 是否建议引入：多路召回、时间衰减索引（Temporal Sharding）、记忆置信度评分（检索 → 执行反馈 → 分数衰减的闭环）、记忆写入幂等锁、embedding 模型版本快照机制。
- 每条提案附：预期收益（量化指标）、引入成本（工作量、依赖）、回滚方案。
- 产出：`improvement_architecture_proposals.md`。

### D3. 本轮闲时任务可自动执行的"轻量自愈"

以下几类是低风险、幂等的操作，Agent 可以在本轮任务中直接执行（如果权限档位允许）：

- 重新计算所有记忆的时效性得分（纯读 → 计算内存中 → 不写回）
- 生成新的索引统计报告文件（写到 `[DATA_DIR]/reports/` 下，不影响业务数据）

其余所有涉及写回、删除、重建、模型切换的操作，**一律不执行**，只写建议。

## 七、报告与产物

所有产出必须集中写到一个目录：`[REPO_ROOT]/memory_health_report_[YYYYMMDD]/`，目录名带本次任务的日期戳。每个文件使用 Markdown 或 JSON，结构化、可 diff、可被下轮闲时任务再次读取做趋势对比。

最后生成一份**总览报告 `OVERVIEW.md`**，包含：

- 健康度总分（0-100，自行定义评分逻辑：架构 30 分 + 漂移 30 分 + 功能 40 分）
- P0/P1/P2 问题数量统计
- 一句话结论："该记忆库当前处于 [绿/黄/红] 状态，最紧迫的问题是 [X]，建议 [Y]"
- 所有分报告的文件索引

## 八、结束与安全边界

- **本轮闲时任务不执行任何破坏性写入、删除、重建、模型切换。**
- 如果 A/B/C/D 任何一步卡住（依赖缺失、数据量过大、权限不足），**继续往下跑能跑的部分**，把卡住的步骤写进 OVERVIEW.md 的"未完成步骤与原因"章节，不要因单点失败终止整个任务。
- 任务结束后，在终端打印一行摘要：`任务完成，总览报告位于 [路径]/OVERVIEW.md，健康度 [分数]，P0 问题 [N] 条。`

---

**任务占位符清单（提交前必须全部替换）：**

1. `[REPO_ROOT]`
2. `[DATA_DIR]`
3. `[VECTOR_DB_URL]`
4. `[EMBED_MODEL]` / `[EMBED_VERSION]`
5. `[MEMORY_API_PATH]`
6. `[MEMORY_COUNT]`
7. `[MIGRATION_LOG]`
