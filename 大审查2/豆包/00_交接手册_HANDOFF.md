# 记忆库自演化沙盒 · 交接手册（HANDOFF）

> 目的：让接手者在**没有原作者上下文**的情况下，零损失理解现状、在任意新机器上一键复现全部 68 个实验，并能立刻按同一套方法论接着做第十一轮。
> 最后更新：2026-09-10（九轮 E1–E61 全部跑通、一键 exit 0；第九轮主题"分布式合谋、身份治理与可观测性"）。配套主文档：`记忆库自演化引擎_沙盒实验总报告.md`（结论与数字的权威叙述）；本手册只讲"怎么接手、怎么继续"。

---

## 0. 一句话现状

围绕真实源码 `bootstrap_v3.py`（agent 记忆库 v3.8.1，2416 行），用一个**零第三方依赖、纯标准库、确定性可复现**的世界模型 `mem_common.py` 忠实复刻其检索/反思/分层/演化机制，已完成 **10 轮共 68 个沙盒实验（E1–E66，含 E22b/E22c/E24b）**，核心结论：**记忆库自演化"可行"，但有七个硬前提**（行为信号换轨且由下游背书、评判尺子冻结在元层、指标与库健康双度量、持续新鲜数据+探索+来源治理、优化目标外部化且治理跑赢扩散、检测面升到子图/身份/时间并多起点搜索、为周期/长尾/多观点/换尺/反身性配元过程设计），并归纳出**七个不可能三角、66 条架构红线**。当前没有未完成的实验，处于"可随时开第十一轮"的干净状态。

---

## 1. 新机器 5 分钟验证（先做这个）

```bash
# 只需 Python 3.10+（开发用 3.14），不需要 pip install 任何东西，不联网、不读外部文件
cd memevo
python -X utf8 run_all.py          # Windows PowerShell / macOS / Linux 均可
# 期望结尾：全部模块运行成功，本机总用时约 30 分钟（第十轮 E65 的理想追踪上界每代多次爬山、较重），exit 0；11 个模块、0 FATAL/Traceback
```

- Windows PowerShell 多条命令用 `;` 分隔（**不要用 `&&` 或裸 `&`**）；存日志：`python -X utf8 run_all.py > results_all.txt 2>&1`。
- macOS/Linux：`python3 -X utf8 run_all.py | tee results_all.txt`，用 `&&` 没问题。
- 中文输出必须带 `-X utf8`，否则 Windows 控制台可能 GBK 报错（与算法无关）。
- 单跑一轮：`python -X utf8 exp_round8.py`（秒级~1 分钟，E51 造万条库稍慢）。
- **可复现自检**：连续跑两次同一轮、diff 输出，应**逐行一致**；唯一允许的差异是 E43/E51 的**计时列**（随机器负载波动，只比较量级，见 §6）。

---

## 2. 文件清单与职责（14 个源码 + 结果 + 两份文档）

| 文件 | 行数 | 职责 | 接手优先级 |
| --- | --- | --- | --- |
| `mem_common.py` | 290 | **唯一世界模型**：造库/查询/检索器/生命周期/反思/爬山演化。所有轮次共享，改它=改全局，必须全量回归 | ★★★ 先通读 |
| `naive_memory.py` | 61 | 被审查的"朴素原型"最小实现，D0–D8 探针的靶子，刻意保留与真实系统同构的缺口 | ★★ |
| `baseline_review.py` | 88 | 机制缺口探针 D0–D8（importance 注水、零命中霸榜、链接农场、分词退化等） | ★★ |
| `exp_round1.py` | 158 | E1–E4 检索打分机制内在缺陷（库龄老化崩塌、分词、links、零命中） | ★ |
| `exp_round2.py` | 116 | E5–E8 反思与合成闭环（自报取材污染、accum 阈值、递归反思） | ★ |
| `exp_round3.py` | 126 | E9–E12 记忆生命周期（长期层、TTL 出仓、死参数、预算逐出） | ★ |
| `exp_round4.py` | 141 | **E13–E16 自演化闭环（核心可行性证据）**：爬山演化 train/hold、自由共演化腐化、eval 棘轮、死参数 | ★★★ |
| `exp_round5.py` | 124 | E17–E19 评测可信度/元层边界/三级综合治理 | ★★ |
| `exp_round6.py` | 585 | E20–E31（14 个）uses 信任根、概念漂移、冷启动、攻击分类学、二阶反思、脏标注、缓存、去重 | ★★ |
| `exp_round7.py` | 556 | E32–E43（12 个）破坏性：MAD 自食、马太/回声室、精准投毒/潜伏不可能三角、多 agent 传染/拜占庭/公地、评测泄漏、DoS | ★★★ |
| `exp_round8.py` | 574 | E44–E55（12 个）对抗动力学与韧性：红皇后、先发锁定、合法擦除、Goodhart、级联、事实变更、多数暴政、规模、相变、OOD、治理代价、种子元审计 | ★★★（最新范式） |
| `exp_round9.py` | ~360 | E56–E61（6 个）分布式合谋/身份/可观测：组合式分布式后门、女巫、检测延迟固化、健康仪表盘、词面鸿沟、多起点路径依赖 | ★★ |
| `exp_round10.py` | ~360 | **E62–E66（5 个，最新）非平稳/公平/元过程**：周期漂移（固定TTL vs 慢性层）、长尾公平（Zipf 下 LFU/时间折扣/探索/主题配额+留存侧指标）、合法多观点 vs 冲突（相似度×uses 悬殊双信号）、金尺漂移（冻结/滑动/理想上界）、反身性闭环（自指评测乘性锁定的内外剪刀差） | ★★★（最新） |
| `run_all.py` | 20 | 顺序子进程跑全部模块，任一非 0 即 abort。**加新一轮就在 MODS 末尾追加** | ★★ |
| `results_all.txt` | ~720 | 最近一次全量存档（10 模块完整输出，权威数字总来源） | ★★★ |
| `results_round6/7/8/9.txt` | — | 四轮单独存档（round1–5 只在 results_all 里） | ★★ |
| `results_verify.txt` | 271 | 早期前五轮验证存档（09-08），仅历史留痕，**以 results_all 为准** | ☆ |
| `记忆库自演化引擎_沙盒实验总报告.md` | ~1300 | **主交付物**：摘要+总表+分轮补篇+返工/推翻+架构红线+结论+复现附录 | ★★★ |
| `00_交接手册_HANDOFF.md` | 本文件 | 接手与接续指南 | ★★★ |

模块依赖：`baseline_review → mem_common + naive_memory`；其余每个 `exp_roundN → mem_common`；轮次之间**互不 import**（每轮自包含，可独立运行）。

---

## 3. 世界模型接口手册（`mem_common.py`，接续必背）

### 3.1 常量
- `TOPIC_WORDS`：20 个主题、每主题 8 个**互斥**中文专属词；`TOPICS`=其键列表；`GENERIC_WORDS`=10 个高频泛词（造噪）。
- 出厂手写参数 `FACTORY=dict(w_kw=3.0, w_c=1.0, w_imp=1.0, w_age=0.1, d=0.5, filter_zero=False)`。
- `REFLECT_THR=50`（importance 累加触发反思）、`LONGTERM_THR=7`（imp≥7 进长期层）、`RETIRE_TTL=90`（失效后 90 天出仓）、`DECAY=.7`、`HOT_BUDGET=200`（**死参数**，真实代码声称"重要度×时效逐出"但未实现）。

### 3.2 条目字段（make_library 产出的 dict）
`id(f"M{i:04d}"), topic, true_quality(0..1 上帝视角,检索器看不见), keywords(list), content(list), importance(自报,可见), honest_imp(诚实应报值), vanity(是否注水bool), created_day, t_invalid(失效日/None), truly_invalid(上帝视角bool), uses(历史真实复用次数), links(list of id), is_farm(bool)`。
**铁律：任何"治理/排序"逻辑只能用可见字段，`true_quality/truly_invalid/honest_imp` 只能用于造数据和 golden 评分，否则等于作弊。**

### 3.3 关键函数签名
- `make_library(n=200, seed=0, vanity_frac=0, farm_clusters=0, farm_size=0, mislabel_frac=0, q_power=2.0, now_day=365, verbose=False)` → list[dict]。质量用 `Beta(q_power,q_power)`；uses=`(quality**1.5)*randint(0,40)`；正常 links 以 .15 概率链同主题另一条。
- `make_queries(ents, n_q=80, seed=100, expected_per_q=3, words_per_q=2)` → **list[dict]**，元素是 `dict(topic, query="词 词", expected=[id...])`（注意不是字符串！E53 曾因此崩过）。主题池不足 expected_per_q 会 skip。
- `rank(ents, query, theta=FACTORY, now_day=365, tokenize="space", drop_invalid=True)` → `[(id,[base,e,word_hit])]` 降序。
- `evaluate(ents, queries, theta, K=5, ...)` → `dict(recall, MRR, precision, zero_share, farm_share)`（return_detail=True 时额外返回最后一次 rank）。
- 生命周期：`split_layers(ents)→(longterm,inter)`；`retire(ents,now,ttl,dynamic=False)→(keep,out,false_kill,miss_invalid)`；`evict_to_budget(ents,budget,policy)→(keep,out)`，policy∈lru/imp_time/random/quality_oracle。
- 反思：`append_stream(seq,thr,honest=False)→(fires,spans)`；**`reflect_materials(ents,topn=6)→(top_list, 坏率)` 是二元组**（E47 曾因漏解包出错，别再犯）；`synthesize(top)→dict(topic,syn_quality,pollution,keywords) or None`。
- 演化：`evolve(ents,q_train,q_hold,theta0,n_gen=30,seed=0,sigma=.15,gate_margin=0,mutate_filter=False,adversary=None)→[(train,hold,theta)...]`，只变异 `w_kw/w_c/w_imp/w_age/d`（mutate_filter 时加 filter_zero 翻转），门控=训练分提升过 margin 才接受。
- 工具：`mean(xs)`/`sd(xs)`（总体标准差，均 round 到 3 位）。

### 3.4 检索器忠实复刻的四个"坑点"（实验结论的物理来源）
1. 打分 `base=3·kw_hit+1·c_hit+1·importance−0.1·age`；**kw_hit>0 时 c_hit=max(c_hit,1)**（源码 L901）。
2. **filter_zero=False：零词命中条目不过滤**，照样靠 importance−age 进排序——绝大多数"霸榜/老化"问题的总根源。
3. **links 一跳扩散只发给"自身 word_hit==0"的目标**，加 `d·命中分`；多条命中桥可累加。这导致：开 filter_zero 会**结构性掐断**图潜伏通道（E38 不可能三角的关键）。
4. importance 是**自报且 validate 不校验范围/类型**，可报 999；它同时参与排序、反思取材、长期层分层——"被排序者掌握打分权"是全报告主线。

---

## 4. 十轮 68 实验全景索引（结论一句话 + 数字在哪个结果文件）

> 完整数字与表格在主报告"实验总表 + 各补篇"。这里给导航地图，便于按需回查。

**第一轮 E1–E4（检索缺陷，results_all）**：出厂召回随库龄 30 天 .920→365 天 .403 单调崩塌；中文无空格分词退化（.398→.173）；links 词面重叠无收益（E3c 否）；零命中不过滤是总根源，开 filter_zero 回 .92+。

**第二轮 E5–E8（反思）**：自报取材 10% 错误→100% 富集污染；accum≥50 阈值可被高 imp 灌水操纵；递归反思只锁死不放大（E26 否，见后）。

**第三轮 E9–E12（生命周期）**：imp≥7 长期层被自报绕过；固定 TTL 误杀/漏放；动态出仓救不了错标（E10 否）；HOT_BUDGET 等是零消费死参数。

**第四轮 E13–E16（核心可行性）**：**E13 静态世界自演化可行** train .388→.997/hold .992、5/6 种子自发发现 filter_zero；**E14 自由共演化可见指标 .956 但库 45% 腐化，元层锁安全键压到 .153**；E15 eval 棘轮（劣化基线被回写）；E16 死参数自演化也改不动行为。

**第五轮 E17–E19（评测与治理）**：评测噪声决定 margin（E17）；改考题满分但隐藏题劣化（E18）；三级治理 L0 .172/L1 .876/L2 .852（E19）。

**第六轮 E20–E31（uses 信任根与边界，results_round6）**：uses 对覆盖稀疏(10%)、流行噪声(坏率.15–.19)极鲁棒，但**一旦写入方能自导伪造 uses，坏率 .125→1.000（E24b 信任根）**；掺自报救冷启动被否(E21)；灾难性遗忘只在零和旋钮发生(E22c)；关键词堆砌是 filter_zero 盲区(E24 .598)；uses 被伪造时结构检测 TPR 归零(E25)；连续维度有代价、结构开关收益压倒(E28)；脏标注使训练分异常低(E29)；工作集快变 LFU .914→.545(E30)；高阈值去重零误并(E31)。

**第七轮 E32–E43（破坏性，results_round7）**：
- E32 MAD 自食：替换式 10 代全库主题 20→12.1、**取材质量 sd .223→.012**、自产占比 .991；每代持续喂新鲜真实数据才保得住（只囤旧数据无效=S12）。
- E33 自食脱钩：内部自测 .27–.53，外部金尺 .373→.042。
- E34 马太：α=1 uses 基尼 .503/top10%占.319；+15%探索覆盖回 .973。E35 回声室：γ=2 正反馈 98% 局锁极端，补弱配额 0 锁死。
- E36 单点毒丸：filter_zero 清随机注水但精准词面毒丸目标主题仍 100%、最小剂量 1–2 条。E37 词表劫持：诚实 imp=5 只注册全主题词，占 top1 .875。
- **E38 潜伏不可能三角**：躲词面审计×平时隐身×触发激活不可兼得（图潜伏被 filter_zero 结构性掐灭）。
- E39 多 agent 传染：坏 1/10 无防护终污染 .874、9 诚实 agent 全感染；带源签名+隔离止于初始、零二次感染。E40 拜占庭：mean 扛到 f=3，trimmed/median 扛到 f=4，f=5（占一半）全崩，逼近"严格少于一半"理论界。E41 公地：agent 2→20，LFU 命中 .913→.496。
- E42 评测泄漏：全泄漏自测 .998、隐藏题恒 .14–.18；屏蔽评测写入回 .388。E43 复杂度 DoS：keywords 超长仅 1.1x，**全库 links=500 放大数十倍（约 22–29x，Python 层图遍历）**，硬限重点是单条 links 度数（建议≤32）。

**第八轮 E44–E55（对抗韧性，results_round8）**：
- E44 红皇后：常规攻击残余 1 代 .91→.10，伪造 uses 恒 1.00（无静态终点）。
- E45 先发锁定：纯优先连接/15%探索全锁死，质量降权只在独占窗口 W≤2 可翻，W≥4 永久。
- E46 合法擦除：普通合并 θ≤.7 擦除率 1.0，θ≥.8 须逐字复制归零，安全合并（要求结论一致）恒 0。
- **E47 Goodhart**：反思条数(代理1.0)/自报imp(9.875)/词表覆盖(2.016)/满意度(3.195)全部代理升、真实 recall .351→.298/.281/.009/.172；唯外部金尺 .586。
- E48 级联：15% 底层污染，OR 式深层合成放大到 .966，多数 .002、AND 0，扁平 .149。
- E49 事实变更：旧 uses≥6 压死新事实，版本链标记失效恒可更新(1.0)。
- E50 多数暴政：从众 λ 增大，易任务集体正确率 .686→.492（破随机），难任务 .523→.424。
- E51 规模不经济：出厂 recall N=100→10000 单调 .408→.129；单次检索近线性 ~0.15→~25ms。
- **E52 相变**：临界清洗率 γc=β=.30，γ>.30 任意初始污染回 0，γ<.30 锁 1−γ/β（.667/.333），终态与 p0 无关。
- E53 OOD（否定结果 S14）：调全局权重对新风格/新主题同幅泛化（无 OOD 惩罚），内容缺口恒 0。
- E54 治理代价：可区分攻击 G0→G3 污染 .456→.036 且洁净 recall .378→.934（双赢），G3→G4 边际递减；折损只在不可区分时。
- E55 元审计：32 种子下 MAD 收缩/马太基尼/拜占庭漏毒三结论方向 100% 一致、最不利种子不跨 0。

**第九轮 E56–E61（分布式合谋、身份治理与可观测性，results_round9）**：
- E56 组合式分布式后门：载荷零激活词+ k 桥，同龄库 now=120 排除年龄伪影（F20 教训）；filter_zero=False 出厂下常规暴露恒 0、触发激活 k=3 .386/k=4 .969/k=6 过密反暴露 .375；逐条审计 0 检出、子图审计（入度≥3+词面贫乏）k≥3 全抓；filter_zero=True 结构性归零（S16，强化 E38 单条三角为分布式版）。
- E57 女巫：固定 8 诚实源、攻击者造 n_sybil 个"温和恶意(.85)"身份；mean 平滑带偏、median 在过半处相变（n=8 .625→n=12 1.00）、trimmed 更缓；身份年龄门槛压新源权重到 1/10 后全程 0。文献 Douceur2002。
- E58 检测延迟固化：未固化污染每代 8% 提升进长期层不可逆；延迟 L=0→20 代最终固化 .018→.461；阻断二次写回 β=0 恒 .032 与延迟无关。注意 F21：队列模型打不出梯度，必须用"存量按速率固化"。
- E59 健康仪表盘：5 故障（MAD/马太/imp注水/分布式后门/主题塌缩）×5 纯内部指标，贪心最小集=自产占比 self_made + uses_gini + imp_mean 三个，g0–g6 内 100% 预警、无需上帝视角。
- E60 词面鸿沟：同义替换 p=0→1 纯词面 recall .328→.138；查询扩展(20%歧义词)救到 .320、写入概念归一 .291，词表不完美均付 precision 代价。
- E61 路径依赖：4 个初始 θ 各 8 种子爬山，坏起点（盲信自报 .205/盲信时效 .085）困次优峰、出厂 .622、多起点取最优 .697。

**第十轮 E62–E66（非平稳、公平与元过程，results_round10）**：
- E62 周期漂移：20 主题正弦明灭（period=24、冷却 T_cold=6、3 周期），gold 取完整库真值（F22 幸存者偏差教训）；固定 TTL 回归召回 .085/末态存储 0，慢性层（曾采用即降权留存、重命中复活）.701/.192。
- E63 长尾公平：Zipf s=1.5、uses 从 0 起纯由采用流累积、预算 400→200、expected 取逐出前完整库（F23 三道坎）；LFU 整体 .151/长尾 .052/覆盖 18.2/热门·长尾存活 16.5·3.5，时间折扣 .166/.054，15%探索 .154/.044（≈无效，S17 限定 E34），主题配额 .198/.163/20/严格 10·10。
- E64 多观点 vs 冲突：冲突簇 Jaccard=.6（uses 30 vs 0）、互补簇 Jaccard .2–.5（uses 20/18 都真）；纯相似度 θ=.3 冲突全并但误杀 47.8% 互补、θ=.7 零误杀却漏全部冲突；θ=.5+「uses 悬殊≥4 倍或其一为 0」矛盾判别=冲突 1.0/误杀 0。
- E65 金尺漂移（合法换尺，区别 E15/E42 污染）：α(g) 正弦在偏词面 A 世界与偏时效 B 世界间摆；冻结 .729、滑动热启 .856、理想追踪上界（同热启每代 15 代）.972、滞后 .115、参数震荡 .207（F24 上界必须同热启且搜索更充分）。
- E66 反身性：外部金尺固定均匀 120 题；自指主题权重乘性马太锁定（F25，加性版打不出正剪刀差）；末态内部 .675/外部 .51/剪刀差 +.165（轨迹 g0→g30 内部升、外部停滞），外部覆盖对照 .976/.976/0。

---

## 5. 顶层结论在哪（写新轮次前先对齐，别推翻已证结论）

- **可行性判定与六前提**：主报告 §6.1（第 1–6 条）。
- **七个不可能三角**：§6.2（自报便利×抗操纵×排序权重；新鲜度×经典留存×统一年龄项；自主演化×评测自洽×元层可达；行为可信×采用可自证×写入侧自主；躲审计×隐身×触发；完全内部自治×静态防御全覆盖×写入侧自主；**开放自由加入×按源聚合抗操纵×身份零成本（女巫，第九轮）**，并把单条潜伏三角强化为"逐条审计×分布式合谋隐身×无子图视图"）。
- **66 条红线**：§5.4（基础 25 + 六轮 12 + 七轮 10 + 八轮 8 + 九轮 6 + 十轮 5，编号 1–66，**下一轮红线从 67 起编**）。第十轮五条主题：TTL 判失效/慢性层判失活（E62）、机会均等靠结构性配额而非随机探索（E63）、去重需词面+uses 双信号区分冲突与多观点（E64）、金尺合法迁移须滑动追踪不可冻结（E65）、评测分布必须独立于被评测系统（E66）。
- **分层架构**：§5.1 四层+锚定层、§5.2 写入—评测—晋升循环、§5.3 与 bootstrap 源码行号对接映射。
- **诚实记录**：第四部分，25 次设计返工 F1–F25（**下一轮 F 从 26 起**）、17 次自我推翻 S1–S17（**S 从 18 起**）。第九轮新增 F20（攻击实验须同龄化、排除环境性伪影）、F21（存量按速率固化 + 又一次 hash 随机化修复）、S16（分布式后门没绕过单条不可能三角、只是把检测面逼到子图）。第十轮新增 F22（回归点 gold 取完整库、排除幸存者偏差）、F23（公平实验三道坎：公平起点/真值取干预前全集/接通"频率→uses→留存"因果链）、F24（上界必须同热启且搜索更充分，否则滞后为负）、F25（反身性需乘性正反馈锁定，加性重造打不出正剪刀差）、S17（15% 随机探索救不了主题级陡马太、把 E34 限定到温和马太，机会均等要结构性配额）。**第十轮未证出新的严格不可能三角，七个保持不变**（E64 是"需引入第二维行为证据"的权衡、E66 是剪刀差，均不单列三角，勿硬凑）。

---

## 6. 可复现铁律（接续实验必须 100% 遵守，否则数字不可信）

Python 的 `hash()` 对字符串**跨进程随机化**、`set/dict` 迭代序不保证、全局 `random` 默认吃系统熵。要做到两次逐行一致：
1. **永远用局部随机源** `rnd=random.Random(seed)`，禁止直接用模块级 `random.xxx`（确定性实验里）。多种子用固定种子列表（现用 `[11,23,37,51,67,83,101,127]`，元审计 E55 用 range(32)）。
2. **任何并列排序加确定性 tiebreak**：`sorted(xs,key=lambda x:(-主键, x["id"]))`，字典序/数值次序兜底。
3. **从集合采样先 `sorted()`**：`rnd.sample(sorted(set(...)), k)`、`rnd.choice(sorted(...))`。
4. 遍历条目用稳定顺序（make_library 产出本就有序）；不要边遍历边改 dict 后依赖其顺序。
5. 汇总用 `statistics`/`M.mean`，浮点比较只比 round(…,3) 后的结果。
6. **计时类实验（E43、E51 单次 ms）不要求逐位一致**，报告只写量级区间，并在结果文件/报告里注明"随机器波动"。
7. 改任何 `exp_roundN` 参数 → 必须重跑该轮并同步 `results_roundN.txt` 与主报告数字；改 `mem_common` → 必须重跑 `run_all` 全量回归（它影响所有轮）。
8. 报告里数字的**唯一权威来源是 results 文件**，禁止凭记忆/转抄写数。

---

## 7. 已被验证"做不通 / 应放弃"的方向（别再花时间重做）

- agora 群体智慧方向（用户判定偏题，workspace/agora 旧产物已不维护）。
- 动态出仓救错标（E10 否）；低维小 golden 过拟合（E17a 否）；词面重叠世界里 links 扩散无收益（E3c）。
- 掺自报字段救冷启动（E21 否）；加性权重下的概念漂移遗忘（E22/E22b 否，遗忘只在零和旋钮 E22c 出现）。
- 递归反思污染逐级爆炸（E26 否，实际只锁死不放大）；只囤旧数据防自食（S12 否，必须每代持续注入**新鲜**数据）。
- 造出"完美潜伏记忆"（S13/E38 否，三性不可兼得）；让 uses 独占排序键（S11 否，会马太，必须配探索）。
- 单纯加随机探索破先发锁定（F17/E45 否，必须叠加下游质量降权，且 W≥4 仍锁死）。
- 假设"自演化调参会在新主题 OOD 退化"（S14/E53 否，调全局权重会泛化，脆弱面在内容缺口而非参数）。
- 假设"层层治理必然牺牲洁净性能"（S15/E54 否，可区分时双赢，折损只在不可区分时）。
- 以为分布式后门（多条分摊合谋）能绕过 E38 单条潜伏不可能三角（S16/E56 否：逐条审计确实 0 检出，但 filter_zero 结构闸门仍在、且必然留子图指纹；另注意 F20——在老库植入新零件得到的"易激活"是库龄伪影，攻击实验必须同龄化）。

---

## 8. 返工方法论教训（F/S 全表在主报告第四部分，摘要）

- 攻击面要想清楚是"单条"还是"多少条带无界字段"（F16 DoS）；MAD 对照必须"每代持续注入新鲜数据"而非只不删（F15）；单条目标命中统计用 binary 命中率、不是恒除以 5 的 top5 份额（F14）；拜占庭恶意条真值必须确实低、否则"无攻击就有恶意"（F 教训）；评测泄漏副本要计入 expected（否则看不到背题虚高）。
- 第八轮新增：F17 等比 uses 衰减救不了先发（要份额动力学）、F18 多防御叠加残余取 min（最有效）不是 max、F19 级联要固定层宽+显式区分 OR/多数/AND 才能打出放大。
- 第九轮新增：F20 攻击实验里攻击者不该白捡环境性优势（在 365 天老库植入新零件会因线性 age 惩罚白捡碾压，测到的是 E2 库龄漏洞而非目标机制——必须同龄化并逐库打印"攻击分 vs 正常 top5 分数线"标定激活阈值）；F21 "延迟固化"若用新增量排队模型会打不出梯度，要让未固化**存量**按速率被提升固化；以及第二次被两次运行 diff 抓到 `hash(str)` 随机化（禁用，改固定整数映射）。
- 第十轮新增（四条都很通用）：①**两处幸存者偏差**——干预（出仓/逐出）后再从"幸存集合"里选 gold/expected，会让被干预掉的样本根本不进考题、虚高结论，gold 必须取干预前完整库，被干预即记 miss（F22/F23）；②**公平实验要公平起点**——造数时绑在质量上的初始 uses 会掩盖马太，对照前先把行为信号清零、让它纯由实验中的反馈流长出；③**因果链要接通**——出厂排序公式不含 uses 时，经静态 top-k 的采用反馈传不到条目存活，主题级马太会被截断，须让需求频率直接作用到行为信号；④**"上界"必须真为上界**——热启累积爬山比每代从出厂重爬更强，oracle 要么同热启且搜索量更大、要么覆盖足够密的参数网格，否则会算出"策略超过上界"的负滞后（F24）；⑤反身性/马太锁定类实验，加性混合往往打不出正反馈，要用乘性自增强（F25）。
- **通用心法**：每个实验平均返工 2–3 版才打对机制；若反复打不出预期，优先像 E38/E53 那样**转成"否定性/不可能性结论"诚实记录**，不要硬凑预期数字。

---

## 9. 接续实验思路（第十一轮及以后，可直接开工）

> 选题原则：避开 §7 已否方向；每个都先锚定文献、在世界模型里复现机制；优先补"现有结论链条上尚未闭合的环"。
> **进度：第十轮（E62–E66）已完成**——周期漂移、长尾公平、合法多观点、金尺漂移、反身性闭环五个均已落地（结果见 §4 第十轮索引，返工 F22–F25、自我限定 S17）。**下一轮实验从 E67 起编号**。

### 第一梯队（第十一轮优先：顺着第十轮五个新方向补"治理/代价/相图"）

**E67　慢性层的代价与再验证（E62 延伸）**
- 未闭合的环：E62 的慢性层用 19% 存储买回回归召回，但没测"无限慢性层"的副作用——陈旧知识长期占位会不会抬高误召回/被 E49 式事实变更利用。设计：给慢性层设容量上限与"再验证"（回归命中若下游不采用则降级出仓），扫慢性层规模×再验证阈值，画回归召回—陈旧噪声—存储三面。

**E68　反身性的治理：最小外部锚定比例（E66 延伸）**
- E66 证明纯自指产生剪刀差，但没给解药。设计：训练/评测金尺按比例 ρ 混入系统外均匀题（ρ=0→1），找压住内外剪刀差所需的最小外部锚定比例与审计频率相图；与 E47 外部金尺、E59 内部指标对接。文献 performative stability。

**E69　金尺漂移速度相图（E65 延伸）**
- E65 只测一个漂移周期。设计：扫金尺漂移周期（快→慢）×每代追踪代数，找"滑动追踪开始追不上、退化为冻结"的临界漂移率，类比 E52 相变；区分可追踪的平滑漂移与不可追踪的跳变。

**E70　多约束逐出的帕累托（E63 延伸）**
- E63 只比单一配额。设计：逐出同时受主题配额、质量下限、时效三约束，画整体 recall×长尾公平×存储的帕累托前沿，找不靠硬配额的软正则（主题惩罚项）能否逼近配额。

### 中长期（需要先扩展世界模型，再做）

- **E71 公共物品贡献博弈**：记忆贡献有成本、收益共享，测搭便车均衡与激励（E41 是缓存容量公地，未做"要不要贡献"的策略侧）。
- **E72 跨 Python/OS 数值稳健性压力测试**：固定字典序求和、统计不同平台 statistics/浮点求和差异，固化"跨平台逐行一致"的最小写法（为开源复现铺路）。
- **E73 从离散词面走向连续嵌入检索**：当前命中是词面子串，可加可选"向量近邻"检索器（仍零依赖、手写余弦），检验全部结论在语义检索下哪些保持、哪些改变；E60 已给出量化动机（同义鸿沟词面无解）、E64 的词面相似度在嵌入空间也需重测。这是最大的一次外部效度升级，建议结论链条收口后再做。

### 第十一轮建议打包
推荐组合：**E67 慢性层代价 + E68 反身性治理 + E69 漂移速度相图 + E70 多约束逐出**（4 个，都是给第十轮"现象"补"治理与边界"，形成闭环）；E71–E73 作为第十二轮及中长期储备。

---

## 10. 新增一轮的标准 SOP（照做即可零偏差）

1. 复制 `exp_round10.py` 为 `exp_round11.py`，改头部注释、实验编号从 **E67** 起、种子列表沿用 §6。
2. 只用 `mem_common` 的接口造世界（§3）；治理逻辑只用可见字段；每个实验先在单种子下跑通机制、再上 8 种子均值。**攻击/对照实验注意 F20：攻击者不得白捡库龄等环境性优势，必要时同龄化（now_day 与 created_day 匹配、rank 同步传 now_day）。**
3. 严格守 §6 确定性铁律（尤其禁用 `hash(str)` 当种子，F12/F21 两次踩坑）；计时类实验单独标注例外。
4. **机制打不对就改机制，不硬凑**；反复不成转否定性结论并记 F/S。
5. 单轮跑两次 diff 验证逐行一致（计时列除外）：`python -X utf8 exp_round11.py > results_round11.txt`，再跑一次用 Compare-Object 比对（第十轮即两次零差异）。
6. 把 `"exp_round11.py"` 追加到 `run_all.py` 的 MODS，更新其 docstring 轮次/实验数。
7. 全量回归 `python -X utf8 run_all.py > results_all.txt 2>&1`，确认 12 模块 0 FATAL。
8. 增补主报告（位置与编号，**严格续接不要重号**）：
   - 头部"68 个/十轮"计数、日期行；摘要表加一行"第十一轮・…"、方法论 F/S 计数；
   - 实验总表末尾（E66 行后）加新行；
   - 在"补篇・第十轮"之后、"# 第四部分"之前插入"# 补篇・第十一轮…"整章；
   - 第四部分：返工标题数与 **F26+**、推翻标题数与 **S18+**；
   - §5.4 红线标题加"第十一轮增补 N 条"、正文红线从 **67** 起；
   - §6.1 加前提（若有）、§6.2 不可能三角（若证出新的，现为七个）、§6.3 收口补一段；
   - 附录：run_all 命令轮次/耗时、模块列表加 round11、存档说明、末尾关系表实验数。
9. 重新打包 zip（命令见 §11），并从 zip 解压到**空临时目录**跑一次冒烟（至少 baseline 秒级 + 新轮），验证与原目录无关、可移植。
10. 最终交付：主报告 .md + zip；回复里讲清新发现、新红线、新否定结果。

---

## 11. 打包与完整性校验

```powershell
# Windows PowerShell，在 memevo 目录内
if (Test-Path memevo_sandbox.zip) { Remove-Item memevo_sandbox.zip }
Compress-Archive -Path *.py,*.txt,"记忆库自演化引擎_沙盒实验总报告.md","00_交接手册_HANDOFF.md" -DestinationPath memevo_sandbox.zip
# 校验：列清单 / 解压到空目录冒烟
Add-Type -A System.IO.Compression.FileSystem; [IO.Compression.ZipFile]::OpenRead("$PWD\memevo_sandbox.zip").Entries | Select-Object FullName,Length
```
macOS/Linux：`zip memevo_sandbox.zip *.py *.txt *.md`；校验 `unzip -l`。
**交接清单核对**：14 个 .py（mem_common/naive/baseline/round1–10/run_all）、results_all+round6/7/8/9/10(+verify)、主报告、本手册，缺一不可。

---

## 12. 世界模型的已知外部效度边界（别把沙盒结论当线上结论）

1. 命中是**词面子串**、主题词互斥、20 主题封闭；真实系统是语义向量、开放词表（见 E69 升级路径）。
2. uses 被建模为"下游真实事件不可伪造的背书"，这是**被假设保护**的信任根；一旦写入方能自证 uses，全部相关结论按 E24b/E44 的失败侧理解。
3. 合成/反思是简化模型（取 top、质量均值、污染比例），没有真实 LLM 生成的语义漂移；结论针对的是**机制结构**而非生成质量。
4. agent 行为（传染、拜占庭、从众、公地）是参数化随机过程，非真实多智能体通信；用于验证**机制上界/阈值**，不代表具体数值。
5. 时间是离散代/天，age 为线性惩罚；真实访问间隔重尾。数值是**世界模型内的相对量级**，跨系统只迁移定性结论与红线，不迁移绝对数字。

---

## 13. 包外的原始材料（不在 zip 内，需要时回原机取）

- 真实被审查源码：`D:\zcode专用！！！！危险！！！！！！！！！\library-bootstrap-v3.0\bootstrap_v3.py`（v3.8.1，2416 行；静态审查在同目录 `review-20260908\`，代码地图 `代码地图-bootstrap_v3.md`）。`mem_common` 注释里的 L 行号对应它。
- 用户要求模仿的样板报告：`C:\Users\26672\Desktop\自演化推演_实验总报告.md`（纯 Markdown 表格/代码块、无图风格的来源）。
- 早期判偏题旧产物：workspace 下 `agora\`（群体智慧方向，已弃，勿接续）。
- 文献锚点（可重新检索）：MAD=Shumailov Nature 2024；奖励黑客=Skalse NeurIPS 2022 / Amodei Concrete Problems / Thomas&Uminsky；级联与恢复=Havlin/Buldyrev、Di Muro 2015、Majdandzic Nat Phys；群体= Lorenz PNAS 2010、Banerjee 1992、Frey&van de Rijt；拜占庭=Krum/几何中位数；投毒=MINJA(2503.03704)/MemoryGraft/MemPoison；第九轮新增：女巫=Douceur 2002（Sybil）、词汇不匹配=vocabulary mismatch / 伪相关反馈 query expansion、优化地貌=multi-start/restart、performative prediction。第十轮新增/复用：周期/循环概念=recurring concepts、continual learning（Robins 1995 / catastrophic forgetting 对照）；长尾=Zipf/Heaps、机会均等=equal opportunity/equality of opportunity、fair ranking；多观点=ensemble diversity / near-duplicate merge vs. viewpoint diversity；金尺漂移=concept drift 滑动窗口/forgetting factor；反身性=Perdomo 2020 performative prediction、reflexivity、Goodhart 发生学。

---

### 交接确认（接手者逐项打勾）
- [ ] 新机器 `run_all.py` 一键 exit 0、10 模块 0 FATAL
- [ ] 单轮两次运行逐行一致（计时列除外）
- [ ] 通读 mem_common.py 与主报告 §6（前提/三角）、§5.4（红线）
- [ ] 理解 §7 被否方向、§9 接续思路、§10 新轮 SOP
- [ ] 知道数字唯一权威=results 文件、改 mem_common 必须全量回归
