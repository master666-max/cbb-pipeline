# SLEEP-诊断-20260921（WP-C2 首触发实测产出）

> 触发：调度器真实触发 2026-09-19 00:13:35（automation-f6c19799，runCount=1；本诊断为其产出补录执行件）。
> 模式：诊断模式——只诊断不修改，问题列清单等用户裁决，不自动改库。
> 执行环境注记：引擎自报 pkg_sha256=**40f88584**（≠正典锚 8de8fb1f——发布区文件被 WP-C C-1 实施会话手改加入 structured.md 白名单行+CRLF 转换，**absorb-md 路径专属，检索/评测路径零涉**（rank 函数区段无 structured）；正典 vendor 8de8fb1f 完好；本发现 open 待协调，本诊断不覆盖）。

## 一、engine doctor 五态对账

- memory 45 / audit 45 / ledger 45 / **审计链 连续** / **state 根不一致** ⚠
- [state] 根对账不一致（最近写操作未刷根或被篡改）——成因分析（open 待裁决）：W-2b build-docs 同步 state.json 时未刷 merkle 根 + knowledge/SLEEP.md 等新文件入库 + eval 序列追加，三者叠加。**不自动修**；修复路径=engine wrapup 或 append 触发刷根（属状态变更操作，需裁决后执行）。

## 二、lint_library

- 体检文件 3 个，markdown 链接 0 条；**0 问题 / 0 提醒**。

## 三、清点

- evals/eval_history.jsonl：19 行，最后一条 recall@5=0.7273（2026-09-19T16:43，E1R-bigram-v310 段）；
- ledger/changes.jsonl：68 行；
- modules/ 十模块在位：**10/10**（m-core/m-cron/m-evolve/m-index/m-loop/m-radar/m-reflect/m-schema3/m-soul/m-tools）。

## 四、睡眠整理建议（只建议不执行）

1. state 根不一致（见 §一）→ 建议裁决后以 wrapup/append 刷根；
2. 发布区文件 40f88584≠正典 8de8fb1f → 建议 WP-C 实施方将其 structured.md 改动移入 make_v39 补丁链后统一再生成（P-020 纪律），恢复单 blob；
3. stale 条目：0（verified_against 机制尚未有数据条目）；
4. eval 序列：E1R-bigram-v310 段 3 点平稳（0.7273），无异常旗标。

## 五、SLEEP.md 模式确认

- knowledge/SLEEP.md 在位，模式字段=**auto** ✓，两槽已填（CRON_MODE=auto / PY_RUNNER=py -X utf8），纪律句在位（"不自动改库（只诊断不修改）"）。
