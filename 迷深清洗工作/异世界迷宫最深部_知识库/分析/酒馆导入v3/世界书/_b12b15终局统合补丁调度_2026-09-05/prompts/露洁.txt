你是《异世界迷宫最深部为目标》SillyTavern 世界书「B12+B13+B14+B15 终局统合补丁」批次工程师，本次只负责一本：角色专属世界书_露洁.json。

目标文件（读改后必须覆写回这个绝对路径）：
D:\DeepSeek Harness专用！危险！！！！！\迷深清洗工作2\异世界迷宫最深部_知识库\分析\酒馆导入v3\世界书\角色专属世界书\角色专属世界书_露洁.json

━━ 幂等条款 ━━
若某 ● 项所指原文已不存在或已完成同等修改，该项视为完成，直接跳过，不得二次改动。

━━ 执行纪律 ━━
- 只执行下方与本书匹配的 ● 项；未列条目/字段/台词一律原样保留（不重写、不润色、不删台词、不改 keys）。
- 各条目 uid 顺序不变；不新增不删除条目（● 项未明确要求时）。
- 引号「」与“”内逐字台词一律不动（除非该 ● 项指明）。
- 顶层 name、description、key/keys 一律不动（除非 ● 项指明）。

━━ 本书 ● 项清单 ━━
●7 uid1「惯以姐姐口吻嗔怪其血气上头、屡屡拦阻其冲动（L7642/L8111）；第十章诀别承诺「地上交给我就好」，嘱其「不遗余力地活下去」（L16108）。」→「惯以姐姐口吻嗔怪其血气上头、屡屡拦阻其冲动（L7642/L8111）；终局诀别见禁用条目。」
  · 即把「第十章诀别承诺「地上交给我就好」，嘱其「不遗余力地活下去」（L16108）。」整段换成「终局诀别见禁用条目。」；L16108 逐字已由禁用 uid8 承载，勿动 uid8。

━━ 通用自检五项（写盘前逐项核对）━━
① root 层 position=4、depth=4、prevent_recursion=true 逐条核对；extensions 内 position 非 4 者改 4（如本书无需改动，核对即可，勿多动）；
② 全书 constant:true 条目数必须为 0；
③ disable 条目 root 与 extensions 双层在位，content 开头有 <!--【手动条目】…--> 标记（含本次新转禁用条目）；
④ 每条 content 末行有 <!-- 触发逻辑：… --> 注释；
⑤ 写盘后重新读回：JSON 可解析、无 BOM、uid 顺序与改前一致、● 项新文案在位且旧文案已消失。

━━ 操作方法（务必照做）━━
用 Python（命令行用 py）执行：
1) raw = open(路径,'rb').read()；data = json.loads(raw.decode('utf-8-sig'))；记录改前 uid 顺序（按出现序）与原文件是否以换行结尾。
2) 按 uid 定位 entry。每个字符串替换先断言旧串在该 entry 的 content 中恰好出现 1 次（出现 0 次且新串已在＝幂等跳过；出现多次＝停下报告，不得盲改）。转禁用类 ● 项按步骤逐项做。
3) 全部改完后 json.dumps(data, ensure_ascii=False, indent=2)；原文件末尾有换行则结尾加一个换行、没有则不加；用 open(路径,'wb') 以 utf-8（禁止写入 BOM）覆写回原绝对路径。
4) 重新读回验证第⑤步五项自检。

━━ 交付 ━━
覆写回原绝对路径后，回复 ≤8 行摘要：每个 ● 项一行（uid＋改动点，或注明"幂等跳过"）。勿在回复中粘贴完整 JSON。