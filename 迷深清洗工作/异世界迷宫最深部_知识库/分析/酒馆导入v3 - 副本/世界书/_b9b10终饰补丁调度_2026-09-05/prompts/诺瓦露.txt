【任务对象】
你要修缮的角色专属世界书 JSON 文件绝对路径：D:\DeepSeek Harness专用！危险！！！！！\迷深清洗工作2\异世界迷宫最深部_知识库\分析\酒馆导入v3\世界书\角色专属世界书\角色专属世界书_诺瓦露.json
请先用文件读取工具读取该 JSON 全文（若首字符为 BOM U+FEFF 先去 BOM 再解析），确认顶层 name 与 entries 结构。本书文件：角色专属世界书_诺瓦露.json。

你是 SillyTavern 世界书终饰工程师（统合版：B9＋B10＋终验新增）。我提供一本角色专属世界书 JSON（顶层 name=角色专属·XX）。只执行与本书 name 匹配的 ●项，其余条目、字段、台词一律原样保留。幂等：所指原文若已不存在或已改毕，视为完成跳过，重复执行不得产生二次改动。引号「」与“”内逐字台词一律不动（唯注明"跨书统一"的叙述行除外）。输出完整 JSON（语法有效、uid 顺序不变、可直接导入）＋≤8 行摘要。

【通用自检（每本必做，结果写入摘要）】
① root 层 position=4、depth=4、prevent_recursion=true 逐条核对；extensions 内 position 非 4 者改 4；
② 全书 constant:true 条目数必须为 0；
③ disable 条目 root 与 extensions 双层在位，content 开头有 <!--【手动条目】…-->；
④ 每条 content 末行有 <!-- 触发逻辑：… -->；标题括号内条数与实际引文行数一致；
⑤ 引号外 法芙纳/玛莉亚/拉斯缇亚拉/缇亚拉/■■■■■/P06/簇N/关系映射/语境锚/（改写示例）应为 0 命中。

【补丁清单】（主代理已按本书 name 匹配筛选；清单未列出的一律原样保留）
【B10·批次二终饰】
●诺瓦露：uid 5 第 4 条「〔第十章 L12323〕要道谢等之后再来！！我说过会辅助你和涡波战斗吧！！更何况，这种好机会！！我可不会蠢到让它平白溜走！！」整行移入 uid 3（终局锚点·禁用条目）末尾；uid 5 标题「（4条）」→「（3条）」（终局战台词与①③口径统一，归禁用态承载）。
【输出】完整 JSON ＋ ≤8 行摘要：各●项执行结果（已完成/本次执行/不适用）、keys 变更、最终条目数。
【交付】用文件读取工具读取该书 JSON 全文（若首字符为 BOM 先去 BOM），修缮后用文件写工具把修复后完整 JSON 覆写回原绝对路径（UTF-8 无 BOM、语法有效、uid 顺序不变、可直接导入）。回复只需 ≤8 行摘要，不要把整个 JSON 全文粘贴进回复（防截断）。
