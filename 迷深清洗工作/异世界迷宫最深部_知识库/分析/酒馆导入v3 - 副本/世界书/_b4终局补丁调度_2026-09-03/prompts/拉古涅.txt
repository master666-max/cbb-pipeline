【任务对象】
你要修缮的角色专属世界书 JSON 文件绝对路径：D:\DeepSeek Harness专用！危险！！！！！\迷深清洗工作2\异世界迷宫最深部_知识库\分析\酒馆导入v3\世界书\角色专属世界书\角色专属世界书_拉古涅.json
请先用文件读取工具读取该 JSON 全文（若首字符为 BOM U+FEFF 先去 BOM 再解析），确认顶层 name 与 entries 结构。本书文件：角色专属世界书_拉古涅.json。

你是 SillyTavern 世界书终局补丁工程师（B4 收官版）。我提供一本《异世界迷宫最深部为目标》角色专属世界书 JSON（顶层 name=角色专属·XX，可能为 v3 修复版或已打补丁版）。只执行【补丁清单】中与本书 name 匹配的 ●项；清单未列出的条目、字段、台词一律原样保留：不重写、不润色、不删台词、不改 keys（除非清单指明）。幂等原则：所指原文若已不存在或已改毕，视为已完成、跳过不算错误；同一修改重复执行不得产生二次改动。引号「」与“”内逐字台词一律原样（含引号内异写如『赛鲁多拉』）；keys 中并列异写一律保留。输出完整 JSON（语法有效、uid 顺序不变、可直接导入）＋摘要与自检；截断则续写「从 uid X 继续」。

【通用自检（每本必做，结果写入摘要）】
① root 层 position=4、depth=4、prevent_recursion=true 逐条核对；extensions 内 position 非 4 者改 4；
② 全书 constant:true 条目数必须为 0；
③ disable 条目 root 与 extensions 双层在位，content 开头有 <!--【手动条目】…-->；
④ 每条 content 末行有 <!-- 触发逻辑：… -->；标题括号内条数与实际引文行数一致（不一致以实际为准改标题并报告）；
⑤ 引号外搜索 法芙纳/玛莉亚/拉斯缇亚拉/缇亚拉/■■■■■/P06/簇N/关系映射/语境锚/（改写示例）应为 0 命中，有则按 G3/G4 惯例修正并报告。

【补丁清单】（主代理已按本书 name 匹配筛选，仅列出与本书相关的 ●项；清单未列出的一律原样保留）
●P3 拉古涅：uid 1 正文「芙兰琉莱・ヘルビルシャイン」→「芙兰琉莱・赫勒比勒夏因」

【输出】完整 JSON ＋ 摘要：①各●项执行结果（已完成/本次执行/不适用）；②自检五项结果；③disable 条目数与新建条目 uid（如有）；④最终条目总数。

【交付】用文件读取工具读取该书 JSON 全文（若首字符为 BOM 先去 BOM），修缮后用文件写工具把修复后完整 JSON 覆写回原绝对路径（UTF-8 无 BOM、语法有效、uid 顺序不变、可直接导入）。回复只需 ≤12 行摘要，不要把整个 JSON 全文粘贴进回复（防截断）；若输出截断续写「从 uid X 继续」。
