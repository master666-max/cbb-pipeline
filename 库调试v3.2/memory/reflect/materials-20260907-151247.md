# 反思素材包（engine reflect 渲染，LLM 合成由 harness 执行）

- 触发原因：importance_accum=228 ≥ 阈值 50
- 近期写入：35 条（entry=PT-002 → longterm, entry=PT-003 → longterm, entry=PT-004 → intermediate, entry=PT-005 → intermediate, entry=PT-007 → intermediate, entry=R-001 → intermediate, entry=R-002 → intermediate, entry=R-003 → intermediate, entry=R-004 → intermediate, entry=R-005 → intermediate）
- 高重要度条目 TOP6（合成时优先取材，无据不写——铁律 2）：

## SOUL-SOUL（importance=9）
agent 人格：原位保留于工作区根 soul/SOUL.md（v3 库无灵魂层目录，M3 裁决=原位保留+指针）。加载惯例：会话开始先读 soul/ 两文件再进 v3 检索。

## SOUL-USER（importance=9）
用户画像 miza：原位保留于工作区根 soul/USER.md（v3 库无灵魂层目录，M3 裁决=原位保留+指针）。加载惯例：会话开始先读 soul/ 两文件再进 v3 检索。

## PT-008（importance=8）
PT-008 架构根治模式：把文书审查的机制层处方升格为架构公理（单一事实源/文档即产物/自注册测试），让病灶失去存在前提而非逐条拦截——四轮审查 60+ 发现经 v3 单文件统合吸收关闭。

## D-001（importance=7）
根工作区归档采用「引用不搬移」（manifest 方式）：mepub、语料分析、comfyui-docx-scratch 三个历史项目本体留在原位，archive/2026/09/ 下各放一个 README 记录其位置、复现方式与经验回链。

## D-002（importance=7）
实验性双库工作区原样保留为体系参考，根双库为唯一活跃体系：不在实验区上继续记账，也不把它合并进根双库；根目录 AGENTS.md 指向根双库，实验区仅作体系设计参照与 PT-002/PT-004 的另一验证实例。

## D-003（importance=7）
常驻上下文预算制（Anthropic context engineering 对齐）：会话常驻层（SOUL.md + USER.md + _index.md）合计硬预算 ≤150 行；条目正文、skills、archive 一律 just-in-time 按需加载，不进常驻层。检索靠 `tools/search_knowledge.txt` 而非整库阅读。

---
合成产出要求：跨任务高层规律 2~3 条（非复述单条经验），每条附依据条目号；
产出走 engine append（id=R-xxx，importance>=7）；完成后手工将 state.json 的 importance_accum 置 0。