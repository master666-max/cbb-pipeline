# library-bootstrap · 超绝统合版 v2.1（三处偏差修正 + 统合修订 26 工单落地）

> 2026-09-06 发布。**本版为 L0-L9 十档全量**。W-016 用户裁决（2026-09-06）：L9 本质是实验档位，保留在统合版内，以「验收未运行」状态标注披露；A1/A2/A3 预注册协议已入档，验收数据收集列 v2.2。
> v2.1 依据《统合修订工单 2026-09-06》完成 P0 全部 16 条 + P1 全部 10 条，P2（W-027~038）列 v2.2。

## v2.1 关键变更
- **SHA-256 换血**（W-010）：manifest 与哈希链 SHA-1→SHA-256 截 16 位；旧库迁移脚本 migrate_sha256.txt 随包分发（幂等，旧 manifest 存 .sha1.bak）；T11 在新算法链复测。
- **威胁模型与外部锚定**（W-011）：哈希链能力边界三档成文；anchor_head.txt 链头外锚（T31）。
- **铁律 6 打包硬排除**（W-013）：soul/、state/、USER.md、env/ 禁入分发包；pack_blacklist_check.txt 发布前必跑（本包已跑：0 命中）。
- **情绪系统规格化**（W-017~019）：Δ 六类事件固定映射表（脚本化，同序列回放逐位一致 T34）；λ=0.7 双速率修正（半衰期约 2 次 wrap-up，快衰减信号）；arousal 四象限仲裁表（复杂度中介，T35）。
- **GWT 打分公式**（W-020）：score = 0.6×词面 + 0.3×图距离 + 0.1×情绪调制，权重单点定义待校准。
- **检索评测闭环**（W-022）：golden query 33 条 + eval_retrieval；基线 recall@5=0.455 / MRR=0.432（2026-09-06）；参数变更劣化 >5% 不得合入。
- **schema v3.1**（W-014）：条目七字段（新增 schema_version=3，迁移幂等）。
- **lint 扩展**（W-023）：L-int-02~06 五项（引用闭包/编号存活/预算合计/打包黑名单/PENDING 计数）。
- **锦标赛修正**（W-025）：direct/spread 归因 + 类型豁免（灾备/pitfall/密钥纪律不参与末位淘汰）。
- **L4 预算执法闭合**（W-012）：常驻三件合计 ≤150 行进 lint（T32）。
- 文档自洽批（W-001~009、015）：断言矩阵重排、交叉引用闭合（PT-006 检查 0 失效）、附录 A 扩至 28 项口径统一、宪章六条、信源口径表、38→58 断言对账、编号治理规则、模块计数、小样本修辞标注。

## 测试
py -X utf8 run_tests.txt → 58 PASS / 0 FAIL（T1-T45，含 v2.1 新增 20 断言）

## L9 状态标注（实验档位）
L9 八件套机制与测试完整（T13-T17、T34-T38 共 20 断言全过），**但 A1/A2/A3 消融/干预/谄媚验收未运行**——按用户裁决，L9 作为实验档位保留在统合版内，以本标注披露验收状态；预注册协议已入档（archive/2026/09/l9-acceptance-protocol/），验收运行后按结果处置（通过即转正，判死刑即按 11.6 下线情绪模块）。
