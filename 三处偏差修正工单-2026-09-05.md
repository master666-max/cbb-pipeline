# 修正工单 · 建库三处偏差（给接手 AI 的自包含提示词）

> 用法：把本文件全文发给接手 AI（建议在同一工作区 `D:\实验性记忆库工作区！！危险！！` 的新会话中打开本文件执行）。本工单自包含，不需要原始会话上下文。
> 日期：2026-09-05 ｜ 签发人：建库 agent ｜ 性质：偏差修正 + 补装，改动范围已标出，须遵守文末铁律。

---

## 一、建库过程具体情况（背景，必读）

2026-09-05 在 `D:\实验性记忆库工作区！！危险！！`（下文称「工作区根」）用安装包 `library-bootstrap-超绝统合版/`（v2.0 统合版，留在工作区根，**未登记进 manifest**）按 library-bootstrap 主流程建成 **L9 全档库**。用户（本库真实用户，称呼暂未提供，**不是**源工作区用户 "miza"）经三级确认闸门批准：L7 三实验全上 + L8 七件套（先展示宪章六条）+ L9 BCA 八件套（先展示四公理与验收三件）。

- 环境探测结果（写于 `env/ENVIRONMENT.md`）：E-full（`.py` 可执行）；runner = `py -X utf8`（Windows py launcher，Python 3.14.7，`python3` 命令不可用）；harness = ZCode；入口 = `AGENTS.md`；cron = auto（已建每月 1 号 9 点定时体检，automation id `automation-e15ebb58-bda4-4a60-8484-ee16a8e2b71f`）。
- 槽位值：WORKSPACE_NAME=实验性记忆库工作区；DATE=2026-09-05；HARNESS_NAME=ZCode；ENTRY_FILE=AGENTS.md；PY_RUNNER=`py -X utf8`；CRON_MODE=auto；OWNER_NAME=（待填充：磨合后回写）。
- 已装模块：m-core / m-index / m-loop / m-tools / m-schema3 / m-evolve / m-soul / m-reflect / m-cron / m-radar（10 个）+ 实验：vector-search / auto-critic / dream-fusion（L7）；constitution / lifelog / mutation-engine / tournament / council / twin / worldsim（L8）；constitution-bca / instrumentation / emotion / goalstack / dual-memory / gwt / sleep-pipeline / dmn（L9，共 18 项）。
- manifest（`.library-manifest.json`）用 `--init preset=l9` 生成，登记 **43 个库文件**（生成时把安装包目录临时移出工作区，避免安装包自身 ~110 个模板混入登记——重跑 `--init` 必须沿用此法，见第六节）。
- 收尾验收（签发时全绿）：`bootstrap_verify` 43 一致 / 0 漂移 / 0 缺失；`lint_library.txt` 24 文件 0 问题；`l8_guard.txt` 0 问题；lifelog 哈希链 3 行连续：创世 `8d540c01` → L8 装毕 `60c52ce0` → L9 装毕 `d3c4ef68`。
- 附加环境事实：已 `pip install scikit-learn`（供 vector-search）；根目录有 `.gitignore`（仅含 `tools/.vec_index.pkl`）；`attic/wrap-up-v2-2026-09-05.md` 为 wrap-up 旧版备份。

## 二、三处偏差与修改建议

### 偏差 1：soul 层画像为【待填充】骨架（有意为之，需转成显式待办 + 守纪律）

- **现状**：`soul/SOUL.md` 的「相处模式/做事风格」、`soul/USER.md` 的「基本画像(称呼)/固定偏好/项目全景/环境雷区」、`soul/TWIN.md` 的「偏好内核五问」、`state/GOALS.md` 的「当前主目标」，全部是 `【待填充：磨合后回写】` 或空值，没有填入任何真实用户信息。
- **为什么**：安装包里 `USER.md`/`TWIN.md` 模板夹带**源工作区用户（miza）的画像数据**——TWIN 五问自带答案（「L7 必做还要更激进」「额度不心疼，火力全开」…）、TWIN 预演记录表自带 2026-09-04 的 L8 计划书样例行。直接照抄 = 给本库装一个**假的用户模型**，违反铁律 2「有据可写」。故建库时清成骨架，OWNER 槽填「（待填充：磨合后回写）」。
- **建议动作（接手 AI）**：
  1. 在 `env/ENVIRONMENT.md` 末尾新增「## 待回填清单（2026-09-05）」小节，逐条列出：①USER 称呼 ②USER 固定偏好/项目全景/环境雷区 ③SOUL 相处模式/做事风格 ④TWIN 偏好内核五问答案 ⑤GOALS 当前主目标。每条注明「等待真实互动后回写，回写时在对应文件的更新史/相处记录注明日期」。
  2. **不要**用 miza 或任何假设值回填；回填只能依据与真实用户的对话与项目产出（铁律 2）。
  3. 顺带自查：全库 grep 不得出现 miza、火力全开、轻松层开吗 等旧库字样（本就不该有；确认无残留）。
- **验收**：ENVIRONMENT.md 有待回填清单；USER/SOUL/TWIN 仍保持骨架且无编造值。

### 偏差 2：PENDING.md / ABLATIONS.md 为手工补的最小文件（包内模板缺失），且 PENDING 说明与实际脚本行为不符

- **现状**：
  - `state/PENDING.md`（建库时手工创建）内容为：
    ```
    # PENDING · GWT 落选队列

    > 广播仲裁落选的候选降级至此（不丢弃）；高分者下次广播重新纳入候选。
    ```
  - `state/ABLATIONS.md`（建库时手工创建）内容为：标题「消融/干预实验留档（A1/A2/A3 验收）」+ 说明「每次消融/干预实验在此记录（操作走 tools/state_intervene.txt）。验收：A1 消融可测差异 / A2 干预方向可预测 / A3 谄媚监控。」+ 空表头 `| 日期 | 实验 | 操作 | 结果 | 结论 |`。
- **为什么**：安装包 `experimental-l9/gwt/install.md` 写了「复制 template/PENDING.md → state/」，但包里**没有** `experimental-l9/gwt/template/PENDING.md`；`experimental-l9/constitution-bca/install.md` 要求建 `state/ABLATIONS.md`，包里也没有模板。两文件只能按 install.md 描述手工补最小版。
- **附带发现（需一并处理）**：PENDING.md 说明中「高分者下次广播重新纳入候选」**与代码不符**——`tools/broadcast.txt` 的 docstring 把 PENDING 列为候选源，但代码实际**不回读** PENDING，只会向它追加落选行。
- **建议动作（接手 AI，二选一，推荐 a）**：
  a. **文档与现实对齐（推荐）**：把 `state/PENDING.md` 顶部说明改为如实描述：「GWT 广播仲裁的落选候选在此排队留档（不丢弃），供人工检视与后续机制取用；当前 tools/broadcast.txt 只写不读本队列，回读功能属机器变异候选」。同时保留 ABLATIONS.md 原样。
  b. **功能补齐**：若要让 broadcast 真回读 PENDING，属**改 live 工具**——按 L8 宪章第一条/第三条**必须走变异流程**（`mutations/candidates/MUT-{NNN}/` 三件套 + 人类令牌），本工单内**不得直接改** `tools/broadcast.txt`。可另开 MUTATION-SOP 提案。
- **验收**：PENDING.md 说明与 `tools/broadcast.txt` 实际行为一致；`py -X utf8 tools/broadcast.txt --task "测试"` 空库输出「无候选」不报错；ABLATIONS.md 表头存在。

### 偏差 3：vector-search 脚本加过空库守卫（改动偏离包模板），且 wrap-up 漏了索引重建步骤（补装缺项）

- **现状**：`tools/exp_vector_search.txt`（自 `experimental/vector-search/exp_vector_search.txt` 复制）在第 27-34 行 `--build` 分支里，比包模板**多出 2 行守卫**：
  ```python
  if not docs:
      print('空库：暂无条目，向量索引留待首条知识写入后再构建'); sys.exit(0)
  ```
  （插入位置：`es = entries()`、`docs = [b for _, b in es]` 之后、`TfidfVectorizer(...).fit(docs)` 之前；文件其余部分与包模板逐字一致。）
- **为什么**：空库（0 条目）时 `TfidfVectorizer.fit(docs)` 抛 `ValueError: empty vocabulary`，`--build` 直接崩。建库期实测复现后加了守卫；该模块自身触发条件本就是「条目 >100 才值得」，守卫符合其语义。
- **附带缺项（补装）**：`experimental/vector-search/install.md` 已知风险里写明「索引需在每次条目变更后重建（wrap-up 追加重建步骤）」——**建库时漏加了这一条**。`knowledge/skills/wrap-up/SKILL.md` 现第 5 步「索引同步」有 bullets：更新 _index/_INDEX → 验证≥3 次 pattern → 更新 trajectory（只追加）→ L9 跑 state_snapshot。**缺 vector-search 重建行**。
- **建议动作（接手 AI）**：
  1. 建议**保留** `tools/exp_vector_search.txt` 的 2 行守卫（修了真实空库 bug）；并把这 2 行同样回填到安装包副本 `library-bootstrap-超绝统合版/experimental/vector-search/exp_vector_search.txt`（安装器维护，非 live 库文件，可直接改），使以后重装/加装他库也带上修复。
  2. **补装 wrap-up 缺项**：在 `knowledge/skills/wrap-up/SKILL.md` 第 5 步「更新 trajectory 轨迹文件（只追加）。」bullet 之后，插入一行新 bullet：「- 若启用 vector-search（L7）且本期有条目新增/修订：跑 `py -X utf8 tools/exp_vector_search.txt --build` 重建向量索引。」此改动是完成 install.md 规定的补装（非机器变异），照 auto-critic/state_snapshot 等既有先例追加即可；改完 `py -X utf8 tools/lifelog_append.txt "补装 wrap-up 向量索引重建步骤"` 入链。
  3. `.vec_index.pkl` 目前不存在属正常（0 条目不建）；待首条知识写入后再 `--build`。
- **验收**：空库跑 `py -X utf8 tools/exp_vector_search.txt --build` 输出「空库：暂无条目…」exit 0；wrap-up 第 5 步含 vector-search 重建行；安装包副本与 tools/ 副本守卫一致。

## 三、铁律与流程约束（接手 AI 必守）

1. **永不删除、永不覆盖已有文件**；只新建、只移动；移动/修改前先输出清单（铁律 4）。涉及用户数据（条目正文/归档/soul 内容）一律不改。
2. **有据可写**：任何回填内容必须有工作区内真实依据；拿不准标【待确认】。
3. **宪章至上**：本库已装 L8/L9（根目录 `L8-CONSTITUTION.md`，已并入 BCA 第七~十条）。**改 live 模板/工具/技能 = 机器变异，必须走 `mutations/candidates/` + 人类令牌，禁止直接改**。本工单中仅「偏差 2-a 改 PENDING.md 说明」属文档文件修正、「偏差 3 的 wrap-up 补装 + 安装包副本回填」属补装/安装器维护，可依上节指示直接执行；其余一律先出提案清单等用户裁决。
4. 密钥不入库；对外措辞用「情绪式调制器」不用「情绪」。

## 四、改完之后的验证清单

按顺序执行并如实汇报：
1. `cd "D:\实验性记忆库工作区！！危险！！" && py -X utf8 tools/lint_library.txt` → 期望 0 问题。
2. `py -X utf8 tools/l8_guard.txt` → 期望 0 问题。
3. 若改动过任何已登记文件（wrap-up、PENDING 等都在 manifest 里），**必须重登记 manifest**：先把 `library-bootstrap-超绝统合版/` 临时 `mv` 到工作区外 → `py -X utf8 <临时位置>/scripts/bootstrap_verify.txt --init l9 m-core,m-index,m-loop,m-tools,m-schema3,m-evolve,m-soul,m-reflect,m-cron,m-radar --exp vector-search,auto-critic,dream-fusion,constitution,lifelog,mutation-engine,tournament,council,twin,worldsim,constitution-bca,instrumentation,emotion,goalstack,dual-memory,gwt,sleep-pipeline,dmn` → 再 `mv` 回原位。最后跑校验模式确认「全一致」。
4. `py -X utf8 tools/lifelog_append.txt "偏差修正工单执行完成"` 入链，再跑一次 `l8_guard`。

## 五、体系级观察（仅记录，本工单不需处理）

manifest 会把 `state/DASHBOARD.md`、`state/PENDING.md`、`state/GOALS.md` 等**运行时会合法写入**的 .md 一并登记；后续工具写入后 `bootstrap_verify`/`l8_guard --check-templates` 会报漂移——属预期（工具合法写入，非未授权篡改）。如需根治（把 state/ 移出登记或改登记策略）是体系级决策，另开议题，不在本工单。
