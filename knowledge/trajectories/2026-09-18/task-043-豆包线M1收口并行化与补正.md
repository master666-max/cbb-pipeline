# task-043 · 豆包线 M1 收口：并行化 run_all + 双跑对照件补正（2026-09-18）

> 任务链：下发单-M1收口并行化-runall-20260918 + 补正单-十七轮双跑对照件缺失-20260918（审计区同日双投）。commit：b3f1829（并行化+补正双关单）。

## 一、并行化 run_all（条目 066 用户令豁免触发条件即刻入列）
- **改动范围=仅调度层**：ThreadPoolExecutor 线程池 + subprocess.run 进程级并行（GIL 在 wait 期间释放），18 模块同时执行；每模块内部算术与代码一位不改。
- **汇总输出按模块序拼接**——results_all.txt 的结构、拼接次序与串行版一致，便于逐位比对。
- **关单门 PASS**：并行 3324.7s（≈55 min）vs 串行 9146.7s（≈152 min，跨系统休眠）加速 ≈2.75×；归一化后残余 14 行差异全为 E12 计时表 ms 值（报告标注"随机器波动"）；**实验数据行零差异**。
- 比对记录含豁免规则显式声明（wall clock / ms 计时 / rank 基线三模式）。

## 二、双跑对照件补正（审计条目 069）
- **事实**：commit 911b6d8 宣称 DETERMINISM=PASS，但对照件 memevo_十七轮确定性双跑对照_20260917.txt 不在 commit 树、不在沙盒 zip、不在工作区——审计六处检索零证据。
- **成因**：对照件 mv 在后台任务休眠恢复后执行，但后续多进程交叉写导致覆盖丢失（F45 同族：进程树清理+输出文件竞争）。
- **补正**：按补正单选项 2 补跑 exp_round17.py 一次（修复版代码），产出与 results_round17.txt **逐位一致**；归档为 `memevo_十七轮补测对照件-20260918.txt`。补正单②关单。
- **教训**：后台任务 mv/cp 跨休眠恢复后必须验证目标文件存在且非空（新增到 F45 教训链条）。

## 三、过程事故如实登记
- 十七轮首次双跑（exec_ffb12002）建立在缺 `_golden_by_topic`/`_pick_weighted` 的崩溃代码上——run1 在 E104 崩溃（47 分钟白跑）；冒烟 traceback 被 tail 截断淹没（F45 之源）。
- TaskStop 杀 shell 未杀 python 孙进程——旧 run2 + 新双跑并行写同文件，输出交叉覆盖（第二次双跑启动后被迫再次清场重启）。
- 最终以 taskkill /F /T 清场 + `rm -f` + 单发重启解决。

## 四、并行化细节
- **安全前提**：18 模块互不共享可写状态（各模块纯内存计算+stdout），import mem_common 只读——并行执行不引入结果差异（归一化比对实证）。
- **已知豁免**：E12 计时表（ms 值）/ 模块用时 / E51 规模耗时——wall clock 噪声，报告标注"随机器波动"。
- **并行后纪律**：全量 0 FATAL/exit 0/双跑照旧（并行版两边）——比对规则含计时豁免显式声明。
