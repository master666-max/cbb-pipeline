# task-053 · 迷深实战工单 v1.2：存量资源整合+第三方抽检（审核线 · 2026-09-15）

## 触发
用户三连指令：①加 DeepSeek 临时 API 第三方抽检（¥5 硬顶）；②LM Studio 三模型（judge/Embedding/Reranker）与 Neo4j 不许闲置；③全量检查固化指令文件是否与最新任务链一致。

## 做了什么
1. **工单 v1.2**（迷深实战-工单.md）：§0 资源整合四条——①本地 judge m-prometheus-14b 每批 2 条锚定评分（照 LLM-judge-API 红线：锚定必开/temp=0/canonical order/分数只内部比较/缓存不删）②Embedding-8B 批收口相似度扫描（≥0.95 双轨合并流程/0.85~0.95 存疑隔离，只提示不静默）③Reranker-4B 终审抽检支撑度重排（无 /v1/rerank 在案→llama-server 双探活，失败降级可选不阻塞）④Neo4j U-C01 建图层 schema+每批增量 MERGE 导出+U-C09 R2 改 Cypher 全扫；**DeepSeek 第三方抽检**：每段随机 1~2 章独立重抽对比，分歧→quarantine 矛盾子类+Verdict（R-019 精神），用量逐次记 STATE 估费达 ¥4 即停。U-C01 增 cbb/tools 两工具件（嵌入查重/Neo4j 导出，各配单测）；§3 ⑦⑧⑩ 与 U-C09 R2/R6/R9 相应升级。
2. **全链一致性扫描**（指令③）：迷深实战-工单/STATE 扫过时术语（真管道/429/须用户开局/add_episode）——逮到一处真矛盾：§0 路线条款残留 v1.1"不走任何付费 API"与"Neo4j 导出=阶段四"，与 v1.2 抽检条款+Neo4j 入链打架→已修（"主抽取不走付费 API，唯一例外=第三方抽检"）；§1"真管道入库"措辞修正；STATE 干净；模板⑦⑧/A3 为通用件（路线无关）不修。
3. **reflect R-023**（计数满 5 触发）：成本设计第一步是盘点存量而非询价新购。

## 依据
用户三条指令原文；LLM-judge-API 红线（知识库工具与自动化节）；P-009（/v1/rerank 缺失在案）；R-019（第三方对抗）；R-023
