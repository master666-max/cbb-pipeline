# task-074 · research-hub 建线：KB主线与F3基础设施族（2026-09-22）

> 编号注记：与 2026-09-20/task-074-GitHubAccel反代复活与官方GitHub插件冒烟.md
> **跨日双占**（环境线取号在先，本文件按先例不改号、文件名+目录区分、索引已登记）。
>
> 触发：用户令「在 https://github.com/master666-max/research-hub 建好自己的目录，
> 迁入目前的研究内容，达到另一个完全隔离记忆的 agent 读到即可开工的详细度」。
> 平行现场：衔尾蛇线（task-073/e4cde06）与 F2 带表族（7343de0）同夜建线。

## 交付

- 新线 `KB主线-自演化知识库与F3基础设施族/`（26 件，298K，远端 f87d64b 已在位）：
  - **START-HERE.md**（隔离接手入口）：三十秒现场/世界地图（绝对路径表+社会关系）/
    三条路线（A 接单执行按 PT-018 三步、B KB 例行、C 值守）/硬边界 8 条/开工五步
    命令行/环境雷区/快照纪律/未闭合清单/术语表/9 条 commit 编年。
  - **INDEX.md**：件级一句话索引 + 6 条已知缺陷/差额。
  - **哈希表.md**：23 件迁移快照 sha256[:16]，**基准态=blob 态（LF）** 显式声明
    （6 件因 .gitattributes eol=lf 归一与工作树态不同）+ 复算命令 + 两件「有意不复制」
    （SOP v1.2 canonical 74f251ca / 审计账本 alias_map:59-62）。
  - `工件/`（本线交付+收文 10 件）、`KB核心快照/`（四账+灵魂+入口 8 件）、`仪器/`（工具+技能 5 件）。
- 根表登记：README 四列表 + AGENTS 三列表各加本线行（只加本行）。

## 过程要点

1. **侦察先行**：克隆侦察（README/AGENTS/两条标杆 START-HERE/接手契约/坑清单）→
   线名与深度对齐既有惯例（`<主题>-<方向>`；START-HERE 标准=「读完即开工，不问任何人」）。
2. **有意不复制两件**：SOP v1.2（15 副本跨线资产，canonical blob 制）与审计账本
   （append-only 真源）——复制进镜像会制造差集静默（T-E），改锚哈希与行号。
3. **哈希基准态事故预防**：初版哈希按工作树态算出，发现 .gitattributes `eol=lf`
   会归一 6 件 → 按 **blob 态**重算并显式声明（审计区条目 104 刚因「宣称未标
   哈希基准态」记瑕疵，同款坑前置规避；复算命令=git cat-file blob | sha256sum）。
4. **并行建线冲突**：同夜三线建线（衔尾蛇/F2/本线），根表同区追加三次撞行——
   处置=**两行并留**（先到在前），rebase 两轮后推送成功；符合仓纪「只加本行、不静默删」。
5. **push 踩坑**：全局 git config 把 HTTPS 改写成 SSH（22 端口拒绝）——用
   `GIT_CONFIG_GLOBAL=<空文件>` 前缀绕过（审计线坑清单 #18 同款）。
6. 远端前进两次（e4cde06→7343de0），均以 fetch→rebase→（撞行则并留）→push 消化。

## 沉淀

- PT-019「研究仓隔离接手镜像建线」（五件套 + 三并留 + 两态哈希 + push 绕过）。
- 本线主区侧工作（F3/KB）状态不变：无在途工单，10-01 体检与第七圈等令。
