# task-074 · F2 线迁入线上研究仓 research-hub（2026-09-19）

> 用户令：「在这个仓内建好自己的目录，迁入目前的研究内容，详细到另一个完全隔离记忆的 agent 读到后能立马开始工作」。

## 执行纪要

1. **探仓**：`github.com/master666-max/research-hub`（私有，多研究线共仓）。仓规读取（根 README+AGENTS.md）：每线一顶层目录线内自足 / 必配 START-HERE+INDEX / **被测件不复制只锚 commit+哈希**（T-E 定理）/ 证据三纪律（绑哈希/正对照/账只追加不 amend）/ 不替别人写线。
2. **建线** `验证方法学-F2带表族/`（40 件）：带表脚本 6（v1→v5.1 全链，各版自带自测门可独立复算）/报告通报 10/校正与检查单/SOP v1.2/数据快照 9（有意第二家，哈希表登记）/下发链 10（工作循环协议样例）。
3. **骨架三件**：START-HERE.md（隔离接手即开工：一分钟版/三层架构/状态快照/数据字典/十纪律/七步滚动配方/复算指南/待办与风险）；INDEX.md（件级索引+已知缺陷 5 条）；哈希登记表.md（41 件 sha256[:16]）。
4. **仓内冒烟**：`py -X utf8 pb3_band_v5.txt` 全绿（selftest+bindcheck+负测试）；零改动运行法=数据 csv 复制到仓根（实测）。
5. **推送**：首推被拒——远端已被并行线推进（e4cde06 衔尾蛇新线）；rebase 后二推再拒，根因=**全局 `url.git@github.com:.pushInsteadOf https://github.com/` 把 push 改写为 SSH，而本网络 22 端口被拒**；本仓 `remote set-url --push` 覆盖为 HTTPS 后成功（7343de0）。

## 给隔离 agent 的可开工性自检（按用户标准）

零记忆读者需要的信息全部在 START-HERE：①这是什么线（§1）；②世界在哪（§2 三层架构）；③现在什么状态（§3 快照：v5.1 全值/SOP v1.2/072①/在途触发器）；④概念与数据字典（§4 亚族/三元组/抽样规则/值谱系）；⑤硬纪律十条（§5）；⑥被测件锚（§6）；⑦怎么干活（§7 七步配方）；⑧怎么复算（§8 实测路径）；⑨待办与风险（§9）；⑩文件地图与同步协议（§10/§11）。可开工性=脚本自足+数据自足+判定先例在案+引用规范（路径@哈希）。

## 教训候选

- 本机 GitHub push 通道：SSH 22 被拒 + 全局 pushInsteadOf 改写 → 新仓首次 push 前先 `git remote -v` 查 push URL，或直接 `git config remote.origin.pushurl https://...`；
- 多线共仓写入=并发场景（R-015 家族）：先 fetch/rebase 再推，冲突按"各行只由其发出者书写"原则解。
