# task-045 · 豆包线无损提速三期（run_all_v2 分片调度）

- 日期：2026-09-19（会话续接 M2 收尾后）
- 触发：用户指认 8945HX=16 全大核（16C/32T）+「未来回归越来越大，必须想尽一切
  办法无损提速，搜索最新论文与社区经验」；此前 v1 模块级并行仅 ~6.5 有效核。

## 做了什么

1. **研究**：CPython 3.14 JIT（PYTHON_JIT=1，典型 2-9%）、GC 调优（freeze/
   disable，批处理可 ~20%）、PyPy 3.11（社区 4-10×）、核心驻留/功耗墙
   （平衡计划是并行塌缩常见根因）。来源清单在研究简报 §七。
2. **机制**（调度层，实验模块零改动）：`run_all_v2.py` + `shard_worker.py`——
   83 个粗粒度 eXXX() 全局并行；LEAF_MAP 叶子网格（参数键=sha1(repr(args))，
   查找包装，键未命中→原函数现算，**映射错只降速不出错**）；stitch 重放
   main()（round6 无 main() → AST 摘录 __main__ 块）；单队列排序（非网格粗活
   →叶子→带网格粗活）；PYTHON_JIT=1 + gc.freeze/disable + HIGH_PRIORITY。
3. **实测**：round18 端到端 400.5s（~10×）；全量 v2.0 2620.5s（2.03×）；
   **v2.1 终版 1289.0s（4.13×）**，exit 0，与 M2 权威件逐位一致（豁免计时行
   1343 行零差异）。
4. **车道探测**：PyPy 3.11.15 逐位一致×3（r2/r4/r15）但 round15 仅 1.57×
   （725s vs 1135s）——指针追踪型负载非其甜区，不换正典；小任务 24 并发吞吐
   14.2× 仍在爬 → 重回归 ~6.5 有效核上限=大工作集内存/功耗墙，非调度。

## 教训（P 候选）

- **P-022a 子进程换行语义**：v2 按字节捕获 stdout 时 \r 残留会造成假性 DIFF；
  必须与 v1 text=True 通用换行管线对齐（归一 CRLF→LF 再 print）。
- **P-022b 守卫判定用结构化 AST**：ast.unparse 字符串比对会因引号风格失配
  （'__main__' vs "__main__"）。
- **P-022c 社区加速数字不可平移**：PyPy 对 dict/str 指针追踪负载实测 1.57×，
  远低于社区 4-10×；一切以本负载实测+逐位门为准。
- **P-022d 模块级并行墙钟=最长模块**：任务粒度必须小于物理核数；粗 expr 内部
  大网格（e102 2072s）需叶子级细分。

## 边界与后续

- 墙钟下限 ≈ e65（760s，叶子首参=库对象，机制边界）+ Phase B round8 内联
  （214s）≈ ~1000s；再压需专用网格或 stitch 侧 leafwrap。
- 用户侧杠杆：电源计划切高性能（本机=平衡，核心驻留+全核降频）。
- 文档：豆包线-无损提速研究-20260919.md（简报）+ memevo/比对记录-v2提速-20260919.md
  + 交接手册 §14。
