# task-072 · 衔尾蛇线 WP-C2：m-cron 定时批量同步启用（含触发注入事故复盘）

- 日期：2026-09-21
- 线：大审查/衔尾蛇（ZCode 责任田）
- 工单：下发单-WP-C2-mcron定时批量同步启用-20260921（WP-C 拆分单 C-2 归线）
- 回执 commit：大审查仓 0e38c46；根仓 9f869bd1

## 做了什么

1. **m-cron 启用**：SLEEP.md 落 knowledge/（auto 档两槽填+每日窗口改写+纪律句保留，模板原件不动）；one-shot 首触发实测件配置并被**调度器真实触发**（00:13:35，runCount=1，非手动）；诊断产出 `knowledge/SLEEP-诊断-20260921.md`。
2. **诊断发现**：①doctor state 根不一致（W-2b 同步未刷根+新文件+序列追加叠加，open 待裁决）；②**发布区 bootstrap_v3.9.py 被 WP-C C-1 实施会话手改**（structured.md 白名单行+CRLF，40f88584≠正典 8de8fb1f，absorb-md 路径专属检索零涉）——P-020 第三例，不覆盖待协调；③其余全绿（lint 0 问题/modules 10/10）。
3. **每日档受阻如实登记**：本会话建第二定时件被会话归属限制拒绝——诊断 prompt 备于预注册，需新会话创建或转 manual 档（环境结论非失败）。

## 事故复盘：CronCreate "子代理 prompt 侵入主会话"

用户质问成立。根因：**CronCreate 的运行模型=本会话续跑（re-invoke THIS session），不是子代理**——我把 one-shot 实测件当成了带外子代理通道，触发时 prompt 直接打进主会话，用户看到的就是这条"入侵"。三条整改：①本会话不再建定时件（会话归属限制+主会话洁净双理由）；②后续定时类需求一律走用户新会话或 manual 档，prompt 文本随预注册交付；③教训入档——**调度器触发与子代理分派是两种通道，选型前先确认交付面**（本次交付面=主会话可见，恰是用户不要的）。

## 经验

- 副本税防线（pre-commit）+会话归属限制两道钩子都在实战中拦截了我——防线比自觉可靠。
- 发布区文件是公共战场：谁都能改、git 会转行尾——锚点宣称必须每次现算 sha，不能引用上次记录。

## 关联

- 大审查/衔尾蛇/reports/WP-C2_回执-20260921.md、WP-C2_预注册-20260921.md
- [[P-020]]（第三例）、[[bootstrap-v39-release]]（正典 8de8fb1f 完好，发布区待协调）

## 补记（2026-09-21 晚）

- WP-C2 验收 **PASS**（审计条目 138，六项全过）；收执勘误：触发时刻 09-19→**09-21 00:13:35**（审计三方旁证，回执两处已改）；P-020 第三例已派混元田修复（本线不出手）。
- **每日档补建待办（需用户新开普通会话）**：CronCreate `0 9 * * *` recurring=true + 预注册 §一.3 诊断 prompt 原文照抄；建后 CronList 三要素随线下批 commit 誊录（本会话归属受限不可建）。
