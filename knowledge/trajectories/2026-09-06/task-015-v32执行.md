# 任务 015 · v3.2 执行（C-000~016，2026-09-06，只追加）

## 依据
- 工单：workspace/v32审查与工单.md；用户裁决「全部修复，按默认建议来」（D1=a 原生渲染/D2 state 不入根/D3 TTL 改名/D4 十字段/D5 顺序执行/D6 规模）。

## 执行记录（一个会话完成全部波次）
- **P0**：C-001 空 bodies 拒装（新用户不再踩空库坑）；C-002 根口径定稿——merkle_root 带 exclude={"state.json"}（根的持有者不自含），install 重排为 deliver→渲染→算根→写 state，install/wrapup 两时点口径一致（t_merkle_root_consistency 锁定）。
- **P1**：C-003 referee 回写 manifest；C-004 校验模式三态（漂移/未在册新文件/根对账，负测试构造 sneaky 文件必报）；C-005 公理 C 落地——_ledger_append 全动作（install/append/wrapup/retire/shadow/charter-confirmed）写 ledger/changes.jsonl；C-006 **guard 子命令**（BASELINE_BLACKLIST 九项，触碰 run_tests 的 diff 实测否决——X-010/T49 在 v3 的真正运行时落地）；C-007 reflect_importance_threshold=50，wrapup 越线输出反思触发；C-008 unverified_ttl_days→invalid_ttl_days（最小真相改名）。
- **P2 + C-000a**：C-010 SCHEMA_V3 八→十字段（keywords ×3 加权/links 一跳扩散，检索语义对齐 v2.x；entry_normalize 兼容旧条目回填）；C-000(D1=a) **渲染层四件**——AGENTS.md 三级入口（铁律+档位+常用命令）/skills/wrap-up.md（含 adaptation 声明）/skills/task-start.md/README.md；C-013 题录 33 条全量定稿入包（absorb-pending 退役，verify 对账面扩到 24 条 arXiv），citations.md 渲染产物；C-014 六新增包最小模板渲染；C-012 absorb 镜像语义（removed 区）；C-015 migrate/append 错误路径友好化。
- **测试 19→27 条**：新增 requires_bodies/merkle_root_consistency/reinstall_reports_unregistered/ledger_changes/guard_rejects_baseline/reflect_trigger/retrieve_keywords_boost/entrypoint_rendered 八条。
- **首跑抓 bug ×2**：①Edit 撞出 REGISTRY 多余闭合括号（SyntaxError）；②样例条目八字段被新 schema 拒绝且 SystemExit 非 Exception 令 run-tests 整进程退出——补十字段修复。「schema 收紧会让所有旧样例失格」的教训记录。

## 发布
- 根目录 library-bootstrap-v3.0/ 升级至 v3.2.0；锚点 sha256=c247f37b8bc8844690bf274791996cff5de575b38cfb9cbd923b07573c75a917；发布目录原地 run-tests 27/27；verify 3/21（match/mismatch/error 三态如实，窗口随机性同前）；RELEASE.md 追加。

## 遗留（v3.3 候选）
- C-009 检索评测吸收（golden 33 条→evals/ 可执行化）本轮未做（工单列 P2，属唯一未落条目——如实记录）；真实库 migrate（用户级）；guard 的锚定表对账深化（现只打印锚点提示）。
