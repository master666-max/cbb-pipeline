# 任务 017 · v3.3 执行（Y-001~010）+ 回归场实证（2026-09-06，只追加）

## 执行记录
- 测试 27→36 条全绿（新增 root_refresh/reinstall_without_data/no_false_unregistered/engine_str_path/eval_retrieval/guard_anchor/bulk_import/large_entry/ledger_consistency 九条）。
- **执行中抓虫 ×3**（全回流）：①「先账本后根」时序——v3.2 首版 Y-001 把 ledger 放在算根之后，账本行不在根内，三处分支（append/wrapup/retire）统一修正；②旧测试 t_reinstall_reports_unregistered 的 sneaky 文件放 memory/ 下，与 Y-003 新口径冲突，测试随语义迁移；③WindowsPath 无 delete 方法（unlink）。
- **回归场实证（库调试v3.2 真实迁移库）**：W2 校验模式可达（--data 寻址）✓；W4 未在册零误报 ✓；W5 根一致 ✓（wrapup 刷根后）；Y-007 批量标注实战出现；Y-006 锚定快照 5 项导出；渲染件手改检测正确报出版本演进差异（v3.2→3.3 的 AGENTS/README）。
- **v3.4 候选（执行中新发现）**：①渲染件版本升级更新通道缺失（build-docs 不刷 AGENTS/README，版本升级必报被改，本次用渲染函数重写过渡）；②宿主痕迹（.v2c/plugin_root）被未在册正确捕获——豁免策略待裁决（保留报警，不改）。
- 发布：锚点 4d2bd114fe08b2ba…，RELEASE 追加；Y-008 PT-006 void 注记已建（根库 patterns-PT006-void.md，未改原文件）。

## 待办
- 真实存量库 migrate（操作说明已交付用户，建议路径=复制制建新库+迁移器，同 库调试v3.2 流程）。
- v3.4 候选两条 + 渲染件升级通道。
