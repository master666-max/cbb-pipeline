# task-030 · F3 基础设施族接单整合记账（2026-09-13）

> 触发：用户令「你负责大审查文件夹-F3文件夹内的工作」——责任田第三次移交
> （混元→豆包→F3）。下发单四要点 + 支线整合评估附件（审计区下发副本）。

## 过程

1. **接手验证（全机械复算）**：审计区 `data/tree-nodes.csv` 按前缀重算=KB 59 /
   DEPLOY 8 / L8LAB 2 / META 4（全图 643 吻合下发口径）；`claim_ledger.csv` 按
   experiment_id 列重算=267/287/10/71（合计 635，审计区此前未给 claim 口径，
   本侧补齐）；锚点抽查 7/7 存在于主区；接口前提 R-012 在案
   （`knowledge/reflections.md:87`）。
2. **口径判定**：读审计区 INCREMENTAL-LOG 条目 010B——alias_map 前缀→家族追加
   系审计区「下圈执行」自领项；R1=主区只读、R7=append-only。故本侧不代写审计区
   文件，改交「追加稿」分流（两区互不写入，与 DISPATCH §三 同构）。
3. **交付落档**（`大审查/F3实验/`，嵌套仓 ac8b996 + 互链补注 commit）：
   - `F3家族整合记账-20260913.md`：四要点处置（①KB 骨干活页跟踪 ②DEPLOY/L8LAB
     并档 ③W-4 接口条款=落地先读 R-012 ④META 归档+5 refuted 如实披露）+
     alias_map 追加稿 4 行 + 账本滞后披露（KB-TASK 止 024，主区已至 029）。
   - `F3-KB骨干跟踪视图.md`：活页（KB 骨干现况/并档两节/META 档案/接口区/滞后区）。
4. **wrap-up**：PT-014 新条目（跨区接单整合记账流水线）；_index 补登 PT-013
   （2026-09-09 建条时漏登的索引缺口）+ PT-014；反思计数 2→3。

## 关键判定

- **归档案≠移动物理文件**：META 源文件（HANDOVER/AGENTS/闲时任务/并入说明）原位
  保留，冻结的是跟踪状态；refuted 声明随档保留不洗白（含 E80 反转候选不回写）。
- **DEPLOY 是四前缀中 claim 最重的**（287>KB 267）：部署线 lifelog/spec 是账本
  最大说写档，并档不降权，只是管理归一。

## 踩坑与经验

- claim_ledger 的前缀不在 claim_id 列而在 experiment_id 列（第 6 列）——按
  claim_id 前缀统计四前缀全为 0，差点误判「账本无此四族」。
- alias_map 末两行既有完全重复行（提取判断方差×2）：登记不代改，留账本属区处置。

## 复用

- PT-014（跨区接单整合记账流水线）——下次接 F1/F2/F4 同构工单直接按五步走。
