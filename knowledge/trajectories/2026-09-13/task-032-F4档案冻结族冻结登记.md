# task-032 · F4 档案冻结族冻结登记（2026-09-13）

> 任务链：用户指派「你负责大审查文件夹-F4文件夹内的工作」（责任田第三次移交：混元→豆包→F4 工位）→ 审计区下发单「F4 档案冻结族」接单 → 对账验证 → 冻结声明落档 → 嵌套仓 commit `2bf1b0c`。任务性质＝纯记账（无实验、无主区实验线写入）。
> 编号注：本轨迹初占 task-030，与 F3 会话撞号；改占 task-031 又与 F1 会话撞号（会话间实时争用）；终占 task-032。同日四个家族会话（F1/F2/F3/F4）并行，**F2 与 F3 双双落在 task-030**（文件名异、编号同）——跨会话轨迹编号无协调机制，P-014「先查并行产出」的又一活形态（详见 §四）。

## 一、接单与对账（PT-007 对账先行第 5 次验证）
- 下发件两份：`下发单-F4档案冻结族-20260913.md` + 附件`支线整合评估-审计区下发副本-20260913.md`。
- 副本 ↔ 审计区原件 `workspace-audit/支线整合评估-20260912.md` **diff 为空**（逐字节一致）。
- 下发单宣称数字全部对账后才落笔：九前缀节点数实测 `tree-nodes.csv`（643 节点）与评估表零差异；claim/文件数实测 `claim_ledger.csv`（6,506 行）；物理落点逐项 ls 实存。

## 二、冻结声明（交付物：`大审查/F4实验/冻结声明-F4档案冻结族-20260913.md`）
- 沿 archive-plan.md 风格新增状态 **FROZEN-READONLY**（完成使命封存，区别于 SUPERSEDED）；冻结不解引用；解除须用户令。
- 九前缀清单（含易漏点两处）：
  - **PART1** 的 3 文件锚在主区 `knowledge/trajectories/2026-09-06/task-010/011/012`（知识库轨迹层，本就只读追加）——F4 族唯一伸进主区知识库的前缀。
  - QWEN/GLM 各有账本行锚在 `衔尾蛇/reports/F10_四线三参数普查.md`——该文件属 F1 活跃区**不冻结**，冻结的只是前缀指向它的账本行语义。
- 活引用两条登记为「冻结不解引」：①DS E57 corr=1 剂量端点（`deepseek/交接包_零损失/doc/batch_r2/结果_批次R2_超详尽大报告.md:28`，账本 `625a38d9` verified）＝P-B1 第二臂依据；②bootstrap_v3 跨线同哈希 `5f8199aa`（谱系表 1-版本谱系.md:10-12）。
- 三触发器维持原状：P-A4（10-branch-proposals:22/128）、P-D4（:24/138 负提案）、spur30 归一（claim_ledger:4713 唯一 open 条目；动作＝alias_map 追加一行，**下圈**执行，本圈账本零接触——审计区 INCREMENTAL-LOG 条目 010.B 已预告家族映射同批）。

## 三、边界纪律
- 只在嵌套仓提交 `F4实验/` 三件（`2bf1b0c`），其他工位未跟踪文件（F1/F2/F3实验、混元下发单、衔尾蛇 proto 等）一律未动。
- wo-k02-close 关单事实核验：嵌套仓 `170e42a`（INCREMENTAL-LOG:257），声明中引用。
- 审计区账本（claim_ledger/alias_map「永不改写」类）零写入；回执通道＝本 commit（审计区下圈 delta 扫描起点已钉，会自行拾取）。

## 四、经验
- 责任田任务谱系再扩一档：从「续作实验」到「档案治理记账」——同一套动作链（接手验证→对账→落档→commit）不变，产出物从实验报告变为登记声明。与同日 F1/F2/F3 三个家族会话共同构成 PT-014 的第 3 次验证（冻结族场景：⑤活页视图适配为静态冻结登记+触发器挂靠既有预注册文件 10-branch-proposals）。
- 跨会话轨迹编号争用（新坑实录）：四个并行会话各自取号，F2/F3 双占 task-030、F1 与我接力撞 031——**取号前必须先 ls 目标目录再 mv，mv 后立刻落 _index 行占位**；编号协调机制缺失应作对区（审计区）下圈增量输入。
- 下发单里「无主区写入」读法：指不动主区实验线（混元/衔尾蛇/豆包目录），下发单副本与声明件落在 F4实验/ 本身就是下发目标，不冲突。
- 外层仓收尾时发现 2026-09-11 会话遗留孤儿（patterns.md PT-007/PT-011 v3/v4 未提交、task-025~028 轨迹 untracked 但已被 _index.md 引用）——本次一并补票入库，消除「已提交索引引用未跟踪文件」的不一致。
