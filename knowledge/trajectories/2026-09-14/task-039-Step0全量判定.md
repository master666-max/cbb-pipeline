# task-039 · Step 0 全量抽取与正式判定（2026-09-14）

## 触发
用户终审通过 4 金标（v5/v2/v2/v2）→ 放全量抽取：DeepSeek×4（API）+ GLM 子代理×4，8/8 完成。

## 判定结论（详见 P1执行区/报告-Tier1正式判定.md）
**混合分支成立**：Graphiti 路线带三条件成立——
1. 实体 adjusted P 0.79-1.0（story 切样全过 0.85 线）、实体 R GLM 全过（0.789-0.912）/DS 差 0.016 边缘
2. 方向 0.857-1.0 全过
3. **时间线双败（0.18-0.67）→ 自研 cbb-anchor 分支正式触发**（预案内）
4. **元文本零抽取双败**（GLM 9E+9R / DS 6E+7R 全 FP）→ R6 领域规则为生产硬前置

## Tier 2 必办清单（报告§四）
R6 规则必配 / reference_time 禁用墙钟（DS 会发明假日期）/ schema 双兼容（name|entity、source|source_entity_name，评分器已内置）/ 反泛指豁免与泛指漏抽双向防御 / 去重环节（DS 产出 2-3 对重复边）

## 过程资产
- 评分器三轮迭代修 bug：关系索引匹配、双 schema 兼容、金标自动映射（曾错配 excerpt1 金标评其余切样——已修并重评）
- excerpt1 金标计数口径字段修正（36/35→实际 34/38，数据无误声明错）
- 8 份抽取 + 8 份评分 JSON + 正式报告全落 P1执行区/step0-抽取精度实验/

## 纪律兑现
- 中性指令保测试效力（excerpt4 不暗示预期）；金标审查通过前零评分行为
- 评分器 bug 三现三修（端点类型比错/键名不一致/金标错配）——对账工具自身也过对账
