# task-041 · 闲时任务机制调研与构建线下发模板（审核线 · 2026-09-14）

## 触发
用户指令：接下来所有正式代码构建任务改由 ZCode 闲时任务执行（自行调研闲时任务是什么），善用 /goal，做好断点续传设计，交付一版可复用闲时任务提示词。

## 调研结论（官方文档核证，源：zcode.z.ai/cn/docs/idle-time-tasks、/docs/goal、/docs/commands）
**闲时任务**：排队一次性任务，算力富余时段免费执行（执行费用 0、套餐额度消耗 0）；全局队列、无保证开始时间；自动化页管理、创建有每日上限；**单次执行有时长上限，到点自动回队列、续同一会话接着跑，不从头来**；App 重启不丢；**后台子智能体暂不支持**（background 派发会失败，前台子智能体免费）；**人工介入消息不免费**；机器须保持唤醒；官方明示"指令写得越完整，无人值守执行的效果越好"。
**/goal 目标模式**：给会话设一个可校验目标，Agent 每轮结束自动校验、未达自动继续；子命令含查看/设置/替换/暂停/恢复/清除；官方定位"需要多轮迭代的长任务"。

## 交付
`正典库构建系统/P1执行区/闲时任务提示词模板.md`（v1）：
- **三层架构**：闲时任务=外壳（免费队列+到点续跑）｜/goal=内核（会话内自动校验循环）｜BUILD-STATE.md+逐件 git commit=磁盘锚。设计原则：**锚层不依赖外壳与内核**。
- **断点续传协议**：开场自举三步=读 STATE→对账（STATE 只是缓存，磁盘+git 才是事实，R-014/P-014 接单即核证）→续跑；推进循环=单位切件（小时级）→完成即 STATE 更新+commit（禁止攒批，commit 频率=断点密度）；幂等可识别（已完成单位跳过）。
- **无人值守条款**：禁提问（不确定就地决策+【待确认】留审核线）；同单位 ≥3 败标 blocked 换件不死磕；子代理一律前台；长驻服务先探活；权限模式禁 build/plan（会卡等确认），edit 起步/纯流水线 yolo。
- **审核线接口**：执行代理无权宣告"验收通过"；差异写 Issue 文书；最终报告固定五段格式。
- C 节预填示例：P1 六技能骨架 M1（U1-U8 切件建议）。

## 判断与依据
- /goal 能否由代理在闲时任务运行中自调**未证实**（未实测），故模板设计为"有接口则接、无接口则把判据抄进 STATE 头部条款自锚"，行为等价（R-010：未测的事标注未测，不夸大）。
- 三层必要性：/goal 与闲时任务续跑都是会话层机制，依赖会话存活与上下文不腐；唯一不依赖会话的断点是磁盘 STATE+git。与 PT-013 断点续传条款同源。
- "STATE=缓存、磁盘=事实"是 R-014（下发文书=外来断言）在恢复协议上的直接应用：断点文书自己也可能滞后于盘上现实，重启第一动作必须是对账。

## 遗留
- 【待确认】代理自调 /goal 的可行性，待首次实单闲时任务观察。
- P1 六技能骨架（M1）尚未实际下发，等用户发令；模板 B 节填槽即用。
- DeepSeek key 轮换提醒仍未执行（key 曾暴露于对话）。

## 关联
R-014 / R-015 / R-010 / PT-013 / P-001 / P-014 / D-004；task-035（P1 计划书）/ task-037（审核工位）/ task-040（Step0 收官）
