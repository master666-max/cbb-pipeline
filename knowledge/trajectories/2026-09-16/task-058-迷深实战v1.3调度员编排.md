# task-058 · 迷深实战工单 v1.3：调度员-子代理编排（审核线 · 2026-09-16）

> 编号注：task-057 已被衔尾蛇线占用（c9f75c10），本线顺延 058。

## 触发与决策链
1. 用户报：闲时窗口跑得慢，受上下文限制，参考工作库报告探讨优化。
2. 审核线诊断：主代理上下文背全部章节原文+候选载荷（ch0038 候选 696 行），压缩循环=上下文税，实测 12 章/小时大半是税。
3. 对报告：**三份详报一致指向同一编排而我们吸收了参数没吸收编排**——U-A11 oh-story 逐章子代理新上下文+降维聚合（914 章 61 批实测）/U-A08 graphify"no accumulation+强制重读磁盘"/施工参数在 cbb-extract 已固化却让主代理单线程硬扛。
4. 用户确认并发 3 非上限（【未实测】按旋钮处理）+问弊端→审核线列五弊端全有护栏：跨章别名（架构本设计，压缩后主代理本来就失忆，隔离=确定性失忆）/判例漂移（我方补法判例.md）/静默失败（三层门禁 P-018+U-A13 机械硬检查）/固定开销（并发摊薄）/回传损耗（子代理自写盘已绕开）。
5. 用户"开干"→ 工单 v1.3。

## 交付
- **工单 v1.3**：§0 编排条款（主代理=调度员不亲抽；逐章前台子代理新上下文；子代理自写候选文件，回执 ≤200 tokens，候选全文走磁盘不过主代理上下文；并发旋钮 CONCURRENCY=3 起步，每段零失败 +1 试探、失败回落-1；库/STATE/commit 主代理串行单写）+规范/判例外置条款。
- **`cbb/tools/抽取规范.md` v1.0**（子代理每章必读）：R6 逐字（自查 cbb_extract.py L32-36 源码取证）/防先验/注入防御/Record v2.0 产出契约/别名四分类/禁词八类/写盘回执格式/Good-Bad 例/引文预检纪律。
- **`cbb/tools/判例.md` v1.0**：跨章裁决附录，主代理追加封顶 30 条淘汰最旧留痕；预置 3 条试车期判例（双名不自动合并/噪音章零抽取/？？？按原样立占位）。

## 设计判断
- 弊端严重度排序：判例漂移>静默失败>别名增多>固定开销>回传损耗——无一架构级否决；判例.md 是唯一新件。
- 预期（非承诺）：上下文税归零+并发 3 → 12 章/时 → 35~60 章/时，余量从 4-6 夜压到 1-2 夜。
- "吸收了参数，没吸收编排"=本条教训核心：固化条款时编排形态与数值参数同等重要。

## 补记（2026-09-16）：GLM-judge 提案否决——专用裁判不可被通用规模替代
用户否决审核线"judge 环节改用免费 GLM-5.3 子代理"提案：**m-prometheus-14B 是为 judge 任务专门训练的模型**（Prometheus 系：rubric 跟随/分数校准/偏见抵抗即训练目标）。审核线错在拿参数规模当裁判力（R-016 自测自中：未实测先宣称"主力档上限更高"）。决定性反证在自家知识库：该 judge 是全项目唯一有实测定标记录的裁判（2026-09-11 活体冒烟+红线标定：canonical order 位置偏见 0.65/锚定必开/temp=0 ±0.05/纯 JSON/缓存不删），GLM-judge 零记录需从零标定。**裁决：judge 维持 m-prometheus 本地，v1.3 显存调度（段尾批评档）原样有效，无返工。**教训：已标定的专用工具是资产，"免费+更大"不构成替换理由——替换的隐含成本是重新标定。

## 关联
task-056（中断接收）/ U-A11/U-A08 详报 / P-018 / 工单 v1.3 / 抽取规范.md / 判例.md
