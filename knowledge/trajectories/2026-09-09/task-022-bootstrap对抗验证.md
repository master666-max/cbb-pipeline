# task-022 · bootstrap_v3 对抗式验证（2026-09-09）

> 轨迹文件（只追加）。任务：以 bootstrap_v3.py 为唯一事实标准，对《自演化推演_实验总报告17轮后.md》代码层结论逐条构造性复现，三态判定。

## 任务一句话
六阶段对抗验证（环境→静态对账→P0 动态→P1/P2→架构诊断→统计复核→总报告），产出证实 26 / 部分证实 3 / 已被修复 1 / 证伪 0 / 无法验证 11+，不符项 6 条（全部为报告保守偏差或版本时点差异，无夸大）。

## 数据流
- 输入：猴子记忆库推演/{bootstrap_v3.py(2415行,只读), 自演化推演_实验总报告17轮后.md}
- 沙盒：sandbox/{bootstrap_v3.py 副本, v2src/ 假源树 98 文件, bootstrap_data/evals/golden 33 条, repro/ 15 脚本, proto/ 2 原型, runs/ 20 日志}
- 产出：reports/{00-验证总报告, 01~05 分报告, progress.md}；git 8 commits（c53c75a 基线 → 256baae 总报告）

## 关键决策
1. git 仓库建在 猴子记忆库推演/ 内而非工作区根——避免触碰外来 AI 档案文件夹（对齐 2026-09-08 裁决）。
2. 数据缺失全按自给协议：假源树对齐 MODULE_MAPPING 全路径 + t_full_render_rule/t_l8_guards 断言所需最低结构；E7/E17/E8 按报告设计规格独立重建模拟器，明确「方向级判定、数字级无法验证」。
3. P0-4 并发实验用 spawn 真进程 + Barrier（Process args 句柄继承）；barrier 与 natural 双时序——barrier 20/20 轮精确复现报告 5/60=92%，natural 61%，另发现撕裂读崩溃（报告未提）。
4. 统计复核 E7 调参三轮（9/24→0/24→24/24），过程本身印证「档案多样性（亲本均匀）是跨谷前提；锦标赛亲本=变相严格化」。

## 踩坑与经验
- [[P-009]]：MSYS gpg 路径三态对照（反斜杠 Fatal / C:/ Fatal / /c/... 通过）+ subprocess errors="replace"。
- 重申 [[P-001]]：复现脚本全部 .txt 后缀跑 py -X utf8；输出重定向到文件（曾有一条命令管道丢输出）。
- E8 判定公式 bug（比错了基准）被数据本身揭穿——判定行也要对照数据自检。

## 复用提示
- 假源树构造脚本 phase0_build_fixtures.txt 可直接复用于任何需要 absorb→install→run-tests 全链路的测试场景。
- 对抗验证流程见 [[PT-011]]；候选复用场景：大审查/ 外来 AI 工单核验。
