# Task-027 · 内核修复交付最终验收（闭环轮）（2026-09-11）

> 只追加。触发：用户投喂 `内核修复交付.zip`（v1.1 审查修复版），指令「根据这个文件，完成最后的任务」。
> 上一轮 = task-026（复跑翻出 T15/K-22/K-23）；本轮 = 云端修复后的最终验收门。

## 时间线
1. 解压至 `内核修复闭环执行区-20260911/deliverables/内核修复交付/`；sha256 自检 kernel.py v1.1 = `b6cbd5ddfb3753aa` ✅
2. ① probe_dx 回归探针：**15 项全绿**（上轮全红的死维度/零回路/零计费/棘轮/重臂/撞号全部转绿；w_age 序列随参数变化、dangers=['drift']、judge_spent=137、drift_detected_at=11）✅
3. ② harden_check：5/5 PASS ✅
4. ③ verify_kernel_v2.1 完整运行（云端配额只跑过冒烟级，完整版明确「待本地执行」）：**验收门 PASS 26 / FAIL 3 + DX 10/11**；重跑逐字节复现 ✅（exit 1 = 套件断言生效）
5. 本地归因取证（说明§判读预案授权范围内）：
   - **K-02b DX 红 = 验收脚本假红，非内核回归**：归因实验三组对照——A 组（v2.1 DX3 构造 run(6)）所有曝光条目 measured_importance 全 1.0（无区分度→序列不变→假红）；B 组（probe_dx 构造 write_every=2+run(10)）有 mi=0 自写入条目→序列翻转→绿；C 组证实区分度来自自写入而非代数。w_imp 接线本体工作正常
   - **T4/T15 FAIL = 审计通道零行为差异**：两组 mean/stdev 完全相同（+0.2668±0.0620 / +0.3129±0.0429）——_probe() 用独立 Random 不扰动主轨迹，审计只在「退化→回退/重锚」分支改行为，演示参数下 cur 恒 ≥ best−degrade_thr（DX K-06 绿佐证）→ 判别式从未触发 → audit 两态走出同一条轨迹
   - **T5 FAIL = 显著性口径下的无显著差异**：差 0.012 << stdev 0.047/0.073（V-04/K-23 口径）
6. 深层信号：演示 world 中 judge 采纳门全员通过（shown=adopted → mi 全 1.0）→ 采纳反馈无选择压力差异——与 T4/T5/T15 组间差异消失同源

## 判定（按工单 v2 §八预案 + 公理 G + K-23）
- **内核 K-01~K-21 修复本体：无回归，全部生效**（探针 15/15 + 加固 5/5 + DX 10/11 + 验收门 26 PASS）
- 验收门 3 FAIL 均属 K-23 遗留观察项（机制效果验收层）：T4/T15=审计判别式在演示参数下测不出差异（公理 G 暂判表演态，需 corrupt/退化注入场景才能验收 E110/E134 机制价值）；T5=不显著
- 「验收门全绿」未达成 → 闭环卡在验收场景设计而非内核修复；K-23 预案命中：判别式回退+重锚的验收场景应回炉
- DX 假红 = v2.1 DX3 构造缺陷，修法=与 probe_dx 构造对齐（write_every=2 + run(10)）

## 发回云端
v2_run.log、v2_run_replica.log（复现证据）、k02b_attribution.py（假红归因实验）、probe_dx_result.txt、harden_check.txt

## 经验
- 「完全相同的两组统计值」本身就是诊断信息：mean+stdev 逐位相同 → 两分支未走过不同代码路径（独立随机源+未触发分支），比数值不同更好归因
- 验收断言红≠被测对象回归：先做构造区分度归因（mi 全 1.0 时 w_imp 必然平移不变）——脚本缺陷与实现状态分开判（PT-007 第 4 次应用）
- 云端「冒烟并列=预期假象」的预案与本地完整跑 FAIL 咬合：两端对同一现象的语气分层（R-010 应用实例）

## 详见
- 执行区：`内核修复闭环执行区-20260911/deliverables/内核修复交付/` + README.md
- patterns#PT-011（第 4 次验证）、PT-007（第 4 次应用）；reflections#R-010
