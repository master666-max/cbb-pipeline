# Task-026 · 内核项目交接包本地复跑（2026-09-11）

> 只追加。触发：用户投喂 `C:\Users\26672\Downloads\内核项目本地交接包.zip`，指令「根据这个文件，本地跑完任务」。
> 交接来源：云端审查会话工单 WO-20260911-K01（自演化内核 kernel.py 全量审查与修复）。

## 时间线
1. 解压至新目录 `内核项目交接执行区-20260911/deliverables/内核项目本地交接包/`（不覆盖任何已有文件）；sha256 自检 kernel.py 前 16 位 `0368ee6667bb7848` 与云端基线一致 ✅
2. ① `probe_dx.py`：诊断全红实证（K-01/02/02b/04/05/06/08/10/11/12/13 共 11 项 + 1 佐证行），数值与工单附录 A **逐位一致**（跨机复现确认）✅
3. ② `harden_check.py`：4/4 PASS（margin 衰减/真 Pareto/探针变化×2），符合云端预期全绿 ✅
4. ③ `verify_kernel.py`：**PASS 26 / FAIL 1**——与云端宣称「PASS 53 / FAIL 0」不符 ❌
   - 唯一 FAIL = T15「漂移世界下审计（含重锚）优于无审计」：有审计 -0.0174 < 无审计 -0.0102
   - **重跑逐字节复现**（verify_run_replica.log 与首跑全等）→ 本机完全确定，非噪声；Python 3.14.7
5. ④ `verify_kernel_v2.py`：验收门 PASS 27 / FAIL 1（同一 T15）+ DX 9 项全红（0/9 绿）——DX 方向与云端一致，但「13 项 DX」实际套件只有 9 项
6. 源码对账：verify_kernel.py 实际 28 个 check 调用点（运行时 27 判定，T11 frozen 分支少 1）——宣称的「53 断言」在源码中不存在；v2 实际 30 check + 9 dx（宣称 54 + 13）

## 关键判读
- 工单 §二自认「完整 53 项复跑受 CPU 配额限制在后台继续」→ **云端从未实测过 T15**；「53/0 全绿」是预期非实测
- T15 FAIL 与工单缺陷互证：K-08（drift 检测一次性永不重臂）+ K-14（reanchor_after 未接线）→ 重锚机制失效 → 漂移世界下审计组收益转负。属未修复内核的功能性后果首次显形
- 统计口径提醒（工单 V-04 自己点过名）：T15 差异 0.0072 << stdev 0.061/0.071，按 V-04 建议口径应报「无显著差异」——弱断言翻出来的红，修复 K-08/K-14 前不宜过度解读为「审计有害」，但断言FAIL本身成立
- ④ 的 DX 数值（0.496/255次/6次/13条重叠/drift=3/AttributeError）与工单附录 A 及 probe_dx_result.txt 三方逐位一致

## 新发现（发回云端修订工单用）
1. T15 首跑翻红（云端宣称 53/0 与实测不符；内核修复回归门需含 T15 转绿）
2. 断言数宣传系统性虚高：53→实际 28 调用点（27 判定）、54→28、13 DX→9、probe_dx「13 项」→11+1
3. kernel.py 行数 718（wc -l，宣称 719 系末行无换行符计数差，实质一致）

## 经验
- 外来交接包的「预期结果」与「实测结果」必须分开判——工单写「复跑预期 PASS 53/FAIL 0」，跑出来才知道 T15 从未在云端亮过相（PT-011 第 3 次验证）
- 断言/诊断数量宣称直接 grep 源码调用点对账，一分钟揭穿「53 断言」（PT-007 第 3 次验证）
- 本机复现性验证手法：同机重跑 + 逐字节 diff，exit code 同时核对（套件自身 sys.exit(1) 是断言生效的证据）
- 云端没跑完的部分恰恰是信息量最大的部分——接手外来实验线先问「哪些它自己没跑过」（P-014 延伸应用）

## 详见
- 执行区：`内核项目交接执行区-20260911/deliverables/内核项目本地交接包/`（4 份发回日志 + verify_run_replica.log + _工单提取.txt + README.md）
- patterns#PT-011（对抗式验证第 3 次验证）、PT-007（第 3 次验证）；reflections#R-010
