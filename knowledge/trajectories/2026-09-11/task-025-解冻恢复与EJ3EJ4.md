# Task-025 · 主工作区解冻恢复 + E-J3 检测器对决 + E-J4 启动（2026-09-11）

> 只追加。触发：用户口令「主工作区已解冻」。执行序按混元线分册交接文件 §6。

## 时间线
1. 解冻恢复：_index.md 公告切换、lint 体检（0 问题 4 提醒）、现场重建——发现冻结两天里其他会话已把混元线推至 35+ 轮（CD 检测器线 E139-148）、完成 LLM-judge 接入工程（E-J1 真 Goodhart +0.31 / E-J2 rubric 压 81%）、留好两线联合设计（E-J3/E-J4）与合流清单
2. **E-J3 检测器对决**（预注册先锁 → 200 流 → 报告）：
   - H2 SUPPORTED（champ 流 CD 延迟 0 vs 窗口法 2.1）
   - H1 反方向推翻：CD 有增量（abrupt 延迟）但 gradrot 下 PH FPR 0.25 不稳；窗口法反而全绿
   - **★ CUSUM 唯一全绿**（两种流型×四场景）→ 建议为 kernel 审计通道选型
   - **⚠ 工程警告：kernel 一步落差判据（degrade_thr=0.01）在探针轮换下 FPR 0.80-0.95**——「探针轮换」红线与「一步落差」判据内部互相拆台
   - H3 标定缺陷如实登记：网格 break 早退致 0.02 档未被测（fpr_at_0.02=None），判 NOT SUPPORTED 属"未测"非"证伪"
3. **E-J4 启动**（真 judge drift 闭链，两线 P0 合并主实验）：
   - LM Studio 8080 可达确认（m-prometheus-14b.i1 在线；注意 judge_adapter 默认 1234 需 JUDGE_API_BASE 覆盖）
   - 冒烟两处实现坑修复（evolve20_shim 路径、m/tol/cap 局部变量）
   - 补 gen 级断点恢复（run_e149 教训）+ 合并 E149/E150 缓存省调用
   - **drift 分支验证即出真发现：both 场景锚定流不降反升**（judge-drift「简洁加分」抵消 world 重洗下降）——网页端 E146c「锚定探针不辨 world/judge 漂移」的真 LLM 版实证；9 代 gap 已 +0.18
   - 全量 9 臂×6 seeds×24 代已挂后台过夜

## 经验
- 内联跑 drift 分支验证（临时 seed=99）是冒烟的正确延伸：只验证「管线通」不够，要验证「事件分支真的触发」
- 外来脚本路径假设逐个核（1234 vs 8080、sys.path、模块位置）——P-009 同族
- 网格早退 break 会让未被遍历的档位成为盲区（H3 缺陷），标定网格如需报告特定档位必须显式测

## 详见
- 大审查/混元/两线联合-EJ3EJ4-20260911/（预注册、run_ej3.py、报告-EJ3、run_ej4.py、res_*）
- PT-012 第 3 次验证（E-J3 预注册循环）；P-014 反向应用（开工前先读合流清单确认并行产出）
