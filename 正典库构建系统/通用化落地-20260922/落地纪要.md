# 落地纪要 · 裁决五项代码件（2026-09-22）

> 用户裁定：全按《裁决信息包-通用化五项-20260922.md》建议——①A ②A ③A+B ④A ⑤新项目auto/存量batch_confirm。
> 本纪要＝执行留痕。全部测试绿后才算 done（机械门纪律）。

## 一、落地清单（9 文件改动/新增，两树同步）

| 裁决 | 件 | 内容 | 测试 |
|---|---|---|---|
| ②A | `cbb/contracts/record.schema.json` | 追加可选字段 `spec_version`（v2.1；v2.0 载荷全兼容）＋$comment 追加⑤ | test_contracts 全绿（未破坏 v2.0） |
| ②附带 | `cbb/cbb-quarantine/cbb_quarantine.py` | `register`/`adjudicate` 增加 `at` 日期位（缺省今日，历史不编造）；新增 `status_report()`（未裁/已裁两数＋裁决滞后按最早登记日计，口径内嵌） | test_cbb_quarantine 全绿 |
| ①A | `cbb/tools/失效宣告.py`＋test | 允许三情形（value_conflict/superseded/termination）＋禁止三情形（extraction_error/alias_unaligned/granularity→ValueError 带理由）；value_conflict/termination 强制 by=human；value_conflict/superseded 强制双侧证据；幂等键=record_id\|kind\|invalid_at；`apply_to_edges` invalid_at=min 语义、输入字节不动 | 6 tests PASS |
| ③A | `cbb/tools/词表.py`＋test | 自举（频次＋包含对探测，四选一归人）／lint_vocab（词表侧粒度纪律）／check_record（NOT_IN_VOCAB／GRANULARITY_VARIANT 带建议／MISSING_REQUIRED）／check_store（只读干跑） | 4 tests PASS |
| ③B | `通用化落地-20260922/样例词表-迷深-entity_type.json` | 对冻结库（只读）自举产出样例词表 #1——见 §二 | 实跑 |
| ④A | `cbb/tools/graphiti_ready.py` | 哨兵 B 改"逻辑语料版本"判断（排除切片局部视图＋excluded 留痕＋可显式传 logical_corpus）；哨兵 D 接日期位算裁决滞后（历史件如实 UNKNOWN）＋pending_total＋口径注 | test_graphiti_bridge 全绿＋冻结库实跑 |
| ⑤ | `cbb/tools/矛盾分流.py`＋test | 四分类（identical/orthographic/granularity/incomplete/direct_conflict）；机械档对齐方向=库内值；`apply` 双模式（auto／batch_confirm 确认件制）；direct_conflict **永不机械执行**；处置台账 append-only；`align_candidate` 纯函数 | 5 tests PASS |
| 同步 | `cbb-pipeline-skill/.../scripts/cbb/` 同 9 件 | 两树一致 | skill 树重跑全绿 |
| 文书 | `SKILL.md` 已知边界更新 | 已落地 6 件 vs 仍未含 2 件 | — |

## 二、样例词表 #1 的关键发现（冻结库实跑，只读）

- **entity_type 取值 348 种**（1790 条非空记录）——自由文本的灾难现场，与裁决前的判断一致。
- **包含对 443 组**（"人物"⊂"人物(迷宫生物)"形态）——词表若照单枚举必然自相矛盾。
- **结构规律**：取值几乎全是 `大类(细分)` 形态 → **正确设计＝两层**：`entity_type` 用大类枚举（44 个大类去重归并后约 20~25 个），括号内细分迁到独立字段 `subtype`（开放值＋别名规范）。这一条已超出"四选一归并"的机械范围，属词表 schema 设计——**建议作为样例词表 #2 的方向**，待你点头后出归并表终稿。
- 干跑口径：本词表未启用为门；启用（U-D04 干跑→批准）另行裁定。

## 三、矛盾分流干跑（冻结库，只读）

扫描 1351 件 → 双轨形态 414 条 → **granularity 195（机械可修）／direct_conflict 219（真冲突进人工）**。
对照裁决前认知：机械档占比 47%——即人工积压的近半可由 ⑤ 的 auto/batch_confirm 消化。工件：`矛盾分流干跑-迷深.json`。

## 四、哨兵实跑对照（冻结库）

- **B：RED(误报) → GREEN**——logical_versions=1（唯一版本=clean_full.txt），切片全部进 excluded 留痕。
- **D**：pending_total=1351／contradiction_pending=604（口径注在案）；滞后=UNKNOWN（历史件无日期位——如实披露，自新件起可算）。

## 五、剩余路线图（2 件）

1. 批次机械自检器脚本（十条失效模式断言；判据=现行工单条款）
2. 双独立复查＋Chapman 流程件（估计器纯函数＋对账单生成）
