# 交接文书 · CBB P1-M1 六技能骨架（构建线 → 审核线）

> 交付：2026-09-14 ｜ 执行：P1-M1 构建线执行代理（闲时任务·交互式执行路径）
> 依据：P1执行区/下发单-P1M1-闲时任务.md（模板 v1.3 B 节，槽位按 C 节填死）
> 判定权声明：本件只交事实与证据，**无权宣告"验收通过"**（⑨）。

---

## 1. 判据达成状态：**达成**（U0-U8 全部 done，无 blocked）

| 判据项（①原文） | 达成 | 证据指针 |
|---|---|---|
| U0-U8 全部 done | ✅ | BUILD-STATE.md 单位清单终态；下节 git 摘要 9 commits |
| 六技能目录齐备、各含契约字段与三态写入桩 | ✅ | cbb-skills/{cbb-coordinate,cbb-anchor,cbb-extract,cbb-gate1,cbb-quarantine,cbb-store}/ 各含 SKILL.md+入口脚本+单测；contracts/ 四契约 schema 照抄 Part IV；每技能自带 three_state_write_stub（三池分目录/ID 命名/已存在即跳过） |
| py -X utf8 单测全绿 | ✅ | **73 tests / 7 套件 / 0 失败**（contracts 11 + coordinate 9 + anchor 10 + extract 11 + gate1 11 + quarantine 10 + store 11） |
| cbb-extract 内置 R6 标配 | ✅ | `R6_CUSTOM_EXTRACTION_INSTRUCTIONS` 逐字照抄 Step0 Tier2 验证文本（四条规则）；单测断言逐条在档 + kwargs 注入形态 |
| cbb-anchor 过时间精度抽检 | ✅ | 18 条抽检电池（次日/当天/N天后含中文数字/半年后=6月/次年/那年冬天/数日后/那天/无锚），日/月/年/季精度与三显式隔离标记全中 |
| 逐单位 commit 可证 | ✅ | 每单位一 commit（含 U1-补 gitignore 修正件），信息含单位 ID 与证据摘要 |
| U7 集成冒烟通过 | ✅ | smoke/run_smoke.py 双跑 PASS 20/20、EXIT=0/0；产物在 smoke/out/ |

## 2. 单位清单终态（摘自 BUILD-STATE，细节以 STATE 原文为准）

- U0 外挂装机 **done**：evals-skills(7)+neo4j-skills(29) 共 36 技能 cp -n 装 ~/.zcode/skills/，SKILL.md 在位 36/36；安检命中 1 处=Neo4j 官方安装器文档（原样保留+警示登记）
- U1 cbb-coordinate+四契约 **done**：contracts/ 四 schema（Record/Issue/Verdict/Case 照抄 Part IV）+ stdlib 最小校验器；坐标(卷,章,段,行)/幂等缓存无时钟字段
- U2 cbb-anchor **done**：伪锚点 2000-01-01+i 天/禁墙钟（静态+功能双检）/锚点=契约 Record 默认 provisional 人工前置/树版本化/三显式隔离标记
- U3 cbb-extract **done**：R6 内置/stub 确定性抽取（元文本章零抽取）/candidate 强制证据四元组/kwargs 组装器（伪锚点+R6）；graphiti 运行时=P2（复用 Tier2 已验证管道）
- U4 cbb-gate1 **done**：四校验（G1-SCHEMA/G1-EVIDENCE/G1-REF/G1-TIME_INVERSION）+原因码+verdict_id 确定性；粗精度时间不硬判
- U5 cbb-quarantine **done**：五类分组/append-only 双日志/登记幂等/人工裁决终态留档/报告（阻塞降序+请你确认）
- U6 cbb-store **done**：三态路由（quarantine 不落任何 library 目录=B1）/置信度路由永不单凭置信给 confirmed/supersede version+1 旧件不动/迁移旁车日志（终态守卫幂等）
- U7 集成冒烟 **done**：六技能串跑最小样本（2 故事章+1 元文本章+1 植入坏件）20/20
- U8 交接文书 **done**：本件 + issues/2026-09-14-P1M1构建线观察三件.md

## 3. git log 摘要（每单位一行）

```
655e5d34 build(P1-M1): U0 外挂装机 done（36 技能/BUILD-STATE 初始化）
766d252e build(P1-M1): U1 done（contracts 四契约+cbb-coordinate，11+9 绿）
d0b284dc build(P1-M1): U1-补（.gitignore+移出测试编译产物）
dc8ef964 build(P1-M1): U2 done（cbb-anchor，10 绿含 18 条精度抽检）
bb6676b1 build(P1-M1): U3 done（cbb-extract，R6 内置，11 绿）
6323fa0b build(P1-M1): U4 done（cbb-gate1 四校验，11 绿）
2c58e6c2 build(P1-M1): U5 done（cbb-quarantine，10 绿）
bc50a579 build(P1-M1): U6 done（cbb-store 三态+supersedes，11 绿）
f9e94f7e build(P1-M1): U7 done（集成冒烟 20/20 双跑；transitions 幂等守卫修复）
（U8 commit 见 git log 最新一条）
```

## 4. 留给审核线的【待确认】清单

1. 【待确认】**U0 命名空间扩容**：neo4j-skills 29 件全量装入使 ~/.zcode/skills/ +36 条（含与 CBB 无关的 driver-java/go/dotnet/snowflake/spark 等）。下发单口径为仓库粒度故按字面全量执行；如需裁剪请裁决保留子集（详见 cbb-skills/u0-外挂装机记录.md）。
2. 【待确认】**门1→隔离区分组映射**（issues/ Issue-1）：G1-SCHEMA/EVIDENCE→low_confidence 等为 M1 暂定。
3. 【待确认】**Record.status 枚举与 candidate 接缝**（issues/ Issue-2）：schema 未动，$comment+代位校验处理。
4. 【待确认】**伪锚点 evidence line=0 约定**（issues/ Issue-3）。
5. 【待确认】**τ 名义阈值**：cbb-store 取 Part XI 名义值 τ_confirmed=0.97/τ_provisional=0.85（M1 无校准器），P2 校准器标定后取代。
6. 【待确认】**锚点树 CLI demo 落点**：U2 验证时曾在系统临时目录出过一棵演示树（/tmp 等价物，不入库不入 git），如需留档请示下。

## 5. 值得入库的经验候选（有实据，供审核线/复盘裁决）

1. **幂等系统的断言口径：断状态不过程**——集成冒烟首批断言用"首次 created=True"当正确性，幂等系统二跑必假红；改为断"落盘存在+内容态正确"后双跑皆绿（实例：smoke/run_smoke.py 两处断言修正）。
2. **append-only 旁车日志的终态守卫**——状态迁移日志若只追加不去重，重跑即膨胀出重复历史行；追加前先查有效终态，已达则跳过（实例：cbb_store.status_transition，冒烟抓出后修复，单测回归）。
3. **跨技能骨架的 sys.path 单一事实源布局**——每技能入口脚本以 `parents[1]/<兄弟技能>` 插路径复用契约/坐标/锚点模块，测试文件同构；六技能+契约零循环依赖（实例：cbb-skills 全部 test_* 的导入头）。

## 6. 运行手册（复核入口）

```bash
cd 正典库构建系统/cbb-skills
# 全量单测（73）
for t in contracts/test_contracts.py cbb-coordinate/test_cbb_coordinate.py cbb-anchor/test_cbb_anchor.py cbb-extract/test_cbb_extract.py cbb-gate1/test_cbb_gate1.py cbb-quarantine/test_cbb_quarantine.py cbb-store/test_cbb_store.py; do py -X utf8 "$t"; done
# 集成冒烟（幂等可重跑）
py -X utf8 smoke/run_smoke.py
```

## 7. M1 边界与 P2 交接（骨架之外的显式留白）

- extract 真实路径：graphiti 运行时未接（kwargs 组装器已测；管道复用 Step0 Tier2 脚本形态，凭证走环境变量）；
- store 图后端：neo4j 显式 NotImplementedError 桩（Tier2 配置在案）；
- 门2a/2b/门3、judge 基线、校准器、冻结回测集：P2 范围（计划书 D9-D10 前置）；
- 锚点人工确认流/真锚点录入：P2（M1 只到伪锚点树+候选态）；
- provisional 机制：P1 保留字段、P2 启用（计划书§一）。
