# U0 外挂装机记录（evals-skills + neo4j-skills · PT-008 手动路径）

> 日期：2026-09-14 ｜ 执行：P1-M1 构建线（闲时任务·交互式执行路径）
> 依据：下发单①路线边界（A 收窄=仅此两件外挂）＋ knowledge PT-008（clone→安检→cp -n→CLI 依赖随行）

## 装机事实

| 项 | 内容 |
|---|---|
| 源 | `正典库构建系统/现成技能侦察/源码/evals-skills/`（7 技能）与 `…/源码/neo4j-skills/`（29 技能）——task-034 已 clone 的本地仓，本单免网络 |
| 方式 | `cp -rn` 逐目录装入 `~/.zcode/skills/`（= `C:\Users\26672\.zcode\skills\`），零覆盖（既存即 SKIP） |
| 验证 | 36/36 目录内 SKILL.md 在位（逐一 file 存在性检查，脚本输出 0 missing）——下发单口径"验证文件在位即算成"满足 |

## 装机清单

- evals-skills（7）：build-review-interface / error-analysis / eval-audit / evaluate-rag / generate-synthetic-data / validate-evaluator / write-judge-prompt
- neo4j-skills（29）：agent-memory / aura-agent / aura-graph-analytics / aura-provisioning / cli-tools / cypher / document-import / driver-dotnet / driver-go / driver-java / driver-javascript / driver-python / gds / genai-plugin / getting-started / graphql / graphrag / import / kafka / mcp / migration / modeling / nvl / query-tuning / security / snowflake-graph-analytics / spark / spring-data / vector-index（均 neo4j-*-skill 后缀）

## 安检记录

- 扫描模式：`rm -rf` / `curl…|sh` / `Invoke-Expression|iex` / `base64 decode` / `eval(` / `exec(`，覆盖 *.md *.py *.sh *.json（排除 .git/）
- 命中 1 处：`neo4j-cli-tools-skill/SKILL.md` 第 5、38 行含 `curl -sSfL https://neo4j.sh/install.sh | bash`——**Neo4j 官方安装器文档**（neo4j.sh 为厂商域名；SKILL.md 是静态说明，装入不执行）。处置：按"永不修改外来内容"铁律原样装入，此处登记警示——后续任何会话若执行该技能中的安装命令，属新增系统改动，需另行裁决。
- 未发现密钥字面量、未发现向外部发送数据的脚本。

## 待确认（留审核线）

1. 【待确认】neo4j-skills 29 件全量装入后，`~/.zcode/skills/` 技能命名空间显著扩容（未来会话技能清单+36 条，其中 driver-java/go/dotnet/snowflake/spark 等与 CBB 无关）。下发单口径为仓库粒度装机故按字面全量执行；如需裁剪，建议保留 cypher / import / driver-python / vector-index / graphrag / agent-memory / getting-started 子集，其余移 attic——请审核线裁决。
