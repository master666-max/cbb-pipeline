# P1-M1 构建线观察登记（交审核线/设计轮裁决）

> 来源：cbb-skills 构建过程（2026-09-14，U1-U7）。均为**不阻塞交付**的设计接缝观察，
> 按⑨"与工单/基线冲突时事实优先、写成 Issue 不擅改基线"登记。

## Issue-1：门1拦截原因码 → 隔离区分组的映射为 M1 暂定

- 现状：`cbb-gate1.REASON_TO_QUARANTINE_GROUP` 将
  G1-TIME_INVERSION→unresolved_time、G1-REF→ambiguous_reference、
  G1-SCHEMA/G1-EVIDENCE→low_confidence。
- 依据：Part VI 只枚举五组名，未给 gate1 原因码到分组的官方映射；
  "结构/证据缺陷归 low_confidence"是构建线保守决策（SKILL.md 已标【待确认】）。
- 请裁决：确认此映射，或给出设计轮版本。

## Issue-2：Record.status 契约枚举与 Part V candidate 态的接缝

- 现状：Part IV 4.1 status 枚举 = confirmed|provisional（B1 库内两态）；
  Part V 状态机含 candidate（入库前）与 quarantine/rejected（不入库/终态）。
- 处理：schema 照抄 Part IV 未改枚举，以 $comment 固化边界说明；
  代码层 `validate_record(allow_candidate=True)` 以代位法校验候选的其余字段。
- 请裁决：后续版本化修订是否把 candidate 显式写进契约注释层之外的正式定义
  （如独立 Candidate schema），或维持现状。

## Issue-3：伪锚点 Record 的 evidence 行号约定

- 现状：锚点 Record 的 evidence 使用章序标记行（`<<<CHAPTER NNNN | …>>>`），
  line=0（cbb-coordinate 口径中章标记不占章内行号）。
- 说明：伪锚点不自充故事内证据（Step 0 纪律：保序不冒充真实日期），
  但 line=0 作为"非正文行"约定需在 P2 锚点人工确认流程里向标注者说明。
