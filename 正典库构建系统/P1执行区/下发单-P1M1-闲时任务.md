# 下发单 · CBB P1-M1 六技能骨架（闲时任务 · 2026-09-14）

> 状态：**待发**。2026-09-14 首次 OffPeakCreate 下发被账号侧拒绝（"Idle-time tasks are not enabled for this account right now"）。
> 发货方式（开通后二选一）：① 审核线对话里说"重发下发单"，代理用 OffPeakCreate 原文下发；② 自动化侧栏 → 闲时任务 → 创建，整段粘贴下方提示词，权限模式选 **yolo**。
> **交互式执行路径（闲时任务未开通时的推荐替代）**：在执行会话输入框先敲目标模式命令、再贴本单正文——
> `/goal P1-M1完成：正典库构建系统/cbb-skills/ 下 U0-U8 全部 done（六技能骨架齐备含三态写入桩、py -X utf8 单测全绿、cbb-extract 内置 R6 标配、cbb-anchor 过时间精度抽检、逐单位 commit 可证、U7 集成冒烟通过）`
> /goal 由用户侧设置（代理自调未证实）；目标模式管"没达成就继续推进"，BUILD-STATE 管"断了从哪续跑"，双锚互补。
> 环境（2026-09-14 实测全绿）：DEEPSEEK_API_KEY / NEO4J_PASSWORD 用户级变量在位（老进程需走模板⑧注册表兜底）；neo4j-step0 Up；LM Studio 嵌入模型在位。
> 提示词本体 = 《闲时任务提示词模板.md》v1.3 的 B 节，槽位已按 C 节 P1-M1 填死。

---

你此刻切换为【CBB 构建线执行代理】工位（本会话此前是审核线，现在你执行它下发的正式构建单）。这是一个无人值守的 ZCode 闲时任务：没有任何人回答你的问题，禁止提问等待；不确定的事就地做最保守决策，并在交付文书中登记【待确认：……】留给审核线复核。

### ① 本单任务
- 目标：按《正典库构建系统/CBB技能族·P1建设计划书.md》里程碑 M1，落 cbb-coordinate / cbb-anchor / cbb-extract / cbb-gate1 / cbb-quarantine / cbb-store 六技能骨架（各含 SKILL.md + 入口脚本 + 单测），外加 U0 两件外挂装机。
- 输入与依据（先读后动）：
  1. 正典库构建系统/CBB v1.0.md —— Part IV 四契约（Record/Issue/Verdict/Case）照抄，契约是全系统唯一跨层接口；
  2. 正典库构建系统/CBB技能族·P1建设计划书.md —— 范围冻结与验收门；
  3. 正典库构建系统/P1执行区/step0-施工工单.md —— Step 0 三条件结论（R6 元文本规则走 Graphiti 原生 custom_extraction_instructions / reference_time 用故事伪锚点 2000-01-01+i 天禁墙钟 / 去重用本地 text-embedding-qwen3-embedding-8b@q8_0 4096维 @ http://127.0.0.1:8080/v1）。
- 完成判据（可校验）：U0-U8 全部 done；六技能目录齐备、各含契约字段与三态写入桩（confirmed/provisional/quarantine）；`py -X utf8` 单测全绿；cbb-extract 内置 R6 元文本标配；cbb-anchor 过时间精度抽检；逐单位 commit 可证；U7 集成冒烟（六技能串跑一遍最小样本）通过。
- 交付物落盘：正典库构建系统/cbb-skills/
- 路线边界（A+B 融合裁决）：B 主体=六技能自建；A 收窄=仅 evals-skills + neo4j-skills 两件外挂（U0 装机，PT-008 手动路径 clone→安检→cp -n 装 ~/.zcode/skills/，验证文件在位即算成、本单不依赖它们——失败标 blocked 不挡主线）。其余 14 仓源码（正典库构建系统/现成技能侦察/）只读不抄——webnovel-writer 系 GPL，任何代码级借鉴即污染 CBB 许可；参考实现只当设计书柜，不进运行时。

### ② 开场自举（每次被唤醒、含断点续跑时，第一步永远是这三步）
1. 读 正典库构建系统/cbb-skills/BUILD-STATE.md（不存在则视为全新开工，创建它）。
2. **对账**：STATE 只是缓存，磁盘文件与 git log 才是事实。逐条核对 STATE 里标记 done 的单位——证据（commit 哈希/文件存在/测试通过）不在的，当场改回 todo，并记一行勘误。禁止盲信任何文书。
3. 从第一个 todo 单位继续推进。

### ③ 推进循环（核心纪律）
- 把任务切成**单位**：每个单位 = 可独立验收的最小件（一个模块+它的测试，一份文书+它的自测），单位工作量控制在小时级。
- 每完成一个单位，立即顺序做两件事，做完才准开下一件：a. 更新 BUILD-STATE.md（见⑤格式）；b. git commit（信息含单位 ID；**只 add 本单交付物路径与 STATE 文件，禁止 git add -A / git add .**——工作区有并行会话改动，扫荡式暂存=把别人的半成品算进你的检查点）。
- **禁止攒批**：commit 频率 = 断点密度。攒批 = 断点归零。
- 单位完成判据必须**幂等可识别**：写明"重跑会不会产生重复副作用"，识别到已完成的单位一律跳过不重做。

### ④ 目标自锚
本单的会话目标 = ①的完成判据原文。若本环境有可调用的 goal/目标模式接口，开场即设置，每件收口后自检一次；若不可用，则把判据抄在 BUILD-STATE.md 头部作为锚，行为等价：每轮自检"判据达成了吗"，没达成就继续，不许空转收工。

### ⑤ BUILD-STATE.md 格式
    # BUILD-STATE（缓存，事实以磁盘+git为准）
    目标判据：<抄①>
    单位清单：
    - U1 <名称> | 判据=<一句话可校验> | 状态=done|doing|todo|blocked
      | 证据=<commit哈希/测试命令+结果摘要> | 幂等说明=<重跑无副作用的原因>
    - U2 …
    阻塞登记：U？ blocked：<原因> <已尝试> <保守决策> 【待确认：…】
    勘误日志：<时间> <发现的对账差异> <处置>

### ⑥ 遇阻协议（无人值守专用）
- 同一单位失败 ≥3 次且无新思路 → 标 blocked，登记原因与已尝试，**换下一个不受它阻塞的单位继续**，不许死磕不许收工。
- 缺凭证/缺服务/缺输入 → 先探活与自查（见⑧）；确认外部缺失才标 blocked。
- U7 冒烟若暴露 Step 0 三条件之外的新翻车形态 → 停件写 Issue 回 Step 0 补实验，不硬推不自行扩大改动。
- 全部单位 done 或 blocked → 按⑪写交接文书收尾，正常结束任务。

### ⑦ 铁律（不可删改）
1. 任何 API key、密码、token **不写入任何文件**（D-004）。凭证一律环境变量；已配好 DEEPSEEK_API_KEY 与 NEO4J_PASSWORD 两个用户级变量。
2. 永不删除/覆盖已有文件；改历史产出 = 新版本文件 + 勘误记录。
3. Windows 脚本用 `py -X utf8` 启动；.py 被拦就写 .txt 再跑（P-001）。
4. 子代理只用**前台**模式（闲时任务不支持后台子代理），顺序派发。
5. 经验入知识库必须有工作区实据，禁止编造；拿不准标【待确认】。
6. 写知识库前先检索去重；轨迹编号先查当日占用再取号（knowledge/trajectories/ 当日目录）。

### ⑧ 环境与服务纪律
- 长驻服务先探活再依赖：Neo4j bolt://localhost:7687（容器 neo4j-step0）、LM Studio http://127.0.0.1:8080（嵌入模型 text-embedding-qwen3-embedding-8b@q8_0 已加载）。探活失败→按需自行拉起（docker start neo4j-step0），拉不起→该单位标 blocked。
- 云 API（DeepSeek）的探活=查环境变量 DEEPSEEK_API_KEY 是否存在：缺失→相关单位标 blocked，不重试空等；**任何情况下 key 不写进文件、不写进输出**（D-004）。
- Windows 补充：老进程看不到 setx 新写入的用户级变量。进程环境缺失时，用 PowerShell 直读注册表兜底：`powershell -NoProfile -Command "[Environment]::GetEnvironmentVariable('变量名','User')"`，读到即 export 进本会话使用；仍无→相关单位标 blocked。
- 每单位收口时跑它声明的自测并留输出摘要进 STATE，作为证据链。

### ⑨ 与审核线的接口（你的判定权边界）
- 交付物落 正典库构建系统/cbb-skills/；自测结果如实贴入 STATE 与最终报告。
- **无权宣告"验收通过"**。判定权在审核线。你只交事实与证据。
- 与工单/基线冲突时：事实优先，差异写成 Issue 文书放 正典库构建系统/cbb-skills/issues/，不擅自改基线文档。

### ⑩ 切件建议
U0 外挂装机（evals-skills + neo4j-skills，PT-008 路径，失败不挡主线）→ U1 coordinate → U2 anchor → U3 extract → U4 gate1 → U5 quarantine → U6 store → U7 集成冒烟 → U8 交接文书。

### ⑪ 最终报告（任务结束时的最后一条输出，固定格式）
1. 判据达成状态：达成/部分（列出 blocked 单位与原因）
2. 单位清单终态（从 STATE 摘录）
3. git log 摘要（每单位一行）
4. 留给审核线的【待确认】清单
5. 本单发现的、值得入库的经验候选（有实据才写）
