# Step 0 验收审核单

> **审核强度分级（2026-09-14 用户校准）**：Step 0 定位为**可行性验证，不动任何代码地基**——本单对 Step 0 按**轻量档**执行：仅三项作门禁——**D4**（指标无事后调线，TP/FP/FN 原始数可复算）、**D5**（结论按计划书三分支落位）、**E1**（密钥不入任何文件，铁律不分级）。其余各项（A 材料组/B 配置披露组/C 金标流程组/其余 E 项）降为"记录在案、抽查不作门禁"。A-C 全组转为 **P1 构建线代码交付**时的正式门禁——那时才是动地基的交付物。
> **附注（一条不妥协的技术提醒）**：抽取精度高度依赖所用 LLM。若 Step 0 结论为"不过线"，报告必须注明抽取所用模型与版本——防把"模型弱"误判成"Graphiti 路线不通"，反之亦然（模型强也不能掩盖流水线缺陷，两头都要留模型信息）。
>
> **审核线**：本会话（只审不建） ｜ **构建线**：另一会话 ｜ **依据**：`../CBB技能族·P1建设计划书.md` 第二节
> **用法（P1 代码交付时恢复全量档）**：构建线交付产物后，审核线逐项核验，每项给 PASS / FAIL / N-A + 证据指针；总判定三态 = **结论有效 / 有条件有效（附缺陷清单）/ 无效重跑**。审核手法按 PT-011（每结论一脚本、静态数字核验、宣称对实物对账）。

---

## 〇、审核基线（审核线 2026-09-14 事前实测，构建方交付与此对账）

构建方的环境声称必须能与下述审核线探针结果自洽，矛盾即疑点：

| # | 事实 | 来源 |
|---|---|---|
| B1 | Docker 29.6.2 已安装，**daemon 未运行** | 探针 2026-09-14 |
| B2 | Python 主版本 3.14.7（py 启动器），uv 另有 3.12.14；graphiti-core 对 3.14 兼容性未知 | 探针 |
| B3 | **无任何 API 凭据类环境变量**（KEY/TOKEN/SECRET 全空） | 探针 |
| B4 | LM Studio `127.0.0.1:8080` 在线，OpenAI 兼容 `/v1/models` 返回：`tifa-deepsex-14b-cot-chat`、`m-prometheus-14b.i1`、`hermes3.6-35b-a3b-uncensored-genesis-…`（响应截断，或不止） | 探针 |
| B5 | 语料 `语料分析/corpus/clean_full.txt` 共 15,916,773 字节；章节标记格式 `<<<CHAPTER NNNN | 标题 >>>`；CHAPTER 0001 = 「新人观众推荐事先阅读本文」（适合作序章型材料） | 探针 |
| B6 | pip 无自定义镜像配置 | 探针 |

## A. 材料真实性（4 项）

- [ ] A1 四份切样与 `clean_full.txt` 可追溯：含 CHAPTER 编号+字节偏移+sha256，可一键复现
- [ ] A2 组成符合计划书：连续 3 章对话密集 + 1 章序章型，各 3-5k token（超出区间须披露理由）
- [ ] A3 切样为原文忠实摘录：无改写、无删注（diff 抽查）
- [ ] A4 材料落 `P1执行区/…/材料/` 且未污染语料源文件（源文件 mtime/size = B5 基线值）

## B. 实验配置披露（6 项）

- [ ] B1′ graphiti-core 版本号固定且记录（pip show 输出入档）
- [ ] B2′ 图后端形态披露：Neo4j（何时启动 daemon？与基线 B1 矛盾须解释）或 FalkorDB/Kuzu，连接参数入档
- [ ] B3′ 抽取用 LLM 模型名+base_url+temperature 披露；**模型选型理由**（B4 三模型中为何选它；用 M-Prometheus 做抽取属角色错位，须特别论证）
- [ ] B4′ 嵌入模型与维度披露（随机向量/降级嵌入属可接受偏离，但必须显式声明，因其影响去重环节可信度）
- [ ] B5′ add_episode 调用参数（reference_time、source 类型）四章一致
- [ ] B6′ 运行命令清单可复现（一条脚本或命令序列）

## C. 金标质量（5 项）

- [ ] C1 标注者与流程披露：金标初稿标注者、独立复核者（**2026-09-14 用户裁决升级：金标必须经用户人工全文审查通过方可用作答案册，原"抽查≥10%"标准作废**；审查记录留档）
- [ ] C2 标注规范在档：实体/关系/时间表述三类的定义与边界（如代词不计、别名主形选取规则）
- [ ] C3 金标覆盖四切样全部三类对象；数量下限：每切样实体 ≥10、关系 ≥5、时间表述 ≥3（不足须说明章节密度原因）
- [ ] C4 金标与图抽取结果**同格式可机读**（JSON），字段对齐
- [ ] C5 金标文件先于对账脚本产出（时间戳/commit 顺序），防"看着答案标注"

## D. 指标与判定（5 项）

- [ ] D1 实体匹配规则明确（精确/包含/别名三类，逐例可查），P/R 计算脚本可独立重跑
- [ ] D2 关系方向正确率定义明确（头尾实体+关系类型三对齐才算对）
- [ ] D3 时间表述可用率定义明确（"抽取到了时间元素且语义可挂靠"两条件）
- [ ] D4 指标对照判定线（0.85/0.70、0.80、0.60）执行，**无事后调线**；结果表含原始混淆数（TP/FP/FN）
- [ ] D5 结论按计划书三分支落位：全过→Graphiti 地基；时间单项不过→cbb-anchor 兜底；崩线→自研抽取+轻存储

## E. 合规红线（4 项）

- [ ] E1 **密钥不入任何文件**（D-004）：全部脚本/日志/报告 grep 无 key 字面量；若用云 API，密钥来源与注入方式披露且不入库
- [ ] E2 judge 红线未违反（若 M-Prometheus 参与任何打分：canonical order + rubric 锚定 + 分数不外报）
- [ ] E3 「不装 16 仓任何技能」约束未被绕过（本实验只装 graphiti-core 及其依赖属计划内）
- [ ] E4 产物全部落 `P1执行区/step0-抽取精度实验/`（材料/金标/脚本/结果/报告五件套），语料源与 16 仓源码零改动

---

*审核线留档：本单通过后，Step 0 结论才可写入 P1 决策；FAIL 项逐条回构建线整改后重审。*
