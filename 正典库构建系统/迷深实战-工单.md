# 迷深实战-工单（全量构筑版 · 阶段三 · 迷深全量入库 · v1.5 · 2026-09-17 · 审核线制）

> 唯一指令源（防漂移）。执行代理不得修改本文件与 STATE 判据栏；冲突→事实优先，写 `cbb/issues/` 并继续可继续部分。
> 变更：**v1.5 = 吸收件（审计区反向输出包 skill反向输出包-20260917）**：U-C03.6 store 账本哈希链+碰撞跳过（参考 merge2/append2 按 cbb 契约改写，禁直拷——参考实现内嵌他方绝对路径）+仪器指纹四元组入校准报告与抽检记录；DC-3 自举标定收录待主队列过半跑首轮；锚点协议 v2"脏树不作锚"入十查⑨；三载体复验挂 U-C09 可选臂；CUSUM 漂移监控挂阶段四。v1.4 = 漂移修正条款（零质量缺口）。v1.3 = 调度员-子代理编排+规范/判例外置。v1.2 = 存量资源整合+第三方抽检。v1.1 = GLM 亲抽单路线。v1.0 = DeepSeek 付费路线。

## §0 依据与实盘参数（2026-09-15 审核线核证）
- 本体：`cbb/`（阶段二验收 10/10 通过，task-050；契约 v2.0+六技能+161 单测+冒烟 74 断言）。
- 语料：`语料分析/corpus/clean_full.txt`（**清洗版**，517 章，`<<<CHAPTER 0001~0557 | 题>>>` 标记，16M）；`mishen_full.txt` 为生肉参照（对质用）。**入库对象=清洗版**；R6 仍/read入侧防御（CHAPTER 0001 读者前言类残留）。
- **抽取路线（用户裁决 2026-09-16 定稿，单路线）**：**ZCode 代理亲抽**——闲时任务 **GLM-5.3 正式版·极致档思考**（引擎固定；v1.1 时代"flash"字样作废，重考随引擎固定取消）免费算力即抽取引擎。代理逐章读 `clean_full.txt` 切片，按 Record v2.0 契约产出候选 JSON（证据四元组带原文引文），经 cbb-gate1 三域校验 → cbb-quarantine 分流 → cbb-store 双轨入 **jsonl 本体库**。主抽取不走任何付费 API（唯一例外=本节第三方抽检条款）；嵌入语义去重由**别名四分类（0.85 门槛+存疑分开建）+UNIQUE 约束族+嵌入相似度扫描+quarantine 兜底**承担（弱于 graphiti Resolver 一档，隔离区兜底，如实披露）。Neo4j 图层随批增量导出（见资源整合④与漂移修正 D1）。
- **编排（v1.3 结构性纠偏；依据 U-A11 oh-story 逐章子代理+U-A08 graphify 新上下文批扫+我方判例补法）**：主代理=**调度员，不亲自抽章**。每章派**前台子代理**（新上下文），子代理提示词=三行指针：读 `cbb/tools/抽取规范.md`（连同 `cbb/tools/判例.md`）+ 读本章切片文件 → 按 Record v2.0 契约产出候选 JSON **自行写入** `迷深实战-工作区/candidates/` → 回传一行回执 ≤200 tokens（文件路径+实体/关系/事件计数+引文预检结果）。候选全文走磁盘不过主代理上下文；主代理收执后核文件存在与行数 → gate1 → store → commit。**并发旋钮 CONCURRENCY=3 起步**（一条消息多子代理并发派发），每段收口若零失败则 +1 试探上行，出现失败/超时即回落-1；子代理只写各自章节文件（路径互不相交），库/STATE/commit 由主代理串行单写。每章失败 ≥3 次跳章 pending。
  **待确认⑤裁定（审核线 2026-09-16：采信合并）**：v1.3 编排为主形态；**首跑前 2 章按构建线 A/B 双盲门放量**（悬空率≤10%/关键事实覆盖≥90%，方案见 `迷深实战-工作区/子代理并行亲抽-评估与放量方案.md`）——过门则并发 3 放量，不过门回落主代理亲抽并呈报数据。发车节奏（用户裁决）：闲时任务一天 3 发，每发同段发车件续游标，引擎固定 GLM-5.3 正式版·极致思考（重考随引擎固定而取消）。
- **抽取规范与判例外置（防漂移+防判例漂移）**：`cbb/tools/抽取规范.md`（R6 逐字文本+Record v2.0 契约要点+别名四分类+防先验/注入防御+Good/Bad 例句+引文预检纪律，子代理每章新鲜读）与 `cbb/tools/判例.md`（跨章裁决判例，**主代理追加，封顶 30 条**——满员淘汰最旧并留痕；子代理连判例同读）。两文件改动=追加不覆写。
- **漂移修正条款（v1.4，用户裁决"零质量缺口"，详《决策补记录-架构漂移修正.md》）**：
  - **D1**：Neo4j=强制派生图层。每批收口增量导出为必做项；daemon 不在→数据照常 commit 但**记导出债务**，债务未清零不得进入 U-C09 终审；导出 schema 补时序字段（valid_at/invalid_at ← tick/instant/status_history）。
  - **D2**：证据 schema 回补 `graph_edge` 字段（可选、导出后回填边 id+valid_at/invalid_at；评分器与金标零改动）——构建线下一发车首单位实施+单测。
  - **W1**：judge（m-prometheus）**补考前置**：考纲=金标候选好样+注入坏引文坏样（中文适配 TRAM/TimE 精神），≥70% 分离度上岗；考不过→评分停用，低分路由改保守默认（可疑候选全入隔离）。已产出评分标注"参考值（在岗未考期）"。
  - **W2**：**召回探针**——每段收口抽 2 章跑专名覆盖率机械审计（切片专名 vs 库内实体 ≥90%），不足→触发该章补抽。
  - **W4**：store 阈值显式三档（τ_confirmed/τ_provisional=0.85/τ_quarantine）+每段收口校准报告（分布 vs 阈值假设，建议制呈报审核线）。
  - 以上实施顺序：D2/W4 代码件+W1 考试=已落地（U-C03.5，commit bd92982f：judge 补考 0.5455 未过线→评分停用保守路由）；W2 探针随段收口生效。
- **吸收条款（v1.5，审计区反向输出包 `skill反向输出包-20260917/`）**：
  - **U-C03.6 吸收件**：①store 账本哈希链——jsonl 追加前记 sha_before/追加后记 sha_after+**碰撞跳过计数**（幂等键已入账即跳过），参考包内 merge2.py/append2.py 语义**按 cbb 契约改写，禁直拷**（参考实现内嵌他方工作区绝对路径）；②**仪器指纹四元组**（ranker/criterion/sort/params）随每次抽检/校准报告入 STATE；均配单测+真库实跑。
  - **DC-3 自举标定**：收录方法（pb3_bootstrap 预承诺规则），主队列过半（>260 章）跑首轮阈值带自举，产出前现行阈值 0.85/10% 维持并在文书标注"手设值，自举待数据"。
  - **锚点协议 v2**：脏工作树不作锚——verified_against/断言只锚已提交状态（十查⑨增核此项）。
  - **三载体复验**：U-C09 可选臂——核心结论（如 confirmed 抽检通过）若需宣称方法学稳健，第二实现复验后升格。
  - **CUSUM 漂移监控**：挂阶段四（批次数据 ≥20 再暖机）。
- **存量资源整合（用户裁决：不许闲置）**：
  ①**本地 judge**（LM Studio m-prometheus-14b）每批抽 2 条候选做锚定评分（引文支撑度），接入照根目录《LLM-judge-API-快速接入.md》红线：rubric 锚定必开／temp=0／canonical order／分数只作内部比较不外报／judge 缓存文件不删；
  ②**Qwen3-Embedding-8B**（127.0.0.1:8080 /v1/embeddings）批收口嵌入相似度扫描：新候选 vs 库内 ≥0.95 疑似重复走双轨合并流程、0.85~0.95 存疑入 quarantine——**只提示不静默合并**；
  ③**Qwen3-Reranker-4B**：U-C09 抽检的"断言-引文"支撑度重排辅助；LM Studio 无 /v1/rerank（在案事实）→ 经 llama-server 暴露，两种端点都探活，全失败→该辅助项降级可选，**不阻塞**；
  ④**Neo4j**（容器 neo4j-step0；**状态不固化——每批收口前探活**：daemon 在→`docker start neo4j-step0` 拉起后 bolt://7687 导出；daemon 不在→数据照常 commit 但**记导出债务**——v1.4 起**债务未清零不得进入 U-C09 终审**（见漂移修正 D1），终审 R2 图层不在时先落 jsonl 全扫、图层补齐后 Cypher 复扫）：U-C01 建图层 schema（含时序字段），每批收口**增量 MERGE 导出**（图谱视图，纯本地），U-C09 R2 孤悬引用优先 Cypher 全扫。
  ⑤**显存调度（12G 单模型档，硬约束）**：三个本地模型**永不同时驻留**。执行顺序=**Embedding 常驻档**（批收口扫描期间独占显存，量小调用快）→ **judge 段尾批评档**（每主队列段收口时卸载 Embedding、加载 judge，一次评完全段累计候选再卸载，不逐批评）→ **Reranker 仅 U-C09 终审档**（终审块内与 judge 二选一先后用）。换装总次数全单 ≈ 15 次以内（≈几分钟总量），禁止逐章逐批触发模型切换。
- **DeepSeek 第三方抽检（¥5 硬顶）**：每主队列段收口随机抽 1~2 章交 DeepSeek（deepseek-flash，谷价）独立重抽对比，分歧条目→quarantine 矛盾子类+Verdict（第三方对抗，R-019 精神）；**用量与估算费用逐次记 STATE，估费达 ¥4 即停用该抽检并登记**；凭证已 setx，D-004 照旧，key 用后作废。
- **verified_against 真实三件套硬条款**（验收裁定 #4）：每条 Record 必须传真实源快照（path=clean_full.txt 实路径+SHA+验证时间），占位值只许出现在测试。
- 成本：**0 元**（抽取=代理算力，闲时任务免费；无 API 无嵌入服务）。时间成本如实披露：代理逐章抽取显著慢于 API 批处理，按夜间窗口+游标消化，不许为赶速度降隔离标准。

## §1 全单完成判据（/goal 目标锚原文）
《正典库构建系统/迷深实战-工单.md》U-C00~U-C10 全部 done：入库章数=517（STATE 游标对账）、每批 §3 十查留痕、U-C09 全库终审十轮留痕且 confirmed 抽检 ≥95%、隔离区报告成型、U-C10 交接文书落盘、逐批 commit 可证。

## §2 单位清单
- **U-C00 单章试车+金标自检**：CHAPTER 0014 代理亲抽端到端（切章→cbb-anchor 双时间轴→按契约产候选→gate1→quarantine→store jsonl 库），产物上**现成金标 excerpt1 v5 + 评分器** 自检。**格式适配（R-018）**：评分器（`P1执行区/step0-抽取精度实验/脚本/score_tier1.py`）消费 Tier1 形态 `{"entities":[…],"edges":[…]}` 且**文件名含 excerpt1** 触发金标映射——写小适配脚本（`cbb/tools/`，Record 候选→Tier1 形态；time_expressions 无对应字段置空数组，本门槛只看实体 P）生成对比件，**评分器与金标本体不得改动**，适配映射规则留痕 STATE。**预注册门槛：adjusted 实体 P ≥ 0.70**（Step 0 GLM 实测下限）——未达→停单写 Issue 等审核线，达→继续。判据：评分数字入 STATE、全链证据（库内可查/四元组真引文/无墙钟）。
- **U-C01 边界表与锚点树+工具件**：解析 clean_full.txt 517 章边界（单一切片真值）+伪锚点映射树（cbb-anchor 版本化）；建 `cbb/tools/` 两件：**嵌入查重脚本**（LM Studio /v1/embeddings+余弦，输出疑似重复/存疑两清单）与 **Neo4j 增量导出脚本**（实体/边 MERGE，各配单测；探活失败标 blocked 不阻塞切章）。判据：章数=517、树 v1 落盘、边界表抽 5 章对原文核对、两工具单测绿。
- **U-C02 小队列试车**：第 1-5 章连跑（含 0001 元文本章——R6 试金石）。判据：0001 零正典抽取或仅元文本入隔离、其余四章正常、抽检 2 条证据对原文。
- **U-C03~U-C08 主队列六段**：6-102／103-199／200-296／297-393／394-490／491-517。**主队列无总章数上限——从游标处按 §0 编排持续派子代理抽取（并发旋钮，新上下文逐章，主代理不背章节内容），直到闲时任务自身时长上限到点自动停（到点回队续跑是常态），禁止任何自行设限的提前收工；段界与批界都不是停止点，允许一班跨段连续抽取。**每 8 章一笔轻收口（游标+scoped commit，秒级断点保护，非停止点）；§3 十查重项（嵌入扫描/召回探针/judge/Neo4j 导出/校准报告）按段收口执行，批级只做引文预检与游标推进。判据：各段章数清零（游标对账）、十查留痕齐、pending 清尾。
- **U-C09 全库终审十轮**（镜头）：R1 全库对账（章/实体/边数 vs STATE+Neo4j 实查）；R2 孤悬引用（**Cypher 图上全扫**）；R3 时间轴一致性（tick 单调/instant 回退合法/无墙钟）；R4 别名合并审计（存疑分开建复核+0.85 门槛抽样）；R5 矛盾清点（quarantine 三子类分布+🔴超期）；R6 **confirmed 抽检 ≥95%**（随机 30 条对 clean_full.txt 原文核证据，Reranker 支撑度重排辅助）；R7 隔离区报告可用性（请你确认清单成型可读）；R8 终局重放双跑幂等；R9 许可/D-004/DeepSeek 抽检费用终对账（≤¥5）；R10 红队终局。留痕格式照《本体构筑-工单》§3。
- **U-C10 交接文书**：判据对照/全库统计/隔离区报告移交/待确认/经验候选。

## §3 每批十查（批收口前逐项留痕，格式同本体构筑自审日志）
①工作区健康（git 干净/STATE 对账）②边界表切片正确（章题与首行对质）③R6 生效（抽取规范含 R6 全文+候选零元文本抽检）④reference_time=伪锚点无墙钟 ⑤抽取量合理区间（空记录须记原因：纯对话/过渡章合法）⑥quarantine 分流正常（矛盾/低置信/超期各归其位）⑦去重与双轨命中入账（别名四分类+UNIQUE 拦截+嵌入相似度扫描/confidence 上调或隔离，不静默）⑧证据四元组抽检 2 条对原文（本地 judge 锚定评分辅助，低分→quarantine）⑨verified_against 真实三件套（**脏工作树不作锚——锚点协议 v2：只锚已提交状态**）⑩STATE 游标+scoped commit（含 Neo4j 增量导出）。

## §4 铁律与服务纪律
照《P1执行区/闲时任务提示词模板.md》⑦⑧ + 《本体构筑-工单》§4（工单不可改/到点回队是常态/不许降标收工/实验版与 cbb/ 代码只读复用）。另加：**单章抽取反复失败 ≥3 次跳章标 pending 不阻塞批次；每 8 章必须收口一次（commit 频率=断点密度）；主抽取路线禁用付费 API——唯一例外=§0 第三方抽检（≤¥5 硬顶，超限即停，D-004 照旧）**。

## §5 终报格式
照《本体构筑-工单》§5，另加：全库统计（章/实体/边/隔离分布/confirmed 抽检结果/DeepSeek 抽检费用对账）、Neo4j 查询样例 3 条、阶段四（P2：门 2/影子审计/reranker 检索重排/KBI 联邦）移交清单。
