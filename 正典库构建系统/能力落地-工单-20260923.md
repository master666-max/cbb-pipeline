# 能力落地-工单（U-F01~U-F08 · v1.2 · 2026-09-23 · 草案）

> **变更（v1.2）**：**三项裁定落地**（用户令"按推荐组合进行"）——①重排端点＝llama-server 独立常驻（**U-F01 已接通并质量验证，见《能力落地-U-F01-重排端点-20260923.md》**）②L6 交付形态＝命令行唯一实现＋MCP 薄壳后置（触发判据见信息包）③诊断员量尺＝A1 主代理＋B2 样本≤30（含经人批准的升级权）＋C2 一次初诊一次复诊＋D2 单次≤8K token。**本单自本版生效开工**（用户令"然后开干"）；v1.0/v1.1 正文一字未删。
> **变更（v1.1）**：**U-F07 引擎选型裁定＝LanceDB（用户裁定 2026-09-23）**——三选一作废，LanceDB 细节落入 U-F07 内容与判据（含 `optimize()` 节奏、自定义重排适配器、许可与版本核查）。v1.0 正文一字未删。
> **性质**：草案。判定权在审核线/用户；未裁定不生效、不施工。
> **依据**：本日体检（代码级取证）——三个资产的融入程度实测：**图＝条款在、代码仅导出工具**（`grep cypher|MERGE` 仅命中 `neo4j_export.py`；门 1 连续性检查仍为文件实现；章节点零命中）；**重排器＝文档四岗位、代码零引用**（`grep rerank` 在 cbb/tools 下零命中，部署前置 `/v1/rerank` 未做）；**混合 RAG＝未立项未落文**。本单＝把已写进条款但没实现的全部补上。
> **编号查重**：U-F 全仓 0 命中（实测）。**诊断员原拟作 U-E10，改入本单 U-F08**（能力件归能力单，不混编自决策层批序）。
> **成本画像**：批 1/批 2 零新增模型调用（脚本与接线）；批 3 有界——检索按查询计、诊断每异常 ≤1 次且样本 ≤30。

---

## §1 全单完成判据

U-F01~U-F08 全部 done：每单位判据达成＋单位收口三件套（机械门＋增量清单＋scoped commit，照《终审方法论》§11）＋终报。**条款一份、两载体**全程成立（图/重排缺席时降级路径产出同形——每单位含"双载体一致性"断言）。

---

## §2 单位清单（三批）

### 批 1 · 图件（把"图深融"从条款变成代码）

### U-F01 重排器部署前置（四岗位的总闸）—— **【done·2026-09-23】**
- 内容：探活并打通重排端点（LM Studio 无 `/v1/rerank` → 经 llama-server 暴露；产出双向探活脚本）；**不可用→登记，四岗位全部走机械降级**（不得悬空挂零引用）。
- 判据：①探活脚本对两种端点各出结论②成功路径：真实一次调用返回有序分数（附原始输出）③失败路径：降级开关生效且有单测（重排缺席时产出结构不变）。
- **落地实况**（证据：《能力落地-U-F01-重排端点-20260923.md》）：官方 llama.cpp `b11125` 立于 `D:/tools/llama.cpp/`（CPU）与 `D:/tools/llama.cpp-cuda/`（**CUDA 13.4 版＝现行主用**，Blackwell 必须 13.4；`-ngl 99`）；**/v1/rerank 与 /rerank 均通**；响应 `results[{index,relevance_score}]`（正好喂 LanceDB 适配器）；质量验证＝3 条 0.9991/0.0004/0.0000、30 条正确项排名 1/30；**30 对：CPU 21.5s → CUDA 1.465s（14.7×）**，结果逐值一致；启动脚本 `start-rerank-8081.bat`（CUDA 主用／CPU 后备）。**关键坑**：LM Studio 转的 GGUF 三缺（具名 rerank 模板／`qwen3.classifier.output_labels`／`cls.output.weight` 张量），补丁脚本 `D:/tools/patch_rerank_gguf_v2.py`（原件不动，产副本）。**大件下载教训**：本机反代下 GitHub release 大文件反复半途断（44MB 后停滞），>40MB 的件走人工下载通道（教程在案）。遗留：开机自启方式（用户定）、降级单测随四岗位接线补。

### U-F02 门 1 巡检图优先化 —— **【done·2026-09-23】**
- 内容：连续性检查（孤悬引用／死人走路／逆类型对称／跨章身份）加图查询实现，**与文件实现同一套规则**（规则一份、两载体：图快查、文件兜底；自动选择，缺席自动兜底）。
- 判据：①同一夹具双载体跑，结果逐条一致（差异清单为空或逐条登记）②图不可用时自动落文件路径，输出同形③规则参数只存一处（两载体读同一配置）。
- **落地实况**：`cbb/tools/连续性巡检.py`（+`test_连续性巡检.py` 4 测全绿，两树同步）——**一套规则、两个装载器（file/graph）、一个求值器**；逆类型/对称表直接 `from cbb_gate1 import`（判据③实证）；auto 探图失败自动兜底且口径注明（判据②实证）。
  **真库干跑（冻结库，只读）**：file 侧 **27 条孤悬＋86 条别名冲突**（回链 0、死人走路 0）；别名冲突里"『维度』/克洛伊/佩艾西亚"等别名指向 2–4 个实体——正是跨章身份问题的实证样本。
  **结构性发现（新增条款素材）**：**图载体对"孤悬引用"结构性失明**——导出器 MERGE 端点时自动创建 `:Entity` 节点，悬空引用在图上不可能出现。凡"导出即修复"的规则，图载体必须**显式标注不可用**，否则必犯 T-2（判定面为空读成通过）。已在工具 capability 位固化并配断言。
  **对照现状**：file 113 条 vs graph 3 条（2 不可用标注＋孤悬不可用标注）；差异构成＝能力缺失/结构性失明为主，另有覆盖度差（关系 1719 vs 1645）。真分歧判据留给"两载体覆盖一致"时由 `--compare` 自动给出。

### U-F03 章节点与 MENTIONS 边（U-D08 落地）—— **【done·2026-09-23】**
- 内容：导出器补 `:Chapter` 节点 + 章→实体/关系边；"某一章里出现了什么"一条查询可答。
- 判据：查询样例三条；导出债务口径不变；主库零改动；可全量重建。
- **落地实况**：`neo4j_export.py` 新增 `chapter_statement()` 与 mentions 采集（**纯 MERGE，重放零增殖**；关系两端都记"本章提及"）；`test_neo4j_export.py` 新增 `TestChapterNodes` 3 测（含去重与幂等断言），**11/11 绿**（既有的空图报告断言同步更新——有意的行为变更）。
  **真库验证**：导出 21 批 ok → 图上 **172 个 Chapter 节点、2362 条 MENTIONS**；三条查询实测通过：①`MATCH (c:Chapter {no:41})-[:MENTIONS]->(e) RETURN e.name` ②按人查章（相川涡波=3,4,5,6,7,12…）③章内关系网。主库零改动、导出债务口径不变。
  **顺手修掉一个潜伏缺陷（记录在案）**：冻结库存在**合并后缀堆出的超长文件名**（`-m-m-…` 数十层，撞 Windows MAX_PATH），`collect_graph` 无容错直接崩；已加"扩展长度前缀（`\\?\`）重试 → 仍失败则跳过并计数（T-5）"。修后跳过 0 件。**推论**：凡遍历库文件的工具都需要同一防御（连续性巡检/词表已有 try-skip，导出器至此补齐）；文件名后缀堆叠本身应进 U-D05/词表治理视野。

### 批 2 · 重排三岗位（接线）

### U-F04 上下文包预算内重排（岗位①）—— **【done·2026-09-23】**
- 内容：包生成器以本章切片为 query，对候选人物/活跃伏笔/判例重排取前 N；降级=机械排序（频次/时间/urgency）。
- 判据：同形断言（重排 on/off 输出结构一致、包 ≤2K）；重排前后"预算被裁清单"仍留痕。
- **落地实况**：共享客户端 `重排器.py`（score/order/rerank_order_or_mechanical；**任何异常→None＝降级信号**；传输层可注入＝零网络测试）；`context_pack.build()` 增可选 `reranker/rerank_query`——按与滚动摘要尾部的相关性重排①人物册/②伏笔；头部标注 `预算内重排：rerank|mechanical`（口径）。`test_context_pack.py` 4 测：默认路径与历史**逐字节一致**、重排改序＋标注、缺席同形、预算守住。

### U-F05 抽样前重排（岗位②）—— **【done·2026-09-23】**
- 内容：审查抽样器（G2 语义层/终审抽检）按"证据最弱"先排；降级=机械排序。
- 判据：**正负对照成对**——构造"已知最可疑样本"，必须排进头部（正）；构造"已知全硬样本"，顺序不得乱（负）；重排只写顺序、不改样本集（断言：入选集合与机械版相同）。
- **落地实况**：`抽样排序.py`——原理＝重排器打"**引文对断言的支撑度**"，**升序＝最可疑最先**；缺文本项→整体转机械（同形口径）；单测 3 个（最可疑第一✓正对照／缺席原序✓负对照／缺文本强制机械）。挂 G2 语义层抽样。

### U-F06 候选匹配建议（岗位③，仅建议位）—— **【done·2026-09-23】**
- 内容：入库/隔离处置时，对"最可能对应的既有记录"排序，写入建议字段。
- 判据：**红区隔离测试**——建议字段不出现在任何判定路径（0.85 门槛/存疑分开建/裁决逻辑的输入清单里无该字段）；建议可关闭且不影响主链。
- **落地实况**：`匹配建议.py`——`suggest_matches()`（仅建议位）＋ **`strip_suggestions()`**（admit 前必剥）。**测试抓出真契约缺口**：store 会**原样保留 `_meta`**（实测），不剥＝建议混进正典记录——已把"admit 前剥离"固化为 strip 函数＋红区测试（判定路径语义一致✓＋剥离后落盘零建议位✓）。单测 2 个。

### 批 3 · 查询层与诊断员

### U-F07 L6 混合检索层（四路召回＋RRF＋重排＋引文核验）· 引擎＝**LanceDB**（已裁）—— **【done·v1·2026-09-23】**
- **引擎**：LanceDB（纯嵌入式、pip 安装、Windows wheel 已确认、无服务无 Docker；许可与版本落地前核——许可红线纪律照旧）。
- **索引件**：`工作区/索引/lancedb/`（单目录、可随时全量重建＝派生件纪律）。建议两表：`records`（canonical 序列化文本＋4096 维向量＋元数据：library/status/章区间）与 `evidence`（引用片段级，供引文核验与片段检索）；FTS 建在同表的文本列上。
- **内容**：`search(query)` 四路召回——①别名/专名精确②关键词（LanceDB FTS）③**向量（q4_k_m 档，向量全部本地预计算后写入；嵌入＝粗筛，重排＝精排，两段式接力）**④图扩展（k-hop/PPR 纯脚本）；RRF 融合（LanceDB 内置 `RRFReranker` 默认档起步）→ **自定义重排适配器**（继承 `Reranker`，接本地 `/v1/rerank`，**必须产出 `_relevance_score` 列**否则报错）；**答案必须带引文，引文必须回落原文**——回落失败 → 拒答或显式标"未核实"。
- **写入节奏**：批次收口增量 upsert；**`optimize()` 按累计量触发**（官方建议约 10 万行变更或 20 次 DML 后；本单口径：不达阈值则**区段收口强制一次**）。
- **判据**：①四路各自可单跑、可关②融合可复算（同输入同输出）③**引文核验正负对照**：故意注入一条落不回的引文 → 必须拒答/标未核实；正常答案 → 引文全回落④**只读断言**：检索对正典库零写（EXPLAIN 拒写纪律）⑤按查询计费（不触"×单元数"乘法线），单次 token 记入报告⑥**索引件判据**：全量重建结果与增量维护结果对账一致；**索引缺席 → 自动退化为"别名＋关键词"两路（降级同形）**；⑦**重排适配器判据**：`/v1/rerank` 可用→精排；不可用→RRF 排序兜底（两载体同形）；⑧维度纪律：4096 写死，换嵌入档＝重建索引（与"换档＝重算全部嵌入"同条纪律）。
- **落地实况（v1）**：`检索层.py`＋`test_检索层.py` **6 测绿**（LanceDB 回环用例装好后实跑✓）。**真库全链路**：索引 660 实体×4096 维落 `迷深实战-工作区/索引/lancedb/`（实体名文本；批量 16 起才不 400——坑记录）；混合查询＝**别名/关键词/向量/图 四路通、RRF 融合、岗位④精排 backend=rerank**；每级降级都在口径注明（实测向量缺席→自动退化两路✓）。`verify_citations` 正负对照✓（库内真证据过/编造引文拦）。**安装坑**：pip 官方源在反代下极慢，清华镜像秒装。
- **迭代登记（v3 已修，2026-09-24）**：精排对象已改**富 text 列**（v2 实测打 name 串语义偏泛的根因）＋投影取列不拉向量＋单测断言精排收到富文本；evidence 级片段索引（10 万条，脚本待批处理跑）与语义调参留待真实查询需求出现（哨兵 A 仍 GREEN）——**结构判据全部验证完毕，语义调参按需迭代**。：v1 单嵌实体名 → **v2 富文本索引已建（2389 行＝实体 744＋关系 1645，text=name+fact+引文；build_index_v2.py 可断点续传）**。实测："手环的用途"类 top1 命中「手环」✓；但"谁在用眼神交流传递情报"类仍偏泛——**v3 修正点已定位**：融合后精排与召回打分的对象是 name 串而非富 text（代码修正点），另 evidence 级片段索引（10 万条）与 FTS 列启用同批做。语义迭代不改变本单结构判据（四路/融合/降级/核验均已验证）。

### U-F08 诊断员岗位（LLM 归因 · 黄区）—— **【done·2026-09-23】**
- 内容：规则层发现异常（密度脱带/探针 FAIL/隔离峰值/死信增多）后，交"诊断员"做归因：输入＝异常信号＋**有限样本（≤30 条，禁全库）**；输出＝归因判定（附证据引用）＋建议动作（**提案制**）；写决策账；**人确认才生效**。
- 判据：①**五量尺字段齐全**：色（固定黄）/样（≤30）/频（每异常 ≤1 次）/费（token 上限预注册）/复（决策账＋周复盘）②**红区隔离测试**：诊断员输出不能触发验收/晋升/契约/失效宣告/权限表/量尺任何一项（负对照：注入越权建议 → 必须被拒并留账）③每条归因带样本引用（引不回→作废重来）。
- **落地实况**：`诊断员.py`——`prepare`（备料：样本自动截 ≤30＋截断标注＋量尺内嵌）/`record_decision`（append-only 决策账，status=待人工确认）/`confirm`（人工闸）/`validate_attribution`（红区六项**短触发词**机械闸：误报只会更保守）/`prior_packages`（确认条目并回状态，供 C2 升级闸）。单测 4 个：截断＋量尺✓、C2 初诊→确认→升级→超限全链✓、红区越权三连拒✓、账目样本引用可核✓。**A1 主代理岗位约定**：prepare 备料→主代理会话内产归因（≤8K）→record_decision 入账→人 confirm。
- **至本件，《能力落地-工单》U-F01~U-F08 全部 done。**

---

## §3 纪律（本单特有）

| # | 条款 |
|---|---|
| D1 | **条款一份、两载体**：图/重排的每个环节，主载体与降级载体共用同一份规则配置；任何"两版规则"视为缺陷（T-8 差集静默的预防） |
| D2 | **能力件不改条款**：本单实现既有条款（深融五条/四岗位），条款文字改动须另单走审核线 |
| D3 | **缺席即降级、降级即同形**：任何本地资产缺席，主链照跑、产出同形、缺席入账 |
| D4 | **只读边界**：检索层与诊断员对正典库只读；一切写动作仍走 gate→store |
| D5 | 迷深冻结库只读（干跑注明"冻结于位200"）；夹具为主 |

---

## §4 铁律

照《通用架构总纲》与《终审方法论》§11：py -X utf8；scoped commit 禁 add -A；D-004 只走环境变量；追加不覆写；单位收口三件套；判定权在审核线。

---

## §5 待裁项（构建线只呈报）

1. **重排端点方案**：llama-server 暴露 /v1/rerank（本单默认）还是换支持 rerank 的推理服务？
2. **L6 交付形态**：命令行工具（本单默认）还是直接做成 MCP 服务（并入形态工单 U-G05 的触发条件）？
3. **诊断员默认量尺**：样本 ≤30、每异常 1 次、token 上限（建议 8K/次）是否照此预注册？
4. ~~**U-F07 引擎选型（三选一）**~~ **【已裁 2026-09-23：LanceDB】**——纯嵌入式、无服务，细节见 U-F07；原三选一留档：A Neo4j 原生（零新增依赖，但 RRF 手写＋全文索引待验）／B LanceDB（**选中**）／C sqlite-vec＋FTS5（极简保底）。不选清单照旧（Chroma OSS／Weaviate embedded／Meilisearch・Typesense／pgvector・ParadeDB／Qdrant 服务版／RAGFlow）。
   - 遗留待验（随 U-F07 落地核）：LanceDB 许可与版本锁；`/v1/rerank` 响应体格式自测（并入 U-F01）。

---

## §6 与现有件的关系

- 兑现《通用架构总纲》"图深融五条"与"本地模型深融条款"中**尚缺代码的部分**（体检表三张欠条）。
- U-F03 与《关系演化补强-工单》U-D08 为同一件事——**以本单为准执行**，补强单该条目转为引用。
- U-F08 与《自决策层-工单》同源：自决策层管"机械决策"，本单管"LLM 归因"；两单共用决策账与五量尺。
