# U-B02 cbb-coordinate · 十轮自审日志

> 工单 §3 协议。单位目标：吸收追加序纪律+查重占位；保留幂等坐标、断点协议。判据=单测绿含乱序插入与查重占位用例。

## R1 契约符合性
检查了 cbb/cbb-coordinate/{cbb_coordinate.py,test_cbb_coordinate.py,SKILL.md} × 17 单测全绿（含 v1.0 回归 7 例）；block_id 形态 v01c0001p0001 与 Record.evidence 定位（vol/chapter/line）坐标口径一致；本单位无直接 Record 载荷产物（坐标是 Record.evidence 的上游事实源，由 gate1 消费）。
发现=零。
处置=零。

## R2 证据链
检查了 NumberingRegistry docstring 与 SKILL.md 吸收声明 × 回溯源：追加序纪律=graphify-novel 详报 §2（SKILL.md:112「Event IDs reflect insertion order, not story position…out-of-sequence IDs are expected」，行号补强 v1.1 #9）+ §3 可借鉴 4；查重占位=knowledge/reflections R-015（编号即占位，2026-09-13 三方争用实录）；story_pos=None 沉底=graphify-novel ch.? 章未知诚实标注（v1.1 #11）。
发现=零。
处置=零。

## R3 幂等与断点
检查了 test_cache_hit_and_identical_output（缓存命中逐键一致）× test_register_idempotent_by_key（同键双注册返回同号+账本仅 1 行）× test_replay_rebuilds_state_identically（断点重放重建全同+计数正确续接）× test_append_only_never_mutates（旧行逐字节不变只追加）× CLI 冒烟（同命令双跑均 EVT-0001）。
发现=零。
处置=零。

## R4 边界与异常
检查了 test_bad_story_pos_rejected（负数/字符串 story_pos 拒）× test_preamble_not_silently_dropped（无章标记文本收容章 0）× locate_quote 章号错/引文不存在返回 None（测试在案）× register 写盘失败时内存态不推进（代码路径：先落盘后更新内存，注释在案）；GBK 坏编码输入：process_file 用 utf-8 解码，坏编码抛 UnicodeDecodeError 即失败可见（不静默）——行为与实验版一致（保留面）。
发现=零。
处置=零。

## R5 Windows 纪律
检查了本单元命令回放 × 测试与 CLI 均 `py -X utf8 -B`；JSONL/JSON 全 UTF-8 ensure_ascii=False 落盘；中文键名（测试事件）CLI 注册实测无 GBK 吃字。
发现=零。
处置=零。

## R6 许可红旗
检查了 cbb_coordinate.py 全文 × 零外部代码复制（坐标/缓存为自产 v1.0 移植，NumberingRegistry 为自设计；吸收项为设计思想：追加序纪律/查重占位纪律，非文本/代码搬运）；grep 无外部仓专名文本。
发现=零。
处置=零。

## R7 Step 0 三条件对照
检查了本单位与三条件关联 × 坐标层不涉 R6 元文本/锚定/嵌入去重实现；block_id 的确定性（sha256 内容哈希）为 U-B07 去重提供内容锚——无配置错位。
发现=零。
处置=零。

## R8 U-A17 吸收项对照
检查了工单 §2 U-B02 行 × 逐条：①事件 ID 追加序纪律（乱序是预期，永不重排）→ test_out_of_order_insertion_expected_never_renumber（story 序视图重排查询、ID 永不改+补录后既有 ID 不变反例断言）✓；②编号先查重即占位（R-015 三方互证）→ test_register_idempotent_by_key+test_occupy_is_immediate_disk_append（重放后不双发）✓；保留幂等坐标（回归 7 例）✓、断点协议（缓存命中+账本重放双机制）✓。
发现=零。
处置=零。

## R9 测试覆盖
检查了 17 用例构成 × v1.0 回归 7（实验版同套件 9 例中坐标面 5+缓存 2 全保留，三态桩 2 例随桩移出 cbb-store 单位）+v2 新增 10（追加序 5+查重占位 5）；新行为乱序插入与查重占位均有正例+反例（永不重排反例断言在案）；总用例数对照实验版 coordinate 9→17（净增）。
发现=零。
处置=零。

## R10 红队终局
检查了审核线视角四个打回候选 × ①「三态桩去哪了？」→ SKILL.md「v1.0→v2 变更」节显式登记移出至 cbb-store（U-B07 接管），实验版原桩 FROZEN 在案可对照，非静默删除；②「跨进程竞态双发号怎么办？」→ 类 docstring 显式披露残余竞态边界（单进程 reload() 纪律，跨进程互斥不在本层职责）——不假装解决没解决的问题；③「by_story_order 会不会隐性重排？」→ 返回新列表、永不写字段，test_append_only_never_mutates 断言调用后账本逐字节不变；④「.gitkeep 删了算不算违规？」→ 系本会话 U-B00 自建占位符，真文件落盘后移除，非既有文件（铁律不适用）；⑤「CLI 双模式无参时行为？」→ 返回码 2+FATAL 提示（代码在案）。
发现=1 条记录项（②竞态边界以披露代替越层解决——保守决策）。
处置=记录（非缺陷，文档化边界）。

## 出口门判定
R1-R10 十轮全部留痕；R9 零发现；R10 唯一发现为边界披露记录项（非缺陷）。**出口门通过**，准予标 done 并收口 commit。
