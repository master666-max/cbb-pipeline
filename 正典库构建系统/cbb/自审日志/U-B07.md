# U-B07 cbb-store 双轨+约束+cbb-merge 并入 · 十轮自审日志

> 工单 §3 协议。单位目标：双轨合并重构（一致重复上调合并/矛盾隔离+Verdict）；吸收 UNIQUE 约束族/漂移钩子/ON CREATE-ON MATCH/时序回放/安全围栏/cbb-merge 并入（Keep/Range/Flag/Pick+冲突表）；保留三态+supersedes。判据=单测绿含双轨分流+约束防重+漂移钩子。

## R1 契约符合性
检查了 admit/supersede 构造内 cbb_contracts.validate_record（v2.0 严格态）+ contradiction_verdict 构造内 validate_verdict（critique 键序/分带一致/conflict 必附 rule_applied 三强制全过——U-B01 语义在库层复用）× test_contradiction_quarantine_and_verdict_not_silent_merge 双保险复验；multiversion_merge 为纯计算不产契约载荷。
发现=零。
处置=零。

## R2 证据链
检查了模块 docstring+SKILL.md × 回溯源：双轨合并=工单 §2 U-B07 原文（一致重复→confidence 上调合并；矛盾→quarantine+Verdict 不静默合并）；UNIQUE 约束族=webnovel-writer 详报 §2（index_manager.py:300-353 aliases 复合 PK/relationships UNIQUE(from,to,type)/appearances UNIQUE(entity,chapter)——**GPL 仓：按其详报合规提示仅按设计重写零源码引用**）；漂移钩子=story-systems 详报 §2（CONVENTIONS.md:26-37 sources+verified_against drift hooks+改场景必须配对 bump）；ON CREATE/ON MATCH=neo4j MERGE 语义（详报 §3 吸收）；时序回放=webnovel-writer 详报 §2（knowledge_query.py:16-45 state_changes WHERE chapter<=? ORDER BY 顺序覆盖）；安全围栏=oh-story 详报吸收⑦（写入安全围栏）+webnovel-writer 吸收⑦（命令白名单思想）；Keep/Range/Flag/Pick=claude-book 详报 §2（style 合并四动作）+§3 吸收③；CORROBORATION_BUMP=2.0/Pick 取最长=SKILL 声明的 CBB 定约（上游无对应数值规则，不冒充）。
发现=零。
处置=零。

## R3 幂等与断点
检查了测试双跑（两次 Ran 18 OK）× admit 已存在跳过（文件写后不改）× status_transition 目标态已达不重复入账（repeated=True）× register_alias/record_appearance 同键二次=False × dual_track 一致重复经 supersede（旧件字节不动有专测）× jsonl 全 append-only。
发现=零。
处置=零。

## R4 边界与异常
检查了 root fence 四层逃逸路径实测拦截（record_id 注释攻击）× 符号链接祖先级拒写（本机 Windows 实测可建 symlink，专测过）× multiversion_merge 空输入/单源退化（flag 语义显式声明）× drift SHA 缺失（bool(va.get)空=False 不误报）× 时序回放章前无状态返回 {} × supersede 不换 id 拒。
发现=1 处当场修复：multiversion_merge 首稿分支序错（单源字段被 all-equal 分支先吞→Flag 永不可达）——被 test_keep_range_flag_pick 抓出，已修（单源判定前置+冲突表含 flag 项），复验 18/18。
处置=修复（已复验）。

## R5 Windows 纪律
检查了本单元命令回放 × `py -X utf8 -B`；symlink 测试带 skipTest 兜底（本机实测可用未触发）；中文 JSONL/路径 UTF-8；无 shell 文本处理。
发现=零。
处置=零。

## R6 许可红旗
检查了模块全文与 SKILL 声明 × **GPL 仓 webnovel-writer 零源码引用**（时序回放/UNIQUE 约束按详报设计重写，SKILL 有专门合规声明节，详报为唯一转述层——该详报合规提示原文要求）；AGPL neuro-book 同前例零接触；claude-book（MIT）/story-systems（MIT）/oh-story（MIT）设计思想转译；四动作词汇 Keep/Range/Flag/Pick 为功能性术语。
发现=零。
处置=零。

## R7 Step 0 三条件对照
检查了三条件 × 条件③去重（本地嵌入）相关项：UNIQUE 约束族+身份键双轨=去重的约束层与规则层实施位（嵌入相似度归 P2 graphiti 管道——工单 U-B08 真管道版）；verified_against SHA=内容锚（与嵌入去重互补的字段层）；条件①②不属库层。配置无错位。
发现=零。
处置=零。

## R8 U-A17 吸收项对照
检查了工单 §2 U-B07 行 × 逐条：双轨合并（consistent-duplicate 上调+证据并集+supersede 新版本/contradiction 隔离+Verdict+不静默合并——三轨专测）✓；UNIQUE 约束族（三约束落地+防重专测+关系三元组唯一接入双轨）✓；verified_against 漂移钩子（drift_check+stale_records 双测）✓；ON CREATE/ON MATCH（admit_or_merge=MERGE 语义，on-create/一致/矛盾三态）✓；时序回放（log_state_change+entity_state_at_chapter 顺序覆盖+章前空态）✓；写入安全围栏（根围栏四层逃逸拦截+symlink 拒写+命令白名单思想=零 shell）✓；cbb-merge 并入（multiversion_merge Keep/Range/Flag/Pick+冲突表，独立技能不拆=用户裁决，SKILL 声明）✓；保留三态+supersedes 版本化（迁移幂等/旧件不动/resolve_latest 链三测）✓。
发现=零。
处置=零。

## R9 测试覆盖
检查了 18 用例构成 × 实验版 store 11 例→本体版 18（保留面全保留：路由/三态/迁移幂等/supersede 链；三态桩旧例已由 store 本体覆盖）；双轨三轨各专测+Verdict 契约复验；约束族三约束各专测；漂移新鲜/stale 两态+全库扫描；回放三断言；围栏两测（其一真实 symlink）；合并四动作+退化+冲突表；每域反例在案（逃逸拒/不换 id 拒/同键拒）；R4 真缺陷 1 处被测试抓住并当场修复——测试纪律自证有效。总用例数 11→18 净增。
发现=零。
处置=零。

## R10 红队终局
检查了审核线视角六个打回候选 × ①「CORROBORATION_BUMP=2.0 与 Pick 取最长是拍脑袋？」→ 双处声明 CBB 定约+上游无对应数值不冒充（同 U-B06 🟡0.8 处理式）；②「一致重复轨 supersede 会不会让版本号虚涨？」→ 是预期语义：每次独立佐证=新版本（可追溯的置信演化），旧件不动不丢数据——版本链本身就是审计记录；③「矛盾轨 Verdict 的 issue_id 是合成的非真 Issue？」→ issue_id=dual-track-<身份键> 合成键，Verdict 契约只约束形态不查重 issue 注册表；quarantine item_id 承担登记职责——双轨文书与隔离条目一一对应（iid 在返回值）；④「iter_records 全库扫描身份键性能？」→ jsonl-file 后端 P1 规模适用，P2 Neo4j 后端换约束索引（backend 分支已在）——边界如实；⑤「relation_triple_exists 每次全扫？」→ 同④；⑥「multiversion Range 把 int 变 'min-max' 字符串丢类型？」→ 如实登记：区间表达为展示态，原数值在 conflicts.values 可回溯——类型损失已知且可恢复。
发现=2 条记录项（⑤规模边界披露、⑥区间类型损失已知）。
处置=记录。

## 出口门判定
R1-R10 十轮全部留痕；R4 一处真缺陷当场修复并复验（18/18）；R9 零发现；R10 两条边界记录项（非缺陷）。**出口门通过**，准予标 done 并收口 commit。
