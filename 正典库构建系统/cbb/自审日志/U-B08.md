# U-B08 集成冒烟 · 十轮自审日志

> 工单 §3 协议。单位目标：离线版（必须）全链断言+双跑一致+EXIT 0；真管道版（条件）探活，失败标 blocked 留痕不阻塞。

## R1 契约符合性
检查了 run_smoke.py 全链 × gate1 Issue 构造期 validate_issue 复验（冒烟第④段断言全量合规+caliber+command）；store Verdict 构造期 validate_verdict（冒烟第⑥段断言）；锚点树 verified_against 三件组随树构造（第②段断言）。
发现=零。
处置=零。

## R2 证据链
检查了冒烟链路设计 × 工单 §2 U-B08 原文逐段对照：coordinate→anchor（双时间轴：as-of/缺坐标不可见/倒叙还原/instant 解析/倒计时五断言）→extract stub（四层防御：元文本章零抽取/注入块拒抽/硬检查/禁词/防先验/别名门槛/施工参数）→gate1 三域（悬空证据+死人走路+Issue+EXPLAIN）→quarantine（三子类+urgency+🔴+[?]）→store 双轨（一致重复上调/矛盾隔离+Verdict/UNIQUE/漂移/回放/Range 合并）——每一环都是前五单位已测行为的**集成级串联**（非新行为）。
发现=零。
处置=零。

## R3 幂等与断点
检查了冒烟双跑三断言（摘要全等/文件集不变/库文件逐字节不变）+双跑零新增失败 × REAL_EXIT=0 实测（重定向文件后取 `$?` 非管道尾状态）× 全程 tempfile.TemporaryDirectory（工作区零残留）。
发现=2 处当场修复：①冒烟首稿三断言不可重入（UNIQUE 断言写死首跑 created=True 形态/fixture 未传 confidence 基线）——双跑即抓出，已修（断言改为「重放永不创建」语义+fixture 显式 confidence=80）；②**集成双跑暴露 store 两个真幂等缺陷**（find_by_identity 可命中已取代旧版本；一致重复轨对已合并观察无限叠版本）——已修（find_by_identity 取活版本最高版次+证据子集幂等守卫，U-B07 补丁随本单提交，store 单测 18→19 含新守卫专测）。
处置=修复（已复验：store 19/19+冒烟 74/74）。

## R4 边界与异常
检查了冒烟 fixture 设计 × 含元文本章（第1章标题命中启发式）+注入行（第20章内嵌 ignore previous）+死亡实体（帕林）三类边界样本同链跑通；gate1 坏件双类型（悬空证据/死人走路）分别命中 validate/continuity 两域；漂移扫描在占位 SHA 库上全 stale 捕获 ≥1。
发现=零。
处置=零。

## R5 Windows 纪律
检查了本单元命令回放 × `py -X utf8 -B` 全程；真退出码取法（重定向+$?，规避管道尾假 0）；探活超时 3 秒不挂死。
发现=零。
处置=零。

## R6 许可红旗
检查了 run_smoke.py 全文 × 零外部代码引用（六技能自产集成）；探活仅布尔存在性。
发现=零。
处置=零。

## R7 Step 0 三条件对照
检查了离线链三条件落位 × ①R6：元文本章零抽取+注入拒抽双断言在链（冒烟第③段）；②伪锚点禁墙钟：锚点树 2000-01-01 起步断言在链（第②段）；③去重：UNIQUE+双轨一致重复断言在链（第⑥段）——三条件在集成层各有可证断言（嵌入去重归真管道 blocked 项）。
发现=零。
处置=零。

## R8 U-A17 吸收项对照
检查了工单 §2 U-B08 行 × 离线版=必须项已全绿（74/74 EXIT 0 双跑一致）；真管道版=条件项实测探活（LM Studio=True/Neo4j=False→**blocked 留痕**=cbb/smoke/真管道探活-2026-09-15.md，阶段三就绪条件已写明，不阻塞收口）——两分支均按工单处置。
发现=零。
处置=零。

## R9 测试覆盖
检查了冒烟 74 断言构成 × 六段链 70 断言+双跑幂等 4 断言；每个技能单位的核心新行为至少 1 条集成断言（非孤立单测）；全库单测总数（此时点）=33+17+23+27+24+18+19=**161≥73 基线**；反例在链（坏件/注入/元文本/矛盾轨四类负样本）。
发现=零。
处置=零。

## R10 红队终局
检查了审核线视角五个打回候选 × ①「双跑是同目录还是双目录？」→ 同目录连跑两遍（幂等强断言：文件集+库字节+摘要三全等），比双目录确定性更强——设计意图在 docstring 声明；②「smoke-out.txt 算不算垃圾？」→ 保留作运行证据（真退出码取证链），随单提交；③「探活会不会误判？」→ Neo4j 用 TCP connect（无驱动依赖），LM Studio 用 HTTP /v1/models——探的是可达性非正确性，判定文本如实写「探活未全通」不写「服务坏」；④「blocked 是不是逃避？」→ 工单原文「探活失败→标 blocked 留阶段三，不阻塞本单收口」——严格按工单分支，且留痕文件含就绪条件；⑤「库修复进 U-B08 commit 会不会污染单位边界？」→ 修复是本单集成双跑**直接暴露**的缺陷（单测盲区），修复+新专测随暴露单位走并 commit message 显式记录——非静默夹带。
发现=1 条记录项（③探针语义=可达性布尔，正确性验证归阶段三实装——边界如实）。
处置=记录。

## 出口门判定
R1-R10 十轮全部留痕；R3 两处当场修复已复验（19/19+74/74）；R9 零发现；R10 一条边界记录项。**出口门通过**，准予标 done 并收口 commit（真管道版=blocked 留痕，离线版=done 全绿）。
