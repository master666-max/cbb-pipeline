# U-B03 cbb-anchor 双时间轴 · 十轮自审日志

> 工单 §3 协议。单位目标：双时间轴模型重构（tick/instant/time+as-of+缺坐标不可见）；吸收倒计时到点兑现+章号轴回放降级；保留伪锚点禁墙钟+18 条精度抽检回归。判据=双时间轴单测绿+18 条抽检回归绿。

## R1 契约符合性
检查了 cbb/cbb-anchor/cbb_anchor.py × make_anchor_record 构造内 cbb_contracts.validate_record（v2.0 含 verified_against 必填）——test_tree_records_contract_valid_v2 断言三件套随构造落位；时间线条目（timeline entry）非 Record 载荷（是 anchor 技能内部数据结构，供 as-of 查询），不涉契约校验——边界清晰。
发现=零。
处置=零。

## R2 证据链
检查了模块 docstring+SKILL.md 双轴表 × 回溯源：双时间轴设计=neuro-book 详报 §2（tick 摄入序单调必填/instant 故事时间可回退「这正是必须双轴的原因」/time 人读不可比较/as-of 双语义=asOfTick 知识边界+asOfInstant 世界状态/缺坐标判不可见宁可漏召回不可泄漏——L24-27 逐条对应）；倒计时=chinese-webnovel 详报吸收项②（时间线绝对锚点+倒计时兑现纪律）；章号轴回放=webnovel-writer 详报（章号轴时序回放）；instant 解析自研=Step 0 判定（时间归一化双败→cbb-anchor 自研触发）+U-A17 §3「16 仓无现成」。
发现=零。
处置=零。

## R3 幂等与断点
检查了测试双跑（两次 Ran 23 OK 纯函数零副作用）× save_tree 同版本跳过+新版本新文件（test_save_versioned_no_overwrite）× asof/story_order/replay 均返回新列表不改输入 × cbb-skills git status 空。
发现=零。
处置=零。

## R4 边界与异常
检查了 test_tick_must_be_strictly_increasing（重复/回退 tick 拒）× make_entry 对负数/布尔/非整数 tick 与非整数 instant 拒（ValueError）× 缺坐标条目（instant=None）全查询面行为显式断言 × 18 条电池含无锚（missing_anchor）/指代不明（ambiguous_reference）/模糊量词（unresolved_time）三类坏输入 × compare_story_time 粗精度返回 None（宁缺毋滥）。
发现=零。
处置=零。

## R5 Windows 纪律
检查了本单元命令回放 × `py -X utf8 -B`；中文时间表述（那年冬天（回忆）/某个雨夜）UTF-8 测试在案无乱码；SKILL.md/py 全 Write 工具落盘。
发现=零。
处置=零。

## R6 许可红旗
检查了 grep CalendarPort/invalidatedAtTick/neuro × 模块与 SKILL.md 命中均为「转述自全量构筑版-分析报告/neuro-book.md」出处指引——正是 neuro-book 详报 §合规提示要求的引用方式（引我方转述文档，不链接其源码）；**AGPL 仓零代码/零标识符接触**（其源码符号零出现，实现为自设计）；其余吸收源（chinese-webnovel/webnovel MIT）仅设计思想。
发现=零。
处置=零。

## R7 Step 0 三条件对照
检查了 reference_time 禁墙钟条件 × ①源码禁墙钟静态检查（.now(/.today(/.utcnow( 零出现）+功能双检（输出无当前年份）单测锁定；②instant 全部来自文本解析（normalize_relative）或调用方输入，tick 来自摄入序——**无任何墙钟进入时间轴**（Step 0 的 reference_time 污染路径在此被结构性阻断：伪锚点 2000-01-01+i 从不冒充真实时间，真实故事时间只认原文解析）；占位 verified_at=1970-01-01 是常量非时钟。
发现=零。
处置=零。

## R8 U-A17 吸收项对照
检查了工单 §2 U-B03 行 × 逐条：重构双时间轴（tick/instant/time 三轴字段+build_timeline 单调强制+asof 双语义+缺坐标判不可见——test_asof_*5 例+test_missing_coordinate_invisible）✓；中文人读时间→instant 解析器自研核心（instant_from_relative，day 精度产出/粗精度不冒充/test_instant_from_relative_day_only）✓；吸收倒计时到点兑现（countdowns_due，到点即浮出+已兑现不算+超期持续浮出三态测试）✓；吸收章号轴时序回放降级（replay，instant 缺失不影响次序 test_replay_chapter_axis_fallback）✓；保留伪锚点禁墙钟（test_no_wall_clock）✓、18 条精度抽检全部回归通过（test_battery+test_battery_count_18 锁条数防缩水）✓。
发现=零。
处置=零。

## R9 测试覆盖
检查了 23 用例构成 × 实验版 anchor 10 例→本体版 23（v1 保留面全数保留：电池 18 条逐字不动+伪锚 3+契约/版本化；三态桩 1 例随桩移出）；v2 新行为 11 例（tick 单调反例/instant 回退正例/as-of 四组合/缺坐标不可见反例/故事序倒叙还原/time 不参与排序/回放降级/instant 解析/倒计时三态）；实验版 anchor 基线 10→23 净增。
发现=零。
处置=零。

## R10 红队终局
检查了审核线视角五个打回候选 × ①「占位 verified_against=1970-01-01 会不会污染生产数据？」→ 占位仅未传参时使用，test_placeholder_verified_against_still_valid 显式断言其存在（不自充真值），SKILL.md 注明生产管线应传真实快照——诚实占位而非伪造；②「normalize_relative 兼容读 tick 是不是魔法？」→ v2 上下文锚接受 day_offset 或 tick 两键（context_anchor.get("day_offset", context_anchor.get("tick"))），v1 锚树 canonical 两键并存（day_offset 兼容位=tick），文档在案；③「asof 双空=全知会不会被误用成默认？」→ 全知是显式调用（不传参），缺坐标不可见已由 test 锁定——泄漏面反例在案；④「story_order 相同 instant 稳定吗？」→ 排序键 (instant,tick) 确定性，test_time_never_participates_in_sorting 佐证；⑤「time 字段会不会被下游拿去排序？」→ 契约级约束（docstring+SKILL 表格「不可比较不参与排序」），gate1 时间反转检测只消费 compare_story_time 确定结论——消费面纪律在 U-B05 复核。
发现=1 条记录项（②双键兼容为过渡设计，v1 锚树退役后可收敛单键——不阻塞）。
处置=记录。

## 出口门判定
R1-R10 十轮全部留痕；R9 零发现；R10 唯一发现为过渡设计记录项（非缺陷）。**出口门通过**，准予标 done 并收口 commit。
