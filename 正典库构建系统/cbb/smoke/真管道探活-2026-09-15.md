# U-B08 真管道版探活留痕（2026-09-15）

> 工单 §2 U-B08：真管道版（条件）——LM Studio+Neo4j 探活通过则跑 graphiti 实入库；探活失败→标 blocked 留阶段三，不阻塞本单收口。

## 探活结果（run_smoke.py --probe 实测输出）

```
[probe] LM Studio(8080)=True Neo4j(7687)=False creds={'DEEPSEEK_API_KEY': True, 'NEO4J_PASSWORD': True} registry={}
[probe] 判定=BLOCKED——探活未全通，真管道留阶段三（不阻塞离线版）
```

| 探针 | 结果 | 方法 |
|------|------|------|
| LM Studio | ✅ True | HTTP GET 127.0.0.1:8080/v1/models → 200 |
| Neo4j | ❌ False | TCP connect 127.0.0.1:7687 超时/拒绝（服务未监听） |
| DEEPSEEK_API_KEY | ✅ 存在 | 环境变量存在性布尔（D-004：不读值不出值） |
| NEO4J_PASSWORD | ✅ 存在 | 环境变量存在性布尔 |
| 注册表兜底 | （未触发） | env 已命中则不查 HKCU\Environment |

## 判定

**BLOCKED（留阶段三）**——Neo4j 服务未运行（bolt 7687 无监听；与 task-037 环境基线「Docker daemon 未运行」一致）。按工单：不阻塞 U-B08 收口；离线版（必须项）已全绿（74/74 PASS，EXIT 0，双跑一致）。

## 阶段三就绪条件

LM Studio+Neo4j 双绿后，经 `cbb_extract.build_episode_kwargs`（伪锚点 reference_time+R6 注入，Step0 Tier2 已验证形态）接 graphiti 实入库（DeepSeek 抽取+text-embedding-qwen3-embedding-8b@q8_0 嵌入去重）——参数组装器已就位（本体版 v2 单测覆盖），仅缺运行时接线（run_graphiti_extraction 的 P2 实装位）。
