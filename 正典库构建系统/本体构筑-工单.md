# 本体构筑-工单（全量构筑版 · 阶段二 · v1.0 · 2026-09-15 · 审核线制）

> 本文件是唯一指令源（防漂移：一切细节以本文件为准，提示词只是指针）。执行代理不得修改本文件与 STATE 判据栏；发现工单与事实冲突→事实优先，写 `cbb/issues/` 并继续可继续的部分。

## §0 依据与裁决已定
- 路线：U-A17《跨仓综合报告与本体路线修订书》§3（阶段一验收 17/17 通过，task-047）。
- Step 0 三条件（硬约束）：R6 元文本防御必配／reference_time 故事伪锚点禁墙钟／去重本地 text-embedding-qwen3-embedding-8b@q8_0 4096 维。
- 用户裁决（2026-09-15）：**第四态 cut 不增设**（supersedes+归档覆盖）；**cbb-merge 先并入 cbb-store**；basic-memory-skills 无 LICENSE 正文按无许可处理（只读参考）。
- 实验版（cbb-skills/，FROZEN）：**只读**——可运行其测试作基线（必须 `py -X utf8 -B` 防写 pycache），禁止修改/删除/覆盖其任何文件。
- 本体落盘根：`正典库构建系统/cbb/`（全新目录，含 contracts/ 与六技能）。

## §1 全单完成判据（/goal 目标锚原文）
《正典库构建系统/本体构筑-工单.md》U-B00~U-B09 全部 done：每单位 §3 十轮自审留痕齐且出口门过；`cbb/` 全量 `py -X utf8` 单测绿且用例总数 ≥ 实验版基线 73；逐单位 commit 可证；U-B09 终报落盘。

## §2 单位清单（顺序执行；每单位判据=目标+十轮自审出口门+收口三件套）

### U-B00 基线与整改
- 目标：①`py -X utf8 -B` 跑实验版全量单测确认 73 绿（只读基线，失败即停单上报）；②U-A08 整改：产出 `全量构筑版-分析报告/graphify-novel-v1.1-行号补强.md`（基于 v1 补 行号引用 ≥10 处，头部标注版本与勘误来源，v1 不动）；③建 `cbb/` 骨架与 STATE 初始化。
- 判据：73 绿输出摘要入 STATE；v1.1 文件在位；cbb/ 目录树成型。

### U-B01 契约层修订（契约先行——唯一跨层接口）
- 目标：`cbb/contracts/` 四 schema 升版+校验器+单测。新增字段（源自 U-A17 §3 契约层修订）：Record +quote 原文引用/+verified_against 三件套(path+SHA+验证时)/+observation 分类词表；Issue +修复动作字段(P0-P3+精确命令)/+confidence 口径(deterministic|judgment)/+Cites 强制列；Verdict +critique 字段且次序强制(critique 先于 verdict)/+裁决规则表(字段级分派：首次出场基准 vs 后书优先两派并陈)/+可信度分带(>90/80-90/<80)；Case +三段式反例格式(原文/错误/正确)/+复盘标签。**不增设第四态**（已裁决）。
- 判据：四 schema 落地、校验器对好/坏样本双向判定正确、单测全绿。

### U-B02 cbb-coordinate（吸收）
- 吸收：事件 ID 追加序纪律（乱序是预期，永不重排）；编号先查重即占位（R-015 三方互证）。
- 保留：幂等坐标、断点协议。判据：单测绿含乱序插入与查重占位用例。

### U-B03 cbb-anchor（重构幅度最大）
- 重构：**双时间轴模型**——tick（叙事摄入序，单调，知识边界）+instant（故事时间，可回退）+time（人读原文，不可比较不参与排序）；as-of 双查询语义；**缺坐标判不可见**（宁可漏召回不可泄漏）。中文人读时间→instant 解析器（自研核心，16 仓无现成）。
- 吸收：倒计时到点兑现（chinese-webnovel）；章号轴时序回放作 instant 缺失降级（webnovel）。
- 保留：伪锚点禁墙钟、18 条精度抽检（全部回归通过）。判据：双时间轴单测绿+18 条抽检回归绿。

### U-B04 cbb-extract（防御扩三层+施工参数）
- 防御（四面并配，R6 独有性经 16 仓复核确认）：读入侧 R6 元文本（照抄 Step 0 验证文本）+输出侧禁词八类（worldbook：比喻转白描/万能修饰删/叙事禁词删/解释性描写删）+模型侧防先验（"AI 已知信息不入档"）+安全侧注入防御（untrusted narrative text 条款）。
- 吸收：证据式抽取条款（每断言须原文引+章号，Bad/Good 例句）；`//原文未提及`占位+反推标注；别名四分类（proper_name/nickname 须证据≥0.85/descriptor/title 永不）+存疑分开建；长篇施工参数（章节边界表单一切片真值+5-8 章/批新上下文子代理+降维聚合 15KB/章→≤8K 回传→√N 合并——迷深 517 章直接可用）；机械硬检查（grep 计数不依赖自报）；编写前重读纪律。
- 保留：stub 离线抽取、record_id 内容哈希幂等、build_episode_kwargs（伪锚点+R6 注入）。判据：单测绿含四面防御逐面用例+元文本章零抽取。

### U-B05 cbb-gate1（重构为三域）
- 重构：validate（结构/schema）/links（引用完整性+关系逆类型 12 对+对称 12 项双向回链）/continuity（死人走路/伏笔时序/契诃夫枪≥3 章超期/矛盾检测）。
- 吸收：Issue 修复动作字段落地；confidence 双口径字段；EXPLAIN 拒写（检索层永不写库）；可执行反例测试纪律。判据：三域单测绿，每域至少 1 可执行反例。

### U-B06 cbb-quarantine（吸收）
- 吸收：`[?]` 内联标记+行号汇总报告（字段级轻量隔离）；🔴超期态（遗漏告警）；urgency 公式（三层级 3.0/2.0/1.0+进度比+🔴🟡🟢）驱动优先级；**quarantine 三子类（已裁决）**：矛盾待裁决/外推待证/超期遗漏。
- 保留：五分组请你确认报告、append-only 裁决通道。判据：单测绿含三子类分流+urgency 排序。

### U-B07 cbb-store（双轨合并+约束族；cbb-merge 并入）
- 重构：双轨合并——一致重复→confidence 上调合并；矛盾→quarantine+Verdict（不静默合并）。
- 吸收：UNIQUE 约束族（别名复合 PK/关系三元组唯一/出场唯一——schema 层防重）；verified_against 漂移钩子（timestamp=最后验证时+SHA+stale 重验门）；ON CREATE/ON MATCH 幂等（neo4j 后端）；时序回放查询（chapter<=? ORDER BY 顺序覆盖）；写入安全围栏（符号链接拒绝+根围栏+命令白名单思想）；多版本对齐合并规则（cbb-merge 并入：Keep/Range/Flag/Pick+冲突表，规模小不拆独立技能——已裁决）。
- 保留：三态写入+supersedes 版本化（16 仓无更优完整替代）。判据：单测绿含双轨分流+约束防重+漂移钩子。

### U-B08 集成冒烟
- 离线版（必须）：coordinate→anchor（双时间轴）→extract stub（四层防御）→gate1 三域→quarantine→store 双轨，全链断言+双跑一致+EXIT 0。
- 真管道版（条件）：LM Studio+Neo4j 探活通过则跑 graphiti 实入库（DeepSeek 抽取+嵌入去重）；探活失败→标 blocked 留阶段三，不阻塞本单收口。判据：离线版全绿；真管道版 done 或 blocked（留痕）。

### U-B09 终报与交接
- 交接文书 `cbb/交接文书-本体构筑.md`（判据逐项对照/单位终态/git 摘要/待确认清单/经验候选）+ 全量单测终跑数字 + 实验版对比表（73 基线 vs 新总数；重构前后行为差异）。

## §3 十轮自审协议（每单位 done 前必须跑完；这是"严格 10 轮自审"的操作化）
- 每轮一个镜头，按序执行；日志 `cbb/自审日志/<单位ID>.md` **逐轮追加**：`R<n> <镜头> | 检查了<文件×方法> | 发现=<逐条或零> | 处置=<修复/记录/零>`。**零发现也必须写明实际检查的文件与方法**，空话零发现无效。
- 十镜头：R1 契约符合性（四 schema 校验器跑本单位产物）；R2 证据链（每项设计决策可回溯 U-A17 §3 条目或 Step 0 结论）；R3 幂等与断点（重跑零副作用、STATE/commit 纪律）；R4 边界与异常（空输入/坏编码 GBK/超长/符号链接/越界章号）；R5 Windows 纪律（py -X utf8/路径分隔/注册表兜底）；R6 许可红旗（零 GPL/AGPL 代码接触复查；CC BY-NC-SA 零文本复制）；R7 Step 0 三条件对照（本单位相关条件配置正确）；R8 U-A17 吸收项对照（该单位吸收清单逐条落实或记录不落实理由）；R9 测试覆盖（新行为有测试+至少一反例；总用例数不降）；R10 红队终局（假设自己是审核线找打回理由；打回理由→修复或登记【待确认】）。
- **出口门**：10 轮全部有留痕 + R9/R10（最后两轮）零新发现（或发现已当场修复并复验）。未过出口门不得标 done、不得 commit 收口。

## §4 铁律与服务纪律（照《P1执行区/闲时任务提示词模板.md》⑦⑧执行，此处不重复；另加两条）
- 本工单与 STATE 判据栏不得修改；STATE 只更新状态/证据/勘误字段。
- 断点续传：收口=STATE 更新+scoped commit（只 add 本单位交付路径与 STATE/自审日志，禁 `git add -A/.`）；闲时任务到点回队续跑是**常态**，每次唤醒从②自举三步开始；轮次与时间无上限，**不许因轮数或时长降低标准提前收工**。

## §5 终报格式（U-B09 产出+最后一条输出，照模板⑩）
判据达成状态／单位清单终态（含每单位十轮自审出口门状态）／git log 摘要／【待确认】清单／经验候选（有实据才写）。
