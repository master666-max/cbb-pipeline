# U-C01 留痕 · 边界表与锚点树+工具件（2026-09-16）

## 判据对照（工单 §2 U-C01）

| 判据 | 结果 | 证据 |
|------|------|------|
| 章数=517 | **PASS** | manifest/boundary-table-v1.json：517 章逐章行区间（marker_line/line_start/line_end/body_lines）；含 30 个 NOISE 章与 1 个 DUP 章（CHAPTER 0347=`[DUP:OF 0346]`，行108516）——DUP 章内容与 0346 全同→候选内容哈希幂等天然去重，tag 落表不特判 |
| 伪锚点树 v1 落盘 | **PASS** | anchors/anchor-tree.v1.json：517 锚点，tick=摄入序 0 起算（=伪锚点 2000-01-01+i）；trial 单章树移档 anchors/trial-v0-anchor-tree.ch14only.json；verified_against 三件套=clean_full blob sha |
| 边界表抽 5 章对原文核对 | **PASS** | logs/uc01-boundary-check.json：随机(种子20260916)抽 5 章，marker 原文前缀+边界衔接（line_end+1=下一章 marker_line）全对 |
| 工具件1 嵌入查重单测绿 | **PASS** | cbb/tools/embed_dedup_scan.py + test（10 tests OK，零网络伪 embed_fn）：余弦/分档边界（≥0.95 含等→疑重；0.85 含下界→存疑）/scan 纯函数；实链探活=LM Studio 8080 在但载入 m-prometheus-14b（judge 档）→ 按设计 blocked 退出码 2 不阻塞（留待嵌入档载入后的首个批收口执行首扫） |
| 工具件2 Neo4j 增量导出单测绿 | **PASS** | cbb/tools/neo4j_export.py + test（8 tests OK，零网络伪 commit）：collect_graph（superseded 排除/事件不进图）/MERGE 语句构造/分批；**实链已通**：docker start neo4j-step0 拉起→HTTP 7474 探活→导出 34 节点+37 边→图上 58 节点（34 新+24 step0 遗留）/37 边→重放 58/37 不变（MERGE 幂等实证）；报告=logs/uc01-neo4j-export-report.json |

## 关键事实与决议

- **摄入序≠章号**：557 编号存 517（缺号如 0002/0010/0011；末章 0557 摄入序 516）；伪锚点树以 marker 顺序为 tick 权威。
- **NEO4J_AUTH 凭据**：工具运行时 `docker inspect` 读取（不落任何文件，D-004）；--password/NEO4J_PASSWORD 优先。
- **约束容错**：step0 期已建 (:Entity {name}) 索引，CREATE CONSTRAINT 撞 IndexAlreadyExists→容错继续（索引在即 MERGE 定位有效；MERGE 幂等不依赖唯一约束）。
- **辅助服务态记录**：Docker daemon UP；Neo4j 容器本次由 Exited(255) 拉起成功；LM Studio 8080 UP 载入 judge 模型（嵌入档未载）。
- 导出工具走 HTTP API（stdlib urllib），规避 neo4j/requests 包缺失（环境实测两包均无，不引依赖）。
