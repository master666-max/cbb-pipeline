# 预注册 · 非空兜底 v3（2026-09-25）

## 与 v2 的唯一差异
词表准入制加**非空兜底**：canon 词表准入全空时（纯描述式查询），降级准许标点碎句进入
（丢弃数记"空表碎句兜底"）——目的=堵死 v2 实测的空表→LightRAG 回退调 LLM 通道（llm_calls=6 违零-LLM 判据）。

## 判据
- 查询集/三臂/主判定线同 v1、v2。
- 硬判据：llm_calls 必须 = 0（v2 此项失守）。
- 对照：B_v3 vs B_v1(91.1%)/B_v2(88.9%)；C_v3 同录；兜底触发次数如实报告。
