# 预注册 · 第二期真实负载复核（2026-09-25 立项，触发条件=真实查询累计）

## 数据底座（已就绪）
- 查询日志 `迷深实战-工作区/logs/query-log.jsonl`：四路（hybrid_search）与第五路（bridge）双写，默认开启（CBB_QUERY_LOG=0 显式关）；
  字段：q / path / top / backend / paths / mode / kw_dropped。

## 触发条件
真实查询（非合成、非测试）在查询日志中累计 **N≥30** 条时启动复核。

## 届时可算判据（现在定死）
1. **问题类型分布**：对 30+ 条真实查询做类型归类（单点/多跳/全局/时序）——分布决定深度路由与摘要层是否解锁（哨兵 A 联动）；
2. **引文回落率**：每查询 top-10 名字解析到库内记录且带证据的比例（机械可算）；
3. **后端降级频率**：backend 字段非 rerank 的比例；
4. **时延分布**：P50/P95（本轮已证固有成本亚秒级，环境噪音需在真实负载下复核）；
5. **第五路参与度**：path=第五路的查询占比与其 names 贡献（人工标注 30 条）。

## 预注册声明
第二期对照仍以"相对增量"为准（B vs A），查询集从日志真实查询中抽取，不再使用合成模板。
