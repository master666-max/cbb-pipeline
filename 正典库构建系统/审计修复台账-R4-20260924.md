# 审计修复台账 · R4 批（2026-09-24）

> 来源：外部七阶段审计 `D:/zcode-workspace-audit/docs/02-fragments/R4-cbb审查.md`（对 `cbb-pipeline-skill` 09-21 快照 + 主线对照）。
> 授权：用户令「开修，都按你的建议来」；冻结线两处写入的裁定引用见 `决策账.jsonl`（ruling 2026-09-24 + hook-bypass 同形条目）。
> 处置口径：每条修复必须带**能证伪旧实现的反例测试**；不能写反例的不算修完。

## 一、修复清单（10 项 · 17 处补丁 · 15 个测试）

| # | 审计发现 | 修复 | 反例测试（旧实现必红） | 验证 |
|---|---|---|---|---|
| A2a | `iter_records` 只捕 `OSError`，撕裂 JSON 使主链全崩 | `except (OSError, JSONDecodeError, UnicodeDecodeError)` 入 `iter_skipped` | 写入 `torn.json` + 非法 UTF-8 件 → 遍历不崩且两件均披露 | 真库实测 6172 件遍历，`iter_skipped=[]` |
| A2b | `_write_immutable` 直写非原子 | temp+`os.replace`；落定失败清理旁车残件 | 注入 `os.replace` 抛错 → 目标件不存在且无 `.tmp` 残留 | store 22 测试全绿 |
| A4 | `status_transition.from` 记文件态非有效态 | `from = effective_status(record_id)` | prov→conf→prov 两行轨迹必须是 `(provisional,confirmed)(confirmed,provisional)` | 同上 |
| A5a | `矛盾分流.propose` 静态 status 过滤：已裁件重入机械档 | 改按 `adjudications` 差集取 pending | 同件加 `adjudications` 行后 `proposals` 必须为空 | 7 测试 PASS |
| A5b | `apply` 无幂等：重放重复落账 | 台账 `item_id` 幂等（`skipped_already_in_ledger`） | 同提案重放两次 → 台账恰好 1 行且 `at` 为首次 | 同上 |
| A6 | 哨兵 D 同病 + 滞后永动（RED 不可逆） | pending/滞后均按差集口径 | 60 件全已裁 → `contradiction_pending=0`、`GREEN`、滞后 `None`；正对照 3 件未裁 → `AMBER` 且滞后可算 | 新建 `test_graphiti_ready.py`（2 项） |
| A7 | `load_latest_tree` 字典序：v10 < v9 | 数值排序 `int(re.search(r"v(\d+)").group(1))` | 同置 v9/v10 → 必须取 v10 | anchor 24 测试全绿 |
| A8 | 读语料不剥 BOM → 首章标记失配、全库坐标偏移 | `raw.decode("utf-8-sig")` | BOM+正文 → `chapters[0].chapter==1`、`chapter_count==2` | coordinate 18 测试全绿 |
| A9 | `extract_stub` 两处静默 `continue`（违 B6） | `skipped_out` 登记跳过块（章节/行号/原因） | 元文本块＋注入块 → 原因分别为 `metatext`/`embedded_instruction`；缺省不传行为不变 | extract 28 测试全绿 |
| A12 | smoke 恒真断言 `or len(stale) >= 0` | 删恒真子句（保留原意：实体件必须真进 stale） | 该断言现可失败（stale 空即红）；`smoke` 仅主线有（包内无 smoke 件） | 见 §三 |
| C1 | 账本 `_rows` 每次全量重读（O(n²)） | 实例内缓存 + 追加续行；**`verify()` 走 `_rows_fresh()` 读盘** | 缓存对象复用断言 + 缓存==盘；篡改检测回归（`test_tamper_detected`）必须仍红 | ledger 9 测试 PASS；真库 `verify` `ok:true rows:4466` |
| A13 | 导出器默认 7474 vs 巡检 7695（其一必错） | `DEFAULT_BASE=http://localhost:7695`（常量+文档同改）＋跨件口径断言 | 断言 `DEFAULT_BASE` 与巡检源码默认值一致 | neo4j 12 测试全绿（容器实映射 7695→7474） |

## 二、P0① 合入主线：run_chapter 账本前置/后置门

- 落点：`迷深实战-工作区/runners/run_chapter.py`（冻结线文件，已授权写入；写入以同形 `hook-bypass` 条目入 `决策账.jsonl`）。
- 内容：`_verify_gate(when)` ＋ `main()` 内 `_verify_gate("前置")` / `_verify_gate("后置")`（与审计副本 12 行逐字一致）。
- 功能双向实测：
  - 正对照（真库）：`_verify_gate("前置")` 放行（`ok:true rows:4466`）；
  - 负对照（注入 `ok:false`）：`SystemExit: run_chapter 后置 verify 未过（账本外改动，先处置再产数据）：['模拟账本外改动（测试注入）']`。
- 纪律入册：发布件 `references/审查与收口.md` §一·补「账本前置/后置门」（含"哈希链证明不了没有漏"的理由与"verify 一律读盘"的注）。

## 三、A12 的范围说明（据实）

`smoke/` 不在发布包 75 件内（包内无 `run_smoke.py`）→ A12 只在主线生效，发布件不受影响、也未被此修复覆盖。若后续把 smoke 纳入发布件，需同步该件。

## 四、S15（超长路径遗留）结论

- 事实（2026-09-24 实测）：
  - 真库 `libraries/**` 6172 件全部可读，`iter_skipped=[]`；本体库最长绝对路径 **258**（<260），工作区最长 129。
  - 遗留件：文件名 >120 字符者 37 件，其中 4 件 >250；均为 2026-09-20 前的 `-m-m-m-…` 连缀链（同一 record 反复合并所致）。
- 根因已修（2026-09-20）：`_chain_base` 剥旧后缀 + 新版本名 `-m{N}` 有界 → 不再累积。
- 裁定：**不回改遗留件**（append-only 与"旧件字节不动"优先）；读侧容忍（`iter_records` 披露 + `neo4j_export` `\\?\` 重试）继续在岗。登记于本件即闭环。

## 五、同步与发布

- 发布树同步（显式清单，逐字节校验）：17 件 → `cbb-pipeline-skill/cbb-pipeline/scripts/cbb/`（16 更新 + 1 新增）。
- **排除件**：`tools/build_evidence_index.py` —— 主线 `HERE.parent.parent` 与包内 `HERE.parent` 是各自的布局正确解，盲拷会打断包内相对路径（差异=设计使然）。
- 顺带修正：`tools/embed_dedup_scan.py` 技能树为滞留旧版（缺超长路径防御/dict 脏行防御/confirmed 目录扫描）→ 已同步主线版本。

## 六、未修项与下步

- **A3**（P1，账本不覆盖 `libraries/**.json` 记录本体 → 正典内容篡改不可检）：单独立项，见 `A3立项草案-账本覆盖记录本体-20260924.md`。
- 审计其余家族（B1 账本并发写纪律、B6 等）按原优先级队列接棒，不在本批范围。
