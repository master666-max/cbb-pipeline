# 重构报告 · 2026-09-26（闲时任务自主执行 · 全裁决权代行）

> 两阶段：legacy-refactor-flow（A-D）→ code-modernization reimagine。
> 判决：**核心管线 PROVEN**（行为等价指纹逐位相等）；长尾工具按裁决 D1 留 v1 增量迁移。
> 提交：`646b8514`（阶段一）/ `3e0b4872`（阶段二）；基线 tag `v0-baseline`。

## 一、测绘结果（阶段一 B）

- 46 个非测试模块 / 9740 行；公共接口面全量入 `analysis/测绘-依赖与接口.json`；
- 被引用最多：cbb_contracts（5 处）、cbb_store/cbb_coordinate（各 2-3 处）；
- 属性调用 Top：`cbb_gate1.make_generic_record`（6 次，冒烟/测试的记录工厂）；
- 头号债务（架构观察）：**46 处 sys.path 手术** + 实例名默认散布（已被外部审计同结论覆盖：19 处实例默认 + 7 处写死端口）。

## 二、定活清单（阶段一 C）

75 个入口点：**66 LIVE**（在发布包或被 runner 引用）、8 件裁定（7 LIVE 保留 + 1 DEAD）：
- DEAD 仅一件：`cbb/tools/adapt_record_to_tier1.py`（历史迁移件，无引用不在包；保留对照不删——LIVE/DEAD 判定见 `analysis/定活-UNKNOWN裁定.json`）；
- 无成片死代码——管线在持续使用中，符合预期。

## 三、裁判覆盖（阶段一 D）

`analysis/characterization.py` 五面特征测试（期望值全部来自真实运行）：
三态双轨（一致重复置信 82.0/矛盾隔离 1 件/迁移有效态）、抽取闸（元文本/注入/禁词/批量计划）、坐标回落（block_id/line_start）、别名检索、账本哈希链（ok/rows）。
- 基线指纹 `aafe38042b28f0c2`；
- **金丝雀证明**：改坏 v1 元文本闸 → exit 1；还原 → exit 0；
- 阶段一未改任何业务代码（改坏经 git checkout 还原，工作树干净）。

## 四、reimagine 架构决策（阶段二 · 裁决记录 analysis/rulings.md）

- **D1 范围**：核心管线重建（六模块+账本+检索机械面）；长尾 66 工具留 v1（无特征锁者不裸写——LRF 铁律）；
- **D2 架构**：`cbb-v2/cbb2` 可安装包——绝对导入（消灭全部 sys.path 手术）、`config.py` 单一配置源（env-first，吸收路径惯例）、九模块各就各位、runner 编排骨架；
- **D3 等价优先**：算法忠实移植（proven 不重写），重建的是架构不是语义；
- **D5 命名**：cbb2（与 cbb 并存不遮蔽）。

## 五、验收测试证据（阶段二）

`cbb-v2/tests/acceptance.py`：与 characterization **完全同构**的五面观察走 cbb2——
- **PROVEN：指纹 `aafe38042b28f0c2` 与 v1 基线逐位相等**（含一致重复置信 82.0、迁移 from=provisional、检索双命中排序、账本 ok/rows=0）；
- 金丝雀对 v2 有效（改坏 is_metatext → exit 1；还原 → exit 0）；
- 修复过程留痕：初版杜撰禁词词表致"禁词面"漂移——改回 v1 忠实词表（`defenses.py` 注记）。

## 六、规格资产（可长期复用）

`analysis/rules.md`：15 张 Given/When/Then 规格卡（R1-R15），全部带 file:line 引用——
后续任何 transform/uplift 轨道的现成规格基线。

## 七、回退方式

- 新版不合用：删除 `cbb-v2/` 即回 v1（两笔 commit 独立，`git revert 3e0b4872` 亦可）；
- v1 完整未动（tag `v0-baseline` 锚定）；数据零改动（正典库/决策账/工作区全程只读，守卫钩子未触发一次旁通——无需旁通）；
- 逐步切换路径：先 `pip install -e cbb-v2` 在新会话试用 cbb2，v1/v2 可同库并行操作（数据布局逐字节兼容）。

## 八、遗留与建议（下一步，需用户裁决）

1. 生产切换时机：建议 ch0004 起用 cbb2 runner 试跑一章（同库写入，特征测试对真库再留一次基线）；
2. 长尾迁移优先级建议：检索层（web_console 已依赖）→ lightrag 三件 → graphiti 三件；
3. v2 尚未覆盖：smoke 集成链、批次自检、web_supplement、graph_chain——均在 v1 且 LIVE，按 D1 增量迁。
