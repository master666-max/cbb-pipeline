---
description: 状态报告（薄壳）——库量/游标/哨兵/副本对账一页汇总
---

生成**状态报告**（只读，零写入）：

1. **副本对账**（T-8 副本多义律）：比对 cbb-pipeline 技能的三处副本哈希——
   `正典库构建系统/cbb-pipeline-skill/cbb-pipeline/`（版本源）vs
   `C:\Users\26672\.zcode\skills\cbb-pipeline\`（安装位）vs
   GitHub `master666-max/cbb-pipeline` 最新 commit（`gh api repos/master666-max/cbb-pipeline/commits/main --jq .sha`）。
   逐文件 sha256 对照版本源；差异文件逐个列出（差集不许静默）。
2. **哨兵读数**：读取 `迷深实战-工作区/logs/trigger-sentinel-U-C03.json`（若存在），报告 A/B/C/D 四触发器状态与值。
3. **库量快照**：`迷深实战-本体库/libraries/` 六库各计 JSON 数、`ledger.jsonl` 行数、`quarantine-zone/` items 与 adjudications 行数。
4. **git 侧**：当前 HEAD、`git status --porcelain` 计数（只报数字与前三条，不展开）。

输出：一页 markdown，每节带"数据时点"。**只读**——本报告零写入（对任何库与文件）。

参数：$ARGUMENTS
